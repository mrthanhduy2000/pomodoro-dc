# Runtime & build cache
.runtime

# Cấu hình công cụ AI cục bộ — chứa đường dẫn tuyệt đối của máy Đàm, không dùng được ở máy khác.
# Quy tắc/tài liệu thật thì KHÔNG nằm ở đây mà ở CLAUDE.md + AGENTS.md (đều được commit).
.claude
.codex

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# Artifact cũ — giữ trên máy nhưng không đưa vào git (dọn 2026-06-20)
backups/
DC Pomodoro.app/
DC Pomodoro - Logo System.html

# Kết quả chạy test thủ công (không có Playwright/E2E config trong repo) — lỡ commit 2026-07-11, đã dọn
test-results/

# ảnh xem thử Thành Phố 3D (scripts/city-preview.mjs) — chỉ để soi bằng mắt lúc phát triển
.city-preview/
