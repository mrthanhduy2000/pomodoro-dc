WEB_PUSH_PUBLIC_KEY=
WEB_PUSH_PRIVATE_KEY=
WEB_PUSH_SUBJECT=mailto:hello@pomodoro-dc.local
SUPABASE_SERVICE_ROLE_KEY=
CRON_SECRET=
# AI Coach đám mây (Gemini). Lấy key free ở https://aistudio.google.com → dán vào Vercel env.
# Thiếu key → AI Coach KHÔNG chạy (đã gỡ Qwen on-device). Model có sẵn mặc định, KHỎI đặt:
#   chuỗi: gemini-2.5-flash → gemini-2.5-flash-lite → gemini-2.0-flash (quá tải thì nhảy model kế).
# Chỉ đặt mấy biến dưới nếu muốn ĐỔI model (tuỳ chọn).
GEMINI_API_KEY=
GEMINI_MODEL=
GEMINI_MODEL_FALLBACK=
GEMINI_MODEL_FALLBACK2=
