# Pomodoro DC — Project Brief

## Người dùng
- Tên: Đàm (non-coder, dùng Codex + Claude Code để code)
- Giải thích đơn giản, tránh jargon kỹ thuật

## ⚠️ HỎI TRƯỚC KHI LÀM (quy tắc tối cao — đặt trên cả NGUYÊN TẮC SỐ 1)
Phân loại lệnh của Đàm thành 2 nhóm:
- **Lệnh NGHIÊN CỨU / TÌM HIỂU / ĐỀ XUẤT / "cho ý kiến" / "theo bạn…"** → CHỈ trình bày phân tích + khuyến nghị rồi DỪNG. KHÔNG sửa code, KHÔNG commit, KHÔNG deploy. Hỏi "bạn có muốn tôi làm không?". Câu mơ hồ → coi là nghiên cứu + hỏi trước.
- **Lệnh LÀM ("làm đi", "sửa", "thêm", "đổi", "nâng cấp", "deploy"…)** → theo đúng 4 bước: (1) **giải thích NGẮN GỌN, dễ hiểu, không hàn lâm** sắp làm gì + công dụng *trước khi sửa*; (2) làm (kèm `npm test`/lint + cập nhật tài liệu); (3) giải thích *sau khi sửa* đã đổi gì + ích lợi (vẫn dễ hiểu); (4) **TỰ ĐỘNG deploy lên Vercel** (commit + push) — KHÔNG hỏi lại, vì lệnh "làm" đã gồm cho phép deploy.
- Lý do: app production của Đàm (push = Vercel deploy ra mọi thiết bị). Anh cần kiểm soát (nghiên cứu thì đừng động vào), nhưng đã ra lệnh làm thì khỏi hỏi tới lui — giải thích cho anh hiểu rồi deploy luôn. Chi tiết: memory `ask-before-acting.md`.

## ⚠️ NGUYÊN TẮC ƯU TIÊN SỐ 1 (mọi phiên AI — quan trọng hơn mọi thứ khác)
1. **TRƯỚC khi làm:** đọc `BAN_GIAO.md` + `CLAUDE.md` (file này), RỒI đọc các file liên quan tới việc sắp làm — hiểu rõ ngữ cảnh trước, đừng sửa mò. (Hook `.claude/session-start-bangiao.sh` tự chèn BAN_GIAO.md đầu mỗi phiên, nhưng vẫn phải đọc kỹ.)
2. **SAU khi có cập nhật (dù nhỏ):** cập nhật ngay `CLAUDE.md` + `BAN_GIAO.md` **và các file liên quan khác** (README, ghi chú...) cho khớp. Làm xong việc mà chưa cập nhật tài liệu = **CHƯA XONG**.
3. Bàn giao thiết kế chi tiết nằm ở thư mục memory:
   `/Users/damduy/.claude/projects/-Users-damduy-Downloads-Claude-Code-B-n-sao-Pomodoro-Game---USING/memory/`
   (đặc biệt `upgrade-roadmap.md` cho AI Coach, `ui-review-2026-06.md` cho UI, `resonance-update.md` cho game loop).
4. Luôn chạy `git status` tươi — đừng tin ảnh chụp git cũ.
5. Muốn biết **file nằm ở đâu** → `PROJECT_STRUCTURE.md`. Muốn biết **bức tranh kiến trúc lớn** (luồng dữ liệu, vì sao chia lớp thế này) → `ARCHITECTURE.md`. Cả hai PHẢI cập nhật cùng lúc với mọi thay đổi cấu trúc (đúng quy tắc số 2 ở trên).
6. ⚠️ **File này là NGUỒN SỰ THẬT DUY NHẤT về quy tắc, cho MỌI AI** (Claude Code, Codex, ChatGPT...) — tên file có chữ "CLAUDE" chỉ vì lịch sử, KHÔNG có nghĩa quy tắc chỉ dành cho Claude. `AGENTS.md` (điểm vào của Codex) **chỉ là con trỏ trỏ về đây**, tuyệt đối KHÔNG chép nội dung sang. **TUYỆT ĐỐI không tạo bản sao tài liệu quy tắc cho từng công cụ AI** — đã thử 2026-07-31 và thất bại: bản sao sinh câu vô nghĩa + đường dẫn `.Codex/` không tồn tại, rồi trôi khỏi bản gốc chỉ sau 5 ngày (thiếu nguyên mục "3 cái bẫy menu bar"). Đây chính là điều quy tắc **Composition over Duplication** ở mục Playbook cấm. Chi tiết: `AGENTS.md`.

## 📋 PROJECT GOVERNANCE PROTOCOL (2026-07-12 — áp dụng vĩnh viễn, mọi phiên AI tương lai)

Project gồm **3 thành phần giá trị ngang nhau**: (1) Source Code, (2) Documentation, (3) Project
Knowledge (quyết định/lịch sử/bối cảnh). Code chỉ nói "hệ thống chạy thế nào" — chỉ tài liệu mới
giữ được "vì sao nó chạy như vậy". Nếu 3 thành phần này lệch nhau, coi như CHƯA XONG việc dù code
đã đúng/build/test/lint đều xanh.

### Definition of Done (áp dụng mọi task, không ngoại lệ)
✓ Source code đúng · ✓ Build thành công · ✓ Test thành công · ✓ Lint thành công ·
✓ **Documentation đã đồng bộ** · ✓ **Project Knowledge đã đồng bộ**. Thiếu 1 mục = chưa xong.

### Bảng: loại thay đổi → tài liệu PHẢI cập nhật
Sau mỗi thay đổi, tự hỏi: có ảnh hưởng kiến trúc/module/workflow/AI/notification/sync/timer/store/
API/database/build/deploy/folder/naming/dependency/testing/performance không? Nếu CÓ, tự xác định
(không đợi Đàm nhắc) tài liệu nào trong bảng sau cần sửa:

| Tài liệu | Vai trò (KHÔNG lẫn với các file khác) | Khi nào phải sửa |
|---|---|---|
| `README.md` | CHỈ trang giới thiệu: project là gì/chạy thế nào/build thế nào/deploy thế nào/đọc tiếp gì. KHÔNG nhồi kiến trúc sâu vào đây. | Đổi cách chạy/build/deploy, hoặc thêm tài liệu mới cần trỏ tới |
| `ARCHITECTURE.md` | Bức tranh lớn: layer/module/dependency/state flow/AI flow/sync flow/notification flow/storage flow/database flow | Bất kỳ FLOW nào đổi |
| `PROJECT_STRUCTURE.md` | Cây thư mục + quy tắc tạo module/chia folder/import/shared module/đặt tên | Đổi cấu trúc thư mục, thêm quy ước mới |
| `CHANGELOG.md` | Tóm tắt CHÍNH THỨC, ngắn gọn theo mốc (mục đích/phạm vi/ảnh hưởng/tương thích) — KHÔNG phải lịch sử commit | Mọi thay đổi quan trọng (không phải mọi commit nhỏ) |
| `MIGRATION.md` | Chỉ ghi khi đổi API/module/đường dẫn/workflow/state/storage/database/folder | CHỈ khi có migration thật — không có thì không cần ghi |
| `ARCHITECTURE_DECISIONS.md` | "Bộ nhớ kiến trúc" — mỗi quyết định: Ngày/Bối cảnh/Vấn đề/Phương án cân nhắc/Lý do loại bỏ/Giải pháp chọn/Trade-off/Ảnh hưởng/Điều kiện xem lại | Quyết định có ≥2 phương án thật sự cân nhắc + trade-off thật + ảnh hưởng lâu dài |
| `TECH_DEBT.md` | Mọi nợ kỹ thuật đã biết (đủ 14 trường: Tên/Module/Priority/Severity/Impact/Root Cause/Current Risk/Future Risk/Recommended Solution/Estimated Complexity/Blocking Conditions/Review Trigger/Owner/Status) | Phát hiện nợ mới mà rủi ro thấp thì xử lý luôn; rủi ro trung bình/cao hoặc ngoài phạm vi task hiện tại → PHẢI ghi vào đây, không bỏ qua |
| `AI_ONBOARDING.md` | Đọc nhanh 10-15 phút — nếu AI mới cần audit cả codebase mới hiểu project thì file này CHƯA đạt yêu cầu | Đổi module quan trọng nhất/rủi ro cao/bài học lớn mới |
| `AI_HANDOFF_KNOWLEDGE.md` | Bàn giao tri thức ĐẦY ĐỦ nhất (domain/flow/ADR/tech debt chi tiết) — để bàn giao cho AI khác hoàn toàn không có quyền đọc code | Thay đổi lớn ảnh hưởng nhiều phần của tài liệu này |
| `BAN_GIAO.md` | Trạng thái hiện tại + nhật ký CHI TIẾT từng việc — luôn cập nhật, đây là NGUYÊN TẮC ƯU TIÊN SỐ 1 có sẵn | MỌI thay đổi dù nhỏ |

### Ngưỡng "Maintenance Sprint"
Khi `TECH_DEBT.md` có **≥8-10 mục Priority High/Critical**, HOẶC một module đã trải qua ≥3 lần vá
lỗi/refactor nhỏ mà chưa từng refactor triệt để, hãy CHỦ ĐỘNG đề xuất mở một "Maintenance Sprint"
(nêu rõ mục tiêu/phạm vi/lợi ích/rủi ro/tiêu chí hoàn thành) thay vì tiếp tục cộng thêm tính năng
mới. Trạng thái ngưỡng hiện tại: xem đầu `TECH_DEBT.md`.

### Tính nhất quán kiến trúc (tự hỏi trước khi tạo mới)
Trước khi tạo folder/module/service/hook/component/store/helper/abstraction/API/utility mới, tự
hỏi: có tăng coupling không? có làm project khó hiểu hơn không? có tạo thêm 1 pattern mới trong khi
pattern tương tự đã tồn tại không (tái sử dụng được không)? có tạo thêm nợ kỹ thuật mới không? Nếu
có cách nhất quán hơn (khớp quy ước đã có ở `PROJECT_STRUCTURE.md`) → ưu tiên cách đó.

### Self-audit trước khi kết thúc task
Kiểm tra: code/test/lint/build, dead code, duplicate logic, unused imports/dependencies,
documentation, kiến trúc, tính nhất quán folder/naming/import, technical debt, knowledge update.
Vấn đề rủi ro thấp/trung bình phát hiện được → xử lý luôn, không cần đợi Đàm yêu cầu (rủi ro cao →
báo trước theo quy tắc "HỎI TRƯỚC KHI LÀM" ở trên).

### Bảo tồn kinh nghiệm (Knowledge Preservation)
Nếu trong lúc làm việc phát hiện: kinh nghiệm mới, bài học mới, bug đặc biệt, edge case, giới hạn
của framework/Supabase/Electron/Vercel/AI/Browser — tự hỏi "thông tin này có giúp phiên sau tránh
lặp lại sai lầm không?". Nếu có, PHẢI bổ sung vào tài liệu phù hợp (bảng ở trên) — không được để
những bài học này chỉ tồn tại trong cuộc hội thoại rồi biến mất khi phiên kết thúc.

### Báo cáo bàn giao cuối phiên (khi hoàn thành một task đáng kể)
1. Đã thay đổi gì · 2. Quyết định kiến trúc mới (nếu có) · 3. Tech debt đã xử lý · 4. Tech debt còn
lại · 5. Migration nếu có · 6. Tài liệu đã cập nhật · 7. Giả định mới của hệ thống · 8. Bài học mới
(Lesson Learned) · 9. Việc phiên sau cần biết · 10. Việc tuyệt đối chưa nên làm + lý do · 11. Đề
xuất bước tiếp theo. Mục nào không đổi → ghi rõ "Không có thay đổi" (đừng bỏ qua im lặng). Áp dụng
mục này ở MỨC ĐỘ PHÙ HỢP với quy mô task — một sửa lỗi nhỏ không cần đủ 11 mục, nhưng một task lớn
(refactor, tính năng mới, sự cố) thì có.

### TECHNICAL ADVISOR REPORT (bắt buộc từ 2026-07-17, sau MỖI task hoàn thành; bổ sung cùng ngày: mục 0 + mục 10 + viết 100% tiếng Việt)
Ngoài báo cáo thường, PHẢI kèm một phần tiêu đề "TECHNICAL ADVISOR REPORT" — viết cho một **AI
Technical Advisor độc lập (GPT)** đánh giá kiến trúc, KHÔNG phải cho Đàm. Ngắn gọn nhưng đủ ngữ
cảnh; tối đa ~1-2 trang A4; không văn dài/không marketing/không tự khen/không lặp changelog.
**NGÔN NGỮ: 100% tiếng Việt** (cả tiêu đề mục lẫn nội dung). CHỈ giữ tiếng Anh cho: tên file/
class/hàm/biến, commit hash, API, framework, và thuật ngữ không có cách dịch tự nhiên (CAS,
debounce, snapshot, whitelist...).
Đúng 11 mục theo thứ tự:
0. **Vì sao làm task này lúc này?** (≤10 dòng) — thuộc Priority nào của Roadmap A · gỡ blocker
   nào · cải thiện điều kiện chuyển Phase nào · nếu KHÔNG làm thì rủi ro gì · vì sao ROI cao hơn
   các task tồn đọng khác. Nếu KHÔNG phải task ROI cao nhất → giải thích vì sao vẫn làm.
1. **Mục tiêu** — "tôi được yêu cầu làm gì?" (≤5 dòng).
2. **Đã thay đổi gì** — chính xác file tạo mới / sửa / xoá; file lớn thì ghi module bị ảnh hưởng.
3. **Quyết định kiến trúc** — QUAN TRỌNG NHẤT: vì sao chọn A thay vì B, vì sao chưa refactor,
   vì sao test trước, vì sao giữ tương thích ngược, vì sao hoãn việc gì.
4. **Giả định** — mọi giả định đã dùng (API không đổi, version luôn tăng, 1 phiên active...).
   Không có → "Không có."
5. **Rủi ro mới phát sinh** — rủi ro MỚI do thay đổi này. Không có → "Không có."
6. **Blocker còn lại** — những gì còn chặn Giai đoạn hiện tại (≤10 dòng).
7. **Tác động lên Roadmap** — chấm TỪNG mục: God File · Duplicate · Test · Sync · Nợ kỹ thuật ·
   Độ ổn định AI Coach, theo thang: Không ảnh hưởng / Cải thiện nhẹ / Cải thiện / Hoàn thành.
8. **Độ tự tin** — %; DƯỚI 90% phải giải thích lý do.
9. **Câu hỏi cho Technical Advisor** — ≤5 câu hỏi kiến trúc/hướng đi chưa chắc. Không có → "Không có."
10. **Đề xuất task tiếp theo** — đúng MỘT task (không phải danh sách): vì sao quan trọng nhất ·
    điều kiện Phase A nào tiến thêm · hoàn thành xong mở khoá điều gì. Nhiều lựa chọn ngang nhau
    → nêu trade-off rồi CHỌN MỘT.

## 🛠️ AI ENGINEERING PLAYBOOK (Operating Manual — quy trình làm việc, 2026-07-12)

> Đây là quy trình làm việc TIÊU CHUẨN cho mọi AI tiếp quản project (Claude Code/Codex/ChatGPT...),
> không riêng phiên nào. Mục Governance Protocol ở trên quản lý "tài liệu có đồng bộ với code
> không"; mục này quản lý "AI thực hiện MỘT task như thế nào, từng bước". Hai mục KHÔNG lặp lại
> nhau — chỗ nào trùng, mục này trỏ ngược lên Governance Protocol thay vì chép lại (đúng tinh thần
> "Composition over Duplication" ngay bên dưới).

### Triết lý
Không tối ưu cho việc hoàn thành nhanh — tối ưu cho khả năng bảo trì nhiều năm. Khi có nhiều cách
giải quyết, ưu tiên phương án: đơn giản hơn, dễ bảo trì hơn, ít coupling hơn, ít nợ kỹ thuật hơn,
khớp kiến trúc hiện tại hơn. Vai trò của AI khi làm việc ở đây không chỉ là "người viết code" —
đồng thời là Senior Engineer, Software Architect, Reviewer, QA Engineer, Technical Writer, và
Maintainer.

### Quy trình chuẩn — 7 giai đoạn (không bỏ qua nếu không có lý do đặc biệt)
1. **Hiểu yêu cầu** — xác định loại task (Feature/Bug Fix/Refactor/Performance/Documentation/
   Architecture/Infrastructure/AI/Database/Deployment) + phạm vi ảnh hưởng đầy đủ TRƯỚC khi viết
   dòng code nào.
2. **Audit** — module đang chạy ra sao? đã có abstraction/helper/util tương tự chưa? có pattern
   project đang dùng không? có ADR/Tech Debt/bug cũ nào liên quan không (xem
   `ARCHITECTURE_DECISIONS.md`/`TECH_DEBT.md`)? Ưu tiên tái dùng cái đã có hơn viết mới.
3. **Thiết kế** — thay đổi nhỏ thì làm luôn; thay đổi vừa/lớn phải tự phân tích phạm vi/ảnh hưởng/
   dependency/migration/rollback/test strategy TRƯỚC. Ảnh hưởng kiến trúc → tạo entry mới trong
   `ARCHITECTURE_DECISIONS.md` (theo đúng format ADR đã có).
4. **Thực hiện** — không copy logic, không tạo abstraction/helper trùng cái đã có, không tăng
   coupling/độ phức tạp nếu không cần. Phát hiện duplicate/dead code/naming lệch/module sai trách
   nhiệm trong lúc làm → xử lý LUÔN nếu rủi ro thấp/trung bình, không đợi task khác.
5. **Self Review** — tự soát logic/naming/readability/maintainability/architecture/performance/
   security/consistency TRƯỚC khi commit.
6. **Validation** — build + lint + test luôn luôn. Task đụng API/Database/Sync/Notification/AI/
   Deployment/Realtime → phải kiểm tra CẢ LUỒNG liên quan, không chỉ file vừa sửa.
7. **Knowledge Update** — dùng bảng "loại thay đổi → tài liệu cần cập nhật" ở mục Governance
   Protocol phía trên (không lặp lại bảng ở đây).

### Quy trình theo từng loại task
- **Feature**: Audit → Thiết kế → Đánh giá ảnh hưởng → Code → Test → Documentation → Knowledge Update.
- **Bug Fix**: Reproduce → Root Cause Analysis → Fix → Regression Test → Lesson Learned → Knowledge
  Update. **Sửa nguyên nhân gốc, không sửa triệu chứng** — đúng tinh thần đã áp dụng cho mọi sự cố
  production trong lịch sử dự án (xem `AI_HANDOFF_KNOWLEDGE.md` Phần 11).
- **Refactor**: Audit → Risk Analysis → Refactor Plan → Refactor → Regression Test → Architecture
  Review → Documentation.
- **Architecture Change**: KHÔNG thực hiện ngay — đánh giá + phân tích trade-off + cân nhắc phương
  án + viết `ARCHITECTURE_DECISIONS.md` TRƯỚC, rồi mới thay đổi.

### Quy tắc kiến trúc (luôn ưu tiên)
Single Responsibility · High Cohesion · Low Coupling · Reuse over Rewrite · Composition over
Duplication · Explicit over Implicit. Không hy sinh kiến trúc để đổi lấy tốc độ hoàn thành.

### Quy tắc về AI: không giả định, không suy diễn
Không chắc chắn → kiểm tra source code. Source code chưa đủ → đọc tài liệu. Tài liệu chưa đủ → NÓI
RÕ điều còn thiếu, không tự suy đoán rồi trình bày như sự thật. Đây CHÍNH XÁC là nguyên tắc chống-
bịa đã áp dụng cho AI Coach (`src/engine/coach/guard.js`, xem `ARCHITECTURE.md` mục 3) — áp dụng
lại cho chính AI đang code, không chỉ cho AI Coach của app.

### Quy tắc Review (bổ sung, không lặp Self-audit ở Governance Protocol)
Ngoài checklist Self-audit đã có ở trên, trước khi kết thúc task tự hỏi thêm: có ADR mới cần ghi
không? có Migration cần ghi không? có cần ghi Lesson Learned không? Câu trả lời Có → xử lý trước
khi kết thúc, không để lại cho phiên sau.

### Quy tắc Commit
Mỗi commit: mục tiêu rõ ràng, phạm vi rõ ràng, KHÔNG trộn nhiều thay đổi không liên quan, có thể
rollback độc lập. Không tạo commit chỉ để "tiện tay".

### Continuous Improvement
Sau mỗi task, tự hỏi: project hiện sạch hơn/ít nợ kỹ thuật hơn/ít duplicate hơn/ít coupling hơn/
tài liệu tốt hơn/AI sau dễ tiếp quản hơn so với TRƯỚC khi bắt đầu không? Nếu Không → cân nhắc làm
thêm vài cải tiến nhỏ rủi ro thấp trước khi kết thúc. Thành công không đo bằng số dòng code viết ra
— đo bằng việc project rõ ràng hơn, ổn định hơn, dễ phát triển hơn sau mỗi phiên.

## Nền tảng hiện tại
App chính chạy trên **web** tại `https://pomodoro-dc.vercel.app`.

- Web Vercel là bản đầy đủ, dùng trên iPhone và Mac.
- Electron vẫn được dùng như app phụ trên Mac để có biểu tượng menu bar/tray.
- Electron mở URL Vercel và đọc trạng thái timer từ Supabase, không phải bản app riêng tách logic.
- Localhost chỉ dùng cho dev/test, không phải luồng sử dụng hằng ngày.

## Tech Stack
- React + Vite + PWA
- State: Zustand + localStorage (key: `dc-pomodoro-v1`, vẫn đọc được key cũ `civjourney-v1`)
- Sync cloud: Supabase
- Hosting: Vercel (auto-deploy từ GitHub)

## Hạ tầng

| Thứ | Chi tiết |
|-----|----------|
| App URL | `https://pomodoro-dc.vercel.app` |
| Mac menu bar | Electron companion app |
| GitHub | `https://github.com/mrthanhduy2000/pomodoro-dc` |
| Supabase | `https://jcefdsdccmnmqvuwelmm.supabase.co` |
| Bảng DB | `game_state` (id, data JSONB, updated_at) |
| Timer tray sync | `timer_live` (id `singleton`) |

## Vercel Hobby: giới hạn 12 Serverless Functions/deploy
- ⚠️ **2026-07-11 — SỰ CỐ + FIX TRIỆT ĐỂ (Đàm yêu cầu xử lý gốc, không vá tạm)**: thêm `api/keepalive.js` làm deploy FAIL — "No more than 12 Serverless Functions can be added to a Deployment on the Hobby plan". **Nguyên nhân gốc**: Vercel (preset "Other"/không framework) coi MỌI file `.js` nằm TRỰC TIẾP trong `api/` (đệ quy) là 1 Serverless Function riêng — kể cả file test — TRỪ file/thư mục có tên bắt đầu bằng `_` (quy ước Vercel đã tôn trọng sẵn cho `api/_lib/`). Lúc đó có 5 file `*.test.js` nằm lẫn trong `api/`/`api/push/` bị tính oan.
  - **FIX CẤU TRÚC (vĩnh viễn, không phải blacklist phải nhớ cập nhật)**: chuyển TOÀN BỘ test của `api/` vào **`api/_tests/`** (mirror lại cấu trúc `api/push/` → `api/_tests/push/`) — dùng ĐÚNG quy ước underscore-prefix mà Vercel đã tự động bỏ qua, giống hệt `api/_lib/`. Vì vậy: test luôn nằm ngoài phạm vi quét Function của Vercel, KHÔNG cần biết trước tên file, KHÔNG cần nhớ xoá/di chuyển gì trước khi deploy — dù sau này có thêm hàng trăm file test (`.test.js`, `.spec.js`, tên gì cũng được) đặt đúng trong `api/_tests/` thì vẫn an toàn tuyệt đối.
  - **⚠️ QUY TẮC BẮT BUỘC cho mọi test API mới**: LUÔN đặt trong `api/_tests/` (mirror đường dẫn của file đang test), KHÔNG đặt cạnh route handler nữa. Cập nhật path import tương ứng (`../coach.js`, `../../push/dispatch.js`...). `package.json` glob test đã trỏ sang `api/_tests/*.test.js api/_tests/push/*.test.js`.
  - **Lớp phòng thủ thứ 2** (`.vercelignore`, phòng khi lỡ tay đặt nhầm file phụ trợ ngay dưới `api/` mà quên cho vào `_tests`/`_lib`): loại thêm `*.spec.*`/`*.mock.*`/`*.fixture(s).*`/`*.stories.*`/`*.bench.*`/`*.e2e.*` — không chỉ `*.test.js`.
  - Hiện tại: đúng **10 function thật** (`coach`, `coach-digest`, `keepalive`, 7 route dưới `api/push/`) — còn dư 2 trước khi chạm trần 12. Thêm route API mới → đếm lại `find api -type f -name "*.js" ! -path "api/_*"`.
  - ⚠️ **PHÁT HIỆN LẠI KHI SOÁT LOG**: commit `8ee264d` (25/6, thêm `api/coach-digest.js` — mảng 6/6 AI Coach) từng bị FAIL build **CÙNG NGUYÊN NHÂN NÀY** nhưng không ai để ý — Vercel giữ nguyên bản deploy trước đó, khiến tính năng "cảnh báo chuỗi sắp đứt qua push" **KHÔNG hề chạy thật trên production suốt 25/6–11/7** dù code/tài liệu đã ghi "hoàn tất" (xem `BAN_GIAO.md`). Bài học: sau mỗi lần push, PHẢI xác nhận tab Deployments trên Vercel hiện "Ready" — code xanh + commit thành công không có nghĩa là đã thực sự lên production.

## Sync (đã hoàn chỉnh)
- `src/lib/supabase.js` — Supabase client
- `src/lib/syncService.js` — pull khi mở app, push debounced 5s
- `initSync()` gọi trong `App.jsx`, ở effect chạy sau khi store nạp xong (`storesHydrated`)
- ⚠️ **Đồng bộ ngừng hoạt động = kiểm tra Supabase project trước tiên, không phải code.** Project Free tier có thể tự PAUSE vì 1 trong 2 lý do khác nhau: (a) vượt hạn mức "Database Size" 0.5 GB — xem sự cố 2026-07-11 ở `BAN_GIAO.md`: `cron.job_run_details` phình tới 795 MB do job push-dispatch chạy mỗi 5s không dọn log, KHÔNG phải do `game_state` — bảng đó luôn chỉ vài trăm KB (đã có job tự-dọn log mỗi đêm `supabase/cleanup_cron_logs.sql` để không tái diễn); (b) project "không hoạt động" ~7 ngày (app 1 người dùng dễ im lặng lâu) — chống bằng cron `api/keepalive.js` (Vercel, 3h sáng mỗi ngày, xem `vercel.json`) gọi 1 query nhẹ qua đúng client Supabase để giữ project luôn "active". Nếu Database Size phình lại, soi `cron.job_run_details` trước; nếu bị pause dù Database Size vẫn thấp, kiểm tra cron `keepalive` có đang chạy không (cần `CRON_SECRET`+`SUPABASE_SERVICE_ROLE_KEY` đã đặt ở Vercel env).
- ⚠️ **"First action wins" (2026-07-11) — chống 2 máy giành nhau ghi đè.** Trước đây `game_state` chỉ có `updated_at` do CLIENT tự ghi (`new Date().toISOString()`) → máy nào ghi CUỐI CÙNG thắng bất kể ai thao tác trước, gây hiện tượng 2 máy nhảy qua nhảy lại + có thể MẤT dữ liệu (xem sự cố cùng ngày ở `BAN_GIAO.md`: mất 1 phiên focus thật vì laptop ghi đè lên phiên điện thoại vừa hoàn thành). ĐÃ SỬA: thêm cột `version` do TRIGGER PHÍA SERVER tự tăng (`supabase/game_state_version.sql`, không phụ thuộc đồng hồ máy khách) — `syncService.js` ghi kiểu compare-and-swap (`.eq('version', expectedVersion)`); ghi bị từ chối (0 dòng khớp) → máy đó THUA, phải tự nhận lại bản đã thắng (`pullFromCloud()`), TUYỆT ĐỐI không được ép ghi đè. Guard cũ dựa trên `localSession?.isRunning` đã bị GỠ (không cần nữa — version là nguồn xác định thứ tự chính xác, không phải suy đoán). Hàm thuần `shouldImportVersion` test ở `src/lib/syncService.test.js`.
- ⚠️ **BẢN VÁ C1 (2026-07-17) — 4 lưới an toàn quanh CAS, ĐỪNG gỡ mà không đọc kỹ.** (a) **Flush khi rời app**: `visibilitychange→hidden` + `pagehide` gọi `pushNow()` **chỉ khi còn thay đổi đang chờ** — vì trên iOS tab bị đóng băng nên timer debounce 5s KHÔNG bao giờ nổ. Điều kiện "còn đang chờ" dựa vào biến `debounceTimer`, nên nó **PHẢI được gán `null`** khi timer nổ hoặc bị huỷ (nếu không, tín hiệu luôn bật và mọi lần ẩn app đều ghi mù). (b) **`hasMeaningfulState()`** (thuần, export): local trắng + cloud có dữ liệu → NHẬN cloud, KHÔNG đẩy; local có dữ liệu thật → vẫn đẩy như cũ (đây là đường hồi phục cho thay đổi offline chưa kịp đẩy — đừng biến nhánh else thành "luôn import"). (c) Nhánh `known < 0` (đường ghi DUY NHẤT không có CAS) phải **đọc cloud trước**; đọc lỗi → hoãn ghi, không ghi mù. (d) Lỗi Postgres **`42703`** lúc initSync → `console.error` chỉ đích danh `supabase/game_state_version.sql`. Test hành vi: `src/lib/syncService.behavior.test.js` (17 bài; file test có stub chặn debounce 5s thật để không flaky). **Giới hạn còn lại có chủ đích**: 2 máy sửa trường KHÁC NHAU khi offline vẫn mất phần của máy thua → `TECH_DEBT.md` #8. ⚠️ Deploy code mới PHẢI chạy `supabase/game_state_version.sql` TRƯỚC (hoặc gần như đồng thời) — thiếu cột `version` thì mọi lần ghi sẽ lỗi (`column "version" does not exist`) → sync ngừng hẳn cho tới khi chạy SQL.

## Web Push iPhone (đã chạy; cần làm lại khi setup máy/dự án mới)
- Biến môi trường trên Vercel: `WEB_PUSH_PUBLIC_KEY`, `WEB_PUSH_PRIVATE_KEY`, `WEB_PUSH_SUBJECT`, `SUPABASE_SERVICE_ROLE_KEY`, `CRON_SECRET`, **`GEMINI_API_KEY`** (AI Coach đám mây Gemini — ĐÃ cấu hình + **bật billing/paid tier (2026-06-24) → hết 429, chạy ổn định** trên `gemini-2.5-flash`; thiếu key → AI Coach KHÔNG chạy vì đã gỡ Qwen on-device; `GEMINI_MODEL`/`GEMINI_MODEL_FALLBACK`/`GEMINI_MODEL_FALLBACK2` tuỳ chọn). Xem `.env.example`. Khoá Supabase frontend đã hardcode, không cần đặt.
- Tạo khoá Web Push: `npm run push:keys` → dán public/private vào biến môi trường Vercel.
- Bảng + scheduler push nằm trong `supabase/*.sql` — chạy tay trong Supabase SQL editor (thiếu thì push hỏng).
- Sửa push phía trình duyệt: `public/push-worker.js` (service worker) + `public/manifest.json` (PWA).

## AI Coach + tầng engine (mảng lớn nhất, KHÔNG được bỏ qua)
App có cả một hệ "huấn luyện viên" và engine game thuần. Đây là phần lớn nhất dự án.
- ⚠️ **CHỈ MỘT ENGINE = GEMINI (đám mây) (2026-06-24 — Đàm: "bỏ Qwen, chỉ còn Gemini").** ĐÃ GỠ HẲN Qwen2.5-3B + WebLLM + dep `@mlc-ai/web-llm` (xoá `webllmEngine.js`, `guard.test.js`, `LLM_MODELS`/`detectWebLLMCapable`/`mapInitProgress`, manualChunk `vendor-webllm`, globIgnores webllm/wasm). App nhẹ hơn, **chạy CẢ iPhone**, KHÔNG tốn RAM/đĩa máy. **Đánh đổi: mất mạng / hết quota / chưa-có-key → Coach NGỪNG** (không còn lưới on-device dự phòng) → báo lỗi + nút "Thử lại".
  - **Cổng đám mây**: `api/coach.js` (Vercel serverless) giữ `GEMINI_API_KEY` ở server (KHÔNG lộ ra client; đã verify key không vào bundle), map `{system, messages}` → Gemini `generateContent`. Client gọi qua `src/engine/coach/cloudEngine.js` (`generateCloud`). Pure helpers `toGeminiBody`/`extractGeminiText`/`shouldFallback`/`buildModelChain` đã tách sang `api/_lib/gemini.js`, test ở `api/_tests/_lib/gemini.test.js`. Pipeline dùng chung "gọi model → sanitize → chống chữ lạ → chống bịa số" cho cả 3 lối vào Coach nằm ở `src/engine/coach/guardedGenerate.js`.
    - **CHUỖI MODEL (tự nhảy khi quá tải/hết lượt)**: `gemini-2.5-flash` → `gemini-2.5-flash-lite` → `gemini-2.0-flash` (env `GEMINI_MODEL`/`GEMINI_MODEL_FALLBACK`/`GEMINI_MODEL_FALLBACK2`). `callModelOnce` gọi từng model 1 lần (KHÔNG retry — đổi model nhanh hơn thử lại model đang sập); `shouldFallback` (503/500/**429**) → model kế; lỗi khác (400/403 key) → dừng. ⚠️ Thêm `gemini-2.0-flash` vì 2 model 2.5 free HAY CÙNG 503 — 2.0 đời cũ, dung lượng riêng, rất ổn định. ⚠️ **2.5 bật "thinking" mặc định → ép `thinkingConfig.thinkingBudget: 0`** (chỉ áp model 2.5; 2.0 không có thinking); để ON nó ăn hết `maxOutputTokens` làm câu CỤT/RỖNG.
      - ⚠️ **TẦNG MODEL "deep" (2026-06-25, mảng 4/6)**: `buildModelChain(tier, env)` (thuần, test ở `api/_tests/_lib/gemini.test.js`). Body có thêm `tier`: `'deep'` → thử `gemini-2.5-pro` TRƯỚC rồi rơi về nguyên chuỗi flash (vừa khôn vừa có lưới an toàn nếu pro quá tải); mặc định (chat/nhắc) chỉ chuỗi flash nhanh+rẻ. CHỈ **"AI phân tích tổng thể"** (`CoachOffline.jsx`) gọi `tier:'deep'` (bài 4 phần — việc khó nhất); CoachChat/CoachNudge giữ flash. Env ghi đè `GEMINI_MODEL_DEEP`. Pro cũng là '2.5' → vẫn `thinkingBudget:0` (nhất quán; muốn bật thinking là commit tuning riêng). Timeout 28s + maxDuration 30 (mảng 1) là NỀN cho pro chậm hơn. **Cấu trúc 4 phần KHÔNG cần JSON mode**: `scrubFabricatedLines` đã giữ khung [1][2][3][4] + bỏ riêng dòng bịa + phần rỗng→"chưa đủ dữ liệu" (một số bịa KHÔNG làm rớt cả bài).
  - **Luồng**: CoachChat/CoachOffline → `generateCloud` (Gemini). Lỗi → câu báo lỗi (no-key / hết-lượt / mạng) + nút "Thử lại". KHÔNG còn `capable`/`progress`/warm-prefetch/fallback on-device.
  - **iPhone DÙNG ĐƯỢC Coach** (qua Gemini): không còn gate WebGPU; `FocusRail` luôn hiện thẻ; `FocusCoachMobile.jsx` render thẳng CoachChat+CoachOffline (App.jsx truyền goalProps).
  - ⚠️ **COACH CHỦ ĐỘNG — câu nhắc sau mỗi phiên (2026-06-25, mảng 3/6)**: `CoachNudge.jsx` (trong thẻ AI Coach ở `FocusRail`+`FocusCoachMobile`). Khi một phiên HOÀN THÀNH gần đây (history[0] hợp lệ + xong trong ~5 phút), Coach tự sinh MỘT câu bám số phiên vừa xong → hiện ngay, KHỎI bấm hỏi. Dùng `buildNudgeContext` (ghép dòng "Phiên vừa xong: N phút…" vào ĐẦU context để guard cho phép nhắc số phiên đó) + `NUDGE_INSTRUCTION` + `buildLLMChatPrompt` (CHUNG prompt+guard). **AN TOÀN**: chạy NỀN không chặn kết thúc phiên; lỗi/chữ-lạ → IM LẶNG; VẪN qua lưới chống-bịa (cứu-câu). Chống lặp: localStorage `dc-coach-nudge-v1` ghi id phiên đã nhắc (mỗi phiên ≤1 lần, kể cả mount lại trên mobile) + gác recency 5 phút (mở app sau nhiều giờ KHÔNG nhắc phiên cũ). ⚠️ Đổi nhãn dòng "Phiên vừa xong" phải khớp `buildNudgeContext`. Chưa gửi push (post-session người dùng đang ở trong app); push để dành mảng 6 (chuỗi sắp đứt, lúc người dùng VẮNG).
  - ⚠️ **COACH CHỦ ĐỘNG — cảnh báo chuỗi sắp đứt qua PUSH (2026-06-25, mảng 6/6)**: CRON `api/coach-digest.js` + helpers thuần `api/_lib/coachDigest.js` (`evaluateStreakRisk`/`pickActiveBucketLabel`/`buildStreakNudgePayload`, test `api/_tests/coach-digest.test.js`). Mỗi ngày **17:00 VN** (`vercel.json` crons `"0 10 * * *"` UTC) đọc `game_state` (id `singleton`) từ Supabase; nếu chuỗi đang treo (còn chuỗi ≥1 NHƯNG hôm nay theo ngày VN chưa có phiên hoàn thành nào) → đẩy thông báo nhắc giữ chuỗi (kèm "buổi hay làm" nếu ≥40% phiên + đủ mẫu — KHÔNG phải "giờ vàng" goal-based, phát biểu trung thực). TÁI DÙNG hạ tầng push (`_lib/push.js`: `getAdminClient`/`listActivePushSubscriptions`/`sendPushNotification`), bảo vệ `CRON_SECRET` (Vercel Cron tự gửi `Bearer`). KHÔNG cần SQL mới (dùng bảng `game_state`+`push_subscriptions` sẵn có). ⚠️ Vercel Hobby cron = 1 lần/ngày (đủ); thêm function vào `vercel.json` thì file PHẢI tồn tại. Muốn linh hoạt hơn 1 lần/ngày → cân nhắc `pg_cron` Supabase.
  - ⚠️ **"BỘ NÃO ĐÃ ĐÀO TẠO" GIỮ NGUYÊN** (model-agnostic, áp lên Gemini): 2 prompt chuyên gia, lưới chống-bịa (`findFabricatedNumbers`/`findMismatchedPairs`/`findFabricatedFractions`/`stripFabricatedSentences`/`scrubFabricatedLines`/`buildCorrectionNote`), `sanitizeLLMOutput`, `hasForeignScript`, tầng số liệu (`buildAnalystContext`+signals), gợi ý (`coachSuggest`). `GEMINI_API_KEY` đã ở Vercel env + đã bật billing (hết 429).
  - **(Lịch sử)** 2026-06-21: chỉ Qwen on-device. 2026-06-24 (sáng): Gemini chính + Qwen dự phòng. 2026-06-24 (sau): **bỏ hẳn Qwen, chỉ Gemini**.
  - **2 lối vào, đều dùng Gemini:**
    - *Hỏi Coach* (`CoachChat.jsx`) = **chat** với Gemini theo **khung CHUYÊN GIA 3 nhịp** (quan sát số → xu hướng/chân dung → 1 lời khuyên), `buildLLMChatPrompt` + `COACH_CHAT_SYSTEM`. ⚠️ **KHÔNG few-shot** (đã bỏ `COACH_CHAT_FEWSHOT` — model nhỏ dễ nhái khuôn → bịa, giữ nguyên tắc dù nay đã đổi sang Gemini). Có luật **"ĐỌC ĐÚNG GIÁ TRỊ"** (chép NỘI DUNG sau dấu hai chấm; ví dụ trong prompt dùng PLACEHOLDER X/A/B/C — KHÔNG để số cụ thể) + luật **"TÊN MỤC ≠ NỘI DUNG"** (tên loại việc thật LUÔN trong ngoặc kép). Có **câu hỏi mẫu** (`STARTER_CHIPS`) + **"Đề xuất tiếp theo"** theo ngữ cảnh (`src/engine/coach/coachSuggest.js`, THUẦN luật).
      - ⚠️ **LƯỚI CHẶN-BỊA-SỐ (tất định, tuyến phòng thủ chính chống fake)**: `findFabricatedNumbers`/`hasFabricatedNumbers` (`src/engine/coach/guard.js`) — mọi con số KÈM đơn vị-dữ-liệu (giờ/tiếng/phút/phiên/ngày/tuần/lần/%/h) model viết ra mà KHÔNG có trong bảng → coi là BỊA. Chuẩn hoá `h`/`tiếng`↔`giờ`, dấu phẩy↔chấm; số trần (không đơn vị) được miễn trừ; GIỮ báo làm-tròn (13.3≠13). Chạy được ở CI. KHÔNG bắt "đọc nhầm nhãn" (số có thật) — cái đó do prompt + định dạng bảng lo.
      - ⚠️ **PHÒNG THỦ THEO TẦNG (2026-06-24, ultracode workflow 6 agent + phản biện)**: (1) **viết-lại-CÓ-HƯỚNG-DẪN** — bắt số bịa → `buildCorrectionNote`/`appendCorrectionTurn` chèn lượt chỉ ĐÍCH DANH số sai rồi chạy lần 2 (không blind); chữ-lạ vẫn viết-lại-blind. (2) **CỨU-CÂU** thay vì nuke: `stripFabricatedSentences` (CoachChat — bỏ riêng CÂU chứa số bịa, giữ câu sạch) + `scrubFabricatedLines` (CoachOffline — bỏ riêng DÒNG, giữ khung 4 phần, phần rỗng→"chưa đủ dữ liệu"). Bỏ hết → fallback. (3) **CoachOffline NAY CŨNG có guard số** (trước chỉ chống chữ-lạ). Tất cả THUẦN, test `src/engine/coach/guard.test.js`. ⚠️ Đã BỎ (rủi ro báo nhầm): entity-guard tên loại, kiểm cỡ-mẫu trong guard, đơn-vị-kép, dung sai làm-tròn. ⚠️ Landmine đã vá: `BAND_LABEL.vua` = `'vừa (26 phút–44 phút)'` (mốc dưới PHẢI có "phút", nếu không guard báo nhầm "26 phút").
      - Prompt 2 system thêm luật **"KHÔNG CÓ SỐ THÌ NÓI KHÔNG CÓ"** + **"RANH GIỚI HỌC vs NÓI"** (chỉ lấy số giữa `=== DỮ LIỆU THẬT ===`/`=== HẾT ===`, ví dụ trong prompt KHÔNG phải dữ liệu).
      - ⚠️ **GIỌNG VĂN MƯỢT, BỚT ROBOT (2026-06-24)**: COACH_CHAT_SYSTEM có đoạn **"CÁCH VIẾT"** (3 nhịp là DÀN Ý NGẦM, viết 2–4 câu CHẢY LIỀN, cụm nối "thật ra/nhìn rộng ra"…); COACH_OFFLINE_SYSTEM có đoạn **"GIỌNG VĂN"** ([1][2] văn xuôi, [3] quan sát người-hiểu-người). ⚠️ RANH GIỚI: nới Ở CÂU NỐI, SIẾT Ở CON SỐ — mọi luật trung thực giữ NGUYÊN; lưới chống-bịa tất định độc lập với prompt nên mượt KHÔNG tăng bịa. Câu tất-định (fallback/chữ-lạ/placeholder/chip) đã làm mềm (giữ token `chưa đủ dữ liệu`/`Người dùng chưa có phiên nào` mà test/guard khớp). **decoding GIỮ 0.2/0.8** (0.25/0.85 chỉ đổi ở commit riêng sau khi xác nhận không tăng trôi).
      - ⚠️ **LƯỚI PHÂN SỐ BỊA**: `findFabricatedFractions` (`src/engine/coach/guard.js`) — bắt cặp `N/M` ghép sai (vd "phiên sâu 7/18" khi bảng ghi "4/18"); lookbehind chừa số thập phân (13.3). `extractPctSamplePairs` chuẩn hoá `phần trăm`↔`%`. Cắm vào strip/scrub + gác cuối.
      - ⚠️ **LƯỚI GHÉP-SAI %↔CỠ-MẪU (2026-06-24)**: `findMismatchedPairs` (`src/engine/coach/guard.js`) — bắt kiểu bịa tinh vi: % ghép SAI cỡ mẫu (cả hai số đều CÓ trong bảng nhưng ở 2 dòng khác, vd "đạt 79% trên 18 phiên" — 79% là tổng, 18 phiên là của "Học"). BẢO THỦ: chỉ bắt cặp kề-nhau rõ ("X% … trên N đv" / "N đv … (X%)"), BỎ QUA % so-sánh không cỡ mẫu + mẫu phân số (nghi thì tha — đã verify 9/9 paraphrase thật không báo nhầm). Cắm vào `stripFabricatedSentences`/`scrubFabricatedLines` + gác cuối CoachChat/CoachOffline.
      - **CoachChat UX**: lưu hội thoại localStorage `dc-coach-chat-v1` (lượt `restored` KHÔNG vào prompt — số cũ khỏi mồi model; nút "Xoá hội thoại"); nút "Thử lại" + lỗi phân loại (no-key / gemini-4xx hết-quota / timeout); câu hỏi mẫu BÁM tín hiệu thật (`pickSuggestions` có `limit`); empty-state khi chưa có dữ liệu. ⚠️ ĐÃ BỎ warm-prefetch Qwen (Gemini là chính → không tải model nặng lên máy).
    - *AI phân tích tổng thể* (`CoachOffline.jsx`, tên cũ "Coach offline") = Gemini viết **phân tích 4 phần** `[1] Quan sát · [2] Xu hướng · [3] Chân dung & mẫu hình · [4] Thử nghiệm` (`buildLLMPrompt` + `COACH_OFFLINE_SYSTEM` + ví dụ vàng + tự-kiểm).
    - Cả hai gọi thẳng `generateCloud` (Gemini) — không còn engine on-device, không còn gate `aiCapable`. Nút hiện trong thẻ "AI Coach" ở `FocusRail.jsx` (luôn hiện, kể cả iPhone).
  - **Tầng SỐ LIỆU (KHÔNG phải AI, GIỮ LẠI làm nguồn cho Gemini):** `buildAnalystContext` (`src/engine/coach/coachContext.js`) gom các SIGNAL trong `gameMath.js` (giờ vàng, nhịp hôm nay, phiên khuya, bỏ giữa chừng, xu hướng tuần, hiệu chỉnh mục tiêu, loại bị bỏ bê…) + hồ sơ/dự đoán trong `src/engine/coach/coachIntel.js` (`buildFocusProfile`, `generatePredictions`) → mỗi % kèm cỡ mẫu, bỏ tín hiệu thiếu mẫu. Gemini chỉ **diễn đạt lại số đã-tính-sẵn**, khỏi bịa. Hook: `useAnalystContext` (`useCoachContext.js`).
    - ⚠️ **ĐỊNH DẠNG DÒNG chống đọc-nhầm + guard-safe** (2026-06-22): dòng "Loại việc" đã TÁCH MỖI LOẠI MỘT DÒNG, tên loại trong NGOẶC KÉP, bỏ dấu `|` (vd `Loại việc dành nhiều thời gian nhất là "Học": 13.3 giờ qua 18 phiên…` + `Loại việc "Làm Việc": …`); dòng khuya đổi nhãn → `Tỉ lệ đạt mục tiêu của phiên làm sau 22 giờ đêm: …%`; mọi `′` (prime) ĐỔI sang chữ "phút" (ở `coachContext.js` + `BAND_LABEL`/momentum blurb trong `coachIntel.js`) để guard nhận đơn vị + model khỏi đổi ký tự lạ. ⚠️ Đổi chữ các dòng này phải sửa kèm **marker `coachSuggest.js`** (`category`=`startsWith('loại việc')`, `lateNight`=`'phiên làm sau'`+`'so với ban ngày'`, `longTrend`=`startsWith('xu hướng dài hạn')`, `portrait`=`startsWith('chân dung của bạn')`) + fixture `coachSuggest.test.js` + assertion `coachContext.analyst.test.js`. Chip mới `coachSuggest.js`: `longTrend`+`portrait` (KHÔNG vào `STRONG_SIGNALS`); `detectTopics` (số nhiều) cho câu đa-ý + `LOOSE_KW` bỏ từ quá chung.
    - ⚠️ **CHÍNH XÁC SỐ (2026-06-24)**: dòng khuya dùng `late.lateGoalTotal` (KHÔNG phải `lateAttempts`) làm cỡ mẫu cho % (trước ghép sai mẫu→phóng đại); Tổng quan <60 phút in theo "phút" (tránh "~0 giờ"); trung vị mục tiêu ngày dùng `cal.medianDisplay` (`roundGoalValue`, nhất quán với suggested).
    - ⚠️ **BỘ NHỚ LỜI KHUYÊN (2026-06-25, mảng 5/6 — cá nhân hoá Đàm ưu tiên)**: `coachAdviceMemory.js` (thuần+test `coachAdviceMemory.test.js`) ghi lời khuyên chỉnh-MỤC-TIÊU-NGÀY đã đưa + số liệu LÚC ĐÓ vào localStorage `dc-coach-advice-v1` → sau **≥3 ngày** (cửa sổ 3–21 ngày) thêm dòng `Ghi nhớ: khoảng N ngày trước gợi ý chỉnh mục tiêu về X…(khi đó đạt A% trên B ngày)…đối chiếu hiện tại…tương quan`. Nối ở hook `useCoachContext` (đọc bộ nhớ → dòng + ghi lời khuyên hiện tại, write thưa/idempotent); engine nhận qua `opts.adviceMemoryLine`, linePriority=1. ⚠️ **`parseGoalAdviceFromContext` PARSE dòng "Mục tiêu ngày … thử chỉnh về Z phiên/ngày"** — đổi định dạng dòng đó (ở `coachContext`) PHẢI sửa regex này. THUẦN tương quan, không nhân-quả (prompt cấm vì/nên/do…); mọi số nằm trong dòng → guard không báo nhầm.
    - **"Chân dung của bạn"** (HIỂU CHỦ): dòng tổng hợp đặc điểm ổn định (buổi mạnh, độ dài hợp, độ đều, loại việc chủ đạo, tỉ lệ phiên sâu) — ghép từ `profile`, mỗi mảnh kèm cỡ mẫu.
    - **Tín hiệu MỚI (2026-06-24)**: `getWeekendVsWeekdayContrast` (cuối tuần vs trong tuần — dòng "Cuối tuần so với trong tuần:") + `getComebackRate` (quay lại sau 1 ngày nghỉ — dòng "Phục hồi sau ngày nghỉ: N/M lần") — đều gác chặt (chỉ hiện khi nổi bật) + prefix riêng. **streak-at-risk** GỘP vào `predictStreakKeep` (`atRisk` → dòng "Giữ chuỗi" mở bằng "Chuỗi N ngày đang treo", KHÔNG thêm dòng). ⚠️ **CAP** `COACH_MAX_CONTEXT_LINES=18` + `capContextLines` (cắt theo ƯU TIÊN, giữ Tổng quan/Chân dung/Hôm nay + nhóm STRONG, "Ghi chú" cắt trước — KHÔNG slice mù) chống bảng dài làm 3B loạn. Marker coachSuggest mới: `weekendVsWeekday`=has('cuối tuần so với trong tuần'), `comeback`=has('phục hồi sau ngày nghỉ'). ĐÃ HOÃN (làm bảng dài/trùng): category-momentum, session-length-trend.
    - ⚠️ **Tín hiệu MỚI (2026-06-25, mảng 2/6) — "phiên trơn vs ngắt quãng"**: `getInterruptionPattern` (`gameMath.js`) đọc `e.pauseSegments` (số lần tạm dừng đã LƯU SẴN mỗi phiên, trước nay Coach KHÔNG hề thấy — chỉ Thống kê hiện) → dòng `Phiên liền mạch (chạy hết không tạm dừng): S/T phiên (P%)…`. ⚠️ **CHỈ tính phiên CÓ trường `pauseSegments` (Array)** — phiên CŨ trước khi có tính năng lưu thiếu trường → BỎ, KHÔNG coi là trơn (kẻo thổi phồng); gác cỡ mẫu **≥8 phiên có dữ liệu**. Chip mới `flow` (`coachSuggest.js`): catalog+RELATED+KEYWORD_MAP(`lien mach`/`ngat quang`/`tam dung`…)+GATE+`detectSignals` marker `lineStarts('phiên liền mạch')`. Đổi chữ dòng này phải sửa kèm marker đó + fixture `coachSuggest.test.js` + `coachEvalFixtures.js`.
    - **"Xu hướng dài hạn"** (HỌC THEO THỜI GIAN): `getMultiWeekTrend` (`gameMath.js`) — đi lên/xuống/giữ qua nhiều tuần. ⚠️ CHỈ tính tuần CÓ dữ liệu (tuần không mở app ≠ làm 0 phút — tránh số 0 "ma"), cần **≥3 tuần có dữ liệu** (minWeeks=3) mới hiện; `weekKeysDesc` (4 tuần) dựng bằng Date Ở HOOK (`useCoachContext`), engine vẫn thuần.
  - **Giải mã** (Gemini — default ở `api/coach.js` + caller `CoachChat`/`CoachOffline` truyền `temperature 0.2`): `temperature 0.2 · topP 0.8 · maxOutputTokens 800` (CoachOffline 1200). ⚠️ **2026-06-25 sửa lệch**: tài liệu chủ trương 0.2/0.8 nhưng code chạy 0.3/0.9 → kéo về đúng 0.2/0.8 (tác vụ CHÉP-LẠI-SỐ, nhiệt thấp thì model ít chế số/trôi). **Chống "trôi" tiếng nước ngoài**: prompt ép TIẾNG VIỆT 100% + tự-kiểm; `hasForeignScript()` bắt CJK/Hangul/Kana; component tự VIẾT LẠI 1 lần nếu dính.
  - ⚠️ **ĐÃ GỠ WebLLM/Qwen** (2026-06-24) → không còn build-safety webllm. Muốn đổi model AI → sửa `DEFAULT_MODEL`/`FALLBACK_MODEL` trong `api/coach.js` (hoặc env `GEMINI_MODEL`/`GEMINI_MODEL_FALLBACK`). Nếu sau này muốn dựng lại on-device thì xem lịch sử git (commit "bỏ Qwen").
  - ⚠️ **TIMEOUT + maxDuration (2026-06-25)**: `cloudEngine.js` tự đặt AbortController **28s** khi không có signal ngoài (quá hạn → `code:'timeout'`, không treo vô tận); `vercel.json` đặt `maxDuration:30` cho `/api/coach`. Đây là NỀN cho việc dùng model mạnh-hơn-nhưng-chậm-hơn (mảng 4). ⚠️ Thêm function vào `vercel.json` thì file đó PHẢI tồn tại, không build Vercel sẽ lỗi.
  - ⚠️ **BỘ CHẤM ĐIỂM CHỐNG-BỊA (2026-06-25)**: `src/engine/coach/eval.test.js` + `coachEvalFixtures.js` — đẩy ~30 câu mẫu (sạch/bịa/chữ-lạ) qua TOÀN BỘ guard, đo **BẮT %** (recall) + **BÁO NHẦM %** (FPR), in 1 dòng điểm số mỗi `npm test`. Ngưỡng: **FPR=0 (siết — báo nhầm = xoá oan câu thật, hướng sai nguy hiểm nhất)**, BẮT≥90%. Nâng guard → thêm câu mẫu; tụt điểm = guard bị nới tay, phải xem lại.
- **ĐÃ GỠ HẲN (2026-06-21):** ⚡Nhanh theo luật (`src/engine/qa/`), Hỏi Claude (`api/coach.js` + `buildCoachContext`), model nhúng MiniLM + đọc-nghĩa ghi chú (`src/engine/semantic/`, `useNoteThemes`), bộ trả lời theo luật (`generateCoachBriefing`/`generateCoachInsight`, `useCoachInsight`, `CoachCard`, `FocusReport`, `useCoachIntel`), giọng cảm xúc (`coachVoice.js`, `useCoachVoice`, `ai-coach-sim/`, `coachPersonality`). Gỡ deps `@huggingface/transformers` + `@anthropic-ai/sdk` → app/cài đặt nhẹ hơn. **Đừng khôi phục các thứ này trừ khi Đàm yêu cầu.**
- **Engine game thuần** tách khỏi state: công thức ở `src/engine/gameMath.js` + `constants.js`, state ở `src/store/gameStore.js`. Sửa công thức → sửa ở engine, đừng nhồi vào store. Dữ liệu ngoài (Supabase/import) PHẢI đi qua `normalizePersistedGameState`.
- ⚠️ Điểm nóng: `completeFocusSession` trong `gameStore.js` rất dài (~760 dòng) — sửa cẩn thận, dễ sinh bug "dùng giá trị cũ".
- Luôn `npm test` trước khi commit + `npm run build`. ⚠️ **`npm test` NAY CHẠY HAI LƯỢT (2026-08-21)**: `test:fast` (mọi bài nhanh — **số bài thật nằm ở dòng cuối của LƯỢT NÀY**, và nó phải hiện `# skipped 1`) rồi `test:cross` (phép đối chiếu chéo `scene-tri` ↔ `plinth-tri`, **~25 giây** sau ADR-048 — trước đó là 70–90 giây tuỳ tải máy, đo được 68,8 · 85,9 · 86,3 giây ở ba lượt, và **chính con số ấy là thứ đã chỉ ra một hồi quy hiệu năng mà không cổng nào canh**; nó **TỰ IN thời gian chạy** thay vì để tài liệu hứa một con số cố định — xem `TECH_DEBT #70`). Muốn xem nhanh số bài → `npm run test:fast`. ⚠️ **Vế chậm được bỏ qua bằng BIẾN MÔI TRƯỜNG `DC_CROSS_SLOW`, KHÔNG bằng `--test-skip-pattern`** — đã thử cờ ấy và nó **không ăn** (Node có liệt kê cờ, không báo lỗi gì, mà bài chậm vẫn chạy ⇒ lượt "nhanh" âm thầm gánh thêm 70 giây). Một cờ bị bỏ qua trong im lặng là đúng thứ dự án này đã bị cắn nhiều lần; cách hiện tại thì `# skipped 1` HIỆN RA, nên nếu ngày nào nó thôi bỏ qua thì con số ấy tự nói. ⚠️ **Glob test ĐÃ ĐỔI 2026-08-12** (`TECH_DEBT #10`): từ danh sách viết tay từng thư mục MỘT CẤP → nay là `'electron/**/*.test.js' 'src/**/*.test.js' 'api/**/*.test.js'`, **để trong dấu nháy đơn** cho chính `node --test` mở rộng (`sh` không có globstar, bỏ nháy là hỏng). Trước đây test đặt trong thư mục con (`src/components/city/…`) sẽ **im lặng không bao giờ chạy** — nay đặt cạnh file nguồn ở bất kỳ độ sâu nào cũng chạy, đúng quy ước ở `PROJECT_STRUCTURE.md`. Thêm thư mục mới KHÔNG cần sửa `package.json` nữa. Công cụ mắt-soi bảng số liệu model nhận: `node --import ./scripts/register-esm-loader.mjs scripts/coach-sample.mjs` (dựng lịch sử mẫu ~24 giờ + in `buildAnalystContext`). Điểm số chống-bịa in ra bởi `src/engine/coach/eval.test.js`.
- ⚠️ **QUÉT XONG PHẢI CHẤM BẰNG SỐ — `node scripts/sweep-score.mjs <ảnh quét>`** (bài học 2026-08-13, Phase 4F). Dựng được bảng 15 kỷ × 6 chặng mới đi được **nửa đường**: mắt chỉ so được các ô KỀ NHAU, mà hai lỗi nặng nhất từng lọt qua đều là hai ô nằm ở HAI ĐẦU bảng (bình minh ↔ hoàng hôn ở Phase 3Y; kỷ 12 ↔ 13 ở `TECH_DEBT #18`). Công cụ này so **cả 15 cặp chặng và cả 105 cặp kỷ**, lấy toạ độ ô THẲNG từ hồ sơ `.geom.json` do chính `city-preview.mjs` ghi ra (không dò mép bằng màu, không đoán lại `--cell`), và có `--selftest` để chứng minh phần cốt lõi của phép đo thật sự chạy. ⚠️ **Và một con số nghiệm thu phải đi kèm CÔNG CỤ đã đo ra nó**: `TECH_DEBT #18` từng ghi "0/105 ✅" mà không ghi cách đo, nên không ai tái lập được và suýt bị đọc thành "đã xong vĩnh viễn". ⚠️ **Vì đúng lý do đó: mọi con số cặp-kỷ ghi trước 2026-08-16 đều đo bằng bộ lọc "8% điểm ảnh tươi nhất ≈ mái" NAY ĐÃ BỎ (xem gạch đầu dòng dưới cùng của mục này) — chúng đo một đại lượng KHÁC, KHÔNG so trực tiếp được với số mới.** Số hiện hành **(đo lại 2026-08-20, sau BƯỚC C «mặt nước 14/15 kỷ» — lệnh `node scripts/city-preview.mjs --sweep --all --theme light` rồi `node scripts/sweep-score.mjs .city-preview/sweep-light-ky1-15.png`; md5 ảnh `c210cbe93adaa8fc5ea2bd7eafd1dead`)**: **15/15 cặp chặng ĐẠT** (gần nhất bình minh↔chiều = **13,96**) và **105/105 cặp kỷ ĐẠT** (gần nhất kỷ 7↔8 = **21,8** · trung vị **40,7**). ⚠️ **CẬP NHẬT 2026-08-21 — VÀ MỘT PHÁT HIỆN KIỂU `TECH_DEBT #43` NGAY TRONG CHÍNH DÒNG NÀY.** Đo lại mốc nền TẠI HEAD (`dfd2b15`, trong một `git worktree` riêng) thì bộ số ở trên **đã trôi**: thật ra là **15,16 · 22,22 · trung vị 39,81** chứ không phải 13,96 · 21,8 · 40,7 — hai commit §1(B) nằm ở HEAD đã đổi chúng mà dòng này không được cập nhật theo. ⇒ ***mỗi phase PHẢI tự đo lại mốc nền của mình, đừng chép cột "sau" của phase trước*** (đúng bài học #43, lặp lại ở một bảng khác). Sau bản vá xoá-cái-bệ (ADR-046 + ADR-047), đo bằng CÙNG lệnh: **cặp chặng gần nhất 16,27 · 0/15** · **cặp kỷ gần nhất 22,13 · trung vị 39,35 · 0/105** (md5 ảnh `3a678cb01e699d0fef6f542f36c30c0a`). ⚠️ **SỐ SAU §1(2) (2026-08-21, cả hai vế dựng lại SẠCH trong cùng phiên): cặp chặng gần nhất **15,36** · 0/15 · cặp kỷ gần nhất **22,32** · trung vị **39,83** · 0/105.** Và điều PHẢI đọc ra: **§1(2) chỉ dịch 0,04** trên trục chặng — toàn bộ 0,87 tụt đi là do **§1(1)**, bản vá chiều quay tam giác đưa diện tích mặt đường NHÌN THẤY ĐƯỢC từ 80,8% lên 100,0%. Mặt đường phản ứng với giờ trong ngày YẾU hơn trung bình cả cảnh ⇒ nó là chi tiết CHUNG trên trục chặng nhưng chi tiết PHÂN BIỆT trên trục kỷ (trục kỷ TỐT LÊN cùng lúc: 22,13 → 22,32, trung vị 39,35 → 39,83). ⚠️ **Nếu chép con số 16,27 ở trên làm mốc nền thì sẽ kết luận sai rằng §1(2) tiêu 0,91** — đúng cái bẫy `TECH_DEBT #43`; mốc nền tại `0fbd47f` được TỰ ĐO ra **15,40**. Biên còn lại trên ngưỡng mắt 12 nay chỉ **28%**, và mốc «<17 = khuyết tật thật» của Đàm **đã chạm từ trước §1(2)**. Bảng đầy đủ + lệnh tái lập: `PERFORMANCE.md` mục «Phase 14 §1(2)». ⚠️ **SỐ HIỆN HÀNH (2026-08-21, sau Phase 14 §1(3) «khu phố»): cặp chặng gần nhất **14,39** · 0/15 · cặp kỷ gần nhất **22,09** · trung vị **38,34** · 0/105** — cổng không-trôi VẪN ĐẠT, nhưng trục chặng tụt **0,97**, **cú tụt một-phase lớn nhất từ trước tới nay** (20,7 → 16,5 → 15,7 → 15,16 → 16,27 → 15,40 → 15,36 → **14,39**), và mốc thứ ba «<14» của Đàm nay chỉ còn cách **0,39**. ⚠️ Và điều PHẢI đọc ra nằm ở phép TÁCH BA DẢI của đúng cặp yếu nhất (6h↔15h): **trời 8,46 → 8,38 (−0,08)** · thành phố 11,95 → 10,73 (−1,22) · đất 22,21 → 20,88 (−1,34). Khu phố **không chạm vào dải trời một chút nào**, mà dải trời từ lâu đã là dải YẾU NHẤT bảng ⇒ **cần gạt để nâng trục chặng nằm ở BẦU TRỜI lúc 6h so với 15h, không nằm ở thành phố**. Thêm chi tiết vào thành phố chỉ pha loãng thêm (đúng họ `TECH_DEBT #22`). Bảng đầy đủ + lệnh tái lập: `PERFORMANCE.md` mục «Phase 14 §1(3)». ⚠️ **ĐÂY LÀ LẦN ĐẦU TRỤC CHẶNG ĐI LÊN sau ba lần tụt liên tiếp** (20,7 → 16,5 → 15,7 → 15,16 → **16,27**), và lý do khớp đúng bài học ngay bên dưới: đồng bằng nay **gợn liên tục thay vì phẳng tuyệt đối**, mà đất gợn thì có sườn hướng nắng ⇒ nó PHẢN ỨNG với giờ trong ngày, tức là chi tiết PHÂN BIỆT trên trục chặng chứ không phải chi tiết CHUNG. Trục kỷ gần như đứng yên (22,22 → 22,13 · trung vị 39,81 → 39,35) — đúng như phải thế, vì bản vá này áp CÙNG một luật cho cả 15 kỷ. ⚠️ **ĐÍNH CHÍNH 2026-08-20 — DÒNG NÀY TỪNG GHI «14,0», VÀ PHÉP LÀM TRÒN ẤY GIẤU MẤT MỘT ĐIỀU KIỆN ĐÃ KÍCH HOẠT.** Đàm ra ba mốc cho trục chặng: ≥20,7 = tự phục hồi · <17 = khuyết tật thật · **<14 = phải làm vùng quê đổi theo giờ, KHÔNG nới ngưỡng**. Giá trị thật **13,9616 < 14**, tức mốc thứ ba ĐÃ chạm; viết «14,0» thì nó đọc thành «vừa đúng ngưỡng, tạm ổn». ⇒ **Một con số dùng để chấm một cái cổng thì phải in ra ĐỦ CHỮ SỐ để phân biệt được hai phía của cổng ấy** — làm tròn là một phép biến đổi, và như mọi phép biến đổi nó phải được hỏi *"nó có nuốt mất câu trả lời không?"*. ⚠️ **VÀ LỜI GIẢI THÍCH ĐI KÈM CON SỐ ẤY CŨNG SAI — ĐÃ ĐO VÀ BÁC BỎ.** Dòng này từng chẩn đoán: *"vùng quê và mặt nước giống hệt nhau ở cả 6 chặng ngày nên chúng pha loãng khác biệt giữa các chặng"*. Nghe cực kỳ xuôi, và **sai về vế vùng quê**. Đo riêng ba dải của chính bản quét ấy (trung bình 15 kỷ, thang RGB/255): dải **mặt đất** đi **68,8 → 82,2 → 97,6 → 79,8 → 61,2 → 36,7** qua sáu chặng — tức vùng quê PHẢN ỨNG rất mạnh với giờ trong ngày. Và ở đúng cặp yếu nhất (6h↔15h) thì mặt đất là dải **KHOẺ NHẤT** (19,14), còn hai dải kéo tổng xuống lại là **trời (9,29)** và **thành phố (11,50)** — cả hai đã DƯỚI ngưỡng mắt 12. Bản vá theo chẩn đoán cũ ("làm vùng quê đổi theo giờ") vì vậy sẽ đi bồi thêm cho dải đang mạnh nhất và không chạm vào hai dải đang hỏng. ⇒ **Trước khi đi sửa theo một chẩn đoán, hãy TÁCH con số gộp ra từng thành phần và hỏi thành phần nào thật sự kéo nó xuống** (cùng họ `TECH_DEBT #22`). ⚠️ **THÊM MỘT ĐIỀU PHẢI BIẾT: TRỤC CHẶNG ĐANG ĐO BẰNG ĐÚNG CÁCH MÀ TRỤC KỶ ĐÃ TỪ BỎ.** `sweep-score.mjs` tính trục chặng bằng cách **gộp 15 kỷ thành một vector trung bình rồi mới so** — đúng thứ mà chú thích của trục KỶ trong chính file ấy cấm (*"gộp trước thì hai kỷ khác nhau ở bình minh nhưng ngược chiều ở hoàng hôn sẽ TRIỆT TIÊU nhau"*). Đo cả hai cách trên cùng hai bản quét: **gộp-trước 15,75 → 13,96 (−1,79)** · **so-từng-kỷ 17,63 → 17,41 (−0,22)**, và kỷ tệ nhất trong cặp ấy còn **TỐT LÊN** (9,09 → 9,70). ⚠️ **KHÔNG được lặng lẽ đổi sang cách B để lấy con số đẹp** — ba mốc 20,7/17/14 đều hiệu chuẩn trên cách A, đổi ruột phép đo mà giữ ngưỡng cũ là tạo ra một ngưỡng chưa hiệu chuẩn (đúng cái phễu Phase 9A). Đây là một quyết định của Đàm, ghi ở `TECH_DEBT #55`. ⚠️ **ĐÂY LÀ LẦN THỨ BA TRỤC CHẶNG TỤT LIÊN TIẾP — 20,7 → 16,5 → 15,7 → 13,96.** Biên còn lại chỉ **16%** trên ngưỡng mắt 12. ⇒ **Phase sau thêm bất cứ thứ gì KHÔNG phản ứng với giờ trong ngày thì phải đo lại con số này TRƯỚC, và biết rằng mình đang tiêu vào 1,96 điểm cuối cùng.** Trục KỶ ngược lại vẫn khoẻ (21,5 → 21,8 · trung vị 40,8 → 40,7, đứng yên) vì mỗi kỷ một hình nước riêng ⇒ nước là chi tiết PHÂN BIỆT trên trục kỷ nhưng chi tiết CHUNG trên trục chặng — cùng một thứ, hai trục, hai dấu ngược nhau. ⚠️ **HAI TRỤC ĐI HAI HƯỚNG NGƯỢC NHAU, VÀ ĐÓ LÀ ĐIỀU PHẢI ĐỌC RA — đừng chỉ đọc dòng "105/105 ✓".** Trục KỶ **đi lên**: trung vị 37,6 → **40,8**, đảo lại đà tụt của các phase 10–12 (41,1 sau Phase 9C → 37,6) — vì vùng quê mang **mật độ và loài cây RIÊNG TỪNG KỶ** (`floraStyle.js`), tức nó thêm chi tiết PHÂN BIỆT chứ không phải chi tiết CHUNG. ⇒ **Bài học chung: một phép đo lấy trung bình trên vùng rộng sẽ tụt điểm mỗi lần ta thêm thứ gì đó KHÔNG phụ thuộc trục nó đang đo** (cùng họ `TECH_DEBT #22` và bài học ngân sách tam giác 2026-08-17). Theo dõi CẢ HAI con số, và khi một trục tụt thì hỏi *"thứ vừa thêm có phụ thuộc trục ấy không?"* — rồi **tách con số gộp ra từng dải để kiểm câu trả lời**, đừng dừng ở chỗ nghe xuôi.
  - ⚠️ **LẦN THỨ 16 CÔNG CỤ NÓI DỐI — VÀ NÓ NẰM NGAY TRONG CÔNG CỤ VỪA VIẾT Ở TRÊN** (2026-08-13, Phase 4G). `sweep-score.mjs` **chép lại** công thức hình học của `city-preview.mjs` kèm mặc định `--cell 260`, trong khi mặc định bên kia là **300**. Cỡ ô không phải tuỳ chọn của phép đo — nó là **sự thật về tấm ảnh**; đoán sai thì bước nhảy hàng lệch 30px, tới hàng cuối lệch **420px**, tức đang chấm màu của một kỷ KHÁC. Chạy đúng như vậy ra một bộ số bịa hoàn chỉnh và rất thuyết phục: *"5/105 cặp kỷ + 1/15 cặp chặng dưới ngưỡng, trung vị 106,4"* — trong đó có cả cặp bình-minh↔hoàng-hôn "hỏng" mà Phase 3Y đã sửa xong từ lâu. Số thật (đo lại bằng công cụ đã vá): **0/15 và 2/105, trung vị 44,6**. ⇒ **Ba luật rút ra**: **(a)** *"một luật một công thức"* áp cho cả **cặp công cụ dựng ↔ công cụ đo**, không chỉ cho mã sản phẩm ↔ bài test — nay `city-preview.mjs` ghi kèm mỗi ảnh một hồ sơ `.geom.json`, và `sweep-score.mjs` **từ chối chạy nếu thiếu** thay vì đoán; **(b)** **phép tự-kiểm phải chạm tới TỪNG CHIỀU nó muốn bảo chứng** — bản cũ so hai ô **cùng nằm ở hàng 0** nên bảo chứng được bước nhảy CỘT mà mù hoàn toàn với bước nhảy HÀNG, đúng chiều đang sai, và vẫn báo ✓ (cùng họ với "duyệt danh sách theo thứ tự" ở `daylight.test.js` và với `--selftest` chỉ chứng minh bộ lọc CÓ chạy chứ không chứng minh nó chọn ĐÚNG); **(c)** ⚠️ **một câu tự trấn an cũng phải được kiểm như một con số** — `TECH_DEBT #19` từng ghi *"ổn định qua hai cỡ ô 260 và 300 nên không phải nhiễu"*, nghe như kiểm chứng chéo nhưng **không thể đúng**, vì hai cỡ ô không thể cùng đúng trên một tấm ảnh. Câu đó chính là cái bẫy, viết bằng chính tay mình.
  - ⚠️ **MỘT "THỨ ĐẠI DIỆN" LÀ MỘT GIẢ ĐỊNH MỸ THUẬT ĐỘI LỐT MỘT PHÉP ĐO — và khi mỹ thuật đổi thì nó chết trong im lặng, ở một phase khác, do tay một người khác** (2026-08-16, đóng `TECH_DEBT #22`). Bộ lọc "8% điểm ảnh tươi nhất **≈ mái**" chưa bao giờ đo mái; nó đo *thứ tươi nhất khung hình* rồi **gọi** đó là mái. Câu "≈" ấy là một giả định không được viết ra, và nó đúng suốt thời kỳ mái suy từ `accentColor` (màu nhấn giao diện, chọn cho chữ nổi trên nền nên luôn rực hơn mọi thứ). **Phase 6B** đổi mái sang vật liệu lợp thật (đá phiến `#586a89`, bê tông `#717b65`, tranh, gạch bùn — phần lớn XỈN) và **Phase 8D** cho mỗi kỷ một thảm thực vật riêng ⇒ thứ tươi nhất còn lại là **CỎ**. Từ đó công cụ chấm cỏ và in ra 15 con số rác rất thuyết phục, ba phase liền không ai biết. ⇒ **Ba luật**: **(a)** ⚠️ **đừng vá một proxy hỏng bằng một proxy khôn hơn** — bản vá đầu tiên nghĩ tới ("đối chiếu điểm ảnh với `roofColor` mà `eraStyle.js` khai") vừa **vòng tròn** (chọn điểm ảnh theo màu đã khai rồi đo xem màu có khớp màu đã khai không) vừa **bất khả thi**: đo ở tầng dữ liệu thì **4/15 kỷ khai mái TRÙNG vật liệu tường** (kỷ 3 mudbrick, 12 và 13 concrete, 14 glass), nên kể cả hỏi thẳng nhà máy hình học "nhóm tam giác nào là mái" cũng trả về một nhóm gộp cả mái lẫn tường — mái **không tách được ra ngay từ NGUỒN**, và đúng cặp 12↔13 hay bị kêu trùng lại là cặp dùng chung CẢ HAI vật liệu; **(b)** **hỏi lại xem cái proxy sinh ra để giải bài toán gì, rồi giải thẳng bài toán đó** — ở đây bệnh gốc là *"trung bình trên vùng quá rộng pha loãng tín hiệu ~10 lần"*, chữa được bằng cách **thu vùng lấy trung bình xuống 1/18** (lưới 6×3 ô con, `scripts/sweepMetric.mjs`) chứ không cần biết "mái" là gì; phép đo mới không chứa giả định mỹ thuật nào nên **không thể chết lần thứ ba**; **(c)** **giữ nguyên ĐƠN VỊ khi thay ruột phép đo** — vẫn là khoảng cách RGB/255 nên ngưỡng mắt 12 hiệu chuẩn ở Phase 3Y còn dùng được; đổi sang biểu đồ tần suất/EMD là tạo ra một ngưỡng chưa hiệu chuẩn, đúng cái phễu Phase 9A đã dạy. ⚠️ Kèm hai bài phụ: **so TỪNG chặng rồi lấy trung bình các khoảng cách**, đừng gộp 6 chặng thành một vector rồi mới so (gộp trước thì hai kỷ khác nhau ở bình minh nhưng ngược chiều ở hoàng hôn sẽ **triệt tiêu** nhau); và **`--selftest` phải đổi nhãn theo việc nó đang làm** — bản đầu vẫn in "lưới 6×3" trong khi đã thu về 1×1, đúng loại lỗi NHÃN (số đúng, tên sai) đã cắn ở `frame-fit.mjs`.
- ⚠️ **CÔNG CỤ ĐO NÓI DỐI LẦN THỨ 20 VÀ 21 — CẢ HAI ĐỀU NẰM TRONG CÔNG CỤ VỪA VIẾT RA TRONG CHÍNH PHIÊN ẤY, VÀ CẢ HAI ĐỀU LÀ CÙNG MỘT LỖI: VIẾT MỘT QUAN HỆ THÀNH MỘT MỨC TUYỆT ĐỐI** (2026-08-16, Phase 9D). `road-score.mjs` sinh ra để đóng `TECH_DEBT #30` — một mục nói về *"đường so với đất"*. Vậy mà cả hai ngưỡng của nó đều viết thành số tuyệt đối, và mỗi cái đẻ ra một loại báo động giả **ngược nhau**. **(a) BÁO OAN "rãnh đen" ban đêm**: `đáy < 0,12` kêu kỷ 3 lúc 22h có rãnh (đáy đường 0,044) — nhưng đáy của ĐẤT trong cùng khung hình là 0,127, tức cả cảnh vốn phải tối; chênh lệch đường↔đất lúc 22h (−0,083) còn NHỎ HƠN lúc 12h (−0,202), nghĩa là ban đêm con đường **hoà vào đất hơn** chứ không thủng ra. Một mức tuyệt đối không tách nổi "đường thành hố" khỏi "trời tối" — đúng bài học sương mù Phase 9A, lặp lại y hệt. **(b) BÁO OAN "đường tàng hình" ở kỷ 7**: `cách` đo bằng riêng ĐỘ SÁNG, mà kỷ 7 lúc ấy có đường nâu-vàng trên đất nâu-vàng nên chênh sáng chỉ 0,001 — trong khi nhìn ảnh vẫn thấy con đường, vì nó tách bằng SẮC. Độ sáng là MỘT trong ba trục của màu (Phase 3Y: đo một trục thì vừa báo nhầm vừa bỏ sót). ⇒ **Ba luật**: (1) đọc mục nợ mình đang đi đóng rồi hỏi *"câu đó có chữ 'so với' không?"* — có thì mọi con số dùng để đóng nó cũng phải là con số so sánh; (2) mỗi ngưỡng phải kèm **đối chứng nhốt bộ số hỏng cũ** (nay `--selftest` có ca dựng lại đúng 0,113-trên-nền-0,406 của #30 và ĐÒI phải bắt được, cạnh một ca cảnh đêm tối đều ĐÒI phải tha); (3) ⚠️ **và cái bẫy sâu nhất: (b) suýt làm tôi kết luận SAI HAI LẦN theo hai hướng ngược nhau** — lần đầu tin công cụ ("kỷ 7 hỏng"), lần sau tin mắt ("công cụ hỏng, kỷ 7 ổn"). Cả hai đều sai một nửa: công cụ HỎNG THẬT (đo thiếu trục), mà kỷ 7 cũng HỎNG THẬT (lấy nhầm đá xây tường làm đá lát đường) — chỉ là hỏng vì lý do khác hẳn thứ công cụ tố. **Sửa công cụ xong PHẢI đo lại, đừng cho rằng cái báo động giả ấy che một sự thật lành.**
- ⚠️ **MỘT NGÂN SÁCH TỰ TÍNH MÀ CHƯA BAO GIỜ ĐƯỢC ĐẶT CẠNH SỰ THẬT THÌ KHÔNG PHẢI NGÂN SÁCH — VÀ ĐÂY LÀ LẦN THỨ HAI CÙNG MỘT HÌNH DẠNG SAI, DÙ BÀI HỌC LẦN ĐẦU ĐÃ ĐƯỢC VIẾT RA Ở ĐÂY** (2026-08-17, Performance Gate). `sceneGraph.js` **dự đoán** số tam giác bằng công thức riêng (`buildingTriangles + surfaceTriangles + residents × 24`), trong khi three biết chính xác qua `renderer.info.render.triangles`. Đặt hai bên cạnh nhau lần đầu tiên: HUD báo **34.622**, máy vẽ **78.748** — **thiếu 56%**, và lệch **đúng 44.126 ở CẢ 15 kỷ**. Hằng số ấy chính là hai thứ công thức không biết tới: **vòm trời** (960) và **rặng núi chân trời** thêm ở **Phase 9A** (43.166); người thêm chúng không sửa công thức và **không có gì đỏ lên**, vì công thức chỉ được so với chính nó. Sai theo hướng **trấn an** — loại sai tệ nhất cho một đồng hồ đo, vì nó khiến mọi quyết định *"còn dư sức, thêm chi tiết đi"* dựa trên một ngân sách bịa. ⇒ **Ba luật**: (a) **đừng DỰ ĐOÁN thứ có thể ĐO** — bản vá là `countSceneTriangles(scene)` duyệt scene graph theo đúng luật `WebGLRenderer`, một phép đo trên chính thứ sẽ được vẽ thì không thể lạc hậu khi phase sau thêm khối mới; (b) một đồng hồ đo (HUD) phải đọc từ **đồng hồ** (`renderer.info`), không đọc từ **dự báo** — nay `publishStats()` đè lên bằng số thật của khung vừa vẽ; (c) ⚠️ **và cái đáng sợ nhất: chú thích của `countTriangles` (`parts.js`) đã tự cảnh báo đúng cái bẫy này từ Phase 8B** (*"một ngân sách tự tính riêng mà lệch với thực tế thì còn tệ hơn không có ngân sách"*) — bài học có sẵn, viết rõ ràng, và vẫn tái diễn ở một file khác. **Một bài học được ghi ra KHÔNG chặn được gì; chỉ một bài TEST mới chặn được** (`sceneStats.test.js`, 3 bài đã thử-cho-đỏ, tự duyệt cảnh bằng mã của nó rồi so với mã sản phẩm — chạy CẢ HAI bên, không bên nào so với hằng số thứ ba).
- ⚠️ **CÔNG CỤ GIAO CHO ĐÀM CHẠY PHẢI ĐƯỢC NGHIỆM THU Ở MÔI TRƯỜNG CỦA ĐÀM, KHÔNG PHẢI Ở HỘP CÁT — VÀ KHI HỎNG THÌ PHẢI IN RA *LỆNH CẦN GÕ*, KHÔNG IN NGUYÊN NHÂN KỸ THUẬT** (bài học 2026-08-17, vòng 4 Performance Gate). Bộ đo đã "xong" và "nghiệm thu đủ" ở vòng 2 — test xanh, lint sạch, thử ngược đạt. Đưa cho Đàm chạy thì anh mất **5 vòng qua lại** vì bốn thứ **không cái nào liên quan tới hiệu năng**: nhánh chưa `git fetch` · `package.json` bẩn chặn `checkout` · `three` chưa cài (đổ ra 20 dòng ngăn xếp Vite) · đường dẫn đầy dấu tiếng Việt chưa ai thử. Cả bốn đều là **khoảng cách giữa môi trường viết ra công cụ và môi trường chạy nó**: hộp cát AI có đường dẫn ASCII không dấu cách, `node_modules` luôn đúng, nhánh luôn có sẵn. ⇒ **Ba luật**: (a) *"một thông báo lỗi đúng về mặt kỹ thuật mà người nhận không biết phải làm gì thì bằng không có thông báo"* — mọi ❌ phải kèm **ĐÚNG MỘT lệnh** copy-paste được; (b) **kiểm điều kiện tiên quyết TRƯỚC, xếp rẻ-trước-đắt-sau** — đừng để người dùng đợi 20 giây gói bundle rồi mới báo thiếu một thư mục kiểm được trong 1 mili-giây, và nhớ rằng **hai triệu chứng giống hệt nhau có thể cần hai cách sửa ngược nhau** (đứng nhầm thư mục vs chưa cài thư viện — cả hai đều "không có `node_modules`", nhưng khuyên `npm install` cho ca đầu là khuyên sai); (c) ⚠️ **với lỗi build thì NGUYÊN NHÂN nằm ở ĐẦU còn ngăn xếp ở CUỐI** — bản cũ `tail -n 20` giữ đúng phần vô dụng (`at viteLog…`) và vứt mất dòng `Rolldown failed to resolve import "three"`, khiến Đàm phải tự chạy một lệnh khác mới thấy. Nay in **đầu + cuối** có nhãn, lọc dòng ngăn xếp khỏi phần trích, và luôn ghi đường dẫn file log đầy đủ. Khoá bằng `scripts/benchMacbookSource.test.js` (8 bài, tất cả đã thử-cho-đỏ).
  - ⚠️ **KÈM THEO — PHÉP THỬ NGƯỢC NÓI DỐI HAI LẦN TRONG MỘT PHIÊN, VÀ CẢ HAI ĐỀU DO CÔNG CỤ PHÁ CHỨ KHÔNG DO MỤC KIỂM.** (1) Bản đầu của bộ thử ngược dùng `git stash` trên **chính kho đang sửa** — nó stash luôn cái script đang được thử, nên mục kiểm cuối thật ra chạy bằng bản CŨ (và treo 5 phút vì bản cũ không có cờ dừng-sớm). Vá: mọi phép phá làm trong **kho git tạm**, không đụng kho thật. (2) Phép phá viết bằng `perl -0pi -e` chứa `$PWD`/`$thu_file` trong chuỗi thay thế — **perl nội suy chúng thành rỗng**, nên thứ chèn vào không phải "biến để trần" mà là một dòng vô nghĩa; bài test đúng thì không đỏ, và tôi suýt kết luận phép kiểm nháy kép bị mù. ⇒ **Khi phá mà không nổ, nghi CHÍNH PHÉP PHÁ trước** (Phase 8A), và cụ thể hơn: **đừng viết phép phá bằng một ngôn ngữ có nội suy biến khi thứ mình chèn vào cũng dùng cú pháp `$`** — dùng thay-thế-chuỗi-nguyên-văn (python `str.replace`) rồi `grep` lại xem dòng chèn vào có ĐÚNG hình dạng mình định chèn không.
  - ⚠️ **KÈM THEO — MỘT CẢNH BÁO KÊU OAN CÒN TỆ HƠN KHÔNG CÓ CẢNH BÁO, và nó đang đúng nhờ một file chẳng liên quan.** Mục kiểm "cây git có sạch không" tự tố cáo **sản phẩm của chính nó**: script ghi báo cáo vào `.city-preview/` **trước khi** preflight chạy, nên `git status` thấy nó là "thay đổi chưa lưu". Trong kho thật điều đó bị `.gitignore` che đi — tức lời cảnh báo ấy xưa nay đúng **nhờ một file không liên quan gì tới nó**, đúng hình dạng quả mìn đã cắn ở Phase 7D (`roadColor` đúng nhờ một hằng số ở file khác). Phát hiện được **chỉ vì** phép thử ngược chạy trong một kho git tạm không có `.gitignore`. ⇒ Một mục kiểm phải **trừ chính đầu ra của mình ra** trước khi phán xét môi trường.
- ⚠️ **DỰ ÁN CÓ NGÂN SÁCH TAM GIÁC VÀ NGÂN SÁCH LỆNH VẼ, NHƯNG CHƯA BAO GIỜ CÓ NGÂN SÁCH *THỜI GIAN DỰNG CẢNH* — VÀ HAI TRỤC ẤY CÓ THỂ ĐI NGƯỢC NHAU** (2026-08-21, ADR-048, `TECH_DEBT #70`). ADR-046 thêm **0 tam giác, 0 lệnh vẽ, 0 nguồn sáng** mà tốn thêm **+28 giây CPU**: một lượt dựng cảnh 15 kỷ đi từ 40,9 lên 69,3 giây, `sceneStats.test.js` từ 564 lên **827 giây** — và **build xanh · lint sạch · 960 bài test xanh**, không một cảnh báo nào. Nó chỉ lộ ra vì một việc đo tình cờ chạy nền. Gốc: `horizon.heightAt` gọi `terrain.nenKho` ở **mỗi đỉnh** của lưới lớn nhất cảnh, mà mỗi lần lấy mẫu nhiễu tốn **4 lần băm FNV-1a trên một chuỗi ~20 ký tự** — cái giá ấy vốn có sẵn, ADR-046 chỉ làm nó lộ ra. Vá bằng cách **nhớ lại giá trị nút lưới** (`noise.js`): 66,41 → **20,18 giây**, tức nhanh hơn cả mốc TRƯỚC ADR-046 (33,52), và **trùng từng byte ở cả 15 kỷ** (băm MD5, có đối chứng) — ⚠️ nhưng phải băm **HAI lượt** mới phủ hết: lượt đầu tôi mới băm mảng đỉnh hai lưới địa hình rồi suýt gọi là xong, trong khi `outskirts.js` và `setting.js` cũng gọi thẳng `valueNoise`; **một lời hứa “không đổi gì” phải được kiểm ở MỌI người dùng của thứ vừa sửa, không chỉ ở người dùng dễ nghĩ tới nhất** (cùng họ *“đổi một luật thì `grep` chính cái luật ấy trên toàn cây”*, Phase 4D). ⇒ **Bốn luật**: **(a)** sau mỗi phase đụng `terrain.js`/`horizon.js`/`noise.js`/`terrainMesh.js` thì **đo lại THỜI GIAN DỰNG**, đừng chỉ đọc bảng tam giác — hai đại lượng ấy đi cùng chiều suốt nhiều phase nên rất dễ tưởng một cổng canh được cả hai; **(b)** ⚠️ **một bất biến ĐÚNG THEO CẤU TẠO thì KHÔNG phải một cái gác — nó chỉ TRÔNG GIỐNG một cái gác**: tôi viết `assert.equal(nut, daGhi)` để bắt đụng khoá với lý lẽ "hai đường đo độc lập", rồi phép thử ngược cho thấy nó **không thể** đỏ, vì `if (co !== undefined) return co;` làm một khoá BỊ ĐỤNG trông y hệt một lần TRÚNG bộ nhớ nên biến đếm không bao giờ đếm thừa. Đây là biến thể khó thấy hơn của *"một bài test chưa từng thấy đỏ thì chưa phải test"* — không phải *chưa* đỏ, mà là **không thể** đỏ, và lý do nằm ở một dòng cách đó vài dòng; **(c)** **một phép đo THỜI GIAN không được chồng lấn với một phép đo thời gian khác** — ba lượt đầu chạy song song trên máy 4 nhân, ba bên giành CPU của nhau, không con số nào so được với con số nào (vế thứ hai của luật "phép đo và lần sửa mã không được chồng lấn", 2026-08-18); **(d)** ⚠️ **và bản vá ĐẦU TIÊN đã bị hoàn tác vì chú thích của nó bị chính số đo của nó bác bỏ**: một phép chặn-sớm trong `nenKho` nghe cực kỳ hợp lý, tôi viết kèm câu "tiết kiệm 34,1 giây", đo thật ra **1%** (67,51 → 66,80). Ship một câu như thế là đúng cái bẫy *"một câu tự trấn an cũng phải được kiểm như một con số"*. ⚠️ Một cái gác **NHANH-CHẬM** được phép rộng tay theo luật khác hẳn một cái gác **ĐÚNG-SAI**: biên nhớ `BIEN_NHO = 4096` rộng gấp ~150 lần biên thật đo được (`ix, iy ∈ [−21, 27]`) và điều đó **không phải** cái phễu Phase 9A, vì ra ngoài biên chỉ CHẬM chứ không SAI.
- ⚠️ **NGÂN SÁCH HIỆU NĂNG THÀNH PHỐ 3D — ĐÃ ĐO TRÊN MÁY THẬT, ĐỌC `PERFORMANCE.md` TRƯỚC KHI THÊM BẤT CỨ GÌ VÀO CẢNH** (2026-08-17, Apple M3 · ANGLE Metal · cửa sổ 1100×700 · DPR 2 · lệnh `bash scripts/bench-macbook.sh`). Kết luận: **CÒN NHIỀU DƯ ĐỊA — dư 3,2 lần** (cảnh chậm nhất 5,20 ms trên trần 16,67 ms của 60 hình/giây; toàn bộ 24 cảnh nằm trong 3,90–5,20 ms). Mô hình chi phí: **≈ 0,87 ms cố định + 1,14 ms mỗi TRIỆU ĐIỂM ẢNH THẬT** (đã nhân DPR 2 — dùng số điểm ảnh CSS là sai gấp 4 lần) ⇒ **80% chi phí theo ĐIỂM ẢNH, 20% cố định**. ⇒ **Hình học thì RẺ** (tam giác thành phố chênh 43% giữa kỷ 3 và 11 mà thời gian chỉ chênh 2,4%; tăng 3–5× tam giác vẫn chưa đo được) · **điểm ảnh và ÁNH SÁNG mới ĐẮT** (bật đèn lúc 22h làm chậm thêm ≈0,8 ms = +19% ở CẢ 4 kỷ). Giữ mức làm việc **8 ms** (chừa gấp đôi phòng hờ) ⇒ **còn ~3 ms mỗi khung để tiêu**: hình học gần như không giới hạn, ánh sáng/shader ~1,6× hiện tại hoặc thêm ~3–4 nguồn sáng. ⚠️ **ĐỪNG HẠ DPR** — đó là cách "tối ưu" hiệu quả nhất và cũng là cách phá hình ảnh nhanh nhất, mà ta KHÔNG cần vì còn dư 3,2 lần (ưu tiên vĩnh viễn: *chất lượng hình ảnh Desktop > hiệu năng*). ⚠️ **ĐỪNG ĐỤNG RẶNG NÚI CHÂN TRỜI**: nó chiếm 54–63% hình học mỗi khung nhưng **0 ms đo được**. ⚠️ Chi phí **dựng lại bản đồ bóng vẫn NẰM DƯỚI NHIỄU ở mọi cảnh ⇒ CHƯA ĐO ĐƯỢC**, tuyệt đối không trích nó thành một con số. ⚠️ Ba giới hạn: chỉ đo phần VẼ 3D (chưa gồm React/ghép ảnh) · gắn với cửa sổ 1100×700 (80% chi phí theo điểm ảnh nên đổi cỡ là đổi số) · Air không quạt nên chạy LÂU chưa kiểm. ⚠️ **iPhone CHƯA ĐO — đừng suy ra** (`TECH_DEBT #23`/`#26`).
  - ⚠️ **BÀI HỌC: BA VÒNG ĐI TÌM "KỶ NÀO NẶNG", VÀ CÂU TRẢ LỜI LÀ *KHÔNG KỶ NÀO*.** Trục đúng hoá ra là **GIỜ TRONG NGÀY** (đèn bật lúc 22h, shader 4→5). Vòng 1 đo sai (thiếu 56% tam giác), vòng 2 vá đúng rồi vẫn đọc nhầm cột (hằng số nền pha loãng 1,43 lần xuống 1,16 lần), vòng 3 đo trên máy thật thì mới lộ ra rằng **cả câu hỏi đã sai ngay từ đầu**: hình học không phải trục tính tiền. ⇒ **Chọn sai TRỤC để đo thì đo bao nhiêu vòng cũng không ra** — mỗi vòng chỉ làm con số chính xác hơn về một đại lượng không liên quan. Trước khi tinh chỉnh một phép đo lần thứ ba, hãy hỏi *"đại lượng này có phải thứ quyết định kết quả không?"* — một phép thử rẻ là bật/tắt một yếu tố KHÁC (ở đây: đổi giờ trong ngày) rồi xem con số có nhúc nhích mạnh hơn không.
- ⚠️ **VÁ XONG MỘT PHÉP ĐO SAI VẪN CÓ THỂ RÚT RA KẾT LUẬN SAI — NẾU CON SỐ ĐÚNG ẤY TRỘN HAI ĐẠI LƯỢNG** (2026-08-17, Performance Gate vòng 2 — và nó nằm ngay trong bản vá của bài học ngay bên dưới, cách nhau vài giờ). Bản vá "đếm thay vì dự đoán" cho ra con số **ĐÚNG TUYỆT ĐỐI**: 78.748 = 78.748, khớp từng đơn vị với `renderer.info`. Rồi tôi lấy chính con số đúng ấy trả lời câu *"kỷ nào nặng"* và kết luận **"15 kỷ đồng đều, chênh 1,16 lần"** — hoàn toàn sai, vì `countSceneTriangles` duyệt CẢ CẢNH nên **44.126 tam giác vòm trời + rặng núi** (một HẰNG SỐ có mặt ở cả 15 kỷ) nằm trong số của mọi kỷ. Trừ nền ra: kỷ 11 (37.494) so kỷ 3 (26.168) = **1,43 lần** trên 4 kỷ của ma trận, và **2,46 lần** nếu xét cả 15 kỷ (kỷ 13 = 41.102 so kỷ 2 = 16.738). Một hằng số cộng vào cả tử lẫn mẫu đã pha loãng 43% khác biệt xuống còn 16% — **đúng hình dạng của `TECH_DEBT #22`** (trung bình trên vùng quá rộng làm loãng tín hiệu ~10 lần), chỉ khác là lần này thứ pha loãng không phải một vùng ảnh mà là một nhóm khối. ⇒ **Ba luật**: (a) sau khi vá một phép đo, hỏi tiếp *"con số đúng này trả lời ĐÚNG câu hỏi tôi đang hỏi chứ?"* — "đúng" và "đúng-việc" là hai chuyện, và một con số vừa được chứng minh là đúng thì rất khó bị nghi ngờ lần thứ hai; (b) dấu hiệu nhận ra sớm: **nếu một phần con số KHÔNG ĐỔI qua mọi trường hợp đang so, phần đó phải được tách ra** — hằng số không mang thông tin so sánh, nó chỉ mang thông tin tổng; (c) tách phải theo **NGUỒN GỐC khối** (`userData.sceneLayer` gắn lúc TẠO, xem `measureSceneGeometry`), tuyệt đối không đoán bằng ngưỡng/kích thước/tên màu — đó chính là sai lầm ba-phase của `TECH_DEBT #22`. Nay `sceneGraph.js` báo **ba** con số (thành phố · nền · tổng) cho cả tam giác lẫn lệnh vẽ, HUD cũng hiện tách, vì Đàm dùng HUD để biết *"xây thêm nhà có nặng không"* mà nhà thì không nằm ở phần nền. **Số tổng vẫn giữ nguyên** — nó đúng cho câu "GPU vẽ bao nhiêu mỗi khung", chỉ là nó không được dùng cho câu kia.
  - ⚠️ **KÈM THEO — MỘT QUẢ MÌN ĐƯỢC DỰ ĐOÁN, ĐI GỠ, RỒI ĐO RA LÀ NÓ CHƯA HỀ ĐƯỢC CÀI.** Dự đoán rất hợp lý: `renderer.info` đếm SAU khi cắt vật ngoài khung, traversal đếm MỌI khối, nên ở `--zoom 0.4` hai cột sẽ lệch và Đàm sẽ đọc thành "bản vá hỏng". Đo thật: **không một khối nào bị cắt, ở mọi kỷ và mọi mức zoom**. Lý do là một sự thật về kiến trúc cảnh mà không ai từng phát biểu — cả thành phố chỉ có **7 khối**, và khối nào cũng hoặc bao trùm camera (vòm trời bán kính 43,2 · rặng núi 51,1, camera đứng cách tâm 4,3–17,2 nên nằm BÊN TRONG cả hai) hoặc có tâm ngay tại gốc toạ độ mà camera thì luôn ngắm vào gốc (mặt đất 13,5 · mặt đường 8,5 · **toàn bộ công trình đã GỘP làm một khối** 7,5). Cắt theo hộp bao là phép cắt rất thô ⇒ nó không bỏ được gì. ⇒ **Ba luật**: (a) đi gỡ một quả mìn thì **kiểm xem nó có ngòi không** trước đã — bản đối chứng đầu tiên tôi viết đòi "phải tồn tại camera cắt bớt thật" và nó **ĐỎ**, đó chính là lúc sự thật lộ ra; (b) vẫn phải **dán nhãn riêng** ("trong cảnh" vs "đã vẽ (sau khi cắt)") dù hôm nay hai cột luôn bằng nhau, vì ngày nào tách công trình thành nhiều khối thì chúng lệch, và lúc ấy lệch là ĐÚNG; (c) bài test khoá **QUAN HỆ** (`đã vẽ ≤ trong cảnh`, và bằng nhau ở camera mặc định), **KHÔNG** khoá "luôn bằng nhau" — khoá thế là gài mìn thật, nó sẽ đỏ đúng lúc mã đang chạy đúng. ⚠️ Và đối chứng phải đứng **THẬT XA** rồi mới quay lưng: đứng ở gốc toạ độ mà quay lưng thì không cắt được gì, vì camera nằm bên trong hộp bao của cả 7 khối — bản đối chứng đầu tiên đỏ vì chính lý do đó, và lỗi nằm ở **phép thử** chứ không ở phép cắt.
  - ⚠️ **KÈM THEO — DẤU HUYỀN TRONG MỘT CHÚ THÍCH LÀM CHẾT CẢ CÔNG CỤ ĐO, VÀ NÓ CẮN HAI LẦN TRONG MỘT PHIÊN.** Toàn bộ mã trang xem thử của `city-preview.mjs` nằm trong MỘT template literal hơn 300 dòng. Viết chú thích kiểu ``// `renderer.info` đếm sau khi cắt`` là **đóng chuỗi giữa chừng** ⇒ `SyntaxError` lúc nạp. ESLint không bắt, `npm run build` không bắt (file này không vào bundle), và triệu chứng chỉ hiện ra khi CHẠY — tức lúc Đàm đang chạy nó trên MacBook. Cùng phiên còn một cái bẫy anh em: **nháy kép ASCII (`"`) trong dòng `console.log`** làm `bench-macbook.sh` cắt cụt dòng (nó lọc bằng `[^"]*`, bắt buộc, vì Chromium bọc mỗi dòng console vào nháy kép rồi dán thêm `", source: http://…`); dòng kết luận quan trọng nhất in ra thành đúng `[stats] ✓ ` rồi hết — **một dòng cụt trông y hệt một dòng chẳng có gì đáng nói**. ⇒ Trong vùng template dùng **nháy đơn** cho tên hàm/biến, và **nháy cong “ ”** cho mọi dòng in ra. Đã khoá bằng `scripts/cityPreviewSource.test.js` (2 bài, đã thử-cho-đỏ; bài thứ nhất bảo chính Node `--check` parse file chứ không đoán bằng regex) — lại đúng bài học *"một bài học được ghi ra KHÔNG chặn được gì; chỉ một bài TEST mới chặn được"*.
- ⚠️ **CÔNG CỤ ĐO NÓI DỐI LẦN THỨ 24 — MỘT PHÉP TIA CHỈ DÒ *TRƯỜNG CAO ĐỘ* THÌ MÙ VỚI MỌI THỨ *ĐỨNG TRÊN* MẶT ĐẤT, VÀ MỘT DÒNG TRONG BẢNG NGHIỆM THU HOÁ RA LÀ MỘT KẾT LUẬN SAI HOÀN CHỈNH** (2026-08-20, nghiệm thu Bước C). `water-view.mjs` bắn tia từ đúng camera của app và hỏi *"tia này chạm nước trước hay chạm đất trước?"* — nghe là **đúng câu**, và nó có `--selftest` 4 mục đều xanh. Nhưng hàm dò mặt đất của nó (`caoDoTai`) chỉ đọc `surfaceHeightAt`/`horizon.heightAt`: nó **không biết cây cối, nhà cửa, đá, cư dân tồn tại**. Tia xuyên qua tán cây rồi chạm mặt nước phía sau được ghi là "nước", còn màn hình vẽ ra một cái cây. Vì tin nó, bảng nghiệm thu ghi *"11/14 kỷ đạt cổng nước 5%"*; đo lại trên ảnh đã dựng (`water-score.mjs`, đọc mặt nạ do chính GPU tô) ra **5/14**. ⇒ **Bốn luật**: **(a)** ⚠️ **sai số của một phép đo hiếm khi ĐỀU** — ở đây nó nói quá 1,04 lần ở biển rộng nhưng **3,01 lần** ở kỷ nước hẹp bờ rậm, tức **lệch nặng nhất đúng ở những ca đang đứng sát ngưỡng**, đúng chỗ một con số được dùng để phán xử; một phép đo "chỉ hơi lệch" mà lệch có hệ thống về phía ca khó thì nguy hiểm hơn một phép đo lệch nhiều mà đều; **(b)** **PHÂN VAI, ĐỪNG GỘP** — `water-view.mjs` vẫn là công cụ ĐÚNG cho câu *"xoay camera đi thì TRẦN là bao nhiêu"* (cây đứng yên khi xoay nên sai số triệt tiêu phần lớn, và nó chạy không cần Chromium); nó chỉ sai khi bị dùng cho câu *"hôm nay Đàm thấy bao nhiêu"*. Sửa một công cụ hỏng và **thu hẹp phạm vi** một công cụ đúng là hai việc khác nhau; **(c)** giả thuyết đầu tiên tôi đi (hộp bao mặt nước nới quá tay) **SAI hoàn toàn** — vá xong thì các con số **không nhúc nhích một chữ số nào**, và chính sự đứng yên ấy mới là thứ chỉ đúng thủ phạm: *khi một bản vá đúng-về-lý-lẽ không đổi được gì, nó vừa loại trừ giúp ta một nghi phạm — đừng vứt kết quả ấy đi, hãy ghi lại*; **(d)** ⚠️ **và cột thứ hai mới là cột cứu bàn thua**: tuy chỉ 5/14 kỷ đủ DIỆN TÍCH, cả **14/14 kỷ** có tương phản nước↔bờ **30,8–115,5** (ngưỡng mắt 12) ⇒ *chỗ nào có nước thì nó ĐỌC RA là nước*. **"Có đủ nhiều không" và "có đọc ra không" là HAI câu hỏi; đừng để một cột gánh cả hai** — kỷ 10 là đối chứng sống: tương phản mạnh nhất bảng (103,2) mà chỉ chiếm 1,18% khung. **Cái cổng không sai — cái THƯỚC mới sai.**
  - ⚠️ **KÈM THEO — MỘT PHÉP LÀM TRÒN *HIỂN THỊ* CHE MẤT MỘT CÁI CỔNG ĐÃ KÍCH HOẠT.** Đàm đặt ba ngưỡng cho trục chặng ngày (`≥ 20,7` · `< 17` · `< 14`). `sweep-score.mjs` in dòng tổng bằng `toFixed(1)`, nên giá trị thật **13,9616** hiện ra thành **"14.0"** — đọc y hệt một con số CHƯA chạm ngưỡng "< 14", trong khi nó đã chạm. Không có gì đỏ lên, và cả phép đo lẫn cái cổng đều đúng: chỗ hỏng nằm ở **số chữ số in ra**. ⇒ **Con số nào được đem so với một ngưỡng thì phải in đủ chữ số để phép so ấy đọc được từ màn hình.** Các dòng LIỆT KÊ vẫn một chữ số — chúng để liếc, không để phán xử. (Đã sửa dòng tổng sang 2 chữ số.)
  - ⚠️ **KÈM THEO — HAI THEME, `md5` KHÁC NHAU, SỐ ĐO GIỐNG HỆT TỚI TỪNG CHỮ SỐ: ĐÓ LÀ MỘT ĐỐI CHỨNG, KHÔNG PHẢI MỘT SỰ TRÙNG HỢP.** Quét 15 kỷ ở `--theme light` và `--theme dark` ra hai file khác `md5` (khung ngoài + dải nhãn khác nhau) nhưng `sweep-score` chấm ra **y hệt**: 13,96 · 21,84 · trung vị 40,73 · 0/15 · 0/105. Nó chứng minh phép đo **chỉ chạm nội dung 3D**, không dính khung trang — và nó xác nhận đúng lời chính công cụ dựng đã khai (*"có truyền giờ ⇒ ĐỒNG HỒ quyết bảng màu, `--theme` CHỈ đổi khung ngoài"*). ⇒ Đây là **chiều ngược** của `TECH_DEBT #50`: ở đó *trùng `md5` ⇒ chắc chắn cùng ảnh*; ở đây *khác `md5` mà cùng số đo ⇒ chỗ khác nhau nằm NGOÀI vùng đo*. Cả hai chiều đều dùng được, miễn là nói rõ đang dùng chiều nào.
  - ⚠️ **KÈM THEO — MỘT CÔNG CỤ TỰ CHỐI CHẠY VẪN CÓ THỂ TRẢ VỀ DỮ LIỆU DÙNG ĐƯỢC; ĐỪNG VỨT CẢ ĐẦU RA VÌ NÓ KẾT THÚC BẰNG CHỮ "DỪNG".** `bash scripts/bench-macbook.sh` tự dừng ở cảnh ĐẦU khi thấy SwiftShader (đúng thiết kế — mọi con số **ms** sẽ vô nghĩa). Nhưng nó đã kịp in khối `[stats]` của cảnh ấy, mà `renderer.info` thì **đếm cùng một thứ bất kể bộ tô hình là GPU hay CPU**. Kỷ 3 · 12 giờ · `--sessions 80 --level 3` ra **13 lệnh vẽ · 122.044 tam giác** — khớp **từng đơn vị** với bảng "Sau VIỆC 2 Bước C" ở `PERFORMANCE.md`, bằng một đường đo hoàn toàn độc lập (Chromium ↔ phép duyệt cảnh thuần của `scene-count.mjs`). Đó đúng là phép **đối chiếu chéo** mà `TECH_DEBT #43` kê đơn sau khi 6/15 dòng của bảng Phase 11 trôi trong im lặng, và lần này nó nói: **bảng Bước C KHÔNG trôi.** ⇒ Khi một công cụ từ chối, hỏi *"phần nào của đầu ra KHÔNG phụ thuộc lý do từ chối?"* — phần đó vẫn là số thật, và nó thường miễn phí.
- ⚠️ **CÙNG MỘT KỶ, BA ĐỊNH NGHĨA "ƯỚT", BA CON SỐ KHÁC NHAU — VÀ HAI TRONG BA *IM LẶNG* KHI BỊ PHÉP PHÁ BƠM VÀO** (2026-08-20, §2, đóng `TECH_DEBT #64`, ADR-044). Kỷ 5 (`meander`) ship ra một **hào vuông khép kín** thay vì một khúc uốn có lối vào. Đi nghiệm thu bằng 720 tia bắn từ tâm, tôi phải chọn *"tia này ướt hay khô"* nghĩa là gì, và có **ba** đại lượng đều có thật, đều đúng, đều nằm trong cùng một module: `insetAt > 0` → **46/720** tia khô · cao độ địa hình dưới `WATER_SURFACE_Y` → **37/720** · `blendAt > 0` → **19/720** (và **0/720** trước bản vá). Chỉ con số thứ ba khớp với ảnh render, vì `render3d/terrainMesh.js` (`buildWaterSurface`) chỉ **bỏ** một ô mặt nước khi `blendAt <= 0` ở **cả bốn góc** ⇒ **`blendAt` mới là thứ quyết định tấm nước được tô ở đâu** — không phải `insetAt`, và cũng không phải cao độ (tấm nước được tô ĐÈ LÊN phần đất vẫn còn cao hơn mặt phẳng nước; đó đúng là cái hào đã ship). Hai định nghĩa kia **không nhúc nhích một tia nào** khi bơm phép phá vào ⇒ nếu bài test hỏi bằng chúng thì nó xanh vĩnh viễn về một thế giới khác. ⇒ **Ba luật**: **(a)** cùng họ `TECH_DEBT #42` (*"assert con số đã KHAI thay vì con số đã DỰNG"*) nhưng ở biến thể khó thấy hơn — ở #42 một bên là bảng và một bên là hình, còn ở đây **cả ba đều là hàm dựng hình**, nên không có dấu hiệu bề mặt nào; câu hỏi phát hiện là ***"con số tôi đang hỏi có phải chính con số mà tầng VẼ đọc không?"*** và cách trả lời rẻ nhất là `grep` xem tầng vẽ gọi hàm nào; **(b)** ⚠️ **khi phép phá không nổ, đừng dừng ở "nghi chính phép phá" (Phase 8A) — hỏi thêm *"bài test của tôi có đang hỏi đúng đại lượng không?"***: lần này phép phá hoàn toàn lành, thứ hỏng là **câu hỏi**; **(c)** thứ đóng được nghi vấn trong 30 giây là **dựng CẢ HAI bản rồi NHÌN ẢNH** — ảnh TRƯỚC hiện rành rành một cái hào vuông khép kín, tức `blendAt` là câu đúng, không cần lý lẽ thêm.
  - ⚠️ **KÈM THEO — "BẢN QUÉT KHÔNG ĐỔI MỘT CHỮ SỐ" KHÔNG CHỨNG MINH ĐƯỢC GÌ CẢ, THEO CẢ HAI CHIỀU.** Sau bản vá, cổng không-trôi in ra **đúng 20 con số y hệt** mốc nền (13,96 · 21,84 · trung vị 40,73 · 0/15 · 0/105). Đọc vội thì hoặc là *"tốt, không trôi"* hoặc là *"chết rồi, bản quét chạy bằng mã cũ"* — **không con số nào trong bảng ấy phân biệt được hai khả năng đó**, vì phép chấm kỷ lấy trung bình trên **dải thành phố** còn cái hào thì nằm ở **vùng quê**, và phép chấm chặng lại gộp trung bình 15 kỷ nên một kỷ đổi bị chia cho 15 (đúng hình dạng `TECH_DEBT #22`). Thứ trả lời được là một phép đo **KHÁC HẲN**: đếm điểm ảnh lệch quá ngưỡng mắt trên TOÀN khung giữa hai bản quét kỷ 5 dựng từ hai cây mã → **2.388 điểm ảnh (0,53%) lệch > 12, lệch lớn nhất 118**, trong khi nhiễu SwiftShader là **±1** (`TECH_DEBT #50`) nên nó **về mặt cấu trúc không thể** tạo ra con số 118. ⇒ **Khi một phép đo ra kết quả y hệt lần trước, câu hỏi đầu tiên KHÔNG phải "vậy là không đổi à?" mà là "phép đo này có NHÌN TỚI chỗ tôi vừa sửa không?"** — và nếu không thì phải đi mượn một phép đo thứ hai, đừng diễn giải sự đứng yên. (`md5` đổi cũng không cứu được: `#50` đã ghi *khác `md5` ⇏ ảnh đã đổi*.)
- ⚠️ **CÔNG CỤ ĐO NÓI DỐI LẦN THỨ 22 — MỘT CÁI CỔNG MỞ KHÔNG CÓ NGHĨA LÀ ĐƯỜNG ĐÃ THÔNG, VÌ CÓ CÁI CỔNG THỨ HAI KHÔNG AI NHẮC TỚI** (2026-08-17, Performance Gate). Đo riêng chi phí **dựng lại bản đồ bóng**: bật `renderer.shadowMap.needsUpdate = true` trước mỗi khung, so với không bật. Kết quả ra **−0,5 ms trung bình** — tức "dựng lại bóng còn NHANH HƠN không dựng". Một con số vô lý, nhưng nó nằm gọn trong khoảng nhiễu nên rất dễ được đọc thành *"bóng đổ không tốn gì, cứ thoải mái nâng lên 4096"*. Sự thật: three có **HAI cổng độc lập** — cổng toàn cục (`renderer.shadowMap.autoUpdate/needsUpdate`) và cổng TỪNG ĐÈN (`sun.shadow.autoUpdate/needsUpdate`). Qua cổng một mà không qua cổng hai thì vòng lặp `continue` bỏ qua đèn, **bản đồ bóng không hề được vẽ lại**, và phép đo đang so hai khung hình y hệt nhau. App mở cả hai cổng qua `city.invalidateShadows()`; phép đo thì chỉ mở một. ⇒ **Ba luật**: (a) một cơ chế có công tắc thì hỏi *"có bao nhiêu công tắc nối tiếp nhau?"* — **phép đo phải gọi đúng hàm mà app gọi**, đừng tự dựng lại chuỗi bằng tay (lại là "một luật một công thức", lần này giữa mã sản phẩm và phép đo); (b) một hiệu số **âm** giữa "có làm việc X" và "không làm việc X" là báo động đỏ, không phải nhiễu vô hại — nhiễu thì đối xứng quanh giá trị thật, mà giá trị thật ở đây không thể âm; (c) **mọi phép đo kiểu bật/tắt phải kèm một nấc vặn tới mức vô lý** (Phase 7A): `--dpr 4` làm frame time đi từ 218,5 → 1337,2 ms mới chứng minh được cần gạt CÓ nối.
- ⚠️ **BA CÂU HỎI KHÁC NHAU VỀ BÓNG ĐỔ — VÀ MỘT BÀI HỌC THỨ TƯ VỀ VIỆC ĐƯỢC PHÉP KẾT LUẬN GÌ TỪ CHÚNG** (2026-08-17, sửa lại ở vòng 2 cùng ngày). Hỏi "bóng đổ tốn bao nhiêu" là hỏi sai — phải tách ba: **(1) LẤY MẪU** bản đồ bóng ở mỗi điểm ảnh; **(2) DỰNG LẠI** bản đồ bóng — chỉ nổ khi thành phố ĐỔI, vì `sun.shadow.autoUpdate = false`; **(3) chi phí ấy KHÔNG nằm trong bảng FPS**, nên đọc bảng FPS rồi kết luận "bóng miễn phí" là đúng-về-một-chuyện-khác. Lượt dựng bóng còn thêm **7 lệnh vẽ + 25.436 tam giác** vào `renderer.info` của đúng khung hình đó — nên khi đối chiếu số liệu phải đọc `renderer.info` **ngay sau loạt ổn định**, đọc sau loạt dựng bóng là so lệch pha. ⚠️ **NHƯNG CÁC CON SỐ ms CỦA BA CÂU HỎI ẤY (209,6 · 205,6 · 239,0 ở khung 400×250) ĐO TRÊN SwiftShader — CPU rasteriser — VÀ KHÔNG SUY RA ĐƯỢC CHO MÁY THẬT, KỂ CẢ DƯỚI DẠNG PHẦN TRĂM.** Tôi đã từ chối xuất FPS vì đúng lý do đó, rồi ngay đoạn sau lại viết *"dựng lại bóng = 14% một khung"* và *"đã đo được GIÁ THẬT 29,4 ms"* — hai câu tự mâu thuẫn với chính lý do từ chối, và chữ "giá thật" là chữ nguy hiểm nhất trong cả báo cáo. Trên GPU thật lượt dựng bóng chỉ ghi ĐỘ SÂU (không shader màu, không lấy mẫu môi trường) nên tỉ lệ của nó là một đại lượng khác hẳn. ⚠️ **Và KHÔNG được nói "lấy mẫu bóng ≈ 0%"**: −4,0 ms nằm gọn trong nhiễu ±15 ms, nên câu duy nhất được phép nói là *"nhỏ hơn mức phép đo này phân giải được"* — huống hồ phép đo ấy bị chi phí CỐ ĐỊNH lấn át (16× điểm ảnh chỉ ra 6,1× thời gian), mà lấy mẫu bóng thì tính tiền theo TỪNG ĐIỂM ẢNH, tức phép đo **về mặt cấu trúc không thể thấy** thứ đang hỏi (đúng bài học Phase 9B). ⇒ **Luật: một con số đo trong hộp cát chỉ được dùng để so các trường hợp TRONG hộp cát ấy với nhau. Phần mang ra ngoài được là phần CẤU TRÚC ("chi phí này chỉ nổ khi cảnh đổi"), không phải phần SỐ.**
- ⚠️ **BA BÀI TEST CÙNG MỘT FILE, BA KIỂU NÓI DỐI KHÁC NHAU — VÀ CẢ BA CHỈ LỘ RA KHI DỮ LIỆU MỞ RỘNG TỪ 2 KỶ LÊN 14** (2026-08-20, Bước C mặt nước). Bước B dựng nước ở đúng hai kỷ (12 và 14); Bước C trải ra đủ 14. Ba bài test xanh suốt Bước B lập tức đỏ, và **không bài nào đỏ vì mã hỏng**:
  - **(a) MỘT PHÉP ĐO TỰ XƯNG LÀ "TOÀN THẾ GIỚI" TRONG KHI NÓ CHỈ NHÌN 1/10 THẾ GIỚI.** Hàm lấy mẫu tên `diemToanTheGioi`, chú thích ghi *"cả vùng chân trời"*, và nó dừng ở bán kính **11,4** trong khi tấm chân trời phủ tới **36**. Kỷ 15 (Dubai khai `reach: 6`) có mặt nước bắt đầu **2,6 ô BÊN NGOÀI** tấm đất thành phố ⇒ `insetAt` **âm ở mọi điểm lấy mẫu**, tức triệu chứng giống hệt một kỷ không hề có nước. Cơ chế vẫn sống — nó sống ở tấm BÊN KIA (`horizon.js` khoét lòng nước bằng CÙNG một `setting`). Ảnh render kỷ 15 đẹp không chê được, biển chiếm 20,8% khung. ⇒ **Trước khi tin một phép đo nói "không thấy gì", hỏi xem nó có NHÌN TỚI chỗ ấy không** — và **cái tên hàm là chỗ nói dối rẻ nhất**: nó được đọc nhiều hơn phần thân, mà không có gì kiểm nó. Nay tên hàm nói đúng phạm vi (`diemQuanhThanhPho`) và có một hàm thứ hai (`diemToiChanTroi`) lấy bán kính **thẳng từ `buildHorizon(era).reach`**, không viết cứng.
  - **(b) MỘT LỜI HỨA VIẾT THÀNH *MỨC* TRONG KHI NÓ LÀ *QUAN HỆ*.** Bài "mặt nước phải thật sự được khoét" đòi MỌI kỷ chạm đáy đầy đủ `WATER_BED_DEPTH`. Không thể đúng và cũng không NÊN đúng: `depthAt` đi từ mép xuống đáy qua đoạn dốc `BED_RAMP = 1,6`, nên chỉ kỷ nào có chỗ lún sâu ≥ 1,6 ô mới chạm đáy — **8/14 kỷ không bao giờ chạm** (kỷ 5 chỉ 20,1% đáy, kỷ 10 27,9%). Kênh Amsterdam rộng 0,9 ô **phải** nông hơn vịnh Tokyo, và `terrainMesh.js` lấy chính `depthAt` làm sắc nước nên nước hẹp render ra nhạt hơn — đúng vật lý. Bài cũ xanh **chỉ vì hai kỷ duy nhất nó chạy tới đều tình cờ thuộc nhóm rộng**. ⇒ Đúng bẫy Phase 7D (*"câu này có chứa chữ 'hơn' không?"*), và một hệ quả mới: **một bài test chạy trên 2 mẫu không phân biệt được "luật" với "trùng hợp"** — cùng họ `TECH_DEBT #38` (trần 13 lệnh vẽ suy từ mẫu 3 kỷ).
  - **(c) MỘT PHÉP ĐO LẤY MẪU THÔ HƠN THỨ NÓ ĐO.** Bài "núi phải lùi khỏi mặt nước" duyệt lưới **số nguyên bước 1 ô**, mà con nước hẹp nhất bảng (khúc uốn kỷ 5, rộng 0,5 ô) lọt trọn giữa hai mắt lưới ⇒ báo *"0 điểm ngập"* cho một mặt nước hoàn toàn lành lặn. Cùng họ *"lưới 12×12 chỉ có ~9 giá trị độc lập"* (Phase 7B).
- ⚠️ **PHÉP THỬ NGƯỢC BÁC BỎ CHÍNH CHÚ THÍCH CỦA BÀI TEST — "một bài test xanh không cho biết có BAO NHIÊU thứ đang giữ nó xanh" nay có thêm một vế: NÓ CŨNG KHÔNG CHO BIẾT THỨ ẤY CÓ ĐÚNG LÀ THỨ CHÚ THÍCH NÓI HAY KHÔNG** (2026-08-20, Bước C). Bài *"núi phải lùi khỏi mặt nước"* có dòng THỬ-CHO-ĐỎ ghi rõ: *"bỏ hệ số `luiNui` ⇒ đỏ ở kỷ 14"*. Thử thật (`luiNui * 0 + 1`): **KHÔNG MỘT BÀI NÀO ĐỎ.** Lý do: `khoetLongNuoc` chạy SAU và kéo mọi điểm dưới nước xuống đáy bất kể ngọn núi cao bao nhiêu — tức lời hứa ấy xưa nay được giữ bởi **phép khoét**, không phải bởi `luiNui`. Hai cơ chế độc lập che cùng một ca, chú thích chỉ kể một, **và kể nhầm cái**. Việc `luiNui` thật sự làm nằm ở vùng KHÔ sát bờ (`inset ∈ (−MOUNTAIN_FADE, −SHORE_BAND]`) nơi phép khoét đã tắt hẳn — vá bằng một vế mới đo **ba vành đồng tâm** và đòi đỉnh cao nhất phải **thấp dần về phía nước** (quan hệ thuần, không hằng số nào; đúng 14/14 kỷ). ⇒ **Ba luật**: (a) dòng THỬ-CHO-ĐỎ trong chú thích **phải được chạy thật ít nhất một lần**, không được chép lại từ bài cũ — nó là một lời khẳng định về nhân quả, và nhân quả thì phải đo; (b) khi phép phá không nổ, hỏi **cả hai** câu *"phép phá có hỏng không?"* (Phase 8A) **và** *"còn cái gì khác đang giữ nó xanh?"* (Phase 4D) — ở đây câu thứ hai mới là câu đúng; (c) muốn canh một cơ chế thì phải tìm **vùng nó là cơ chế DUY NHẤT**, chứ đừng canh ở chỗ nhiều cơ chế chồng lên nhau.
- ⚠️ **KHI PHÉP PHÁ KHÔNG KHỚP MỘT CHỖ NÀO, ĐÓ LÀ MỘT KẾT QUẢ — ĐỪNG ĐỂ NÓ IM LẶNG THÀNH "KHÔNG ĐỎ"** (2026-08-20). Bộ thử ngược của phiên này đếm số chỗ khớp trước khi thay và in `❌ PHÉP PHÁ HỎNG: 0 chỗ khớp` thay vì chạy tiếp — nhờ đó một ca (nới bề rộng kỷ 6) lộ ra ngay là **tôi nhớ sai `reach` của kỷ ấy**, chứ không bị đọc thành *"bài test này không có răng"*. Không có vế đếm ấy thì một phép phá trượt và một bài test mù **trông giống hệt nhau**. ⇒ Mọi bộ thử ngược phải: đếm số chỗ khớp và ĐÒI đúng 1 · `grep` lại xem thứ chèn vào có đúng hình dạng định chèn không · và **chạy trong một `git worktree` riêng**, không đụng kho đang sửa.
- ⚠️ **MỘT LỜI HỨA ĐANG XANH CÓ THỂ ĐANG SỐNG NHỜ CHÍNH KHUYẾT TẬT MÀ TA SẮP SỬA — và lúc sửa xong nó mới lộ ra** (bài học 2026-08-15, Phase 9B). Mặt đường có luật *"đường và đất phải cách nhau ÍT NHẤT 0,13"*, nhưng con số ấy được **CỘNG THÊM** vào chênh lệch riêng của vật liệu chứ không làm SÀN cho tổng, nên vật liệu nào vốn đã xa mức trung tính thì bị đẩy HAI LẦN: nhựa đường kỷ 11 nhận tổng đẩy **0,289** và render ra độ sáng **0,113** trong khi mặt đất 0,406 — tức nằm DƯỚI ngưỡng 0,12 mắt còn đọc ra chi tiết, **xét riêng vật liệu, trước khi bóng đổ chạm vào**. Luật có SÀN mà không có TRẦN, và chưa ai từng hỏi *"đẩy xa bao nhiêu thì là quá xa?"*. Vá đúng (phép đẩy **bão hoà**, có cả sàn lẫn trần mà vẫn đơn điệu ngặt) làm **ĐỎ** bài `15 KỶ RA 15 MẶT ĐƯỜNG`. Đo tiếp mới thấy chuyện đáng nói hơn: **kể cả nới trần tới mức gần như không bão hoà nữa, cặp 3↔10 cũng chỉ lên 9,8/10** — nghĩa là lời hứa ấy xưa nay **chỉ đạt nhờ 3% biên (10,3)**, và nó đạt được **chính nhờ phép đẩy vô hạn đang thổi phồng khác biệt ở đầu tối**. ⇒ **Ba luật**: (a) khi một bản vá đúng làm đỏ một bài test cũ, hỏi *"bài ấy xưa nay xanh nhờ cái gì?"* trước khi kết luận bản vá sai — ở đây nó xanh nhờ đúng thứ đang được sửa; (b) đo **BIÊN** của mọi lời hứa, đừng chỉ đọc xanh/đỏ: một lời hứa đạt nhờ 3% biên là một lời hứa sắp gãy mà không ai biết; (c) gặp tình huống này thì **KHÔNG nới ngưỡng và KHÔNG ship nửa vời** — ghi thành nợ kỹ thuật kèm bản vá đã đo sẵn và **nối cứng hai mục với nhau** (`TECH_DEBT` #30 ↔ #27), vì phương án "chấp nhận vĩnh viễn" của mục cũ nay đã chết mà không ai để ý.
- ⚠️ **KHI CÔNG CỤ ĐO NÓI "KHÔNG ĐỔI GÌ", THỨ ĐÁNG NGỜ ĐẦU TIÊN LÀ NÓ CÓ NHÌN THẤY ĐẠI LƯỢNG ẤY KHÔNG** (bài học 2026-08-15, Phase 9B — và nó suýt làm tôi kết luận sai HAI lần liên tiếp, theo hai hướng ngược nhau). Thử `VSMShadowMap` để làm mềm mép bóng: mọi con số của `shadow-score.mjs` **giống hệt nhau tới ba chữ số thập phân**. Kết luận vội sẽ là *"VSM không hoạt động"*. Sai — công cụ ấy đo **PHÂN BỐ độ sáng**, mà làm mềm mép bóng chỉ đổi một dải mỏng vài phần trăm điểm ảnh, tức nó **về mặt cấu trúc không thể thấy** thứ tôi đang hỏi. Vặn `radius` lên 60 (vô lý) thì các con số CÓ dịch ⇒ cần gạt vẫn nối. Nhưng rồi nhìn ảnh thì các mảng đen **vẫn y nguyên**, và kết luận vội thứ hai sẽ là *"VSM vô dụng"* — cũng sai, vì thứ "không chịu mờ đi" ấy **chưa bao giờ là bóng đổ**: nó là MẶT ĐƯỜNG. Chỉ khi tắt hẳn `sun.castShadow` mới rõ (9,6 trong 11,1 điểm phần trăm bị nghiền vẫn còn nguyên). ⇒ **Ba luật**: (a) trước khi tin một phép đo "không đổi", hỏi *"đại lượng tôi vừa vặn có nằm trong thứ công cụ này đo không?"*; (b) phép thử vô lý (Phase 7A) chứng minh cần gạt CÓ NỐI, nó **không** chứng minh cần gạt tác động lên thứ mình đang nhìn; (c) muốn tách "vật liệu tối" khỏi "bóng tối" thì **tắt hẳn nguồn bóng**, đừng đổi giờ trong ngày — đổi giờ vừa làm ngắn bóng vừa đổi góc nắng rọi xuống mặt phẳng, tức trộn hai nguyên nhân (chính phép thử "giữa trưa" đã làm tôi kết luận NGƯỢC một lần).
- ⚠️ **MỘT NGƯỠNG NỚI RỘNG "CHO CHẮC" LÀ MỘT CÁI PHỄU, VÀ NÓ IM LẶNG ĐÚNG LÚC CẦN KÊU NHẤT** (bài học 2026-08-15, Phase 9A). Sương mù có hai bài test canh đúng thứ cần canh — *"không được tràn vào thành phố"* (`< 0,45`) và *"không được xoá sạch rặng núi xa"* (`< 0,92`). Mật độ sương đầu tiên tôi chọn phủ **18% lên chính thành phố và 76% lên rặng núi**, tức đúng cái màn sữa mà hai bài ấy sinh ra để ngăn — và nó **qua cả hai một cách thoải mái**. Ngưỡng được đặt rộng vì lúc viết chưa ai biết giá trị đúng nằm đâu, rồi nó ở lại mãi. ⇒ **Ba luật**: (a) mỗi lần đặt một ngưỡng, hỏi *"giá trị THẬT hiện là bao nhiêu?"* — khoảng cách giữa giá trị thật và ngưỡng chính là phần dự án đang không được bảo vệ; (b) **kèm một ĐỐI CHỨNG nhốt bộ số hỏng cũ**, bắt phép đo phải còn bắt được nó, nếu không ngưỡng sẽ bị nới dần cho tiện; (c) ⚠️ **và đối chứng ấy phải so THEO TỪNG CHẶNG, không so với một ngưỡng chung** — bản đầu của chính đối chứng này viết `fogAt(CITY_EDGE, OLD) >= 0.20` rồi ĐỎ ngay, vì ở chặng "morning" mật độ cũ chỉ phủ 15% (lọt lưới), mà siết thêm nữa thì lại đá vào bình minh — chặng CỐ Ý nhiều sương (14%). Không một ngưỡng tuyệt đối nào tách được "bình minh mới" khỏi "buổi sáng cũ"; thứ phân biệt chúng là **cùng một chặng thì bản mới phải trong hơn bản cũ** — một QUAN HỆ, phải viết thành quan hệ (cùng họ Phase 7D). Đối chứng đã làm đúng việc của nó: nó bắt lỗi trong chính bản vá vừa viết ra.
- ⚠️ **"TIẾN TỚI 1 NHƯNG KHÔNG BAO GIỜ CHẠM 1" LÀ MỘT CÂU ĐÚNG VỀ TOÁN VÀ VÔ NGHĨA VỀ NHÌN — 93% với 100% thì mắt không phân biệt được** (bài học 2026-08-15, Phase 9A). Sương tuyến tính cũ có mặt phẳng `far`: qua khỏi nó mọi thứ bị thay bằng đúng một màu. Tôi đổi sang `FogExp2` và viết vào chú thích một lý lẽ kiến trúc rất chặt: hệ số `1 − e^(−(dρ)²)` **không bao giờ chạm 1**, nên không tồn tại khoảng cách nào cảnh vật bị xoá sạch. Đúng từng chữ. Nhưng ở mật độ tôi chọn, mép thế giới ra **93% sương** trong một ngày thường — dãy núi vẫn bị xoá, chỉ là bằng một tiệm cận thay vì một mặt phẳng. Cái sai không nằm ở MÔ HÌNH mà ở MẬT ĐỘ, và chính câu nói kia đã che mất điều đó: nó khiến tôi tin việc đổi mô hình là đủ, nên tôi không đo tiếp. ⚠️ Cùng đoạn chú thích còn một câu thứ hai cũng không sống sót: *"`FogExp2` ở mật độ 0,011 chỉ phủ ~6%"* — con số ấy đo ở `haze = 0` (trời quang tuyệt đối) rồi được viết như thể đúng chung; ngày thường `haze ≈ 0,3` cho ra **14%**. ⇒ **Một câu đúng về mặt tiệm cận, và một con số đúng trong MỘT điều kiện, đều phải được kiểm như mọi con số khác** (cùng họ "một câu tự trấn an cũng phải được kiểm như một con số", Phase 4G) — khác ở chỗ lần này cả hai câu do chính tôi viết ra trong chính phiên đang sửa, và chúng nghe thuyết phục vì chúng THẬT SỰ đúng, chỉ là đúng về một chuyện khác với chuyện đang cần.
- ⚠️ **PHÉP ĐO PHẢI CHẠM ĐÚNG ĐẠI LƯỢNG MÌNH ĐỊNH NÓI — nếu không nó sẽ CHÊ OAN một cơ chế đang chạy tốt** (lần thứ 19 công cụ tự chế nói dối, 2026-08-15, Phase 9A). Để chứng minh fBm nhiều tầng thật sự làm được gì, tôi đo "độ gồ ghề" bằng `|Δ|` giữa hai đỉnh lưới kề nhau, rồi thử ngược (ép `octaves = 1`). Hai bộ số ra **giống hệt nhau tới hai chữ số** ở cả 15 kỷ, và tôi suýt kết luận *"fBm vô dụng, gỡ đi"*. Nguyên nhân: `|Δ|` bị **cái dốc từ chân núi lên đỉnh** át hoàn toàn (dốc trung bình 0,10/bước, lớn hơn cả phần nhiễu), mà dốc ấy thì hai bản giống nhau. Đổi sang **sai phân BẬC HAI** (độ cong — dốc thẳng cho ra 0) thì sự thật lộ ra ngay: kỷ 13 **0,0111 so với 0,0038 — gấp 2,9 lần**, kỷ 5 gấp 2,6 lần, còn thảo nguyên/đụn cát đứng yên (0,00088 ↔ 0,00083) đúng như `rough` thấp phải thế. ⇒ **Trước khi tin một phép đo, hỏi "đại lượng này có chứa thứ mình KHÔNG muốn đo không?"** — ở đây là một thành phần bậc nhất che mất thành phần bậc hai. Đây là lần đầu công cụ nói dối theo hướng *chê oan*: mười tám lần trước nó trấn an sai hoặc gây hoảng sai, lần này nó suýt làm tôi gỡ bỏ một bản vá đúng.
- ⚠️ **MỘT CHÚ THÍCH NÓI "CÓ TEST ĐỐI CHIẾU HAI BÊN" KHÔNG PHẢI LÀ MỘT BÀI TEST — và lời hứa ấy sống sót 6 tháng vì nó nghe đã đủ yên tâm** (bài học 2026-08-15, Phase 8B). `countTriangles` (`parts.js`) là nguồn của cả ngân sách hiệu năng lẫn bảng HUD, và chú thích của nó tự bảo chứng: *"phải khớp CHÍNH XÁC với số tam giác nhà máy hình học thật sự sinh ra — **có test đối chiếu hai bên**, vì một ngân sách tự tính riêng mà lệch với thực tế thì còn tệ hơn không có ngân sách"*. Câu ấy đúng về mặt lý lẽ và **sai về mặt sự thật**: bài test duy nhất tồn tại chỉ so hàm ấy với **những con số viết cứng** (`countTriangles({shape:'prism', sides:4, taper:1}) === 12`), trên những khối **không hề có `w`/`d`/`h`** — tức những khối không thể tồn tại trong thành phố thật. Nó chưa bao giờ `import` nhà máy hình học. Suốt từ Phase 3B, hai bên có thể lệch nhau tuỳ ý mà không gì đỏ lên: ngân sách vẫn in ra một con số, chỉ là con số ấy nói về một thành phố khác. Nó không cắn ai chỉ vì **mỗi khối khi ấy có số tam giác cố định**; Phase 8B làm số tam giác phụ thuộc KÍCH THƯỚC của chính khối, và cái lỗ hổng lập tức thành nguy hiểm thật. ⇒ **Ba cách nhận ra sớm**: (a) một chú thích khẳng định "đã có test X" thì `grep` xem bài ấy có `import` đúng hai thứ nó nói là đang đối chiếu không — đối chiếu hai bên mà chỉ nạp một bên là một câu nói dối có cấu trúc; (b) **một bài test so với hằng số viết tay chỉ khoá được công thức, không khoá được sự KHỚP NHAU** — muốn khoá "hai bên bằng nhau" thì phải chạy CẢ HAI rồi so, không được so mỗi bên với một con số thứ ba; (c) test đầu vào phải là **dữ liệu thật** (khối có đủ `w`/`d`/`h` từ `buildBuildingSpec`), vì khối rút gọn cho tiện lại chính là khối lọt qua mọi nhánh mã mới. Cùng họ với "một câu tự trấn an cũng phải được kiểm như một con số" (Phase 4G) — khác ở chỗ lần này câu tự trấn an nằm trong chú thích của mã sản phẩm, nơi không ai nghĩ tới việc phải kiểm.
- ⚠️ **LUẬT "MỘT BÀI TEST CHƯA TỪNG THẤY ĐỎ THÌ CHƯA PHẢI TEST" PHẢI ÁP CHO CẢ PHÉP ĐO — nếu không, một cơ chế CHẾT vẫn được ship kèm một chú thích dài giải thích nó chạy ra sao** (bài học 2026-08-15, Phase 8D). Phase 8D thêm cơ chế cho cây "mọc thành lùm" thay vì rải đều. Nhìn ảnh chụp: có lùm cây thật, rõ ràng, thuyết phục. Nếu dừng ở đó thì xong. Phép thử ngược (bật `GROVE_JOIN_CHANCE` = 6 rồi = 0 rồi đo lại) nói ngược hẳn: chỉ số phân tán **đứng yên tới hai chữ số thập phân ở cả bốn kỷ** — cơ chế ấy chưa bao giờ làm gì cả, và những "lùm cây" trên ảnh chỉ là cụm ngẫu nhiên mà mắt tự gom lại. **Hai nguyên nhân, và cái thứ hai mới đáng sợ**: (a) mọi cảnh vật vừa đặt lại được ghi thành một tâm lùm mới, nên sau chục ô thì "bám vào một lùm" thoái hoá thành "bám vào một cảnh vật bất kỳ" ≈ đúng bằng rải đều — *một cơ chế tụ tập mà số tâm tụ bằng số vật thì không tụ được*; (b) ở 120+ phiên lưới **kín 144/144 ô**, nên **không một chiến lược đặt nào có thể đổi được gì** — cơ chế vẫn chạy, chỉ là không còn chỗ trống nào để sắp xếp. ⇒ **Ba luật**: (1) mắt người rất giỏi tìm ra cụm trong nhiễu, nên "nhìn ảnh thấy có cụm" KHÔNG phải bằng chứng — phải bật/tắt rồi đo; (2) trước khi tin bất kỳ cơ chế phân bố nào, hãy đếm **còn bao nhiêu ô trống** — một cơ chế sắp xếp trong một cái hộp đầy là mã chết; (3) **hai lần đo khớp nhau mới phân biệt được tín hiệu với nhiễu**: chỉ số phân tán theo ô vuông (Ic) ở cỡ mẫu n≈30 cho bốn kỷ ra bốn chiều khác nhau (kỷ 12 tăng, kỷ 1 giảm) — đổi sang Clark–Evans (chỉ phụ thuộc chính các điểm, không phụ thuộc cách chia ô) thì hai cỡ thành phố ra cùng chiều, cùng độ lớn ≈0,13, và lúc đó mới được phép ghi con số vào chú thích.
- ⚠️ **MỘT CON SỐ TUYỆT ĐỐI KHÔNG DIỄN ĐẠT ĐƯỢC "MẬT ĐỘ" TRONG MỘT KHÔNG GIAN HỮU HẠN — nó chỉ diễn đạt được "tỉ lệ lấp đầy", và nó tăng cho tới khi kín** (bài học 2026-08-15, Phase 8D — cùng họ với bài học mặt đường ở Phase 7D, nhưng lần này tôi tự gây ra nó trong chính phase đang sửa). Để 15 kỷ có mật độ cây khác nhau, tôi nâng trần cảnh vật 34 → 48. Nghe hợp lý. Đếm ô trống của một thành phố trưởng thành (80 phiên) thì **10/15 kỷ có ĐÚNG 0 ô đất trống** — lưới 12×12 kín đặc, tức đúng cái "rải đều trên lưới" mà cả phase sinh ra để xoá, và mọi cơ chế lùm thành vô nghĩa. Bản vá đầu (trần phủ **cố định** 72% đất trống) cũng sai và cũng chỉ lộ ra khi đo: kỷ 14 (rậm nhất, 1,42) và kỷ 15 (thưa nhất, 0,66) ra **cùng 21 cảnh vật**, vì ở thành phố trưởng thành nhà dân ăn hết đất nên cả hai chỉ còn 30 ô và cái trần chung đè bẹp cả hai đầu. ⇒ **Bản đúng: chính TỈ LỆ ấy phải mang mật độ** (`coverShare = clamp(0,55 × density, 0,28…0,80)`), và **giữ song song cả hai trần** vì chỗ thắt cổ chai đổi theo tuổi thành phố — lúc trẻ đất mênh mông nên trần tuyệt đối trói ("kỷ này bao nhiêu cây"), lúc đông đất hiếm nên trần tỉ lệ trói ("chừa lại bao nhiêu đất"). **Cách nhận ra sớm**: đọc tên một hằng số rồi hỏi *"cái này là một LƯỢNG hay một PHẦN của cái gì đó?"* — "mật độ", "độ phủ", "tỉ lệ", "đông đúc" đều là PHẦN, và một con số đếm không nhìn thấy mẫu số. Xem ADR-021.
- ⚠️ **MỘT KẾT LUẬN ĐÚNG CÓ THỂ HẾT ĐÚNG MÀ KHÔNG AI ĐỘNG VÀO NÓ — vì TIỀN ĐỀ của nó bị gỡ ở một phase khác** (bài học 2026-08-15, Phase 8C). `terrain.js` mở đầu bằng một khối chú thích dài, lý lẽ chặt chẽ, giải thích *"vì sao PHẢI là thềm bậc chứ không phải dốc liên tục"*. Lập luận ấy **đúng từng chữ** — nhưng nó đứng trên đúng một tiền đề nằm ngay trong câu thứ hai: *"nền thành phố là 144 ô hộp"*. Chừng nào mặt đất còn là hộp thì hộp không dốc được, nên chênh cao độ chỉ có thể là bậc. Đổi mặt đất thành một tấm lưới liền thì tiền đề chết, và **kết luận đi theo nó chết cùng** — dù không một dòng nào của lập luận sai. ⇒ **Đọc một chú thích kiểu "phải là X chứ không phải Y" thì hỏi ngay: câu này dựa trên điều gì, và điều đó CÒN đúng không?** Đây là mặt trái của việc dự án ghi chú thích rất kỹ: chú thích càng thuyết phục thì càng ít ai nghĩ tới việc kiểm lại tiền đề của nó, và nó cứ thế cai trị thêm nhiều tháng. Cách xử lý ĐÚNG khi lật: viết một ADR mới nói rõ *"đảo ngược nửa đầu của ADR-014, vì tiền đề Z đã bị gỡ"* (ADR-019), **giữ nguyên bản ghi cũ**, và sửa mọi chú thích/bài test còn kể lý do cũ — kể cả khi bài test ấy vẫn xanh, vì **một lời giải thích sai là thứ phiên sau kế thừa rồi dựa vào** (cùng họ với "sửa đúng không chứng minh hiểu đúng", Phase 3Y).
  - ⚠️ **KÈM THEO — RÀNG BUỘC CHẴN-LẺ: có những thứ không chỉnh khéo mà thoát được, và tôi đã mất một vòng mới nhận ra.** Bản đầu nhét mặt đường vào chung lưới mặt đất cho "gọn, đỡ một lệnh vẽ". Lưới đất chia 3 ô con, nên bề rộng ngõ phố bị làm tròn về bội của 1/3; mà muốn ngõ nằm **cân giữa** ô thì số ô con của nó phải **cùng chẵn-lẻ** với 3 ⇒ chỉ còn 1/3 (0,33 — mảnh như sợi chỉ, cư dân đi lòi ra ngoài) hoặc 3/3 (bằng đại lộ, mất hẳn thứ bậc đường). Lấy 2/3 thì đúng bề rộng nhưng **lệch tâm 1/6 ô**. Không có giá trị nào vừa đúng vừa cân — đó là ràng buộc số học, không phải sai số cần tinh chỉnh. Và cái giá bị đồn thổi của việc tách ra hoá ra **bằng không**: hai nhóm vật liệu trong một khối hình học vốn đã là hai lệnh vẽ, đúng bằng hai tấm riêng. ⇒ Khi một tham số "gần đúng nhưng lệch một chút" ở mọi giá trị thử, hãy hỏi *"có phải nó bị khoá bởi một ràng buộc rời rạc không?"* trước khi thử giá trị thứ tư; và **kiểm lại cái giá mình đang tránh — nó có thể bằng không.**
  - ⚠️ **VÀ HAI LỖI CỦA PHASE NÀY ĐỀU DO BÀI TEST BẮT, KHÔNG DO ĐỌC MÃ.** Lưới đỉnh neo ở `-0,5 − padSteps × du`: cái `-0,5` hợp lý về mặt kể chuyện (mép lưới thành phố nằm ở nửa ô) nhưng **không chia hết cho bước lưới 1/3**, nên các đỉnh rơi vào …, −0,17, +0,17, … và **không đỉnh nào nằm đúng tâm ô** — mà tâm ô là chỗ nhà, cây, cư dân đứng. Cả thành phố lệch vài phần nghìn: ảnh vẫn đẹp, mặt đất vẫn mượt, build/lint/test cũ đều xanh. Bài test hỏi thẳng *"có đỉnh nào ở đúng toạ độ thế giới của tâm ô không, và nó có đúng cao độ `heightAt` không"* thì đỏ ngay. ⇒ Với mọi lưới lấy mẫu, **hỏi bằng TOẠ ĐỘ THẾ GIỚI xem có đỉnh ở đúng chỗ cần không** — kiểm trường cao độ (hàm) là chưa đủ, vì hàm đúng mà lưới đặt lệch thì hậu quả y hệt.
- ⚠️ **PHÉP THỬ NGƯỢC CÓ THỂ TỰ NÓ HỎNG: `diff` chứng minh FILE đã đổi, KHÔNG chứng minh HÀNH VI đã đổi** (bài học 2026-08-15, Phase 8A — và nó suýt cấp cho hai bài test một giấy chứng nhận giả). Dự án đã có luật *"một bài test chưa từng thấy đỏ thì chưa phải test"*, và Phase 7A đã bổ sung *"phải kiểm bằng `diff` xem file có đổi thật không"* sau hai lần regex sửa file bị trượt vì thụt lề. Phase 8A cho thấy cả hai lớp đó **vẫn chưa đủ**. Để thử ngược bài "nhà phải có chân tường", tôi gỡ khối ấy bằng cách đổi `parts.push(prism({…}))` thành `parts.push(prism({…})) && 0`. `diff` báo 2 dòng đổi — bằng chứng rõ ràng, đúng theo luật đã ghi. Test vẫn XANH, và tôi suýt kết luận *"bài test này vô dụng, bỏ đi"*. Sự thật: `&& 0` chỉ vứt **giá trị trả về** của `push`, còn khối thì **vẫn được đẩy vào danh sách** — tôi đã sửa file mà không hề sửa hành vi, nên bài test xanh vì mã vẫn đúng chứ không vì test mù. (Gỡ thật bằng `[].push(…)`: cả hai bài đỏ ngay.) ⇒ **Một phép thử ngược phải nêu trước NÓ MONG ĐỢI ĐỎ Ở ĐÂU, và khi không đỏ thì nghi CHÍNH PHÉP THỬ trước khi nghi bài test** — cùng đúng cái phản xạ "kiểm CÔNG CỤ trước, kiểm mã sau" đã cứu dự án 17 lần, chỉ khác là lần này công cụ nói dối chính là *phép thử ngược*, thứ đứng cuối cùng để bảo chứng mọi thứ khác. Cách rẻ để tự bảo vệ: gỡ bằng cách làm khối **biến mất khỏi kết quả** (`[].push`, `return` sớm, gán `0`), đừng gỡ bằng cách sửa một biểu thức trông-như-vô-hiệu-hoá.
  - ⚠️ **KÈM THEO: HỎI THEO `role` THÌ BA NGUYÊN MẪU TÀNG HÌNH.** Phép đo đầu tiên đếm gờ tầng bằng cách tìm mảng nhà qua `role === 'wall' || role === 'wall2'`, rồi in ra một bảng rất thuyết phục nói *"thành luỹ không có gờ tầng nào ở cả 15 kỷ"*. Sai hoàn toàn: nguyên mẫu phòng thủ khai mảng nhà là `role:'stone'` (thành luỹ xây bằng đá) và xưởng khai `role:'wood'` — hai nguyên mẫu ấy tàng hình với phép đo chứ không thiếu gờ. Đo lại đúng thì thành luỹ có tới **15 dải**. Vai màu là *vật liệu*, không phải *chức năng kết cấu*; dùng nó làm khoá tìm kiếm là đang hỏi sai câu. Trong bài test, nhận diện bằng **hình học** (khối nào nở ra đúng `w × (1 + SPREAD)` tại cùng `x`/`z`) mới đúng — và phải khớp cả `x`/`z`, vì hạ tầng epic có hai mảng phụ RỘNG BẰNG NHAU đặt đối xứng, khớp mỗi bề ngang thì mỗi mảng "nhận vơ" gờ của mảng kia và số đếm nhân đôi.
  - ⚠️ **VÀ MỘT BÀI TEST ĐỌC HẰNG SỐ THÌ TRÔI THEO HẰNG SỐ ẤY.** Bài canh "không mảng nhà nào quá `MAX_COURSES` gờ tầng" import chính `MAX_COURSES`, nên nâng trần 3 → 8 thì **cả assert lẫn mã cùng dịch chuyển và test vẫn xanh** — trong khi 8 dải chính là "sọc ngựa vằn" mà cái trần sinh ra để ngăn. Nhập hằng số vào test là đúng (chép lại giá trị mới là "một luật hai công thức"), nhưng khi làm vậy thì phải kèm **một trần cho chính cái trần** (`MAX_COURSES <= 4`) — hợp lệ vì nó khoá một QUYẾT ĐỊNH mỹ thuật có lý do đo được, không khoá một phép làm tròn.
- ⚠️ **MỘT CON SỐ TUYỆT ĐỐI KHÔNG DIỄN ĐẠT ĐƯỢC MỘT LUẬT NÓI VỀ QUAN HỆ — và khi nó gãy thì gãy trong im lặng, ở một phiên khác, do tay một người khác** (bài học 2026-08-15, Phase 7D). Mặt đường có đúng một lời hứa, ghi ngay trong chú thích của nó: *"NHẠT hơn đất rõ rệt … mắt cần đọc ra lối đi"*. Lời hứa ấy là một **QUAN HỆ với mặt đất**, nhưng nó được viết thành một **HẰNG SỐ**: `material(48, 0.10, 0.10, 0.68, 0.42)`. Nhiều tháng sau, Phase 3M nâng độ đậm mặt đất ban đêm 0,286 → 0,400 vì một lý do hoàn toàn khác và hoàn toàn đúng đắn (đêm đang bị làm tối ba lần) — mặt đường **không có cách nào biết**, vì nó không tham chiếu tới mặt đất. Đo ra: ban ngày đường cách đất 0,129–0,145 (đọc được), **ban đêm 0,012–0,020 — tàng hình**, và nó đã chạy như vậy trên production nhiều ngày. Không có gì đỏ lên: build xanh, lint sạch, 640 test xanh, ảnh chụp vẫn "có vẻ tối thì phải thế". ⇒ **Cách nhận ra sớm**: đọc chú thích của một hằng số rồi hỏi *"câu này có chứa chữ 'hơn' không?"* — "nhạt hơn đất", "cao hơn nhà bên", "đến trước lượt kia". Mọi chữ "hơn" đều là một quan hệ với một thứ KHÁC, và một con số thì không thể nhìn thấy thứ khác ấy. Bản vá đúng là **đo cái mốc thật lúc chạy rồi tự đặt mình cách ra**, chứ không phải chỉnh 0,42 thành một số cao hơn (bản vá ấy sẽ gãy lại y hệt vào lần kế tiếp có ai chỉnh mặt đất — mà mặt đất đã bị chỉnh ba lần: Phase 3C, 3M, 7B). ⚠️ **Kèm hai bài phụ, cả hai đều chỉ lộ ra khi ĐO chứ không khi nhìn**: (a) **KẸP thì phá thứ tự, ĐẨY thì không** — bản vá đầu của chính phase này viết `|offset| < MIN ? ±MIN : offset`, bảo đảm đúng khoảng cách tối thiểu nhưng dồn mọi vật liệu nằm gần mặt đất về đúng ±0,13, nên pavé Paris (0,50) và bê tông Singapore (0,63) ra CÙNG một độ đậm; phải dùng phép đẩy đơn điệu `sign(off) × (MIN + |off| × SPAN)` mới giữ được cả hai vế; (b) **sửa xong một đường dẫn không có nghĩa là đã sửa xong cái luật** — ngõ phố (2/3 số ô đường) tô bằng `palette.roles.stone`, một vai màu chẳng liên quan gì tới mặt đường, nên sau khi đại lộ đã đúng thì nhìn ảnh vẫn thấy "đường đã đổi màu" trong khi hai phần ba mạng đường chưa hề đổi. Đúng họ với Phase 4D: **đổi một luật thì `grep` chính cái luật ấy trên toàn cây, đừng sửa chỗ đầu tiên nghĩ ra rồi cho là xong.**
- ⚠️ **MỘT TRƯỜNG GÁNH HAI VIỆC THÌ KHÔNG BAO GIỜ TÁCH ĐƯỢC HAI THỨ NÓ ĐANG TRỘN** (bài học 2026-08-14, Phase 5B — và đây là loại lỗi *thiết kế dữ liệu*, không phải lỗi mã). Đàm nhìn thành phố rồi nói *"không thể nào nhà hiện đại lại giống nhà thời đồ đồng được"*. Đo ra: kỷ 1 (lều da thú) cao trung bình **1,81**, kỷ 14 (tháp kính) **2,05** — chênh **13%**; cả 15 kỷ chỉ trải **1,88 lần** và còn SAI CHIỀU (lâu đài kỷ 5 = 2,28, cao hơn cao ốc kính). Thủ phạm: `storyHeight` trong `eraStyle.js` vừa là *"một tầng cao bao nhiêu"* (dùng chia ra số hàng cửa sổ) vừa là hệ số *chiều cao tổng*. Ngoài đời một túp lều và một toà nhà chọc trời có chiều cao TẦNG gần bằng nhau (2,5m vs 3,5m) — nên trường ấy **về mặt toán học không thể** tách hai thứ đó ra, dù có chỉnh khéo tới đâu. Thứ khác nhau giữa chúng là **SỐ TẦNG**, và không ai từng ghi nó. ⇒ Tách đôi: `storyHeight` giữ nghĩa hẹp, thêm `massScale` (chiều cao tổng) + `spread` (bề ngang). Kết quả trải **3,16 lần**, kỷ 14 cao gấp **2,96 lần** kỷ 1. **Cách nhận ra sớm**: khi một hằng số phải "vừa … vừa …", nó đang trộn hai đại lượng; hỏi *"ngoài đời hai thứ này có tỉ lệ thuận với nhau không?"* — nếu không thì phải tách, và mọi lần chỉnh sau đó đều chỉ là đổi chỗ vấn đề. ⚠️ Kèm ba bài phụ, cả ba đều chỉ lộ ra khi **nhìn vào ảnh quét**: (a) làm nhà cao lên thì **khung hình cố định cắt mất nóc** — tháp kỷ 11/14/15 nằm ngoài mép trên đúng 1,3°, không có gì đỏ lên; camera nay co giãn theo chính `massScale` (`cityOrbitOptions(gridSize, era)`), và phải co **CẢ HAI CHIỀU** — bản đầu kẹp `Math.max(0, …)` nên chỉ biết lùi ra, để kỷ 1 đứng nguyên khoảng cách đóng cho nhà cao gấp ba; (b) `CITY_CAMERA_FOV` — số 38 từng viết cứng ở **ba** nơi (`CityScene3D.jsx` + 2 chỗ trong `city-preview.mjs`), tức trang xem thử có thể lặng lẽ đóng khung khác app; (c) **một phép đo trong bài test cũng già đi**: bài "kỳ quan đối xứng" lọc tháp góc bằng ngưỡng tuyệt đối `|x| > 0.5`, và khi `spread` co kỷ 1 còn 0,72 thì bốn tháp lùi về ±0,475 → test đỏ với thông báo *"kỳ quan epic phải có 4 tháp góc"* trong khi bốn tháp vẫn còn nguyên và vẫn đối xứng hoàn hảo — **phép đo hỏng, không phải mã hỏng**.
- ⚠️ **LẦN THỨ BA CỦA "MỘT TRƯỜNG GÁNH HAI VIỆC" — và lần này cái bẫy chỉ lộ ra khi thành phố ĐÔNG LÊN** (bài học 2026-08-15, Phase 7C). Phase 7C dựng 17–30 nhà dân quanh 5 kỳ quan. Ảnh chụp kỷ 7 cho thấy ngay: **25 căn nhà nhỏ đều đội mái vòm terracotta y hệt Duomo**, nên nhà thờ chính toà — thứ Đàm yêu cầu phải *"nhận ra được từ xa"* — chìm nghỉm giữa một đám bản sao tí hon của chính nó. Thủ phạm là `style.roof` đang trả lời HAI câu khác nhau: *"công trình BIỂU TƯỢNG của nền văn minh này lợp mái gì?"* và *"nhà THƯỜNG ở đây lợp mái gì?"*. Ngoài đời hai câu đó gần như không bao giờ cùng đáp án, vì mái kỳ đài tốn kém và thường bị luật lệ giới hạn (mái chồng nhiều tầng từng bị cấm với dân thường ở Trung Quốc; giật cấp New York là luật quy hoạch cho cao ốc; Firenze có đúng MỘT mái vòm). ⇒ Tách ra trường `vernacularRoof`, **bắt buộc cả 15 kỷ** (9 kỷ khai khác, 6 kỷ khai trùng có chủ đích — thời đồ đá thì nhà nào cũng là lều). Bắt buộc chứ không tuỳ chọn: trường tuỳ chọn sẽ lặng lẽ rơi về `roof` và cái bẫy quay lại ở kỷ thêm sau này. ⚠️ Và phải thay mái **Ở NGUỒN** (`getVernacularStyle` trả về cả bộ tham số) chứ không thay trong `emitRoof` — vì `roofRise` cùng vài chỗ khác cũng đọc `style.roof`, thay một chỗ thì nửa công trình dựng theo mái vòm còn nửa kia theo mái dốc. **Cách nhận ra sớm vẫn là câu hỏi cũ**: *"ngoài đời hai thứ này có luôn đi cùng nhau không?"* Không ⇒ phải tách, và mọi lần chỉnh khéo sau đó chỉ là đổi chỗ vấn đề.
  - ⚠️ **KÈM THEO: MỘT SỐ TUYỆT ĐỐI ÁP LÊN NHỮNG KHỐI CHÊNH NHAU NHIỀU LẦN THÌ SỚM MUỘN CŨNG SAI — và dự án đã vá TRIỆU CHỨNG của nó một lần rồi.** `eaves` (diềm mái) là số tuyệt đối, còn `rw = w + eaves × 2`. Với kỳ quan rộng 1,4 thì `eaves` 0,4 là mái hiên sâu 29% mỗi bên — đúng nét Á Đông. Với nhà dân rộng 0,56 thì vẫn con số ấy thò ra **71% mỗi bên**, mái rộng gấp **2,4 lần** cái nhà nó đậy: một cái ô, hoặc một cây nấm. Kỷ 1 từng đọc ra thành CÂY NẤM và được vá bằng cách hạ tay `eaves` xuống 0,04 — đó là vá cho MỘT kỷ, còn bệnh gốc quay lại ngay khi có 30 công trình nhỏ mỗi kỷ. Vá gốc: kẹp theo TỈ LỆ (`eaveOverhang`, `EAVE_MAX_RATIO = 0,28`) → rộng nhất còn 1,41 lần. ⚠️ **Và câu tự trấn an tôi viết kèm bản vá — *"kỳ quan đủ to nên cái kẹp không bao giờ chạm tới chúng"* — là SAI**: đếm ra thì nó chạm **115/215 mảng nhà** của 75 công trình, tức quá nửa (một kỳ quan không phải một khối to, nó là 3–7 mảng và các mảng phụ nhỏ ngang nhà dân). Câu ấy còn tự mâu thuẫn với chính câu sau nó trong cùng đoạn — dấu hiệu kinh điển của một câu viết ra để yên tâm chứ không phải để đúng. Cùng họ với bài học *"ổn định qua hai cỡ ô 260 và 300"* ở Phase 4G: **một câu tự trấn an phải được kiểm như một con số.**
  - ⚠️ **VÀ HAI PHÉP ĐO TRONG BÀI TEST CŨNG GIÀ ĐI, cả hai đều là con số viết cứng về phần mã mình KHÔNG nhìn.** (a) `buildingSpec.test.js` có `assert.equal(seen.size, 4)` ("4 loại công trình") và đỏ ngay khi thêm 3 nguyên mẫu nhà dân — mã đúng, phép đo hỏng; sửa thành `Object.keys(ARCHETYPES).length` để nó hỏi chính cái bảng nó đang duyệt. (b) Tệ hơn, bài test MỚI tôi vừa viết cho Phase 7C cũng dính đúng lỗi đó: `assert.equal(picks.length, 1)` để canh "nhà dân không được gắn `addPickTarget`" — **đỏ ngay trên mã đang chạy đúng**, vì `addPickTarget` vốn được gọi HAI lần hợp lệ (công trình + giàn giáo), một sự thật tôi không kiểm trước khi viết con số. ⇒ Muốn canh "khối X không được làm Y" thì **cắt đúng khối X ra rồi hỏi**, đừng đếm tổng số lần Y xuất hiện trong cả file — cùng bài học với `/envMap,/` xanh oan ở Phase 7A ("assert 'có ít nhất một chỗ' là cái phễu, không phải hàng rào").
- ⚠️ **LẦN THỨ TƯ CỦA "MỘT BẢNG GÁNH HAI VIỆC" — và lần này cái cờ dùng để lọc chỉ che được MỘT NỬA** (bài học 2026-08-15, Phase 8D). Bảng loài cây theo kỷ (`floraStyle.js`) ban đầu để `bush` (bụi) nằm chung với 6 loài cây thân gỗ, rồi lọc bằng một cờ `allowBush`. Đo ra: cảnh vật loại "bụi" ở kỷ 1 dựng ra **5 khối, 212 tam giác, CAO 0,94** — tức một cái CÂY hoàn chỉnh đứng ở chỗ đáng lẽ là bụi thấp ngang đầu gối, vì bảng kỷ 1 khai `broadleaf` nặng hơn `bush` nên **6/10 lần "bụi" bốc trúng cây**. Cái cờ ấy chặn bụi lọt vào rổ CÂY, nhưng **không chặn cây lọt vào rổ BỤI** — một cái lọc một chiều đội lốt một cái lọc hai chiều, và nó trông hoàn toàn hợp lý khi đọc mã. Bảng đang trả lời hai câu khác nhau: *"ở kỷ này mọc CÂY GÌ?"* và *"tầng cây bụi dày bao nhiêu?"*. Ngoài đời hai câu ấy độc lập (rừng sồi Anatolia và đất hoang Manchester đều nhiều bụi, vì lý do khác hẳn nhau) ⇒ phải tách: bảng chỉ trả lời câu đầu, câu sau thành trường `undergrowth` riêng. **Cách nhận ra sớm, bổ sung cho ba lần trước**: khi phải thêm một CỜ để lọc bớt kết quả của một bảng, hãy hỏi *"cờ này chặn được cả hai chiều chưa?"* — thường thì không, và cái cờ chính là dấu hiệu bảng đang gánh hai việc chứ không phải cách sửa.
- ⚠️ **MỘT CÁI NÚM LÀM ẢNH ĐỔI "MỘT CHÚT" THÌ KHÔNG CHỨNG MINH ĐƯỢC GÌ — PHẢI VẶN TỚI MỨC VÔ LÝ RỒI ĐÒI MỘT HẬU QUẢ VÔ LÝ** (bài học 2026-08-14, Phase 7A — mất nửa buổi chỉnh một cái núm không nối vào đâu cả). Sau khi đổi cả thành phố sang vật liệu PBR (`MeshStandardMaterial`) + bản đồ môi trường, bảng màu **nhạt hẳn đi**: sáng 33,8 → 52,2 (+54%), tươi 24,4 → 16,1 (−34%), chiaroscuro 40,3 → 30,3 (−25%) — đúng thất bại "pastel như sữa" mà dự án đã bác một lần (AgX tone mapping). Giả thuyết đầu tiên rất hợp lý: *"môi trường đang đóng vai đèn nền thứ ba"* ⇒ hạ đèn bán cầu 0,34 → 0,10 và đèn nền 0,07 → 0,02. Đo lại: **52,2 → 51,0**, gần như không nhúc nhích. Lý lẽ đúng, con số vô dụng. Thủ phạm thật: gắn bản đồ bằng `scene.environment` thì **three BỎ QUA `material.envMapIntensity` hoàn toàn** — môi trường luôn rọi ở mức 1,0 bất kể khai gì. Cách phát hiện là phép thử vô lý: tô quả cầu dò **đỏ chói** rồi chạy với `ENV_DIFFUSE = 0`; cả thành phố **vẫn đỏ** (tươi 53,0) ⇒ cái núm chắc chắn không nối. Vá bằng cách gắn `envMap` vào TỪNG vật liệu (kéo theo: phải dựng môi trường TRƯỚC mọi vật liệu, vì vật liệu tạo sớm hơn sẽ nhận `undefined`). ⇒ **Ba luật**: (a) một thay đổi làm ảnh đổi "một chút" là hình dạng của cả nhiễu lẫn tín hiệu — chỉ hậu quả vô lý mới phân biệt được; (b) `catch` im lặng biến một tính năng HỎNG thành một tính năng VÔ HÌNH (chính khối `catch` tôi tự viết trong `createSkyEnvironment` đã giấu mất lỗi thật — nay là `console.warn`, và `city-preview.mjs` đã thêm cờ `--enable-logging=stderr`); (c) lý lẽ đúng + số sai vẫn ra bản vá vô dụng, nên **mọi thay đổi ánh sáng phải chụp-rồi-đo**, không được tin vào suy luận. Giá trị cuối `ENV_DIFFUSE = 0,12` chọn bằng bảng đo, không bằng cảm giác (0 → 32,4/24,5/43 · **0,12 → 35,3/20,3/38** · 0,20 → 37,0/18,1/36 · 1,00 → 51,3/14,4/29).
- ⚠️ **PHÂN BỐ MỘT TRƯỜNG NHIỄU TRÊN LƯỚI NHỎ KHÔNG TUÂN LUẬT SỐ LỚN — và cả ba lần đoán sai nguyên nhân đều nghe rất có lý** (bài học 2026-08-14, Phase 7B). Yêu cầu: 15 kỷ, mỗi kỷ khai 1–5 bậc thềm, và mọi kỷ phải DÙNG ĐỦ số bậc mình khai. Đo lần 1: **4 kỷ phẳng lì tuyệt đối**, 15 kỷ chỉ ra 11 trường khác nhau — vì hàm hình dạng gánh luôn cả biên độ (`plain: (n) => n * 0.35`) rồi `Math.round(0.35 × 1)` **làm tròn về 0**, tức nguyên một tham số bị nuốt sạch. Đo lần 2: kỷ 8 có **85% số ô nằm cùng một bậc** — vì trục dốc viết là `(x + y)`, mà tổng hai biến đều thì có **phân bố tam giác**, dồn hết về giữa; đổi sang `y` là xong. Đo lần 3: vẫn 5 kỷ dồn cục — và đây mới là nguyên nhân thật: lưới 12×12 với ô nhiễu 4,5 chỉ có **~9 giá trị mắt lưới độc lập**, tức cỡ mẫu là 9 chứ không phải 144. Luật số lớn **không áp dụng được**, nên phân bố thô không bao giờ trải đều bất kể chỉnh hàm hình dạng khéo tới đâu. Vá gốc: **căng trường ra trọn 0..1 (min→0, max→1) rồi mới chia bậc**, và chia bằng `floor` chứ không `round` (ô biên của `round` chỉ rộng bằng NỬA ô giữa, nên hai bậc đầu-cuối luôn thiệt). ⇒ **Ba luật**: (a) trên lưới nhỏ, hãy hỏi *"thật ra có bao nhiêu giá trị độc lập?"* trước khi tin bất kỳ trực giác thống kê nào; (b) hai bản vá đầu **đều đúng** mà **đều không đủ**, nên "sửa xong thấy đỡ hơn" là tín hiệu tệ nhất — phải đo lại từng lần chứ đừng cộng dồn cảm giác; (c) ⚠️ bản vá thứ ba **nuốt luôn** hai lỗi đầu (sau khi có bước căng, nhân hệ số nào vào hàm hình dạng cũng ra kết quả y hệt từng con số) — nên hai bài test tôi viết cho hai lỗi ấy **thành vô dụng mà vẫn xanh**, và chú thích của chúng thành lời nói dối. Đã thử ngược rồi viết lại cho đúng. Cùng họ với Phase 3Y: **sửa đúng KHÔNG chứng minh hiểu đúng**, và lời giải thích mới là thứ phiên sau kế thừa.
- ⚠️ **MỘT CÁI CỔNG CACHE «CÓ FILE RỒI THÌ BỎ QUA» LÀ QUẢ MÌN "TÊN FILE LÀM BẰNG CHỨNG" ĐỘI LỐT MỘT PHÉP TỐI ƯU — VÀ NÓ ĐẺ RA MỘT BẢNG SỐ HOÀN TOÀN HỢP LÝ** (2026-08-21, Phase 13 VIỆC B). Script dựng ảnh nghiệm thu có một dòng trông vô hại: `[ -f "$png" ] || node scripts/city-preview.mjs …`. Nó tiết kiệm thật — và nó biến **sự tồn tại của một tên file** thành **bằng chứng về nội dung file**, đúng quả mìn `MAI-SAU-ky9.png` (Phase 11) ở một hình dạng khó thấy hơn, vì lần này không ai chép nhầm gì cả: cái tên vẫn đúng, chỉ có NGÀY là cũ. Lượt so ảnh đầu tiên báo **kỷ 1 đổi 74,2% khung hình, lệch trung bình 61,82**; soi mốc thời gian thì ảnh "SAU" của kỷ 1 đề ngày **hai hôm trước**, kỷ 6 và 7 cũng cũ. Dựng lại sạch: **0,30% / 0,18**. ⇒ **Ba luật**: **(a)** thứ lộ ra sự thật KHÔNG phải một cổng nào cả — build xanh, lint sạch, test xanh, `md5` hai vế khác nhau, phép cộng các lớp vẫn ~100% — mà là **một MÂU THUẪN NỘI TẠI**: 74,2% khung hình đổi trong khi thứ vừa thêm chỉ chiếm 0,25% khung hình là điều **không thể cùng đúng**. Luôn giữ sẵn một đại lượng thứ hai để đối chiếu, vì một bảng số sai mà "hợp lý" thì không có cổng nào bắt được; **(b)** cổng cache phải so **DẤU VÂN TAY CỦA ĐẦU VÀO** (commit + tham số), không so sự tồn tại của tên file — hoặc bỏ hẳn cache đi, vì 25 giây dựng lại rẻ hơn nhiều một kết luận mỹ thuật sai; **(c)** bắt được vài phần tử cũ thì **dựng lại TOÀN BỘ tập**, đừng chỉ vá đúng những phần tử đã bắt — một bộ ảnh nhiều mốc thời gian chính là cái bẫy *"một phép đo trải trên NHIỀU trạng thái mã"* (2026-08-18), và phần chưa bắt được mới là phần nguy hiểm.
- ⚠️ **KHÔNG BAO GIỜ DỰNG LẠI MỘT CÔNG CỤ TỪ TRÍ NHỚ — VÀ CÁI KHO CHỨA MÃ CŨNG LÀ MỘT THIẾT BỊ ĐO CÓ THỂ HỎNG** (2026-08-21, Phase 13 VIỆC B). Giữa phiên, kho trong hộp cát **bị khôi phục về một ảnh chụp cũ hơn**, xoá sạch nguyên một vòng đo. Phản xạ đầu tiên — *"viết lại công cụ ấy từ đầu, mình vừa viết nên còn nhớ"* — là phản xạ SAI, và nó sai theo đúng cách nguy hiểm nhất: một công cụ dựng lại từ trí nhớ **trông giống hệt** bản gốc, chạy được, in ra số, và không có gì nói rằng một hằng số đã lệch (đúng hình dạng `BUILDING_SCALE = 0.86` chép tay ở `plinth-tri.mjs`, đếm 3 bệ thay vì 31). ⇒ **Ba luật**: **(a)** khôi phục từ git (`git show <commit>:<đường-dẫn>`); không có trong git thì viết MỚI và **đối chiếu chéo với mã sản phẩm** bằng một đường đo thứ hai, tuyệt đối không tin bản nhớ lại; **(b)** **commit + push sau MỖI mốc có giá trị**, đừng dồn tới cuối phiên — một `git push` là thứ duy nhất sống sót qua việc kho bị khôi phục; **(c)** **in `git rev-parse HEAD` ở đầu mỗi khối việc** rồi so với commit vừa đẩy: lệch nhau ⇒ DỪNG và đồng bộ trước khi làm gì tiếp, vì mọi phép đo chạy sau đó sẽ mô tả một cây mã khác với cây mã mình tưởng (cùng họ bài học *"một phép đo chạy nền là ảnh chụp của cây mã tại NHIỀU thời điểm"*, 2026-08-18). Đây là lần đầu thứ nói dối không phải một công cụ đo mà là **môi trường** bên dưới nó.
- ⚠️ **KHI PHÉP ĐO CÃI LẠI CON MẮT, KIỂM CÔNG CỤ TRƯỚC — lần thứ 17, và lần này nó nói dối bằng NHÃN chứ không bằng SỐ** (Phase 7B). Công cụ mới `scripts/frame-fit.mjs` (đo công trình nào bị mép khung hình cắt) báo "bị cắt ở mép TRÊN" cho gần như mọi kỷ, trong khi ảnh chụp cho thấy rành rành các mái nhà đều nguyên vẹn còn thứ bị xén nằm ở **mép DƯỚI**. Thủ phạm: vector `right` viết ngược dấu (`fwd.z, 0, -fwd.x` thay vì `-fwd.z, 0, fwd.x`), kéo theo `up` suy ra từ nó cũng lộn 180°. Điểm hiểm: **ĐỘ LỚN của biên vẫn đúng tuyệt đối** vì phép đo lấy trị tuyệt đối — nên mọi con số đều chính xác và mọi kết luận định lượng đều dùng được, chỉ có nhãn mép là đảo. Một công cụ sai kiểu này rất dễ được tin, vì phần dễ kiểm nhất của nó (các con số) thì đúng. ⚠️ Và phép tự kiểm phải chạm **từng trục**: bản `--selftest` đầu chỉ vặn khoảng cách camera, tức chỉ bảo chứng trục DỌC và mù hoàn toàn với trục NGANG — đúng chiều đang sai. Nay có 4 ca, trong đó hai ca bóp/nới riêng tỉ lệ khung và một ca đòi đúng TÊN mép. Cùng họ với bài học `--selftest` ở Phase 4C/4G: **một phép tự kiểm chứng minh bộ lọc CÓ tác dụng, không chứng minh nó có tác dụng ĐÚNG.**
- ⚠️ **MỘT TRƯỜNG "KHÔNG PHỤ THUỘC DỮ LIỆU NGƯỜI CHƠI" PHẢI ĐƯỢC KHOÁ BẰNG TEST GỌI KÈM DỮ LIỆU RÁC** (Phase 7B). `buildTerrain({era, gridSize})` cố tình không nhận danh sách công trình — nếu cao độ đổi theo tiến độ thì mỗi lần Đàm xây xong một căn nhà cả quả đồi sẽ nhích, nhà cũ lún hoặc nhô, và **không có gì báo**. Nhưng "hàm hiện không nhận tham số đó" là một sự thật rất dễ mất: người sau chỉ cần thêm một tham số tuỳ chọn là bất biến chết mà mọi test vẫn xanh. Cách khoá rẻ và hiệu quả: gọi hàm **kèm dữ liệu rác** (`{era: 7, gridSize: 12, built: ['a','b'], buildings: [1,2,3]}`) rồi đòi kết quả **y hệt** lần gọi sạch. Áp dụng được cho mọi bất biến dạng "thứ này không được phụ thuộc thứ kia" — cùng họ với ADR-007 (vị trí) và `tier` (thứ tự mở đường).
- ⚠️ **NHỮNG THỨ ĐỨNG TRÊN MẶT ĐẤT NHIỀU HƠN TA TƯỞNG — sáu chỗ, và quên chỗ nào cũng im lặng** (Phase 7B). Đổi mặt đất từ phẳng sang có cao độ thì mệnh đề *"y của mọi thứ trên mặt đất = 0"* hết đúng, và nó được phát biểu lại ở **sáu** nơi trong `sceneGraph.js`: ô nền · đường sá · công trình · bệ kè · cảnh vật · cư dân. Quên một chỗ ⇒ thứ đó lơ lửng giữa trời hoặc lún dưới đất, trong khi **build xanh · lint sạch · 625 test xanh · không một cảnh báo nào**. Đúng hình dạng sai của Phase 4D. ⚠️ Và có một nửa RẤT dễ mất: đọc `top` (đứng ở cao độ cao nhất dưới bóng mình) thì hiển nhiên, nhưng bỏ `drop` (phần hụt → **bệ kè**) thì nhà vắt qua mép thềm sẽ treo một góc giữa không khí — cũng im lặng. Đã khoá bằng bảng `GROUND_ANCHORS` trong `sceneGraphWiring.test.js`, cả 7 assert đều đã thử ngược và thấy đỏ. **Cách làm chung: đổi một luật nền tảng thì liệt kê MỌI chỗ phát biểu lại nó thành một bảng có tên, đừng sửa theo triệu chứng bắt gặp.**
- ⚠️ **KIM LOẠI KHÔNG CÓ GÌ ĐỂ PHẢN CHIẾU THÌ RENDER RA ĐEN — bản đồ môi trường là ĐIỀU KIỆN CẦN, không phải điểm tô thêm** (Phase 7A). Kim loại gần như không có thành phần khuếch tán: màu nó hiện ra hầu hết là thứ nó phản chiếu. Nên `metalness: 0.9` trong một cảnh không có bản đồ môi trường cho ra một khối đen tuyền — và vì nó **vẫn "ra màu"**, lỗi này rất khó truy. Hệ quả thực tế: `CityScene3D.jsx` và **cả hai** chỗ gọi trong `scripts/city-preview.mjs` đều PHẢI truyền `renderer` vào `createCityScene`; quên một chỗ thì trang xem thử lặng lẽ đóng khung khác app (đúng bẫy "một luật ba chỗ phát biểu"). Đã khoá bằng `sceneGraphWiring.test.js`.
- ⚠️ **HỎI `/tênHàm\(/` TRÊN MÃ NGUỒN THÌ DÒNG `function tênHàm(` TỰ NÓ LÀ MỘT MATCH — gỡ sạch mọi lời GỌI vẫn không đỏ** (bài học 2026-08-14, Phase 7A; **cắn ba lần liên tiếp trong cùng một file test**). Bài `sceneGraphWiring.test.js` canh "hàm X có được gọi không" bằng regex. Lần 1: `paintSkyGradient` — đòi "≥2 lần xuất hiện" trong khi định nghĩa + 1 lời gọi đã đủ 2. Lần 2: `createSkyEnvironment` — vá lần 1 bằng `(?<!function )` nhưng quên chỗ này. Lần 3: **ngay trong cái assert vừa viết ra để vá lần 2**. Vá theo triệu chứng thì chỗ thứ tư viết sau này sẽ lại quên. ⇒ Vá GỐC: lọc mọi dòng định nghĩa hàm ra một hằng số riêng (`CALLS`) và mọi phép kiểm "có được gọi không" đều hỏi trên đó — một luật một công thức. ⚠️ Kèm hai bài phụ cùng phiên: (a) **assert "có ít nhất một chỗ" là cái phễu, không phải hàng rào** — `/envMap,/` xanh oan vì file có 4 vật liệu mang `envMap`, gỡ đúng cái DUY NHẤT chứa kim loại vẫn còn 3 cái kia đỡ; phải hỏi ĐÍCH DANH khối cần canh; (b) **phép thử ngược phải kiểm bằng `diff` xem file có đổi thật không** — hai lần "thử ngược không thấy đỏ" đầu tiên hoá ra là do regex sửa file bị trượt do thụt lề, tức tôi suýt kết luận sai về chính bài test của mình.
- ⚠️ **MỖI KỶ LẤY MỘT ĐẤT NƯỚC LÀM BIỂU TƯỢNG** (Đàm yêu cầu 2026-08-14): `country`/`landmark` trong `eraStyle.js`, 15 nước KHÔNG trùng nhau — Thổ Nhĩ Kỳ · Ai Cập · Iraq · Trung Quốc · Đức · **Việt Nam** (kỷ 6) · **Ý** (kỷ 7) · Bồ Đào Nha · **Pháp** (kỷ 9) · Anh · Mỹ · Nga · Nhật Bản · Singapore · UAE. Hai trường này KHÔNG phải nhãn dán cho đẹp — chúng là **lời giải thích cho những con số nằm cùng dòng**: muốn đổi `roof`/`massScale`/`motifs` của một kỷ thì phải trả lời được *"công trình có thật nào ở nước ấy trông như vậy?"*, nếu không thì con số ấy là tuỳ hứng, và tuỳ hứng chính là thứ đã sinh ra 15 kỷ cao bằng nhau. Màn Thành Phố hiện một dòng "Kiến trúc lấy mẫu từ …" đọc thẳng từ bảng này, KHÔNG chép lại thành bảng riêng ở tầng giao diện.
- ⚠️ **HAI CÁI CỔNG NỐI TIẾP NHAU: MỘT PHÉP PHÁ CHỈ MỞ MỘT CỔNG THÌ KHÔNG NỔ — VÀ ĐÓ LÀ MỘT PHÁT HIỆN VỀ KIẾN TRÚC, KHÔNG PHẢI MỘT PHÉP PHÁ HỎNG** (2026-08-20, Bước C). Để thử bài *"kỷ 1 là kỷ khô duy nhất"*, tôi thêm kỷ 1 vào danh sách `ERAS_WITH_WATER_GEOMETRY` rồi chạy test. **KHÔNG đỏ.** Phản xạ đúng của dự án (Phase 8A) là *nghi CHÍNH PHÉP PHÁ trước* — và lần này nghi đúng, nhưng lý do thì đáng nhớ hơn cả bản vá: `waterIsBuilt(era) = DANH_SÁCH.includes(era) **&&** hasWater(era)`, mà `hasWater` đọc thẳng cột `water` của **BẢNG** `settingStyle.js`, nơi kỷ 1 khai `water: 'none'`. Tức **BẢNG mới là thứ cai trị**; danh sách chỉ là cái van THỨ HAI, và mở một van của hai van nối tiếp thì nước vẫn không chảy. Phép phá đúng (MS1b) phải mở CẢ HAI, và nó đỏ ngay đúng chỗ đã nêu trước (`+ 'water'`). ⇒ **Ba luật**: (a) trước khi kết luận một bài test mù, hỏi *"thứ tôi vừa sửa có phải điều kiện ĐỦ không, hay chỉ là một trong nhiều điều kiện CẦN?"* — `grep` chính cái hàm quyết định và đếm số toán tử `&&` trong nó; (b) đây là mặt TRÁI của bài học *"một nút bịt đặt ở chỗ trung tâm chỉ bịt được đường nào đi qua nó"* (Phase 11): ở đó một cửa sau **đi vòng** qua nút bịt, ở đây một phép phá **không đi hết** chuỗi cửa — cùng một câu hỏi *"có bao nhiêu cửa, và tôi đang đứng ở cửa nào?"*; (c) và điều đáng giữ lại nhất: cái "thất bại" ấy **chứng minh một bất biến mà chưa bài test nào phát biểu** — không thể làm một kỷ có nước chỉ bằng cách sửa danh sách hình học, phải sửa bảng địa thế trước. Ghi ra thì nó thành tài liệu; im lặng thì phiên sau sẽ lại mất một vòng đúng chỗ này.
- ⚠️ **MỘT CẶP ẢNH TRƯỚC/SAU CHỨA SẴN ĐỐI CHỨNG CỦA CHÍNH NÓ, NẾU TRONG ĐÓ CÓ THỨ BẮT BUỘC PHẢI KHÔNG ĐỔI** (2026-08-20, Bước C). Bảng số Bước C có **ba** con số 0 tuyệt đối: kỷ 1 (khô), kỷ 12 và 14 (đã dựng nước từ Bước B) không đổi **một đơn vị** ở cả cột tam giác lẫn cột lệnh vẽ, và trong phép so ảnh `sweep-diff` chúng cũng ra đúng **0,0**. Ba số 0 ấy không phải kết quả — chúng là **cái cân**: nếu phép đo bị lệch phiên bản (đo hai cây mã khác nhau), bị lẫn tải máy (`TECH_DEBT #50`), hay bị chép nhầm cột (`TECH_DEBT #43`), thì gần như chắc chắn chúng sẽ không còn là 0. ⇒ **Khi thiết kế một phép đo trước/sau, hãy cố ý để lại trong tập đo vài trường hợp BẮT BUỘC phải đứng yên** — nó rẻ bằng không, nó chạy mỗi lần đo, và nó là thứ duy nhất phân biệt *"thay đổi này không ảnh hưởng chỗ kia"* với *"phép đo của tôi đang nhìn nhầm chỗ"*. Cùng họ với luật *"gác chạy-rỗng"* trong test, nhưng áp cho BẢNG SỐ chứ không cho vòng lặp.
- ⚠️ **SỬA MỸ THUẬT THÀNH PHỐ 3D thì PHẢI QUÉT, đừng soi từng ảnh rời** (bài học 2026-08-12, Phase 3G): `node scripts/city-preview.mjs --sweep --theme light --eras 1,2,3,4,5` ghép 5 kỷ × 6 chặng ngày vào MỘT tấm ảnh ở `.city-preview/` (~21 giây; đổi `--theme dark`, `--eras`, `--cell`). Lý do phải là bảng chứ không phải ảnh rời: **lỗi mỹ thuật gần như luôn là lỗi SO SÁNH.** Một mặt đất màu cỏ trông vẫn "bình thường" cho tới khi đặt cạnh mặt đất đất-son của kỷ bên; một chân trời xám chỉ lộ ra khi nằm giữa chân trời hồng của bình minh và của hoàng hôn. Đợt quét đầu tiên bắt được **6 lỗi đã chạy trên production nhiều ngày** mà cách soi từng ảnh rời trước đó không hề thấy — trong đó có mái tím sen ở 6/15 kỷ và theme tối làm giữa trưa tối như nửa đêm. Chụp một cảnh duy nhất (có đủ lớp tối góc, đúng như Đàm thấy) thì bỏ `--sweep`; thêm `--pending 3` để soi công trình đang xây (giàn giáo). ⚠️ **`--t <giây>` chọn THỜI ĐIỂM hoạt hoạ** (trước 2026-08-22 mọi ảnh đều dựng ở đúng `updateResidents(17.5)` nên cư dân luôn ở **một dáng duy nhất** và không ai soi được dáng đi); `--lowdetail` dựng bản LOD thấp — dùng làm **ĐỐI CHỨNG** khi đo cư dân, vì mô hình 2 hộp không có khớp nào. ⚠️ Nhưng `--lowdetail` là cờ TOÀN CẢNH (nó cũng hạ chi tiết nhà, cây), nên **đừng lấy hiệu số tam giác giữa hai bản dựng để suy ra tam giác cư dân** — hỏi thẳng `buildHumanBody(era).parts.length × số cư dân × 12`. Muốn biết cư dân cao mấy điểm ảnh trên khung thật của Đàm: `node --import ./scripts/register-esm-loader.mjs scripts/human-scale.mjs` (có `--gait`, `--eras`, `--selftest`). ⚠️ **Nhưng bản quét chỉ soi được thứ TO** — mỗi ô rộng ~300px cho cả một thành phố, nên một cái cây chỉ chiếm chục điểm ảnh. Soi những thứ NHỎ (cây, đèn, đá, cư dân) thì phải chụp một kỷ ở `--width 1500` (`node scripts/city-preview.mjs --era 14 --hour 12 --width 1500`); chính ở cỡ đó mới thấy được cọ kỷ 14 dẹt thành dấu "✳" khi nhìn từ đúng trên xuống (`TECH_DEBT #29`), thứ mà bản quét không hé một lời.
- ⚠️ **MỘT BÀI TEST CANH "THỨ ĐÀM NHÌN THẤY" MÀ ĐỌC CON SỐ ĐÃ *KHAI* THAY VÌ CON SỐ ĐÃ *DỰNG* THÌ ĐANG CANH MỘT THẾ GIỚI KHÁC — và nó xanh suốt nhiều tháng về một đại lượng chưa bao giờ tới được mắt** (2026-08-18, ADR-033, đóng `TECH_DEBT #42`). Bảng mặt đường có trục `walk` (bề rộng vỉa hè) và một bài test canh *"kỷ hiện đại phải có vỉa hè rõ"*. Bài ấy hỏi `STREET_STYLES[era].walk` — thứ bảng KHAI. Màn hình thì dựng `streetCrossSection(...).walk` — thứ còn lại **sau** phép kẹp `walk ≤ 0,5 − avenue/2`. Hai số ấy lệch **9,5 lần** ở kỷ 12 (khai 0,19 · dựng 0,02 ≈ 1,3 điểm ảnh), và 8/15 kỷ bị bóp. Không có gì đỏ lên, vì bài test và màn hình đang nói về hai đại lượng khác nhau cùng tên. ⇒ **Ba luật**: **(a)** với mọi lời hứa về thứ NHÌN THẤY, hỏi *"con số tôi đang assert có phải chính con số đi tới điểm ảnh không, hay còn một phép biến đổi nào ở giữa?"* — chỗ nguy hiểm là bất kỳ hàm `min`/`clamp`/`??` nào nằm giữa bảng và hình học; **(b)** ⚠️ **hai con số hiệu chuẩn của MẮT (`CELL_PIXELS = 64`, `EYE_PIXELS = 4`) khi ấy chỉ là bản chép tay nằm TRONG FILE TEST**, nên `isValidStreetStyle` **không thể** canh ngưỡng mắt dù có muốn — mã sản phẩm không biết chúng tồn tại. Đúng bẫy "một luật một công thức", và đúng cái vế người ta hay quên: một luật nằm ở phía test thì phía sản phẩm mù hoàn toàn. Nay cả hai `export` từ `streetStyle.js`, test `import` về; **(c)** cách sửa ĐÚNG là **TỪ CHỐI THẲNG ở validator, không tự kẹp** — và Đàm ra thêm một luật mà ba phương án tôi đề xuất đều thiếu: *nới cho vượt ngưỡng nhìn thấy được, HOẶC khai thẳng `0` — **không có gì ở giữa***. Khoảng giữa mới là chỗ độc: một vệt 1,3 điểm ảnh không phải vỉa hè mà cũng không phải "không có vỉa hè", nó chỉ là nhiễu, và nó khiến chú thích của kỷ ấy nói dối.
  - ⚠️ **KÈM THEO — CÁI KẸP CHỈ LÀ TRIỆU CHỨNG; BỆNH GỐC LÀ MỘT TRƯỜNG ĐƯỢC *VIẾT* MỘT NGHĨA VÀ ĐƯỢC *ĐỌC* MỘT NGHĨA KHÁC.** Ba phương án đầu tôi nghĩ ra đều xoay quanh hình học (cho vỉa hè lấn sang ô đất · hợp thức hoá phép kẹp · ép bảng khai nhỏ lại) — cả ba đều nhận ngầm rằng *"đại lộ rộng thì tất nhiên hết chỗ cho vỉa hè"*. Sai. `avenue` được **viết** như thể nó trả lời *"đại lộ này oai tới đâu"*, còn mã **đọc** nó là *"bao nhiêu phần mặt cắt dành cho XE"* — và ngoài đời hai câu ấy gần như NGƯỢC nhau: Champs-Élysées rộng 70m thì **21m mỗi bên là vỉa hè**, tức hơn 60% mặt cắt dành cho người đi bộ. Khai Paris `0,94` không phải "chật quá", mà là **sai lịch sử**. ⇒ Đây là lần thứ **năm** của hình dạng *"một trường gánh hai việc"* (`storyHeight` · `roof` · bảng loài cây · `avenue`), nhưng ở một biến thể mới và khó thấy hơn: trường này chỉ gánh MỘT việc trong mã — nó gánh việc thứ hai **trong đầu người điền bảng**. Câu hỏi phát hiện vẫn là câu cũ (*"ngoài đời hai thứ này có luôn đi cùng nhau không?"*), nhưng phải hỏi thêm một vế: ***"cái tên trường này gợi cho người điền bảng nghĩ tới đại lượng nào, và mã đang tính đại lượng nào?"*** Lệch nhau ⇒ bảng sẽ bị điền sai một cách rất tự tin, kèm `note` giải thích rành mạch cho con số sai ấy.
  - ⚠️ **KÈM THEO — HAI PHÉP ĐO CÃI NHAU VÌ ĐẦU VÀO, KHÔNG VÌ PHÉP ĐO; VÀ NÓ TÁI DIỄN CHỈ VÀI NGÀY SAU KHI BÀI HỌC ẤY ĐƯỢC GHI RA.** Đối chiếu chéo tam giác: Chromium báo `−1.648` ở kỷ 9, phép đếm thuần báo `−2.136`. Đủ gần để tưởng là sai số, đủ xa để không thể lờ. Nguyên nhân: phép đếm thuần dựng thành phố ở `sessionCount: 400` (chép từ fixture của `terrainMesh.test.js`) còn `city-preview.mjs` mặc định **40**, mà mạng đường MỞ DẦN theo số phiên ⇒ hai bên đếm hai thành phố khác nhau. Cho bằng đầu vào thì khớp **từng đơn vị** ở cả 5 kỷ. ⇒ *"một con số nghiệm thu phải đi kèm CÔNG CỤ **và ĐẦU VÀO** đã sinh ra nó"* (Phase 11) — và cụ thể hơn: **khi chép một fixture từ một file test sang một phép đo, cái được chép là một LỰA CHỌN của file ấy, không phải mặc định của hệ thống.** Hai con số cùng tên `sessionCount` ở hai nơi là đủ để đẻ ra một bảng số sai mà không bên nào kêu.
- ⚠️ **MỘT CÁI BỆ KHÔNG CẦN MỘT CÁI BẬC ĐỂ ĐỌC RA LÀ BỆ — BẬC LÀ *GIÁN ĐOẠN*, BỆ LÀ MỘT *KIỂU PHÂN BỐ ĐỘ DỐC*. Ba vòng con số xanh trong khi mắt Đàm vẫn thấy sai, vì mọi phép đo đều đi tìm gián đoạn** (2026-08-21, ADR-046). Đàm bác cả 15 kỷ: *"VẪN CÒN CÁI BỆ."* Anh cũng chỉ ra chỉ thị trước đó của chính cố vấn — *"trong lưới thành phố thì thoải, ngoài lưới mới gồ ghề"* — **chính là định nghĩa của một cái bệ**: *"Không phải thực thi sai; chỉ thị sai."* Phát biểu lại cho đúng: **địa hình là MỘT mặt liên tục; thành phố NẰM TRÊN một phần đất vốn đã bằng hơn, và ranh giới của vùng bằng ấy TUYỆT ĐỐI KHÔNG được trùng ranh giới lưới 12×12** (*"ngoài đời không có nơi nào mà đất bằng kết thúc đúng ở mép nhà cuối cùng"*). Đo bằng đại lượng ĐÚNG (`scripts/plateau-score.mjs` — vành đồng tâm bước 0,5 ô, **chỉ số bệ = dốc lớn nhất vành 6–9 ÷ dốc trung bình vành 0–5**, một QUAN HỆ chứ không phải một mức): **10/15 kỷ ≥ 5**, tệ nhất kỷ 14 = **26,98**, và **cả 15 kỷ nhảy trong đúng dải bán kính 7,25–8,75** ⇒ bước nhảy do **LƯỚI** quyết định, không do địa hình. ⇒ **Ba nguồn, và cả ba đều là *một hằng số được chọn ĐỂ LÀM RA cái bệ*** — đó mới là bài học, không phải ba con số: **(a)** vùng bằng rộng 2,6 ô ⇒ cả một khoảng tụt bị nhồi vào chưa tới ba ô (nay `APRON_CELLS = 7,5` **nhân** `APRON_SPREAD = 0,62` bằng một tầng nhiễu **RẤT thô, cỡ ô 9** ⇒ bề rộng thật **2,85…12,15 ô tuỳ hướng**; ⚠️ nới rộng THÔI thì chưa đủ — mép vẫn cách đều mép lưới, tức vẫn là hình vuông bo góc chỉ to hơn; và nhiễu MỊN thì vô dụng, nó chỉ làm răng cưa một đường tròn mà mắt vẫn đọc ra đường tròn ấy); **(b)** `APRON_DROP = 0,62` là **một cái bậc được KHAI BÁO**, chọn để *"đủ để đọc ra thành phố nằm trên cao"* — đặt cạnh sự thật thì **`terrainMaxHeight` của 11/15 kỷ nhỏ hơn 0,62** (kỷ 11 chỉ 0,14), tức cái vành quanh thành phố cao **gấp 4,4 lần** toàn bộ địa hình bên trong: không có ngọn đồi nào cả, chỉ có một mặt bàn (nay **0,18** — nhỏ nhất còn giữ được mặt nước dưới mọi đất khô, và **nhỏ hơn biên độ gợn của chính đồng bằng**, nên có chỗ đồng bằng **CAO HƠN** nền phố); **(c)** phép `settle`-về-phẳng tại `APRON_EDGE` ép **mọi kỷ về cùng một mặt phẳng ở cùng một bán kính**, đẻ ra một vành **phẳng tuyệt đối rộng 5,7 ô** — *một mặt bàn đứng giữa một sàn nhà*, và **không phép đo gián đoạn nào thấy được vì cả hai bên đều phẳng** (nay **xoá hẳn**; hằng số đổi tên `APRON_EDGE` → `PLATE_PAD_CELLS` vì cái tên cũ đã thành một lời nói dối). ⚠️ Kèm ba bài phụ: **(1)** `nenRoll` phải **BÃO HOÀ KHÔNG ĐỐI XỨNG** — cái trần tồn tại vì đúng MỘT lý do (đất khô không được chui xuống dưới mặt nước) và lý do ấy **chỉ nói về chiều XUỐNG**; áp cho cả chiều LÊN là bẫy Phase 7D ở dạng ngược, và chính cái kẹp thừa ấy giữ cho đồng bằng vĩnh viễn nằm dưới nền phố; **(2)** lời hứa Phase 9A với `horizon.js` KHÔNG mất mà chỉ được **phát biểu lại thành một QUAN HỆ** — nó đòi *hai tấm KHỚP NHAU tại chỗ giáp*, chứ không đòi cả hai bằng một hằng số; nay `horizon.heightAt` đọc thẳng `terrain.nenKho(...)` nên chúng khớp **theo cấu tạo**, ở mọi hướng, mà không bên nào phải phẳng; **(3)** ADR-007 nguyên vẹn vì trong lưới `nenKho` trả về **y hệt** `smoothHeightAt` — không đổi một chữ số.
- ⚠️ **MỘT PHÉP HOÀ MÀU ĐẶT SAI THỨ TỰ TRONG MỘT CHỒNG TẦNG KHÔNG SAI VỀ CÔNG THỨC — NÓ CHỈ *XOÁ HAI TẦNG NẰM SAU NÓ*, VÀ CHỈ Ở ĐÚNG CÁI RÌA** (2026-08-21, ADR-047). Sau khi hình học đã hết bệ, mắt vẫn đọc ra một **đường viền vuông sắc lẹm** quanh thành phố ở cả 15 kỷ (trên màn hình là một hình thoi, vì camera nhìn chéo). `groundColorAt` có ba tầng (nền theo cao độ → vết loang → sườn dốc lộ đất) và **một phép hoà** ra màu vùng đất bao quanh; phép hoà ấy đứng **CUỐI**, mà tại rìa hệ số hoà đúng bằng **1**, nên nó **xoá sạch** tầng 2 và tầng 3 trong khi tấm chân trời ngay bên cạnh vẫn còn đủ cả hai. Đo trên chính tầng màu thuần: bước màu qua chỗ giáp **trung vị 20,0–36,4 · p99 59,9–74,3** trên thang RGB/255 ở **cả 15 kỷ** (ngưỡng mắt 12); độ tản màu tại rìa **đúng 0** — một dải màu chết — so với **47,6–55,6** sau bản vá. Chuyển phép hoà lên **giữa tầng 1 và tầng 2** ⇒ bước màu trên ảnh dựng thật **26,8 → ~6**, và trong lưới kết quả **không đổi một chữ số**. ⇒ **Ba luật**: **(a)** mỗi lần thêm một tầng vào một chồng tầng, hỏi *"tầng này nằm trước hay sau phép hoà, và ở rìa thì hệ số hoà bằng bao nhiêu?"* — hệ số bằng 1 nghĩa là **mọi thứ nằm sau nó không tồn tại** ở rìa; đây là biến thể mới của *"một trường gánh hai việc"*: **một `lerp` gánh hai việc** (vừa trả lời *"đất này thuộc phố hay thuộc vùng quê"*, vừa vô tình trả lời *"đất này có vân hay không"*); **(b)** ba giả thuyết đầu **đều bị chính số đo bác bỏ** — số hạng đỉnh chói (≤ **8,8**/255), bóng đổ (`--no-shadow` thì hình thoi **vẫn nguyên**), pháp tuyến hai tấm lệch (kỷ 15 chênh Lambert tối đa **3,9**/255) — và ghi lại ba cái bác ấy là có ích, vì *một bản vá đúng-về-lý-lẽ mà không đổi được gì thì nó vừa loại trừ giúp ta một nghi phạm*; **(c)** bài test đi kèm phải có **vế đối chứng đòi độ tản màu ở rìa còn ít nhất một nửa so với trong lòng** — không có vế ấy thì "hai tấm khớp màu" đạt được bằng cách làm **cả hai** tấm chết như nhau.
- ⚠️ **MỘT BÀI TEST HỎI «CÁC ĐỈNH CÓ NẰM ĐÚNG CHỖ KHÔNG» THÌ MÙ HOÀN TOÀN VỚI «TAM GIÁC NÀY CÓ ĐƯỢC VẼ RA KHÔNG» — 13,9–34,4% MẶT ĐƯỜNG CHƯA TỪNG HIỆN LÊN MÀN HÌNH, SUỐT NHIỀU THÁNG, VỚI MỘT BÀI TEST XANH ĐỨNG NGAY CẠNH** (2026-08-21, Phase 14 §1(1), ADR-050). Đàm: *«cái đường có nét đứt trông giả tạo kinh khủng»*. Chữ **«tự dưng»** chỉ vào một hồi quy nên việc đầu tiên là **BISECT, không phải đoán** — và bisect nói: nét đứt **y hệt ở `main`**, VIỆC B vô can (nó chỉ làm khuyết tật DỄ THẤY hơn, vì trước đó mắt không có gì liền mạch bên cạnh để so). Rồi **bốn** giả thuyết lần lượt bị chính số đo bác: ô đường thiếu (**80/144 ô, 88 cặp kề, 0 khe**) · hình học hở (rasterise 6 mẫu/ô ra mạng `#` liền tuyệt đối) · đất chôn mất đường (hiệu số đất−đường = **−0,0140 ở cả p25/trung vị/p75**, đúng bằng `ROAD_LIFT`) · z-fighting (nhân `ROAD_LIFT` lên **10 lần**, **ảnh không đổi một chút nào**). Thứ trả lời được là một đại lượng **chưa ai từng hỏi: CHIỀU QUAY** — `FrontSide` (mặc định của three) vứt mọi tam giác xếp ngược chiều, nên ba đỉnh nằm **đúng chỗ** vẫn có thể **không tồn tại trên màn hình**. ⇒ **Bốn luật**: **(a)** ⚠️ **VỊ TRÍ và ĐỘ HIỂN THỊ là hai đại lượng** — bài `LÒNG ĐƯỜNG DỰNG ĐÚNG HÌNH MÀ carriagewayShape KHAI` đã canh đúng vị trí từng đỉnh và vẫn xanh rực rỡ về một mặt đường thủng một nửa; cùng họ `TECH_DEBT #42` (*assert con số đã KHAI thay vì con số đã DỰNG*) nhưng ở bậc khó thấy hơn, vì ở đây assert ĐÚNG con số đã dựng mà vẫn mù; **(b)** luật *"tấm nằm ngang phải ngửa mặt lên trời"* xưa nay được phát biểu ở **SÁU** chỗ gọi (mọi lời `quad(...)` của vỉa hè/vạch kẻ đều bọc `Math.min`/`Math.max`) và bị quên ở **chỗ thứ bảy** — đúng bài học Phase 11 *"bịt mười lăm chỗ thì chỗ thứ mười sáu sẽ quên"*; bản vá phải đặt ở **cái cửa DUY NHẤT** (`quad4`, nơi mọi tấm ngang đi qua), không phải đảo dấu ở bốn chỗ gọi; **(c)** ⚠️ **một bản vá đúng MỘT NỬA là loại nguy hiểm nhất** — cách "bắt `dai()` tự chuẩn hoá `from`/`to`" chữa được cánh TÂY nhưng **không chữa cánh NAM** (cánh nam đúng thứ tự mà vẫn ngược chiều, vì nguyên nhân nằm ở phép đổi trục `u,v → x,z`), và nó làm con số tụt đủ nhiều để trông như đã xong; **(d)** ⚠️ **`side: DoubleSide` làm triệu chứng biến mất ngay và phải BỊ LOẠI** — nó không sửa gì, nó TẮT phép kiểm, và nhân đôi số mảnh phải tô cho cả những tam giác đang đúng. **Số**: diện tích mặt đường nhìn thấy được **80,8% → 100,0%** (15 kỷ), hai kỷ Đàm kêu chính là hai kỷ tệ nhất bảng (**kỷ 1 = 65,8%** · **kỷ 2 = 71,5%**); tổng diện tích hình học **trùng tới ba chữ số thập phân ở cả 15 kỷ**, tam giác và lệnh vẽ trùng từng đơn vị ⇒ bản vá chỉ đổi THỨ TỰ ba đỉnh. Phép đo dùng để nghiệm thu là **tổng diện tích tam giác NGỬA chiếu xuống mặt phẳng** — thuần Node, không cần Chromium, nên nó miễn nhiễm với nhiễu SwiftShader (`TECH_DEBT #50`) và với trần 4 MiB của ổ cắm CDP.

- ⚠️ **VÀ CÁI GIÁ CỦA BẢN VÁ MÀU ẤY RƠI ĐÚNG VÀO MỘT PHÉP TỰ-KIỂM ĐANG ĐƯỢC VIẾT THÀNH MỘT MỨC** (2026-08-21). Vết loang nhân vào một màu đã nhạt hơn ⇒ vùng đất xa tối đi **0,01–0,02** (vì `Math.min(1, c × m)` chỉ kẹp phía sáng) — dưới ngưỡng mắt, nhưng **đủ để làm rơi cổng tự-kiểm hình học của `sweep-score.mjs`**, thứ khi ấy hỏi `dL > nL + 0,15` với biên thật vỏn vẹn **0,010 ở kỷ 1 và kỷ 4** = đúng **một đơn vị làm tròn**. Cách vá KHÔNG phải nới con số (phễu Phase 9A) mà là **viết lại thành QUAN HỆ**: `dL > nL × 1,6`, ngưỡng nằm giữa hai đầu ĐO ĐƯỢC (tỉ số nền thật **1,93 … 2,45**), **cộng một đối chứng BẮT BUỘC** — lấy mẫu lệch nửa hàng thì cổng PHẢI kêu (đo được: kêu ở **4/15** hàng), không kêu thì công cụ tự thoát lỗi. ⇒ **Đụng vào một công cụ ĐO thì phải nói thẳng ra với Đàm kèm bằng chứng biên cũ mỏng cỡ nào** — sửa thước trong lúc đang đo là việc dễ bị đọc thành gian lận, và cách duy nhất để nó không phải là gian lận là công khai con số biên + thêm răng cho cái thước.
- ⚠️ **ĐỊA HÌNH CÓ NGỮ PHÁP RIÊNG: NHIỄU PHẢI **BẺ CONG** ĐƯỜNG ĐỒNG MỨC, KHÔNG ĐƯỢC **CỘNG** VÀO CAO ĐỘ — cùng một hạt nhiễu, cùng một biên độ, mà một cách dùng đẻ ra 36,7 lần đổi chiều còn cách kia đẻ ra 15,8** (2026-08-20, §1(B), ADR-045). Đàm nhìn ảnh thu nhỏ và nói đất *"lòi lõm như tấm chăn nhàu"*. Đo ra bốn con số định nghĩa chữ "nhàu": chênh cao trong lưới 12×12 tệ nhất **2,70 đv** · bậc giữa hai ô KỀ NHAU **1,15** ⇒ dốc **172%** (gấp 5 lần Baldwin Street) · **đổi chiều cao 36,7 lần** dọc một đường cắt (đồi thật đổi 1–2 lần) · **R² hướng 0,174** — chỉ 17% biên độ cao độ giải thích được bằng một mặt phẳng nghiêng, tức đất cao thấp gần như KHÔNG có lý do. Nguyên nhân gốc là **một phép cộng**: `h = hình + nhiễu` **CẮT VỤN level set** (mỗi bướu nhiễu đẻ một cực trị mới), trong khi bẻ cong toạ độ **LẤY MẪU** (domain warping) chỉ **uốn lượn** level set nên sườn dốc vẫn là sườn dốc. ⇒ **Ba luật**: **(a)** một trường ngẫu nhiên có HAI chỗ để cắm vào — *giá trị* hoặc *vị trí lấy mẫu* — và chúng cho ra hai thế giới khác hẳn nhau; hỏi *"tôi đang làm nhiễu cái GÌ?"* trước khi chỉnh biên độ; **(b)** hạ biên độ nhiễu là **mua phẳng bằng cách bỏ luôn 15 vùng đất khác nhau** — nó xoá gợn LẪN đồi, tức trả bằng đúng thứ Phase 7B sinh ra để có; **(c)** làm mượt sau khi sinh thì vừa **phá ADR-007** (quét theo thứ tự ô ⇒ không có điểm bất động duy nhất) vừa vô ích (làm mượt một cái chăn nhàu thì được một cái chăn nhàu mờ, số lần đổi chiều gần như không đổi). ⚠️ Và mỗi kỷ phải khai một **HƯỚNG THẤP** (`drain` bắc/nam/đông/tây + trọng số `tilt`) buộc vào đúng `country` — không có nó thì cái "hướng" duy nhất trong cảnh là hướng của hạt nhiễu, tức đất cao thấp **không có lý do**. Khuôn ba lớp lần thứ **BẢY**.
  - ⚠️ **VÀ BUỘC VÀO `country` VẪN CHƯA ĐỦ — MỘT ĐẤT NƯỚC CÓ BỐN PHÍA, MỘT DÒNG SÔNG CHỈ CÓ MỘT: NƯỚC ĐANG CHẢY LÊN DỐC Ở 9/14 KỶ** (cùng ngày, cùng ADR). Bảng `settingStyle.side` (nước nằm phía nào) đã tồn tại từ trước, nói về **cùng một thế giới**, và hai bảng **chưa bao giờ được đặt cạnh nhau**. Đặt cạnh lần đầu thì 9/14 kỷ lệch hoặc **ngược hẳn** — kỷ 5 khai đất thấp về tây trong khi suối Elzbach chảy ở đông. Không một bài test nào đỏ, vì không bài nào biết cả hai bảng cùng tồn tại. ⇒ **Ba luật**: **(a)** trước khi tin một bảng đã "buộc đúng chỗ", đi tìm xem **có bảng nào KHÁC đang nói về cùng một sự thật vật lý không** — có thì buộc thẳng vào nó, đừng buộc cả hai vào một thứ chung ở xa hơn (`country` là cái tên, `side` là cái hướng; chỉ cái hướng mới ràng được cái hướng); **(b)** ⚠️ **cái giá của việc sửa cho ĐÚNG phải được TRẢ, không được giấu** — sửa 9 dòng ấy làm cổng "thấy nước" TỆ ĐI ở hai kỷ (kỷ 4 5,11%→4,95%, kỷ 5 5,54%→3,51%), vì đất thoải xuống phía nước thì **bờ XA tụt xuống, khuất sau sống đất gần**: một hệ quả vật lý, không phải một lỗi. Hai cách làm test hết đỏ đều bị bác — **hạ cổng 5%** là cái phễu Phase 9A, **quay `drain` về giá trị sai** là mua một con số bằng cách nói dối địa lý (ADR-025 cấm); cách đúng là ghi ngoại lệ ra **tường minh đếm được** (`TRUOT` `[6,7,10]` → `[4,5,6,7,10]`) kèm bảng ba cột nền/sai/đúng ngay trong chú thích; **(c)** cùng phiên còn dính **lần thứ hai** bẫy Phase 7D: biên độ lượn của vành đất viết cứng `0,42`, đúng **nhờ** `WATER_DROP_BELOW_PLAIN = 0,30` ở một file mà `terrain.js` không tham chiếu tới ⇒ đất KHÔ kỷ 8 tụt **0,0288 ô dưới mặt nước** (một vũng nước ma giữa đồng). Vá bằng cách **nói ra quan hệ** (`ROLL_HEADROOM_SHARE = 0,70 × WATER_DROP_BELOW_PLAIN`), và phép nén phải **BÃO HOÀ `tanh`, KHÔNG KẸP** — kẹp thì mọi kỷ có triền mạnh bị dồn về đúng ±0,21 và **thứ tự giữa các kỷ bị phá**.
  - ⚠️ **PHÉP ĐỐI CHIẾU CHÉO BẮT ĐƯỢC LỖI TRONG CHÍNH NÓ — VÌ MỘT HẰNG SỐ CHÉP TAY, VÀ NÓ IN RA MỘT BẢNG 15 DÒNG TRÔNG HOÀN TOÀN BÌNH THƯỜNG** (cùng ngày). Để chứng minh *"toàn bộ chênh lệch hình học nằm ở bệ kè"*, `scripts/plinth-tri.mjs` đếm bệ kè bằng một đường độc lập với `scene-tri.mjs` — và mở đầu bằng `const BUILDING_SCALE = 0.86` chép từ trí nhớ. Giá trị thật trong `sceneGraph.js` là **1.3**; `span` sai thì `terrain.footprint` hỏi một ô KHÁC ⇒ nó đếm được **3 bệ thay vì 31**. Không có gì đỏ lên, và bảng in ra không có một dấu hiệu bất thường nào. Thứ lộ ra sự thật là đúng phép đối chiếu ấy: bảng bệ nói **+16** trong khi `scene-tri` nói **−176**. ⇒ **Ba luật**: **(a)** *"một luật một công thức"* áp cho cả **HẰNG SỐ của phép đo**, không chỉ cho công thức của nó — hằng số không export được thì **đọc thẳng từ mã nguồn** (một `readFileSync` + regex + `throw` nếu không khớp) chứ đừng chép; **(b)** **nếu chỉ có MỘT phép đo thì không có gì để cãi nhau, tức không có gì để phát hiện** — mọi con số hình học đáng đưa vào tài liệu phải có một đường đo thứ hai độc lập; **(c)** khi hai phép đo cãi nhau, bên MỚI VIẾT là bên đáng nghi trước (kiểm CÔNG CỤ trước, kiểm mã sau). Sau khi vá: 31 → 23 bệ, 820 → 644 tam giác, **−176 khớp từng đơn vị ở cả 15 kỷ**.
  - ⚠️ **KÈM THEO — BA BÀI TEST CANH MỘT QUAN HỆ BẰNG MỘT MỨC TUYỆT ĐỐI, NÊN CHÚNG KÊU OAN NGAY KHI BẢNG ĐƯỢC CO GIÃN.** Sau bản vá có **9 bài đỏ**, và phản xạ sai nhất lúc đó là *"vá hỏng rồi"*. Đi kiểm từng bài: **4 bài chỉ đỏ vì `terrainMaxHeight` tụt** (camera cận cảnh tính khoảng lùi theo nó — mã đúng, **phép đo già đi**), còn 3 bài kia canh một QUAN HỆ bằng một CON SỐ — cổng làm phẳng đường viết `soODuongLe > 200`, mà "200" là số ô đo được trên bảng `relief` CŨ. Vá đúng là hỏi chính cái quan hệ: tập kỷ được làm phẳng phải **BẰNG** tập kỷ có `TERRACE_STEP × relief > maxRoadRise()`. ⇒ Đây là bẫy Phase 7D (*"một con số tuyệt đối không diễn đạt được một luật nói về QUAN HỆ"*) tái diễn **ở tầng bài test**, nơi ít ai nghĩ tới — và nó nguy hiểm theo hướng ngược với thường lệ: nó không im lặng, nó **kêu oan**, và một bó test đỏ là thứ dễ khiến người ta rút lại một bản vá đúng.
  - ⚠️ **KÈM THEO — MỘT PHÉP PHÁ KHÔNG NỔ, VÀ LẦN NÀY PHÉP PHÁ ĐÚNG CÒN BÀI TEST SAI.** Giết hàm hình dạng mà bài *"khuôn hình học phải sạch nhiễu"* vẫn xanh. Theo luật đã ghi thì nghi phép phá trước — nhưng nó vô can: `truongTho` áp `tilt` **NGOÀI** hàm hình dạng (`raw = hình × (1 − tilt) + triền × tilt`), nên một hàm hình dạng **đã chết** vẫn cho ra một khuôn biến thiên nhờ phần `triền`. Bài test đang đo `hình + tilt×triền` rồi **gọi nó là `hình`**. Vá bằng một hồ sơ kỷ **song sinh có `tilt: 0`** để cô lập đúng đại lượng cần đo. ⇒ Bổ sung cho luật *"nghi phép phá trước"*: khi phép phá vô can, câu hỏi kế tiếp là ***"đại lượng bài test đang đo có lẫn thứ tôi KHÔNG muốn đo không?"*** — cùng họ bài học fBm Phase 9A (`|Δ|` bị cái dốc át) và `TECH_DEBT #22`.
  - ⚠️ **KÈM THEO — MỘT CÁCH VÁ "HIỂN NHIÊN" CÓ THỂ BIẾN BÀI TEST THÀNH RỖNG, VÀ CÁI THỨ HAI CÒN NGUY HIỂM HƠN VÌ NÓ TRÔNG NHƯ MỘT QUYẾT ĐỊNH CÓ CHỦ ĐÍCH.** Kỷ 7 vượt cổng 60% (một bậc thềm nuốt 63% số ô đất). Hai cách vá nghĩ ra ngay: **nới ngưỡng** hoặc **thêm `7` vào danh sách ngoại lệ `TRUOT`**. Cả hai làm test hết đỏ mà không chữa gì. Nguyên nhân thật là **cấu trúc**: cộng hai trường rồi chuẩn hoá min/max + chia bậc **đều bề rộng** thì phân bố **luôn dồn về giữa** (định lý giới hạn trung tâm thu nhỏ). Chữa bằng **địa lý**: `tilt` 0,26 → 0,44 (Firenze nằm TRONG lòng thung lũng Arno) + `relief` 0,80 → 0,55 (đồi Toscana vốn thoải; 0,80 làm ruộng Tuscany dốc gần bằng Lisbon). Sau đó cổng **tự hết đỏ** và `TRUOT` về **rỗng** — kể cả kỷ 4 vốn đã là ngoại lệ từ ADR-032(b). ⇒ **Một ngoại lệ BIẾN MẤT là bằng chứng mạnh hơn một ngoại lệ được THÊM VÀO**; nếu bản vá của bạn làm danh sách ngoại lệ dài ra, gần như chắc chắn bạn đang vá triệu chứng.
  - ⚠️ **KÈM THEO — HAI BẢNG ĐỒNG BIẾN 85% KHÔNG PHẢI LÀ MỘT LỖI, VÀ SỬA CHO CHÚNG LỆCH NHAU LÀ NÓI DỐI ĐỊA LÝ.** Bài test đòi bảng chân trời và bảng địa hình *"không được là một"*, ngưỡng thứ hạng 0,4/0,6 nổ. Đo ra hai bảng đồng biến ~85% — và **đó là đúng**: vùng đất gồ ghề thì chân trời cũng cao. Câu hỏi đúng không phải *"chúng có giống nhau không"* mà là **có đủ cặp kỷ NGƯỢC CHIỀU không** (≥8) và **có kỷ nào lệch THỨ HẠNG rõ không** (≥0,20), kèm **đối chứng bơm một bảng suy thẳng từ `relief`** và đòi nó ra **0 cặp ngược chiều**. Không có đối chứng ấy thì bài test không còn răng.
  - ⚠️ **KÈM THEO — PHÉP PHÁ PHẢI TỰ ĐẾM XEM NÓ ĐÃ ĐỔI ĐÚNG BAO NHIÊU CHỖ.** Hai cái bẫy công cụ trong cùng một phiên, cả hai đều trông y hệt *"phép phá không nổ"*: (a) `--test $T.test.js` bung ra `terrain.js.test.js` (không tồn tại) ⇒ **không bài nào chạy**; (b) regex `(\d+): \{ rise:` chỉ khớp dòng kỷ **hai chữ số** (dòng một chữ số có hai dấu cách) ⇒ chỉ 6/15 dòng đổi. Cả hai vá bằng cùng một luật: `assert n == 15` ngay trong phép phá. Cộng một bài phụ đã cắn nhiều lần: **nháy ngược trong một nhãn bash sinh ra thay-thế-lệnh** (`rolling: command not found`) — trong shell dùng nháy đơn cho mọi nhãn có tên biến/hàm.
- ⚠️ **ĐƯỜNG PHỐ LÀ MỘT HỆ THỐNG CÓ NGỮ PHÁP RIÊNG, KHÔNG PHẢI MỘT DẢI MÀU — ĐỪNG SỬA BẢN SẮC ĐƯỜNG BẰNG CÁCH CHỈNH `roadColor`** (Phase 9D, ADR-025). Ba lớp, mỗi lớp một việc: `city3d/streetStyle.js` = **BẢNG 15 kỷ × 10 trường** (bề rộng đại lộ · bề rộng ngõ · vật liệu lát · cỡ viên · độ mòn · bó vỉa · vỉa hè · vạch kẻ · kiểu mép · `country`) + 3 hàm thuần (`pavingSubdivision`, `streetCrossSection`, `carriagewayShape`) · `render3d/terrainMesh.js` = chỉ DỰNG theo bảng · `palette3d.js` = chỉ lo MÀU, và chỉ còn hai lời hứa về màu. **Luật gốc**: trước 9D toàn bộ bản sắc nằm trong MỘT mã màu, nên mọi sức ép "15 kỷ phải khác nhau" dồn vào ĐỘ ĐẬM — mà độ đậm có đáy, và kỷ 11 đã tụt xuống 0,113 trên nền đất 0,406 (`TECH_DEBT #30`). ⚠️ Bốn cái bẫy đã trả giá: **(a)** bề rộng là đại lượng của **MẶT CẮT NGANG** — bản đầu áp nó lên cả chiều DỌC nên con đường vỡ thành những mảng nhựa rời rạc như sân đỗ xe; `carriagewayShape` (mép giáp ô đường thì vươn tới ranh giới, mép giáp đất thì dừng ở nửa bề rộng) sửa gốc và **xoá luôn việc phải phân biệt `variant`**; **(b)** `stone` bị chặn dưới bởi `MIN_STONE = 1/7` — khai mịn hơn thì cái kẹp nuốt mất phần chênh **trong im lặng** (đúng bẫy Phase 7B: bốn kỷ khai bốn số, dựng ra một kết quả), nên `isValidStreetStyle` **từ chối thẳng**; **(c)** mỗi dòng phải trả lời được *"đi bộ ở nước ấy giẫm lên cái gì?"* và `country` bị **khoá cứng vào `eraStyle.js`** bằng test — không có ràng buộc ấy thì 15 dòng là 15 lần chọn bừa, mà chọn bừa chính là thứ đã sinh ra 15 kỷ đường giống hệt nhau; **(d)** ⚠️ **ĐỪNG NHÉT ĐẶC ĐIỂM HIỆN ĐẠI VÀO KỶ CỔ CHO ĐẸP** — bó vỉa là phát minh La Mã (kỷ 1–6 không có), vạch kẻ là thế kỷ 20 (kỷ 1–10 không có), vỉa hè tách cao phổ biến từ thế kỷ 18–19; có **test khoá cả hai chiều** (kỷ cổ không được có, kỷ hiện đại không được thiếu), vì không có nó thì cách dễ nhất để "15 kỷ khác nhau" là rắc bó vỉa khắp nơi, tức mua điểm bản sắc bằng cách nói dối lịch sử.
  - ⚠️ **VÀ BẢN VÁ 9D ẤY CHỈ ĐÚNG MỘT NỬA — MỘT Ô ĐƯỜNG KHÔNG PHẢI MỘT HÌNH CHỮ NHẬT** (2026-08-18, Phase 12, ADR-031). Luật *"mép giáp ô đường thì vươn tới 0,5"* chữa đứt điểm cái khe cỏ, và **đẻ ra khuyết tật thứ hai** mà mãi ba phase sau mới đo: một hình chữ nhật chỉ có **hai** bề rộng, còn một ngã tư cần tới **bốn**, nên ngã ba buộc phải phình ra **trọn ô** theo hướng có nhánh — dù cái nhánh ấy chỉ rộng bằng một phần ba đại lộ. Đo bằng `scripts/road-fit.mjs` (15 kỷ × 3 mốc tuổi): **45% số mép đường có một bậc vuông góc**, bậc lớn nhất **0,380 ô** — tức con đường lởm chởm ở gần MỘT NỬA số chỗ nối, đúng như Đàm nói. ⇒ Luật mới: **một LÕI ở giữa + tối đa BỐN CÁNH TAY loe**; bề rộng chỗ nối là `min(nửa của tôi, nửa của hàng xóm)`. Ba bài học: **(a)** cái sai gốc là một **giả định về HÌNH**, không phải một con số sai — chỉnh khéo con số nào cũng không thoát, cùng họ *"một trường gánh hai việc"* nhưng lần này thứ gánh hai việc là một HÌNH DẠNG; **(b)** thứ xoá bậc là **tính ĐỐI XỨNG** của phép `min`, không phải giá trị nào cả — hai ô kề nhau cùng suy ra một con số từ CÙNG một biểu thức thì không có cách nào lệch; một cái bậc chỉ sinh ra khi mỗi bên tự tính bề rộng của mình một cách ĐỘC LẬP (⇒ hàm phải nhận **bề rộng** của hàng xóm, biết "có đường bên cạnh hay không" là KHÔNG ĐỦ); **(c)** ⚠️ **một phép đo ra 0 THEO CẤU TRÚC thì không còn là một phép đo** — sau bản vá, `road-fit.mjs` không thể khác 0, nên nó giữ một **đối chứng bơm LUẬT CŨ** vào và đòi phải bắt lại được bộ số hỏng cũ (0,18 và 0,32); thứ thật sự canh mã DỰNG có bám luật không là bài đếm đỉnh ở `terrainMesh.test.js`. ⚠️ Kèm hai quả mìn: **tam giác SUY BIẾN** (dài bằng 0, vô hình trên màn hình) làm hỏng mọi phép gom-tam-giác-theo-ô vì trọng tâm rơi đúng ranh giới rồi bị làm tròn sang ô BÊN CẠNH — bài test đỏ với thông báo trỏ vào một ô hoàn toàn lành; và **cỡ viên lát phải là đại lượng của THẾ GIỚI, không phải của mảnh** — chia đều `sub` cho năm mảnh dài ngắn khác nhau thì viên của cánh tay 0,14 ô nhỏ hơn viên của lõi 0,72 ô tới năm lần, tức cỡ viên thôi là một trục bản sắc và bắt đầu kể chuyện về hình dạng ngã tư.
  - ⚠️ **ĐỪNG CHÉP CỘT "SAU" CỦA PHASE TRƯỚC LÀM CỘT "TRƯỚC" CỦA PHASE MÌNH — BẢNG SỐ CHỈ ĐÚNG CHO ĐÚNG HAI COMMIT ĐÃ SINH RA NÓ** (2026-08-18, Phase 12; `TECH_DEBT #43`). Lấy mốc nền cho Phase 12, tôi định dùng cột "sau" của bảng Phase 11 trong `PERFORMANCE.md`. Bảng ấy mô tả `d888fae`, còn HEAD là `e95cdf1`: **Phase 11-B sửa `roofStyle.js` — hình học thật — mà không đụng `PERFORMANCE.md`**, để **6/15 kỷ sai số tam giác** (tổng lệch +14.360) suốt từ đó. Chép sang thì kỷ 8 ra −5.558 thay vì −1.998 và kỷ 11 ra +3.902 thay vì +126: **cả bảng bịa, và bịa rất thuyết phục vì 9/15 dòng vẫn đúng.** ⇒ **Ba luật**: **(a)** mỗi phase **PHẢI tự đo lại mốc nền của mình** trong một `git worktree` ở đúng HEAD — hai phase không liên quan nhau về nội dung (một đụng mái, một đụng đường) vẫn dính nhau qua **cột số**; **(b)** thứ bắt được là một phép **ĐỐI CHIẾU CHÉO** đo cùng một đại lượng bằng hai đường độc lập — ở đây đếm riêng tam giác mặt đường (thuần, không Chromium) rồi so với chênh lệch tổng (Chromium): 14/15 kỷ khớp *từng đơn vị* còn kỷ 8 lệch đúng 3.560, và **chỗ lệch chính là chỗ hỏng**; nếu chỉ có một phép đo thì không có gì để cãi nhau, tức không có gì để phát hiện; **(c)** ⚠️ và điều đáng sợ nhất: trong **cùng một bảng**, cột **lệnh vẽ** KHÔNG trôi còn cột **tam giác** thì trôi — khác biệt duy nhất là lệnh vẽ có `drawCallBudget.test.js` canh. Definition of Done có ghi "tài liệu đã đồng bộ", nhưng **một câu chữ thì không đỏ lên được**; lặp lại y nguyên bài học *"một bài học được ghi ra KHÔNG chặn được gì; chỉ một bài TEST mới chặn được"*.
  - ⚠️ **MỘT LỜI HỨA CÓ THỂ ĐANG ĐƯỢC GIỮ XANH BỞI CHÍNH THỨ NÓ SINH RA ĐỂ NGĂN — 80 ô đường pha loãng một phép đếm về ĐẤT, và nó che một khuyết tật có sẵn từ trước** (2026-08-18, Phase 12-B; `TECH_DEBT #44`). Bài `kỷ khai TỪ 3 BẬC TRỞ LÊN thì không bậc nào được chiếm quá 60% số ô` sinh ra để canh bài học Phase 7B (trường nhiễu dồn cục trên lưới nhỏ). Nó đếm **cả 144 ô**. Nhưng 80 trong 144 ô (**56%**) là ô ĐƯỜNG, và ô đường nằm rải đều mọi bậc, nên chúng kéo mọi tỉ lệ về gần trung bình — tức phép đo bị pha loãng đúng cái đại lượng nó đang hỏi. Khi Phase 12-B buộc phải tách ô đất ra đếm riêng (vì ô đường thôi là bội số của bậc thềm), kỷ 4 lộ ra **64%** — và đo lại trên `be261ef` thì **nó đã 64% từ trước**, bài test cũ chưa bao giờ nhìn thấy. ⇒ **Ba luật**: **(a)** trước khi tin một phép đếm theo tỉ lệ, hỏi *"mẫu số của tôi có lẫn thứ không thuộc câu hỏi không?"* — đây là `TECH_DEBT #22` (trung bình trên vùng quá rộng) lặp lại ở một hình dạng khác: lần trước thứ pha loãng là một vùng ảnh, lần này là **một nửa số ô của chính bảng đang đếm**; **(b)** khi một bản vá đúng làm đỏ một bài test cũ, hỏi *"bài ấy xưa nay xanh nhờ cái gì?"* trước khi nghi bản vá (đúng bài học Phase 9B) — ở đây nó xanh nhờ một thứ **chẳng liên quan gì tới điều nó tuyên bố canh**; **(c)** và cách xử lý ĐÚNG khi phát hiện là **KHÔNG nới ngưỡng 60%** (nới là bỏ răng cho cả 15 kỷ) mà ghi ngoại lệ ra **tường minh đếm được**: `assert.deepEqual(TRUOT, [4])` — kỷ thứ hai trượt thì đỏ, mà kỷ 4 được sửa xong cũng đỏ. Một con số trong bài test là cái hẹn giờ duy nhất chạy được.
  - ⚠️ **MỘT TIỀN ĐỀ ĐÚNG VẪN CÓ THỂ DẪN TỚI MỘT KẾT LUẬN SAI, NẾU NÓ ĐÚNG VỀ MỘT THỨ HƠI KHÁC VỚI THỨ ĐANG DÙNG** (2026-08-18, Phase 12-B). Cố vấn bảo: *"`ROAD_CELLS` là hằng số cấp module, nên địa hình được phép biết trước đường nằm ở đâu mà không phá ADR-007."* Đi kiểm thì tiền đề ấy **ĐÚNG**. Nhưng nếu dừng ở đó rồi cho `terrain.js` đọc mạng đường **đang hiện** thì đã phá ADR-007 tan tành — vì mạng ĐÃ HIỆN không hề bất biến: `deriveProps` vừa mở dần theo `sessionCount` vừa **bỏ những ô đã bị công trình chiếm**, mà công trình thì đặt theo kỷ. Đo ra: **1.818/2.265 tổ hợp không phải tiền tố của nhau**, 10 tập đường khác nhau qua 15 kỷ ở cùng mốc 40 phiên, và `built: []` cho ra **0 ô đường**. Hai thứ tên gần giống nhau, một cái bất biến một cái không. ⇒ **Ba luật**: **(a)** khi ai đó (kể cả chính mình) nói *"X là hằng số nên cứ dùng"*, hỏi tiếp *"tôi sắp dùng X, hay dùng một thứ ĐƯỢC SUY RA từ X?"* — phép suy ra mới là chỗ tiến độ lọt vào; **(b)** cách sửa là đặt cho cái bất biến một **CÁI TÊN RIÊNG** (`roadCellCandidates()`) chứ không dùng chung tên với cái biến thiên, vì tên chung là thứ khiến phiên sau nhầm lại; **(c)** khoá bằng **BA** bài test, không phải một: ứng viên bất biến · ứng viên là **tập cha thật sự** của mọi mạng đã hiện · và — quan trọng nhất, dễ quên nhất — mạng đã hiện thì **KHÔNG** bất biến, viết hẳn thành một bài test mang tên *"đừng dựa vào nó"*. Bài thứ ba không bảo vệ mã; nó bảo vệ **phiên sau khỏi hiểu nhầm**, và đó là thứ vừa suýt cắn tôi.
  - ⚠️ **KẸP THÌ PHÁ THỨ TỰ, TRUNG BÌNH THÌ PHÁ RÀNG BUỘC — muốn giữ HAI trần cùng lúc thì phải lấy TRUNG VỊ** (2026-08-18, Phase 12-B). Bài toán: đường phải (a) không dốc quá Baldwin Street 34,8% **và** (b) không được bỏ xa đất bên lề quá một bờ kè 1:1. Bản vá đầu dựng hai "bao" Lipschitz (bao dưới và bao trên) rồi lấy **trung bình** — nghe rất cân đối, và nó làm độ dốc ngang kỷ 5 tệ đi từ 101% lên **184%**. Vá thêm một vòng (nhét mốc đất vào cả hai bao rồi vẫn trung bình) chỉ kéo được 184% → **183%**: cơ chế sai thì tinh chỉnh bao nhiêu cũng vô ích. Lý do toán học: **trung bình của hai hàm thoả một ràng buộc thì KHÔNG thoả ràng buộc ấy**, còn **trung vị của ba hàm C-Lipschitz thì vẫn C-Lipschitz** (vì min/max của các hàm C-Lipschitz đều C-Lipschitz, mà trung vị viết được bằng min/max). Đổi sang `median(bao_dưới_từ_đất, giữa_hai_bao, bao_trên_từ_đất)` thì cả hai trần cùng đạt, và số chỗ vượt trần Baldwin về **0/1320**. ⇒ **Hai luật**: **(a)** gặp bài "phải thoả đồng thời hai ràng buộc đối nghịch", đừng đi tìm một điểm dung hoà bằng phép trung bình — hỏi xem có phép toán nào **bảo toàn** cả hai tính chất không (min/max/median bảo toàn Lipschitz; trung bình không bảo toàn gì cả); **(b)** phép quét lan truyền phải có **điểm bất động DUY NHẤT** thì mới tất định, tức không phụ thuộc thứ tự duyệt ô — đây là điều kiện bắt buộc của ADR-007, và nó là lý do chọn inf/sup-convolution chứ không phải một vòng lặp làm mượt tuỳ tiện.
  - ⚠️ **BẢN SẮC ĐO BẰNG 8 TRỤC CẤU TRÚC, KHÔNG ĐO BẰNG KHOẢNG CÁCH RGB** (`streetStyle.test.js`). Bài `15 KỶ RA 15 MẶT ĐƯỜNG` đã **chuyển nhà** khỏi `palette3d.test.js`: khi vá xong cái rãnh đen thì bài cũ ĐỎ ở cặp **11↔13**, và nó đỏ **đúng đắn** — Manhattan và Tokyo **đều lát nhựa đường**, gần nhau về màu là sự thật vật lý. Bước lượng hoá của 8 trục suy từ đúng **hai** phép đo đã có (một ô ≈ 64 điểm ảnh; ngưỡng mắt 12/255), không có hằng số thứ ba chọn tay; trục "viên" **hỏi thẳng `pavingSubdivision`** chứ không dựng lại công thức tương đương. Số hiện hành: **105/105 cặp ≥ 3/8 trục** (yếu nhất 3, trung vị 6), **kỷ liền nhau** yếu nhất cũng 3/8. Có **hai bài đối chứng** nhốt thế giới trước-9D (15 kỷ giống hệt ⇒ phải ra 0 trục; 15 kỷ chênh 0,001 ô ⇒ sàn 3 trục phải TỪ CHỐI). ⚠️ Tầng bảng màu chỉ giữ lại lời hứa mà **màu** thật sự chịu trách nhiệm được: bảng không dẹt (trung vị 105 cặp ≥ 90) · không hai kỷ nào trùng khít mã màu · và **cặp gần nhau nhất về màu PHẢI là hai kỷ dùng chung vật liệu** (gạch nung mà cùng màu nhựa đường thì lúc ấy mới là lỗi bảng màu thật).
- ⚠️ **TẦNG TRỆT LÀ MỘT NGỮ PHÁP THỨ TƯ — VÀ LẦN NÀY MỘT BÀI TEST CANH MỸ THUẬT ĐÃ DẠY LẠI TÔI MỘT SỰ THẬT KIẾN TRÚC** (Phase 10, ADR-026). Cùng khuôn ba lớp đã dùng ba lần (`vernacularRoof` · `undergrowth` · `streetStyle`): **BẢNG** `groundFloor` ở `eraStyle.js` (bắt buộc đủ 15 kỷ, buộc vào `country`, có test bắt) · **HÌNH** ở `city3d/groundFloor.js` · `buildingSpec.js` **chỉ ĐỌC**. Nó vá hai khuyết tật thật: cửa cũ là **một khối `dark` bề ngang viết cứng 0,14** áp cho mọi khối chênh nhau vài lần (đúng bệnh `eaves` Phase 7C), và **15 kỷ chung đúng một cái cửa**. ⚠️ Bốn điều đã trả giá: **(a) một bài test mỹ thuật có thể biết kiến trúc hơn tôi** — bài `kỳ quan của MỌI kỷ vẫn đối xứng tuyệt đối` ĐỎ ở kỷ 13 vì cửa lùa Nhật vốn so le hai tấm; câu trả lời đúng KHÔNG phải nới bài test mà là **4 tấm trên 2 rãnh**, đúng cách cổng đền lớn Nhật Bản làm thật. Bài test đứng đó để canh mỹ thuật, và nó đã chỉ ra một sự thật lịch sử. **(b) một trạng thái "chưa làm" phải TƯỜNG MINH ĐẾM ĐƯỢC** — 12 kỷ chưa nghiên cứu khai thẳng `door: 'legacy'` và có test khoá **đúng 12**, thay vì im lặng rơi về mặc định; im lặng thì Bước 2 sẽ không ai biết còn thiếu bao nhiêu. **(c) `isValidGroundFloor` TỪ CHỐI THẲNG bảng sai**, không tự chữa — tự chữa là cách một bảng 15 dòng lặng lẽ thoái hoá về 1 dòng (bẫy `MIN_STONE` Phase 9D). **(d) mọi kích thước là TỈ LỆ có TRẦN, và TRẦN LUÔN THẮNG SÀN** — khối quá hẹp thì **không có cửa**, chứ không phải có một cái cửa tí hon; kẹp sai thứ tự thì mái đua lại thành cây nấm lần thứ ba.
  - ⚠️ **KÈM THEO — PHÉP ĐO ĐÚNG NHƯNG ĐO SAI CẤP ĐỘ, VÀ LẦN NÀY NÓ GÂY HOẢNG CHỨ KHÔNG TRẤN AN.** Bài test "không thêm một lệnh vẽ nào" của chính tôi báo *"kỷ 9 kỳ quan: tầng trệt kéo thêm họ vật liệu `wood`"* — nghe như đã vi phạm ràng buộc cứng nhất của Đàm. Sai: cả thành phố **gộp lưới theo HỌ VẬT LIỆU trên TOÀN KỶ**, nên một lệnh vẽ chỉ sinh ra khi một họ **mới toàn kỷ** xuất hiện — mà kỷ 9 đã có cửa hàng và xưởng dùng `wood` từ trước. Tôi đã đếm ở cấp MỘT CÔNG TRÌNH trong khi đại lượng cần đếm sống ở cấp MỘT KỶ. Đúng hình dạng `TECH_DEBT #22` (trung bình trên vùng sai làm loãng tín hiệu) và bài học ngân sách tam giác 2026-08-17 (hằng số nền pha loãng 43% xuống 16%), chỉ khác là **hai lần trước sai theo hướng trấn an, lần này sai theo hướng gây hoảng** — và hoảng thì nguy hiểm kiểu khác: nó suýt làm tôi gỡ bỏ một bản vá đúng. Đo lại đúng cấp: **11→11 · 10→10 · 9→9 lệnh vẽ**, không đổi một đơn vị. ⚠️ Và bài test đếm-gộp ấy có thể chạy RỖNG mà vẫn xanh, nên phải kèm một **đối chứng bắt buộc cờ `ground` thật sự được gắn**.
  - ⚠️ **KÈM THEO — BA LỜI KHẲNG ĐỊNH YẾU CHỈ LỘ RA KHI THỬ NGƯỢC, VÀ CẢ BA ĐỀU LÀ TEST TÔI VỪA VIẾT TRONG CHÍNH PHIÊN ẤY.** (1) Bài "mọi kiểu cửa đều dựng khối thật" dùng bảng thử có `frame: 'wood'`, nên **khung cửa** đã thoả điều kiện "có khối gỗ" — gỡ sạch phần cánh cửa vẫn xanh; vá bằng cách đổi bảng thử sang `frame: 'stone'` (cùng họ với `/envMap,/` xanh oan ở Phase 7A). (2) Phép phá "đảo thứ tự kẹp" **không thể** vượt trần vì một cái gác khác đã loại sẵn những ca ấy — tức **hai** thứ đang giữ bài test xanh mà chú thích chỉ kể một (đúng bài học Phase 4D); tôi ghi thẳng sự thật đó vào chú thích và đổi phép phá thành phá CẢ HAI. (3) Bài "biến thể theo hạt giống" ký tên gồm cả `p.x`, mà độ lệch cửa tự nó đã theo hạt, nên viết cứng số ô cửa vẫn ra 40 chữ ký khác nhau; phải **cô lập từng chiều** (5 ca riêng: đếm khối ở nhà đối xứng, đo phía tương đối so với khối `dark`). ⇒ Luật chung: **một phép thử ngược phải nêu TRƯỚC nó mong đợi đỏ ở đâu**, và khi không đỏ thì nghi chính phép thử — nhưng cũng phải hỏi tiếp *"còn cái gì khác đang giữ nó xanh?"*, vì hai câu hỏi ấy khác nhau và cả hai đều bắt được lỗi thật ở đây.
- ⚠️ **"TỪ CHỐI THẲNG" CHỈ AN TOÀN KHI CÓ NGƯỜI ĐẾM SỐ LẦN TỪ CHỐI — một cơ chế phòng thủ ĐÚNG vẫn đẻ ra một lỗ trống IM LẶNG** (2026-08-18, Phase 10 Bước 2). Bảng tầng trệt cố ý **không tự chữa** dòng sai: `isValidGroundFloor` trả `false`, `emitGroundFloor` trả `false`, hết. Luật ấy đúng và đã được viết thành ADR (tự chữa là cách một bảng 15 dòng lặng lẽ thoái hoá về 1 dòng — bẫy `MIN_STONE` Phase 9D). Rồi kỷ 14 khai `doorWidth: 0.46` vượt trần `DOOR_MAX_WIDTH_RATIO = 0.42`: validator từ chối **đúng**, hàm dựng trả `false` **đúng**, và **cả kỷ ấy không có cửa** — build xanh, lint sạch, test xanh, không một cảnh báo nào. Hai lời "đúng" cộng lại thành một lỗi. ⇒ **Ba luật**: (a) mỗi cơ chế "từ chối thẳng" phải kèm một phép đếm ở đầu bên kia — nay có assert *"kỷ nào khai hợp lệ mà KHÔNG dựng ra khối nào"*, và nó là thứ bắt được lỗi này; (b) *"hợp lệ"* và *"dựng ra thật"* là **hai** câu hỏi, đừng để một câu trả lời cho cả hai (cùng họ "một trường gánh hai việc", lần này là một bài test gánh hai việc); (c) chỗ nguy hiểm nhất luôn là **giá trị nằm sát trần** — 0,46 với trần 0,42 trông như một con số hợp lý, không như một lỗi.
  - ⚠️ **KÈM THEO — MỘT MỐC LỊCH SỬ ĐẶT THEO THỨ MÌNH NHỚ, KHÔNG PHẢI THỨ MÌNH ĐANG DỰNG.** Bài test lịch sử của Bước 2 cấm `shutters` trước kỷ 7, với lý do rất chắc: cửa chớp lá sách (persienne) là chuyện thế kỷ 17. Câu ấy **đúng**, và cái luật thì **sai** — vì hình đang dựng không phải lá sách mà là **hai cánh ván trơn**, thứ cổ ngang chính cái cửa sổ. Nó lập tức cấm nhà Fachwerk Đức thời trung cổ, tức luật lịch sử quay ra cấm chính lịch sử. Bài test đỏ ngay ở kỷ 5. ⇒ **Trước khi viết một cái mốc, hãy đi ĐỌC hình mình đang dựng** rồi hỏi *"cái mốc này là của THỨ TÔI NHỚ hay của THỨ TÔI DỰNG?"*. Và khi có thể thì thay mốc bằng một **điều kiện cấu trúc** — ở đây: *kỷ nào khai `windows: 'none'` thì không được có cửa chớp, vì cửa chớp che một Ô CỬA SỔ*. Điều kiện ấy hỏi chính cái bảng chứ không hỏi trí nhớ, nên nó không già đi.
  - ⚠️ **KÈM THEO — LUẬT "MỘT BÀI TEST CHƯA TỪNG THẤY ĐỎ THÌ CHƯA PHẢI TEST" ÁP CHO TỪNG *ASSERT*, VÀ HAI ASSERT CỦA CHÍNH PHIÊN NÀY ĐÃ RỚT.** (a) Bài "15 kỷ ra 15 tầng trệt" duyệt 105 cặp với sàn 3/8, rồi có thêm một vòng lặp riêng cho **kỷ liền nhau** cũng sàn 3/8. Cặp kề nhau là tập CON của 105 cặp ⇒ vòng lặp ấy **không bao giờ có thể đỏ một mình** — nó chép lại một điều vừa được chứng minh. Thay bằng hai phép đo THẬT SỰ thêm thông tin: **trung vị** (cực tiểu là con số gộp, nó đứng yên dù có một cặp yếu hay bốn mươi) và **mỗi trục phải còn sống** (trục nào cả 15 kỷ khai giống nhau thì bảng thật ra hẹp hơn nó trông — đúng hình dạng cơ chế "lùm cây" chết im lặng ở Phase 8D). (b) Đối chứng "hạt bụi không được tính thành một trục" cộng cả ba lệch 0,001 vào một dòng rồi hỏi `< 3`; nới riêng **một** ngưỡng chỉ làm một trục sáng lên, mà 1 < 3 nên đối chứng vẫn XANH — **một cái phễu nằm ngay trong thứ sinh ra để chống phễu**, và nó chỉ bắt được ca cả ba ngưỡng cùng bị nới, trong khi cách hỏng thật là nới đúng một cái cho tiện. ⇒ **Đối chứng phải hỏi TỪNG chiều một**, đừng hỏi tổng — cùng bài học "gộp 6 chặng thành một vector rồi mới so thì hai kỷ triệt tiêu nhau" (`TECH_DEBT #22`).
  - ⚠️ **KÈM THEO — MỘT CON SỐ TRONG BÀI TEST LÀ CÁI HẸN GIỜ DUY NHẤT CHẠY ĐƯỢC.** Bước 1 ship một trạng thái dở dang (`door: 'legacy'` cho 12 kỷ) kèm `assert.equal(soKyLegacy, 12)`. Con số ấy — chứ không phải mục `TECH_DEBT #36`, chứ không phải dòng "sẽ làm tiếp" trong `BAN_GIAO.md` — là thứ buộc Bước 2 phải mở lại bài test mới chạy xanh được. Một mục nợ trong tài liệu chỉ được đọc khi có người đi tìm; **một con số trong bài test thì tự đòi được đọc**. ⇒ Phải ship dở dang thì làm nó **ĐẾM ĐƯỢC trong một bài test**, đừng chỉ ghi vào `TECH_DEBT.md`. (Và Đàm đã chốt: **đừng** gắn hạn bằng NGÀY — một bài test tự đỏ theo lịch là một quả mìn hẹn giờ; đếm được là đủ.)
  - ⚠️ **KÈM THEO — KHI HAI KỶ ĐỌC RA LÀ MỘT, SỬA BẢNG CHỨ ĐỪNG HẠ SÀN; VÀ SỬA KỶ MỚI, ĐỪNG SỬA KỶ ĐÃ DUYỆT.** Phép đo 8 trục báo **kỷ 4 (Trung Quốc) với kỷ 6 (Việt Nam) chỉ khác nhau 1/8 trục**. Điều đó ĐÚNG về họ hàng — nghề mộc Việt thừa hưởng từ đây — nhưng nó bỏ mất thứ thật sự phân biệt hai nền: **điện Trung Hoa NÂNG mình lên và LÙI vào; đình Việt HẠ mình xuống và ĐƯA ra** (bức bàn phải tháo rời được để ngày hội mở toang cả mặt trước). Hai cách sửa sai và một cách đúng: hạ sàn xuống 1 là **mua điểm bằng cách bỏ phép đo**; chỉnh kỷ 6 là **đụng vào thứ Đàm đã duyệt ở Bước 1**. Cách đúng: kỷ MỚI phải tự tìm chỗ đứng của mình (kỷ 4 lùi cửa 0,50 → 0,62 và mở rộng 0,38 → 0,41, cả hai đều có lý do quy chế viết được vào `note`) ⇒ 3/8 trục.
- ⚠️ **MỘT NÚT BỊT ĐẶT Ở CHỖ TRUNG TÂM CHỈ BỊT ĐƯỢC ĐƯỜNG NÀO ĐI QUA NÓ — VÀ NHÉT THAM SỐ VÀO *CHUỖI KHOÁ* LÀ ĐI VÒNG QUA NÓ MÀ TRÔNG NHƯ ĐANG DÙNG NÓ** (2026-08-18, Phase 11). `emitRooftop` gom ba lối rò rỉ mất đối xứng vào ba hàm dùng chung (`at(k, off)` lấy hạt giống theo `|off|` · `lateral()` · `spin()`), kèm hẳn một chú thích giải thích vì sao phải bịt ở một chỗ: *"bịt mười lăm chỗ thì chỗ thứ mười sáu viết sau này sẽ quên"*. Chỗ thứ mười sáu ấy được viết **ngay trong cùng file, dưới chính câu cảnh báo đó**: `emitPlanter` dựng khoá `` `lobe${i}|${off.toFixed(4)}` `` rồi gọi `at(k, 0)`. Phép lấy `|off|` chỉ áp cho **tham số thứ hai**, nên `off` nằm trong chuỗi khoá đi thẳng qua, và hai bồn cây soi gương nhau ở `±0,2` nhận hai khoá khác nhau ⇒ hai bụi cây to nhỏ khác nhau ⇒ kỳ quan lệch (ADR-007 vỡ). Mã ấy *gọi* đúng hàm bảo vệ, nên đọc lướt thì hoàn toàn yên tâm. ⇒ **Ba luật**: (a) một nút bịt kiểu này phải được hỏi *"tham số nào của tôi đi qua phép biến đổi bảo vệ, tham số nào không?"* — chỉ những thứ đi qua đúng cửa mới được bảo vệ, và **ghép chuỗi là cửa sau**; (b) ⚠️ **MỘT TỔ HỢP MÀ BẢNG CHƯA DÙNG TỚI LÀ MỘT TỔ HỢP CHƯA ĐƯỢC KIỂM** — bài đối xứng sẵn có ở `buildingSpec.test.js` đo trên kỳ quan THẬT của 15 kỷ nên nó chỉ chạm 8/11 kiểu, và lỗi này **không nằm trong 8 kiểu đó theo cách nhìn thấy được**: kỷ 15 có dùng `planter` cho kỳ quan, nhưng mái nó chỉ nhét vừa MỘT bồn, mà một bồn thì `off = 0` nên khoá tự cân. Bài cũ sẽ chỉ đỏ vào ngày có ai gán bồn cây cho một kỳ quan rộng hơn — tức **đỏ MUỘN, sau khi bảng đã đổi**. Bài mới duyệt cả 6 × 11 tổ hợp và đỏ NGAY; (c) cùng họ với *"một bài test chỉ khoá được thứ nó chạm tới"* — khác ở chỗ lần này thứ không được chạm tới không phải một nhánh mã, mà là một **giá trị dữ liệu chưa ai khai**.
- ⚠️ **ĐỨNG ĐÚNG *TRÊN* MỘT NGƯỠNG THÌ CÂU TRẢ LỜI DO SAI SỐ DẤU PHẨY ĐỘNG QUYẾT ĐỊNH — GHIM NGƯỠNG PHẢI GHIM TỪ HAI PHÍA** (2026-08-18, Phase 11). Đối chứng *"bước lượng hoá không được quá thô"* dựng `nền + bước` rồi đòi phép đo (`>= bước`) phải thấy. Nghe chặt chẽ, và nó ĐỎ — vì `(0,35 + 0,0890…) − 0,35` ra **0,08903133903133903**, nhỏ hơn chính cái bước vừa cộng vào. Bài test đang hỏi một câu mà câu trả lời là ngẫu nhiên. ⇒ Ghim **99,9% phải KHÔNG thấy · 100,1% phải thấy**: chặt HƠN bản cũ (bản cũ chỉ chặn phía "quá thô", bỏ ngỏ phía "quá mịn") và không phụ thuộc sai số. ⚠️ **Và cái đáng giá hơn là thứ lộ ra trong lúc sửa**: đo mới thấy bước của kiểu `barrel` (**1,459**) *rộng hơn cả dải giá trị hợp lệ* (1,25) ⇒ hai kỷ cùng lợp ngói ống **không bao giờ** phân biệt được bằng trục ấy, dù khai số nào. Cách xử lý đúng KHÔNG phải nới dải cho vừa, mà là **ĐẾM ca ấy ra tường minh** (`assert.deepEqual(KHONG_VUA_DAI, ['barrel'])` — tự đỏ nếu có kiểu thứ hai rơi vào, và cũng tự đỏ nếu barrel rơi ra) rồi ghi `TECH_DEBT #39`. Một `continue` im lặng ở đúng chỗ đó sẽ giấu mất một trục đã chết.
- ⚠️ **MỘT PHÉP PHÁ CÓ THỂ GHIM ĐÚNG MỘT TRƯỜNG MÀ PHẦN LỚN MÃ KHÔNG ĐỌC — "file đã đổi" vẫn chưa đủ, phải hỏi *thứ tôi ghim có ai dùng không*** (2026-08-18, Phase 11 — lần thứ ba luật "nghi phép phá trước" cứu một kết luận). Để thử bài *"không có hai cái mái giống hệt nhau"*, tôi ép `anchors.rw`/`rd` về hằng số. `diff` xác nhận file đổi, phép thay thế khẳng định đúng 1 chỗ khớp — mà test **vẫn xanh**. Nguyên nhân: `deck` và `ridges` được tính từ biến `rw` **cục bộ** trong `emitRoof`, không từ `anchors.rw`, mà hầu hết hàm dựng đọc `a.deck.w`/`a.ridges[].w`. Tôi đã ghim một trường gần như không ai đọc. Phá đúng (ghim cả `x`/`z`/`deck`/`ridges`) thì đỏ ngay. ⚠️ **Và đo tiếp mới ra chuyện đáng nói hơn**: bài ấy được giữ xanh bởi **HAI** thứ độc lập — `rooftop.js` bám theo neo (thử: cho `emitTank` không dựng gì ⇒ đỏ) **và** neo tự nó khác nhau (thử: ép mọi mái nhận đúng một neo ⇒ đỏ); ép chung MỘT trong hai thì không đỏ. Nói cho đúng thì nguồn biến thể nằm ở **bộ sinh khối nhà**, không ở file đang được canh — đã ghi thẳng vào chú thích bài test, vì đây chính là bài học Phase 4D (*"một bài test xanh không cho biết có BAO NHIÊU thứ đang giữ nó xanh"*) lặp lại ở một file khác.
- ⚠️ **CHI TIẾT TẦNG TRỆT (Phase 10) VÀ CHI TIẾT MÁI (Phase 11) ĐỀU LÀ PHẦN THƯỞNG KHI *NHÌN GẦN*. Ở góc nhìn mặc định của app, cả hai gần như VÔ HÌNH — đã đo: 90/90 ô dưới ngưỡng mắt** (Đàm chốt 2026-08-18). Đây là **sự thật đã đo được, không phải một thất bại** — ghi ra để phiên sau không lặp lại cùng một kỳ vọng rồi lại ngạc nhiên. Con số cụ thể: Phase 11 thêm 110.076 tam giác lên mái (+20,6%) mà bản quét 15 kỷ vẫn không phân biệt được với bản trước (trung vị 2,2 trên ngưỡng mắt 12); ở khung app thì có thấy (1,2–8,4% điểm ảnh đổi), zoom sát mái thì thấy rõ (15,1%). Nguyên nhân gốc **không phải** đặt chi tiết sai chỗ mà là **cả thành phố quá nhỏ trong khung hình** — mỗi căn nhà chỉ cao chừng 40–60 điểm ảnh ở góc mặc định, nên mọi chi tiết cỡ ống khói đều còn 3–5 điểm ảnh, bất kể nằm trên mái hay dưới đất.
  - ⚠️ **HỆ QUẢ 1 — ĐIỀU KIỆN NGHIỆM THU ẢNH ĐÃ ĐỔI, ÁP CHO MỌI PHASE SAU**: **bản quét 15 kỷ CHỈ dùng để canh KHÔNG-TRÔI** (15 kỷ còn phân biệt được với nhau không — `sweep-score.mjs`). Nó **KHÔNG** dùng để chứng minh một phase chi tiết có tác dụng: nó không đủ độ phân giải cho việc đó, mỗi thành phố chỉ rộng ~300 điểm ảnh. Muốn chứng minh tác dụng thì **cận cảnh `--width 1500` cộng một ảnh ở đúng khung app** (`sweep-diff.mjs --frame`). Dùng nhầm công cụ thì hoặc kết luận "phase vô dụng" khi nó có tác dụng thật, hoặc ngược lại.
  - ⚠️ **HỆ QUẢ 2 — THỨ SỐNG SÓT Ở XA LÀ THỨ ĐỔI *ĐƯỜNG VIỀN*, KHÔNG PHẢI THỨ THÊM *BỀ MẶT*.** Đo trên chính Phase 11: kỷ 7 (lan can — phá đường viền cắt lên nền trời) đổi **8,4%**, kỷ 9 (cửa sổ mái) **5,3%**, còn kỷ 8 (ngói bò — thuần bề mặt) tốn **NHIỀU hình học nhất bảng (+48,5% tam giác)** mà chỉ đổi **1,2%**. Trước khi thêm bất kỳ chi tiết nào, hỏi: *"cái này đổi đường viền hay đổi bề mặt?"* — đổi bề mặt thì phải biết trước rằng mình đang mua một thứ chỉ thấy được khi zoom vào.
  - ⚠️ **HỆ QUẢ 2b — LUẬT ĐÀM RA 2026-08-18, ÁP CHO MỌI PHASE SAU: trước khi thêm bất kỳ chi tiết nào, TRẢ LỜI TRƯỚC — nó dành cho khung TOÀN CẢNH hay khung CẬN CẢNH?** Chi tiết cỡ **dưới ~12 điểm ảnh ở toàn cảnh** thì chỉ đáng làm nếu nó **phục vụ cận cảnh**. Câu trả lời phải viết ra TRƯỚC khi viết mã, không phải bào chữa sau khi đo. Đây là thứ ngăn lặp lại Phase 11: +110.076 tam giác lên mái để rồi bản quét không phân biệt nổi (trung vị 2,2 trên ngưỡng 12).
  - ⚠️ **HỆ QUẢ 4 — ĐÃ CÓ LỐI ĐƯA MẮT TỚI GẦN (VIỆC 2, 2026-08-18, ADR-034): CHẠM VÀO MỘT CÔNG TRÌNH THÌ CAMERA BAY TỚI.** Đây là câu trả lời cho `TECH_DEBT #41`, và nó **không** chữa bằng cách thêm chi tiết to hơn mà bằng cách **đưa mắt lại gần**. Con số chứng minh (cùng một thay đổi mã `b98a47d`→`e95cdf1`, cùng một dòng lệnh, chỉ khác khoảng cách camera): lệch trung bình cả khung **5,54 ở khung toàn cảnh — DƯỚI ngưỡng mắt 12** so với **15,45 ở khoảng cách cận cảnh — TRÊN ngưỡng**; điểm ảnh vượt ngưỡng **7,0% → 17,0%**. ⚠️ **Luật cốt lõi, đừng đọc thành "mỗi kỷ một con số cho đẹp"**: thứ được khoá là **KHOẢNG CÁCH THẬT** (`FOCUS_VIEW_DISTANCE = 7,5`), còn mức thu phóng thì tự khác nhau theo kỷ (0,395…0,557) — vì `cityOrbitOptions` đã lùi camera theo `massScale` nên MỘT tỉ lệ chung cho ra công trình cao nhất phủ **44% khung ở kỷ 1 nhưng 122% ở kỷ 15**. Chỉ khi khoá khoảng cách thì một cái ống khói ở kỷ 1 và ở kỷ 15 mới chiếm bằng nhau số điểm ảnh — đó mới là thứ một "mức thu phóng riêng mỗi kỷ" sinh ra để giữ. ⚠️ Và lưới an toàn phải canh **CẢ ĐƯỜNG BAY** chứ không chỉ điểm đến: đo 1200 chuyến thì **9 chuyến thoáng ở đích mà vi phạm giữa đường** (điểm đến ở rìa phố, điểm xuất phát trên đỉnh đầu, đoạn giữa đi ngang chỗ đông nhà nhất). Giá phải trả từng ghi thành `TECH_DEBT #46` (kỷ 15 phải ngẩng 65,3° nên cận cảnh ngả thành nhìn-từ-trên-xuống) — **ĐÃ ĐÓNG 2026-08-18, ADR-035**: Đàm chốt **LÙI RA TRƯỚC, NGẨNG SAU**, nên nay kỷ 15 giữ nguyên 34,4° và chỉ đứng xa hơn (`khoảng cách 11,00 · ngẩng thêm 0,0° · lùi thêm 3,50`). Lý do chọn thế, đúng lời Đàm: *(a) giữ được LỜI HỨA, (b) giữ được CON SỐ* — phương án kia cắm thêm một ngưỡng 50° chưa hiệu chuẩn ngay trước cùng một phép lùi, đúng cái phễu Phase 9A.
    - ⚠️ **VÀ CÁI BẪY NHÁY NGƯỢC ĐÃ VIẾT RÕ Ở NGAY FILE NÀY VẪN CẮN LẦN NỮA, TRONG CHÍNH PHIÊN ẤY.** Thêm `--focus` vào `city-preview.mjs` thì viết một chú thích ``(`planCityFocus`)`` và một `console.log` dùng nháy ngược **bên trong** template literal 300 dòng ⇒ đóng chuỗi giữa chừng ⇒ `SyntaxError` lúc CHẠY (ESLint không bắt, `npm run build` không bắt vì file này không vào bundle). Thứ bắt được là `scripts/cityPreviewSource.test.js` — bài test đã có sẵn từ lâu, và nó chỉ cứu được vì tôi **CHẠY** nó. ⇒ Bổ sung cho luật *"một bài học được ghi ra không chặn được gì; chỉ một bài TEST mới chặn được"*: **bài test ấy phải nằm trong `npm test`, và phải được chạy TRƯỚC khi tin vào một công cụ vừa sửa** — sửa xong một script thì chạy ngay bài test canh nó, đừng đợi tới cổng nghiệm thu cuối.
    - ⚠️ **CẮN LẦN THỨ BA (2026-08-19, VIỆC 2 Bước A) — VÀ LẦN NÀY ĐÃ ĐO XEM CỔNG NÀO BẮT ĐƯỢC, THAY VÌ ĐOÁN.** Chú thích cho lần gộp cờ `tachDeDo` chứa ``truyền `null` `` bên trong template literal ⇒ đóng chuỗi giữa chừng. Bơm lại đúng ca ấy rồi chạy từng cổng: **`node --check` ĐỎ · `scripts/cityPreviewSource.test.js` ĐỎ · `npm run lint` ĐỎ** (báo `Parsing error: Unexpected token null`). ⇒ **Đính chính cho hai gạch đầu dòng trên: với ca dấu nháy ngược làm file HẾT PARSE ĐƯỢC thì ESLint CÓ bắt** — câu "ESLint không bắt" chỉ đúng cho ca dấu nháy rơi vào vị trí để file vẫn parse được (khi ấy chuỗi chỉ đóng sớm rồi mở lại, và chỉ bài test đọc mã nguồn mới thấy). ⚠️ Bài học thật sự KHÔNG đổi và nay đã lặp ba lần: **lưới an toàn vẫn nguyên vẹn, chỗ hỏng là tôi không CHẠY nó ngay sau khi sửa script.** Ba cổng cùng canh một chỗ mà vẫn lọt tới tận cổng nghiệm thu cuối thì vấn đề là THỜI ĐIỂM chạy, không phải số lượng cổng.
    - ⚠️ **HAI CÁCH MỘT CẶP ẢNH "TRƯỚC/SAU" TỰ BIẾN THÀNH MỘT CẶP ẢNH VÔ NGHĨA, cả hai gặp trong một phiên.** (a) **`cd` trong một lệnh ghép sống sót sang lệnh kế tiếp** — hai lượt dựng "trước" và "sau" đều chạy trong kho git CŨ, ghi đè lên cùng một tên file; nếu không `md5sum` thì đã có một bảng số hoàn chỉnh so bản cũ với chính nó. (b) **Tên file không mang tham số khung hình**: `city-preview.mjs` xưa nay không đưa `--zoom`/`--focus` vào tên file, nên ảnh cận cảnh **ghi đè** ảnh toàn cảnh trong im lặng (đã vá bằng hậu tố `-focusN`, cùng lý do với `-mask-*`). ⇒ Cả hai đều là biến thể của bài học `MAI-SAU-ky9.png`: **`md5sum` mọi ảnh nghiệm thu trước khi trích số**, và với mỗi cặp phải hỏi thêm *"hai tấm này ra từ HAI cây mã khác nhau chứ?"* — dựng ảnh ở kho git khác thì **`cd` vào từng kho trong lệnh RIÊNG**, đừng ghép.
- ⚠️ **MỘT NHÁNH `default` BIẾN «THIẾU MỘT `case`» THÀNH «LẶNG LẼ ĐỔI KIỂU» — VÀ TÔI ĐÃ VIẾT MỘT CÂU TRẤN AN SAI VỀ ĐÚNG ĐOẠN MÃ MÌNH VỪA SỬA** (2026-08-21, Phase 14 §1(2), ADR-051). Thêm `case 'ziggurat'` vào `emitRoof` xong, tôi viết bài test canh *"mọi kiểu mái phải dựng ra hình riêng"* và nó **xanh oan HAI lần liên tiếp, vì hai lý do khác nhau**. **(a)** Bài đầu hỏi qua **kỳ quan đã lắp ráp** rồi đo "khối cao nhất nằm trên đỉnh tường" — nhưng `emitSignature` dựng chữ ký kiến trúc ở **ĐÚNG x/z/độ cao của mái**, nên gỡ sạch `case 'ziggurat'` vẫn còn khối ở đó và bài test vẫn xanh (đúng bài học Phase 4D: *"một bài test xanh không cho biết có BAO NHIÊU thứ đang giữ nó xanh"*). **(b)** Bài thứ hai tôi vá bằng cách hỏi thẳng nhà máy mái rồi assert `out.length >= 1`, kèm chú thích *"`switch` trong `emitRoof` không có `default`"* — **một lời khẳng định về đoạn mã tôi vừa tự tay sửa, và nó SAI**: `emitRoof` **CÓ** `default` phát ra một tấm phiến trơn. Nghĩa là thiếu một `case` **không bao giờ** cho ra 0 khối; nó cho ra một tấm phiến **trông rất hợp lý**, và đó mới là hình dạng nguy hiểm. ⇒ **Ba luật**: **(1)** trước khi assert *"thiếu nhánh này thì không có gì được dựng"*, đi `grep` xem cái `switch` ấy có `default` không — một `default` là một **cái lưới đỡ im lặng**, nó biến mọi lỗi thiếu-nhánh thành lỗi sai-kiểu; **(2)** cách canh đúng là **so DẤU VÂN TAY HÌNH HỌC** của từng kiểu với dấu vân tay của chính nhánh `default` (dựng nó ra bằng một kiểu không tồn tại), chứ không đếm số khối — nay phép phá đỏ với đúng câu *"kiểu mái «ziggurat» dựng ra ĐÚNG tấm phiến của nhánh `default`"*; **(3)** cùng họ *"một câu tự trấn an cũng phải được kiểm như một con số"* (Phase 4G) nhưng ở biến thể tệ nhất — **câu ấy nói về đoạn mã mình vừa viết ra vài phút trước**, tức đúng chỗ mình tự tin nhất và ít đi kiểm nhất.
  - ⚠️ **KÈM THEO — TỪ VỰNG CỦA MỘT BẢNG PHẢI ĐƯỢC ĐẾM, ĐỪNG ƯỚC LƯỢNG.** Chỉ thị bắt hỏi *"bộ từ vựng mái có đủ giàu không?"* thay vì vá riêng kỷ 2. Đếm ra hai con số rất khác nhau: mái **KỲ QUAN** dùng **9 giá trị / 15 kỷ**, mái **NHÀ DÂN** (`vernacularRoof`) chỉ **3 giá trị / 15 kỷ** (`flat`×7 · `gable`×7 · `cone`×1). Nếu chỉ nhìn kỷ 2 thì sẽ sửa xong một kỷ và bỏ lỡ hoàn toàn cái nghèo lớn hơn gấp ba — mà cái nghèo ấy sắp thành thứ **nhìn thấy nhiều nhất trong khung hình** ngay khi §1(3) nâng số nhà dân từ 6–30 lên 120–300 (`TECH_DEBT #76`). ⇒ **Trước khi vá một dòng của một bảng, in ra HISTOGRAM của cả cột ấy** — nó rẻ bằng ba dòng mã và nó thường đổi hẳn câu hỏi.
  - ⚠️ **KÈM THEO — HAI CÔNG TRÌNH CÓ THẬT KHÔNG ĐƯỢC DÙNG CHUNG MỘT GIÁ TRỊ CHỈ VÌ CHÚNG «CÙNG KIỂU GIẬT CẤP».** Kỷ 3 (ziggurat Ur) và kỷ 11 (setback cao ốc New York 1916) cùng khai `stepped`. Nghe hợp lý — cả hai đều thu nhỏ dần lên cao. Nhưng chúng khác nhau ở đúng ba chỗ, và cả ba đều nhìn thấy được: ziggurat thu vào từ **mép TƯỜNG**, mặt tường **NGHIÊNG** (batter), có **đền thờ** trên đỉnh; setback thu vào từ **mép MÁI**, mặt **THẲNG ĐỨNG**, đỉnh trống. Vì `stepped` bắt đầu từ `rw = w + eaves×2` (rộng hơn tường), bậc đầu tiên của kỷ 3 **không tạo ra thềm nào đọc được** — cái nó dựng ra chỉ là một cái mũ. ⇒ Khi hai kỷ khai chung một giá trị, hỏi ***"hai công trình thật ấy khác nhau ở chỗ nào, và chỗ ấy có nhìn thấy được không?"*** — thấy được ⇒ chúng là **hai** giá trị, không phải một.
  - ⚠️ **HỆ QUẢ 3 — ĐÚNG LỊCH SỬ LÀ ĐIỀU KIỆN CẦN, KHÔNG PHẢI ĐIỀU KIỆN ĐỦ.** Bản vá đầu của Phase 11-B đổi `crown` kỷ 8 sang `balustrade` với lý do thật (lan can đá đục lỗ tu viện Jerónimos, Manueline chính hiệu) — và bài `15 KỶ RA 15 MÁI` **ĐỎ NGAY**, vì kỷ 7 đã khai đúng giá trị ấy, làm Ý và Bồ Đào Nha tụt xuống chỉ còn khác nhau 1/6 trục. Một giá trị còn phải **không giẫm lên hàng xóm**. Cùng lần đó, `vernacularCrown` kỷ 13 khai `ridge` với lý lẽ cũng thật (sống mái ngói kawara Nhật) và bài `MÁI PHẢI ĐỠ ĐƯỢC THỨ ĐẶT LÊN NÓ` đỏ: `vernacularRoof` của kỷ ấy là **`flat`** — tôi đã kể một câu chuyện lịch sử đúng về một loại nhà mà kỷ này **KHÔNG dựng**. ⇒ **Đi ĐỌC hình mình đang dựng trước khi viết một luật lịch sử** (đúng bài học `shutters` ở Phase 10 Bước 2), và nhớ rằng thứ bắt được cả hai lỗi này là **bài test cấu trúc**, không phải trí nhớ hay lý lẽ.
- ⚠️ **MỘT Ô KHÔNG PHẢI MỘT CĂN NHÀ — MỘT Ô LÀ MỘT KHU PHỐ. VÀ SỐ CĂN NHÀ ĐÃ CHẠM TRẦN TỪ LÂU MÀ KHÔNG AI ĐO** (2026-08-21, Phase 14 §1(3), ADR-052). Đàm nhìn ảnh VIỆC B và nói *«mọi thứ vẫn nhỏ, thành phố không mở rộng mà chỉ là cụm nhỏ»* — trong khi cả ba cổng số của VIỆC B đều xanh. Đi ĐẾM thay vì đoán: `ROAD_LINES = {0,4,8,11}` ⇒ **80/144 ô là đường (55,6%)**, cộng 45 ô vùng kỳ quan (chỉ 5 ô có công trình) ⇒ **chỉ còn ~30 ô chứa được nhà dân**, và **cả 15 kỷ đã đứng ở đúng cái trần ấy từ rất lâu**. Nghĩa là *"thêm nhà"* là việc **bất khả thi theo cấu tạo**, không phải việc chưa ai làm — đúng bài học *"đo TRẦN của cơ chế trước khi tiêu ngân sách cho nó"* (2026-08-19), lần này áp cho một cơ chế **đã tồn tại sẵn**. Lối ra không nằm ở số ô mà ở **ý nghĩa của một ô**: mỗi ô nhà dân nay dựng **4–10 khối nhỏ ghép lại thành một khu phố** (371 ô → **1.812 khối**, ×4,88), theo khuôn ba lớp lần thứ **CHÍN** (`blockStyle.js` BẢNG 15 kỷ × 7 trường → `block.js` HÌNH → `buildingSpec.js` chỉ ĐỌC), mỗi dòng buộc vào `country` (Çatalhöyük dính liền không ngõ · Deir el-Medina hàng thẳng chung tường · siheyuan quây sân · nhà ba gian Việt **KHÔNG** dính tường, tụ lỏng quanh ao · Haussmann mặt phố liền · terrace back-to-back Anh · shophouse Singapore). ⚠️ **Bốn ràng buộc phải giữ, và cả bốn đều đã suýt gãy**: **(a)** ADR-007 — ô neo và 5 toạ độ kỳ quan phải **trùng từng byte**, khoá bằng một bài **LIỆT KÊ đủ 1…120 phiên × 15 kỷ** (≥30.000 phép so), không phải ba mốc mẫu (bài học *"ba mẫu là đủ" là một giả định*); **(b)** kẹp theo **TỈ LỆ, và TRẦN LUÔN THẮNG SÀN** — ô chật ⇒ **ÍT nhà hơn**, tuyệt đối không phải nhà tí hon (`MIN_UNIT_CELLS = max(3·EYE_PIXELS/CELL_PIXELS, ROOFTOP_MIN_SPAN·BUILDING_SCALE) = 0,312 ô`, và khối hẹp nhất toàn cảnh đo được **0,314 ô** ⇒ cái kẹp CÓ cắn thật); **(c)** gộp thành **MỘT mô tả duy nhất** cho mỗi ô nên `cityParts.js` vẫn ra đúng bằng ấy chỗ đặt ⇒ **lệnh vẽ không đổi một đơn vị ở cả 15 kỷ**; **(d)** `attach: 'court'` **tự hạ về `'party'`** khi ô quá nhỏ để có vành ngoài — nếu không thì nhà biến mất trong im lặng. ⚠️ **VÀ MẶT TRÁI PHẢI NÓI THẲNG, KHÔNG ĐƯỢC GIẤU SAU CON SỐ ×4,88**: cạnh ngắn một căn đi từ **0,907 → 0,406 ô (×0,448)**. Nếu chữ «vẫn nhỏ» của Đàm nói về **từng công trình** thì hướng này đi NGƯỢC lại — phải trình ảnh ra và nói câu đó, đừng bù bằng con số (§0: *cổng số xanh không chứng minh được điều gì với mắt*).
  - ⚠️ **KÈM THEO — DẤU VÂN TAY HÌNH HỌC LÀ MỘT HÀM BẬC THANG, NÊN PHÉP LẶP TÌM ĐIỂM BẤT ĐỘNG **PHÂN KỲ** CHỨ KHÔNG HỘI TỤ.** Chia nhỏ một ô làm ngắn mái đi (`pitch = max(0,08, roofPitch) × max(w,d)`) trong khi `massHeight` **không** phụ thuộc mặt bằng ⇒ phải bù bằng cột `storey`, và phải đo lại mặt bằng THẬT sau khi thu nhỏ. Bản đầu tôi định lặp cho tới khi hội tụ; đo ra thì lượt thứ ba **đi xa hơn** lượt thứ hai (0,186 → 0,234 ô) — vì `specFootprint` là một **hàm BẬC THANG của `fx`** (số cửa sổ, số cột, số bậc đều là số nguyên), mà hàm bậc thang thì không co rút. ⇒ **Chốt đúng HAI lượt** (dựng ở `fx = 1` → đo → dựng lại ở `fx = doiW / gocU.w`) và ghi rõ lý do, chứ đừng viết một vòng `while` trông có vẻ chặt chẽ hơn. **Trước khi lặp tới hội tụ, hỏi *"hàm này có liên tục không?"*.**
  - ⚠️ **KÈM THEO — PHÉP PHÁ THỨ NHẤT KHÔNG NỔ, VÀ THỦ PHẠM LÀ **DỮ LIỆU THỬ**, LẦN THỨ TƯ.** Thử ngược bài *"vành ngoài của `court` phải rỗng ruột"* bằng cách ép `coNha` luôn `true`: **không đỏ** — vì kỷ tôi chọn để thử có ô nhỏ tới mức `attach` đã **tự hạ về `'party'`** ở gác (d) ngay bên trên, tức bài test chưa bao giờ đi tới nhánh `court`. Đúng họ với *"ba dải bằng nhau"* (`pngProbe.test.js`) và *"gable ra hình chữ nhật vì lăng trụ 4 cạnh"* (§3a). ⇒ **Một phép phá phải nêu TRƯỚC nó mong đợi đỏ ở kỷ nào, rồi kiểm rằng kỷ ấy THẬT SỰ chạy qua nhánh đang phá** — `console.log` cái nhánh đã chọn là đủ, và rẻ hơn nhiều một kết luận sai về chính bài test của mình.
  - ⚠️ **KÈM THEO — `bpId` CHÉP TAY LÀ MỘT QUẢ MÌN, VÀ NÓ KHÔNG BAO GIỜ NỔ NGAY.** Hạt giống của một khu phố là `bpId` của ô ấy; viết lại `` `dw|${era}|${x}|${y}` `` bằng tay trong công cụ đo/bài test thì hôm nay đúng, và **sai vĩnh viễn trong im lặng** vào ngày ai đó đổi `dwellingBpId` — cùng hình dạng với `BUILDING_SCALE = 0.86` chép tay ở `plinth-tri.mjs` (đếm 3 bệ thay vì 31). ⇒ `import { dwellingBpId }` chứ tuyệt đối không dựng lại chuỗi; hằng số không export được thì đọc thẳng từ mã nguồn rồi `throw` nếu không khớp.
  - ⚠️ **KÈM THEO — SUÝT CHẤM MỘT BẢN QUÉT CŨ LẦN THỨ HAI.** Bản quét 15 kỷ mất ~25 giây và lệnh chấm thì chạy tức thì, nên rất dễ chấm đúng lúc lượt dựng chưa xong — và cái sai ấy **không có triệu chứng nào** (`sweep-score` vẫn in ra một bảng 15 dòng chỉnh tề). Lần này kiểm bằng hai vế trước khi chấm: `ps aux | grep -c '[c]ity-preview'` phải bằng **0**, và `mtime` của ảnh phải **mới hơn** file nguồn mới nhất. ⇒ Đây là quả mìn *"tên file không phải bằng chứng về nội dung file"* ở dạng **thời gian** thay vì dạng tên; cùng họ cổng cache của Phase 13 VIỆC B.
- ⚠️ **TRƯỚC KHI TIÊU NGÂN SÁCH CHO MỘT PHASE NỘI DUNG, ĐO **TRẦN** CỦA CƠ CHẾ ẤY TRƯỚC — MỘT CƠ CHẾ SẮP XẾP TRONG MỘT CÁI HỘP GẦN ĐẦY LÀ MÃ CHẾT** (Đàm chốt thành luật 2026-08-19, sau §2-C; cùng hình dạng với bài học lùm cây Phase 8D và với `TECH_DEBT #41`). §2-C lấp đất trống bằng sân/vườn/bãi quây và **đạt**: 45/45 ô đều giảm. Nhưng phép đo trần — ép `share = 1,00`, tức phủ **MỌI** ô đất trống còn lại — chỉ đưa kỷ 1 từ 60,29% xuống **53,16%**. Nghĩa là **ô lưới thành phố chỉ chiếm ~12–16% chỗ "đất" nhìn thấy được**; 84–88% còn lại là **vạt đất ngoài lưới**. Mà nhà dân cũng chỉ mọc trong lưới ⇒ **§2-B sẽ đụng đúng cái trần ấy**, và Đàm đã DỪNG nó lại vì lý do đó. ⇒ **Ba luật**: **(a)** phép đo trần phải chạy **TRƯỚC** khi viết mã của phase, không phải sau — nó rẻ (vặn một tham số tới mức vô lý rồi đo, đúng khuôn Phase 7A) và nó trả lời câu hỏi duy nhất đáng hỏi: *"nếu cơ chế này chạy hết công suất thì được bao nhiêu?"*; **(b)** Phase 11 đã trả giá đúng bài này theo hướng ngược lại — tiêu 110.076 tam giác lên mái rồi mới biết bản quét không phân biệt nổi (trung vị 2,2 trên ngưỡng mắt 12); khác biệt duy nhất giữa hai lần là **lần này ta BIẾT TRƯỚC**, nên không có cớ nào để lặp lại; **(c)** khi trần đã lộ ra, câu hỏi đúng KHÔNG phải *"làm sao đẩy cơ chế này mạnh hơn"* mà là *"84–88% kia thuộc về tầng nào?"* — ở đây nó thuộc tầng **khung hình/địa hình**, tức một bài toán khác hẳn, cần quyết định mỹ thuật chứ không cần thêm nội dung.
  - ⚠️ **ĐÍNH CHÍNH (2026-08-19, cùng ngày) — CON SỐ HÌNH HỌC TÔI GIẢI THÍCH KÈM BÀI HỌC TRÊN LÀ SAI, VÀ NÓ SAI VÌ TÔI ĐỌC MỘT BÁN KÍNH HỘP-BAO THÀNH MỘT BÁN KÍNH ĐĨA.** Bản đầu viết *"đĩa đất bán kính 13,5 so với thành phố ~7,5 ⇒ ~69% diện tích đất nằm ngoài lưới"*. Ba con số 13,5 · 8,5 · 7,5 lấy từ `sceneStats.test.js` và chúng là **bán kính HÌNH CẦU BAO**, không phải bán kính của một cái đĩa: mặt đất là một tấm **VUÔNG** 19×19 (9,5×√2 = 13,435 ⇒ làm tròn 13,5), mặt đường 12×12 (6×√2 = 8,49 ⇒ 8,5). Suy từ hình vuông thật: **361 − 144 = 217 ⇒ 60,1%** diện tích tấm đất nằm ngoài lưới, không phải 69%. ⚠️ Con số **84–88%** ở trên thì KHÔNG đổi — nó được ĐO trên ảnh chụp, không suy ra từ hình học; chỉ có lời giải thích đi kèm là sai. Đúng bài học Phase 4D/3Y: **sửa đúng không chứng minh hiểu đúng, và lời giải thích mới là thứ phiên sau kế thừa rồi dựa vào.** ⇒ Luật rút ra: *một bán kính chỉ là một bán kính khi vật thể TRÒN* — trước khi chia hai diện tích, hỏi **hình gì**, vì `computeBoundingSphere()` vui vẻ trả về một con số cho mọi hình và con số ấy lớn hơn nửa cạnh đúng √2 lần.
- ⚠️ **MỘT TẤM ẢNH ĐO ĐƯỢC CÓ THỂ BỊ *RÁCH NGANG* MÀ TRÔNG HOÀN TOÀN BÌNH THƯỜNG — và ba cổng đang có đều cho nó qua** (2026-08-19, §2-C, `TECH_DEBT #52`). `TRUOC-A-s20-ky09.png` báo đất trống **37,37%** trong khi sự thật là **41,61%** (hai lượt dựng lại độc lập, cùng md5). Nó qua cả ba cổng của bảng nghiệm thu: md5 hai vế khác nhau ✓ · 0 điểm màu mốc ✓ · các lớp cộng đúng 100% ✓ — vì tấm ấy **hợp lệ về mọi mặt**, chỉ là một dải ngang của nó thuộc về một khoảnh khắc khác. Thứ lộ ra sự thật hoàn toàn TÌNH CỜ: hai mặt nạ khác nhau cãi nhau về cùng một đại lượng. ⇒ **Ba luật**: **(a)** ⚠️ **tôi đã đoán sai nguyên nhân, và câu chuyện nghe rất xuôi** — *"chụp thành nhiều dải nên một dải đến từ khung hình cũ"* — rồi chính số đo bác bỏ: chỗ rách ở **hàng 441**, mốc chia dải là **hàng 476**. Suýt ship một phép kiểm **chỉ soi mốc dải**, tức mù hoàn toàn với đúng ca đã cắn. **Khi không truy được nguyên nhân, hãy hỏi một câu không cần biết nguyên nhân** (`soiVetRach` quét MỌI mép hàng: *"có chỗ đứt nào nổi bật hẳn so với mọi chỗ khác không?"*) — một phép đo không mang giả định thì không chết khi giả định sai; **(b)** ngưỡng phải có **hai vế** và cả hai hiệu chuẩn bằng số đo thật (120 ảnh lành: mép lớn nhất 0,0582 · tỉ số lớn nhất 14,5× — đối chứng rách: 0,180/66× và 0,361/132×) ⇒ sàn 0,12 và hệ số 30× đều nằm giữa hai đầu ĐO ĐƯỢC; một mức tuyệt đối thôi thì mù ngữ cảnh (bẫy Phase 7D), một tỉ số thôi thì nổ tung trên ảnh phẳng; **(c)** ⚠️ **và ngưỡng ấy hiệu chuẩn trên ảnh MỘT-CẢNH rồi đem áp cho BẢNG QUÉT thì kêu oan đúng 30 chỗ** — cách nhau đều 208 hàng = `CELL_H(186)+LABEL_H(22)`, tức các **dải nhãn** mà chính trang xem thử vẽ ra: một tấm bảng dán ảnh thì CÓ mép sắc lẹm, rất nhiều. Đúng hình dạng `TECH_DEBT #38` (một con số đo trên MỘT quần thể được đọc thành luật của CẢ TẬP). Chữa bằng cách **kể tên những hàng mà mép sắc lẹm là ĐÚNG THIẾT KẾ**, suy từ CÙNG công thức bố cục — và bài test phải đòi danh sách miễn trừ **BẰNG** đúng 30 hàng ấy, không phải "bao gồm": *"bao gồm" là cách một bản vá lặng lẽ thành một cái chăn trùm.*
- ⚠️ **MỘT TỈ LỆ CÓ HAI VẾ, VÀ VẾ DƯỚI LÀ VẾ KHÔNG AI KIỂM — MẪU SỐ CỦA PHÉP ĐO MẬT ĐỘ ĐÃ SAI HAI LẦN LIÊN TIẾP, THEO HAI CÁCH KHÁC NHAU** (2026-08-19, đo mật độ nhà). Câu hỏi rất đơn giản: *"nhà chiếm bao nhiêu phần khung hình?"*. Tử số (đếm điểm ảnh theo kênh màu do bên dựng tô) đúng ngay từ đầu. Mẫu số thì sai hai lần: **(1)** ảnh chụp RỘNG HƠN canvas (đệm 16px + dòng số liệu bên dưới), nên **12,9% tấm ảnh là nền trang** — ở chế độ mặt nạ nền ấy được tô ĐEN, tức lẫn hoàn toàn vào "không có lớp nào" của cảnh 3D, và mọi ô trong bảng 15 kỷ × 3 mốc đều thấp hơn sự thật một cách có hệ thống; **(2)** bản vá đầu — khai toạ độ canvas ra `.geom.json` rồi cắt theo — **vẫn sai**, vì khung nhìn thật chỉ cao **693** chứ không phải 780: canvas bị xén mất **23 dòng cuối**, nên con số KHAI (700) lớn hơn số dòng THẬT SỰ vẽ ra (677), và phần chênh lại rơi vào mẫu số. ⇒ **Ba luật**: **(a)** cùng họ `TECH_DEBT #44` (80 ô đường pha loãng một phép đếm về đất) và `TECH_DEBT #22` (trung bình trên vùng quá rộng) — trước khi tin một tỉ lệ, hỏi ***"mẫu số của tôi có lẫn thứ không thuộc câu hỏi không?"***, và nhớ rằng mẫu số hầu như không bao giờ có đối chứng riêng; **(b)** **một toạ độ KHAI không phải một toạ độ ĐO** — bản vá đúng là để bên DỰNG tự đánh dấu phần không-phải-khung-hình bằng một màu mốc `rgb(1,2,3)` (mặt nạ chỉ tô ba màu thuần một kênh, nên mọi điểm ảnh kể cả viền răng cưa đều có đúng MỘT kênh khác 0 ⇒ bộ ba ấy không thể sinh ra được), rồi bên ĐẾM loại nó khỏi mẫu số; nay phép đo không còn phụ thuộc bất kỳ con số nào có thể trôi; **(c)** ⚠️ **thứ lộ ra sự thật là một PHÉP CỘNG** — cộng tất cả các lớp lại thì ra 84%, thiếu 15,6% mà không lớp nào nhận; nếu chỉ đọc riêng dòng "nhà 21%" thì con số ấy trông hoàn toàn hợp lý và sẽ được dùng để ra quyết định. **Mọi phép đo chia-một-cái-toàn-thể-thành-nhiều-phần phải in ra TỔNG và đòi nó bằng 100%.**
  - ⚠️ **KÈM THEO — MỘT CÁI "SỌT ĐEN" KHÔNG CÓ NHÃN LÀ MỘT LỜI MỜI ĐOÁN BỪA, VÀ TÔI ĐÃ ĐOÁN BỪA HAI LẦN.** Phần 15,6% hụt kia lần lượt bị tôi gán cho *"bầu trời"* rồi *"cư dân"*, cả hai đều nghe rất hợp lý. Đo ra: **trời chiếm ĐÚNG 0,00% khung hình** (camera ngẩng 34,4° trừ nửa FOV dọc 19° ⇒ mép trên khung nằm 15,4° DƯỚI tầm mắt — một sự thật đã được ghi rõ trong chú thích `sceneGraph.js` từ Phase 9A mà tôi không đọc), còn **cư dân chỉ 0,14%**. ⇒ **(a)** công cụ mặt nạ nay **kể tên mọi khối bị tô đen** (`[mask] tô đen: sky×1, horizon×1, props×1, (không tên)×2`) — chính dòng "(không tên)×2" là thứ chỉ ra cư dân chưa có tên, và nay chúng tên `residents`, có test khoá; **(b)** trước khi gán một vùng ảnh cho một lớp, **hỏi thẳng bên dựng** thay vì suy từ trực giác về bố cục — đúng bài học `TECH_DEBT #22`, chỉ khác là lần này thứ bị đoán nhầm không phải màu mà là **danh tính của phần còn lại**.
- ⚠️ **MỘT CÔNG CỤ ĐO PHẢI *HỎI*, ĐỪNG *KHẲNG ĐỊNH* — và ba cái cờ tiện tay đều là lời khẳng định về thứ chỉ trình duyệt mới biết** (2026-08-19, đóng `TECH_DEBT #49`, ADR-036). `city-preview.mjs` — công cụ sinh MỌI ảnh nghiệm thu của thành phố 3D — chụp bằng `--window-size=(width+34, height+80)`, trong đó `+80` là một ƯỚC LƯỢNG cho khung cửa sổ. Trong hộp cát này nó **thiếu 23 điểm ảnh**: canvas 1100×700 đặt ở `y=16` chỉ được vẽ ra **677 dòng**, còn ảnh PNG vẫn cao 780 vì Chromium phủ nốt phần thiếu bằng nền trang. Nghĩa là mọi ảnh đơn nhiều tháng qua là một khung hình **1100×677 mang tên 1100×700**, tỉ lệ thật 1,625 trong khi camera dựng theo 1,571 (**ảnh bị kéo dãn dọc**), cộng **12,9% tấm ảnh không phải khung hình** — và 12,9% ấy nằm trong mẫu số của mọi phép tính tỉ lệ. ⇒ **Ba luật**: **(a)** bản vá ĐÚNG không phải nới `+80` thành `+103` (số đo được) — *"thay một con số đoán bằng một con số đoán khác, và nó trôi lại ngay khi ai đó đổi bố cục trang, thanh cuộn, hay chạy ở DPR khác"* (lời Đàm) — mà là **hỏi `getBoundingClientRect()` rồi chụp CDP `Page.captureScreenshot` với `clip` đúng hộp bao ấy**; **(b)** ⚠️ **một toạ độ KHAI không phải một toạ độ ĐO** — bản vá ĐẦU TIÊN khai toạ độ canvas ra `.geom.json` rồi cắt theo, và **vẫn sai**, vì con số khai (700) lớn hơn số dòng thật sự vẽ ra (677); **(c)** cả ba cờ `--window-size` · `--screenshot` · `--virtual-time-budget` đều cùng một bệnh (cỡ khung nhìn, vùng cần chụp, thời điểm vẽ xong — không cái nào script biết được), nên đã bỏ CẢ BA và có một bài test đọc mã nguồn canh chúng không quay lại. Cổng `kiemKhungNhin` là hàm thuần, có `--selftest` **nhốt đúng ca 23 dòng bị xén**.
  - ⚠️ **VÁ XONG CÁI XÉN THÌ ĐỤNG NGAY MỘT CÁI TRẦN KHÁC, VÀ NÓ KHÔNG TỰ XƯNG TÊN: Ổ CẮM CDP CHỈ CHO 4 MiB MỘT TIN NHẮN.** Lượt dựng lại mốc nền đầu tiên chạy 5 phút rồi chết với đúng một dòng `Page.captureScreenshot: ổ cắm CDP lỗi` — không một chữ nào nói tới cỡ ảnh. Đo bằng cách chụp một canvas nhiễu mỗi lúc một cao: **4.194.264 byte base64 thì chạy, nhích thêm là đứt** ⇒ trần đúng bằng `4 × 1024 × 1024`. Bản quét 15 kỷ (1864×3120 ≈ 9 MB base64) **không bao giờ đi lọt**. ⇒ **Ba luật**: **(a)** một lỗi TRUYỀN TẢI đội lốt một lỗi ngẫu nhiên — khi một công cụ chết ở đúng bước gửi/nhận dữ liệu lớn, **hỏi cỡ dữ liệu trước khi đi tìm lỗi logic**; **(b)** cách vá là **chụp thành DẢI NGANG rồi ghép ở phía Node** (ngân sách mỗi dải = **một nửa** trần, tính theo trường hợp ảnh KHÔNG nén được chút nào — trần kia là một cái vực, vượt qua thì không có thông báo); **(c)** ⚠️ phép thử ngược đầu tiên của chính bản vá này **không nổ**, và **phép phá mới là thứ hỏng**: nó dồn dải theo bước cố định, mà bài test lại chia 13·13·4 nên hai dải đầu BẰNG NHAU ⇒ bước cố định cho ra đúng cùng kết quả. Chia lại 7·18·5 thì đỏ ngay. **Một bộ dữ liệu thử "giống hình dạng thật" có thể chính là bộ dữ liệu duy nhất không phân biệt được đúng với sai.**
  - ⚠️ **`md5sum` CỦA ẢNH DỰNG ĐỔI THEO *TẢI MÁY* — nên nó chỉ đọc được MỘT CHIỀU** (`TECH_DEBT #50`). Đo được: cùng một lệnh, cùng một cây mã, máy rảnh 5 lượt liên tiếp ra `2ad06f97…`; bật 4 vòng lặp bận trên máy 4 nhân thì ra `28992bba…`; hết tải quay lại `2ad06f97…`. Chênh **±1 trên một kênh, ~2% điểm ảnh** (SwiftShader chia ô rasterise theo số luồng dùng được). ⇒ **✅ trùng md5 ⇒ ảnh y hệt** — chiều này vẫn là bằng chứng mạnh nhất dự án có, và mọi lời hứa cũ dựa vào nó (ADR-034 *"khung mặc định bất biến"*) **vẫn đứng vững**; **❌ khác md5 ⇏ ảnh đã đổi** — ai chạy lại phép chứng minh của ADR-034 trên một máy đang bận sẽ thấy nó "trượt" và tưởng có hồi quy. Muốn chứng minh KHÔNG đổi thì chụp hai lượt **liền nhau trên máy rảnh**; md5 lệch thì **đo chênh lệch điểm ảnh rồi so ngưỡng mắt 12/255** (±1 thấp hơn ngưỡng 12 lần ⇒ không đổi được kết luận mỹ thuật nào). Luật nghiệm thu *"từ chối nếu cặp trước/sau TRÙNG byte"* vẫn nguyên giá trị — nó bắt lỗi chép nhầm/đặt sai tên, là chuyện khác hẳn.
  - ⚠️ **HỆ QUẢ CHO MỌI PHIÊN SAU — MỌI CON SỐ ĐO TRÊN ẢNH TRƯỚC 2026-08-19 KHÔNG SO TRỰC TIẾP ĐƯỢC VỚI SỐ MỚI** (khung 1134×780 bị xén ↔ khung 1100×700 đúng thật). Xử đúng cách `TECH_DEBT #22` đã xử bộ lọc "8% mái": ghi thành một mục riêng trong `PERFORMANCE.md` thay vì lặng lẽ đo lại. **KHÔNG ảnh hưởng**: tam giác · lệnh vẽ · ms mỗi khung — chúng đọc `renderer.info`, và bộ đệm vẽ luôn là 1100×700; thứ bị xén là phần HIỂN THỊ, không phải phần RENDER.
- ⚠️ **CÁI BÚT VẼ CŨNG LÀ MỘT PHẦN CỦA PHÉP ĐO — và nó nói dối to hơn cả thứ ai cũng đang nghi** (2026-08-19, §3a). Cố vấn hỏi *"phép đo độ phủ mặt bằng dùng HỘP BAO thay vì hình thật, sai bao nhiêu?"* — một câu hỏi đúng, và nó khiến tôi (lẫn cố vấn) nhìn chằm chằm vào **hình được tô**. Đo tách bạch ra thì nguồn nói-quá lớn nhất **không nằm ở hình mà nằm ở LUẬT TÔ**: `for (x = floor(x0); x < ceil(x1))` — ô mẫu nào bị hình CHẠM VÀO là tô trọn. Một cạnh rơi giữa hai ô mẫu vẫn ăn nguyên ô, phần dôi nằm ở **rìa ngoài** nên phép lấy hợp không xoá đi được, và nó cộng ở **cả bốn cạnh của mọi khối**. Bảng ba nguồn: **luật tô 6,11 đpt · hộp bao cả công trình 4,86 đpt · hình thật của từng khối 0,13 đpt**. ⇒ **Ba luật**: **(a)** một phép đo raster có HAI nửa — *tô hình gì* và *tô thế nào* — và câu hỏi "phép đo này sai bao nhiêu" phải hỏi cả hai; nửa thứ hai không có tên nên không ai nghĩ tới nó, y hệt chuyện **mẫu số** không có đối chứng riêng (bài học đo mật độ cùng ngày); **(b)** ⚠️ **thứ cố vấn (hoặc chính mình) chỉ đích danh có thể là phần LÀNH NHẤT** — ở đây hình của từng khối chỉ đáng 0,13 đpt, tức nếu tin lời đoán mà chỉ sửa hình thì đã tốn công cho 1% vấn đề và để nguyên 99%; **(c)** cách bóc là dựng một **cái thang** mỗi nấc đổi ĐÚNG MỘT thứ (A hộp-bao+luật-bao → B đổi luật tô → C đổi phạm vi hộp → D đổi hình), và nấc A **phải tái lập ĐÚNG con số cũ** — không có vế đó thì mọi hiệu số chỉ là "hai chương trình khác nhau", chứ chưa chứng minh nguồn nào sinh ra chênh lệch nào. ⚠️ Kèm ba bài phụ: **(1)** một phép phá KHÔNG đỏ mà thủ phạm là **dữ liệu thử** — ca "gable phải ra hình chữ nhật" dùng khối không có `sides`, mà lăng trụ 4 cạnh có đáy TRÙNG KHÍT hình chữ nhật (`rx = (w/2)/cos(π/4)`, đỉnh ở 45°), nên hai nhánh ra cùng kết quả và bài test không phân biệt được chúng; phải gài `sides: 8` mới có răng (cùng họ "ba dải bằng nhau" ở `pngProbe.test.js`); **(2)** một phép phá KHÔNG đỏ mà **đúng là không nên đỏ** — đổi tâm ô mẫu thành góc ô làm số dịch 0,15–0,44 đpt **theo CẢ HAI CHIỀU**, dấu vân tay của một phép TỊNH TIẾN lưới chứ không phải PHÓNG TO; phân biệt được nhờ ĐO chứ không nhờ lý lẽ, và phải ghi ra để phiên sau khỏi tưởng là lỗ hổng; **(3)** ngưỡng của bài đối chứng phải lấy từ **phép đo ở đúng độ mịn đang chạy** — bản đầu chép sàn "4 đpt" từ bảng lưới 8 mẫu/ô trong khi mặc định là 16, và nó ĐỎ ngay (giá trị thật 3,65).
- ⚠️ **CỜ CHỈ-DÙNG-CHO-PHÉP-ĐO PHẢI CÓ TEST KHOÁ NÓ TẮT — không thì nó là một lệnh vẽ mất trắng, im lặng** (2026-08-19). Để hỏi được "điểm ảnh nào là nhà", `createCityScene` nhận thêm `splitCityMesh` cắt khối thành phố làm hai (`buildings`/`props`). Cái giá là **lệnh vẽ**: một khối gộp tốn một lệnh vẽ mỗi HỌ VẬT LIỆU, nên cắt đôi làm mọi họ có mặt ở cả hai nửa bị đếm hai lần — đo ra kỷ 1 tốn **+2**. Nếu cờ ấy lặng lẽ bật trong app thì **không có gì đỏ lên**: ảnh y hệt, tam giác y hệt, chỉ ngân sách lệnh vẽ bị thủng. ⇒ Ba assert: mặc định TẮT (15 kỷ, phải có đúng MỘT khối tên `city`) · bật thì PHẢI cắt thật (không có đối chứng này thì assert đầu vẫn xanh khi cờ hỏng và mọi mặt nạ sẽ khớp nhầm) · và **khoá QUAN HỆ chứ đừng khoá "+1"** — giá thật bằng số họ dùng chung nên nó khác nhau theo kỷ; viết cứng +1 là gài mìn, viết cứng một bảng 15 số là khoá một phép làm tròn.
- ⚠️ **TÊN FILE KHÔNG PHẢI BẰNG CHỨNG VỀ NỘI DUNG FILE — và một con số nghiệm thu không truy được về đúng hai file cùng đúng dòng lệnh thì phải ĐO LẠI, không được dùng** (2026-08-18, Phase 11). Ảnh nghiệm thu cận cảnh mái tên là `MAI-TRUOC-ky9.png` / `MAI-SAU-ky9.png`, và hai con số rút ra từ chúng (4,5% ở khung app · 16,5% khi zoom sát mái) đã sẵn sàng đưa vào báo cáo. `md5sum` cho thấy `MAI-SAU-ky9.png` **trùng từng byte** với `city-era09-light-h12.png` — tức nó là ảnh khung THƯỜNG đang mang cái tên "cận mái", còn vế TRƯỚC thì không còn cách nào biết đã chụp bằng cờ gì. Nghĩa là hai con số ấy hoặc đúng, hoặc là kết quả so một tấm zoom với một tấm không zoom; **không phân biệt được hai khả năng đó thì con số bằng không**. Đo lại từ đầu (worktree ở `e089c00` cho vế TRƯỚC, cây làm việc cho vế SAU, **cùng một dòng lệnh**, tên file ghi rõ mức zoom) ra **5,3%** và **15,1%** — gần với bộ cũ, nhưng "gần đúng" không phải lý do để giữ một con số không truy được nguồn. ⇒ **Ba luật**: (a) đây là họ hàng của *"một con số nghiệm thu phải đi kèm CÔNG CỤ đã đo ra nó"* (Phase 4F), mở rộng thêm một vế — phải đi kèm cả **ĐẦU VÀO**, vì một công cụ đúng chạy trên đầu vào sai vẫn ra số sai mà không kêu; (b) ảnh chụp là thứ **duy nhất trong dự án không có kiểm tra tự động nào** — mã có test, tài liệu có người đọc, còn một tấm PNG đặt sai tên thì im lặng tuyệt đối, nên **`md5sum` mọi ảnh nghiệm thu trước khi trích số từ chúng** (rẻ, một dòng, bắt được đúng loại lỗi này); (c) ⚠️ và đừng "tiết kiệm" bằng cách chép ảnh cũ sang tên mới — mỗi lần chép là một cơ hội để cái tên và nội dung tách khỏi nhau. Chụp lại tốn 25 giây; đọc sai một kết luận mỹ thuật tốn cả một phase. ⚠️ **LUẬT MỞ RỘNG (2026-08-21, Phase 14 §3-Q2): MỌI BẢNG SỐ trong `PERFORMANCE.md` PHẢI ghi kèm ĐÚNG DÒNG LỆNH đã sinh ra nó VÀ một dấu hiệu nhận dạng ĐỜI ẢNH** (thư mục chứa ảnh + mốc thời gian, hoặc commit của cây mã đã dựng ảnh). Lý do: mục §1 của bảng Phase 13 hoá ra được tính từ một **đời ảnh khác** với dòng lệnh nó trích dẫn, và không có gì trong bảng ấy để lộ điều đó — số nào cũng chỉnh tề, không một dấu hiệu bất thường. Ba vế **CÔNG CỤ · ĐẦU VÀO · ĐỜI ẢNH** phải đủ cả ba thì một con số mới truy được nguồn; thiếu vế nào thì phải **ĐO LẠI**, không được dùng. Bảng cũ được sửa **tại chỗ bằng một ĐÍNH CHÍNH** khi có ai chạm tới, không mở phiên quét lại toàn bộ.
- ⚠️ **MỘT CON SỐ NGHIỆM THU SUY TỪ MỘT MẪU NHỎ SẼ ĐƯỢC ĐỌC NHƯ MỘT LUẬT CỦA CẢ TẬP — và cái mẫu ấy không bao giờ tự khai ra rằng nó là mẫu** (2026-08-18, Phase 10 Bước 2). Bảng "Sau Phase 10" ở `PERFORMANCE.md` ghi trần **13 lệnh vẽ**. Con số ấy đo trên đúng **ba kỷ** (6 · 9 · 13 — ba kỷ Bước 1 làm), rồi giá trị lớn nhất trong ba ấy được viết ra thành một hàng rào cho cả 15 kỷ, và thành một trong sáu mục cổng nghiệm thu của cả chương trình Phase 10–12. Đo đủ 15 kỷ lần đầu tiên: **kỷ 10 ra 14** — và ra 14 **ở CẢ lượt đo trên `HEAD`**, tức nó đã vượt trần từ trước khi Phase 10 bắt đầu. Kỷ 10 (Anh, thời công nghiệp) là kỷ duy nhất dùng cùng lúc cả `brick` lẫn `slate`, nên nó có nhiều hơn mọi kỷ khác đúng một họ vật liệu. ⇒ **Ba luật**: (a) trước khi viết một ngưỡng vào tài liệu, hỏi *"con số này đo trên bao nhiêu trường hợp, và tập thật có bao nhiêu?"* — nếu hai số ấy khác nhau thì phải ghi **cỡ mẫu ngay cạnh con số**, vì phiên sau sẽ chỉ đọc con số; (b) cùng hình dạng với *"một ngân sách tự tính mà chưa bao giờ được đặt cạnh sự thật"* (Performance Gate 2026-08-17) — cái trần chưa bao giờ được kiểm với cả 15 kỷ, **nó chỉ được kiểm với chính cái mẫu đã sinh ra nó**; (c) ⚠️ và cách xử lý ĐÚNG khi phát hiện là **đặt lại trần cho đúng sự thật rồi báo**, tuyệt đối không (i) nới trần cho vừa kết quả, cũng không (ii) gộp `brick` với `slate` để lấy lại con số 13 — mua một con số đẹp bằng cách nói dối vật liệu chính là thứ ADR-025 đã cấm với mặt đường. Ghi ở `TECH_DEBT #38`.
- ⚠️ **MỘT PHÉP ĐO CHẠY NỀN LÀ MỘT ẢNH CHỤP CỦA CÂY MÃ TẠI *NHIỀU* THỜI ĐIỂM — và nó không hề nói cho ta biết điều đó** (2026-08-18, Việc 2). Để chứng minh việc dọn bảng tầng trệt sang file riêng "chỉ là dọn nhà", tôi cho chạy nền một lượt đo 15 kỷ lấy mốc nền, **rồi bắt tay sửa file ngay trong lúc nó chạy**. Mỗi kỷ mất ~15 giây, nên bảng kết quả trả về là một thứ pha trộn của **ba trạng thái mã khác nhau**: kỷ 1–4 đo trên mã cũ (khớp tài liệu từng đơn vị), kỷ 5–9 đo đúng lúc bảng đã bị cắt khỏi `eraStyle.js` mà `buildingSpec.js` chưa được nối lại (**cả thành phố mất tầng trệt**, tam giác tụt 6.000–8.000 mỗi kỷ), kỷ 10 trở đi đo trên mã đã nối xong. Bảng ấy trông **hoàn toàn hợp lý**: 15 dòng, số nào cũng chỉnh tề, không một dấu hiệu nào cho biết nó không mô tả một phiên bản nào của phần mềm cả. Nếu đọc vội thì kết luận sẽ là *"việc dọn nhà làm mất 15% tam giác ở 5 kỷ"* — một hồi quy không hề tồn tại, và tôi sẽ đi chữa một cái không hỏng. ⇒ **Ba luật**: (a) **một phép đo và một lần sửa mã không được chồng lấn nhau về thời gian**; muốn đo mốc nền trong lúc vẫn làm việc thì dựng một **kho git tạm** (`git worktree add`) rồi đo ở đó, đúng cách phép thử ngược đã phải làm sau bài học 2026-08-17; (b) cùng họ với *"`shot.mjs` phục vụ `dist/` và không tự build"* (Phase 4D) — cả hai đều là chuyện **phép đo và mã không cùng một thời điểm**, chỉ khác là lần này độ lệch nằm GIỮA các dòng của cùng một bảng, nên không có một hành động sửa nào (như "nhớ build trước") che được nó; (c) cách nhận ra: **đối chiếu vài dòng với một bảng đã biết trước khi tin cả bảng** — chính việc kỷ 1–4 khớp tài liệu còn kỷ 5 lệch 5.912 tam giác là thứ lộ ra rằng chỗ hỏng là phép đo chứ không phải mã, và một lượt đo lại đúng kỷ 5 (ra 32.424, khớp tài liệu) đã đóng nghi vấn trong 20 giây.
- ⚠️ **MỘT NGÂN SÁCH PHẢI ĐƯỢC PHÁT BIỂU BẰNG ĐÚNG THỨ NGUYÊN NÓ ĐANG NÓI — và "cái trần chung" gần như luôn sai thứ nguyên** (2026-08-18, đóng `TECH_DEBT #38`). Cổng nghiệm thu ghi *"số lệnh vẽ không quá 13"*; đo đủ 15 kỷ thì kỷ 10 ra 14. Bản vá hiển nhiên — **nâng trần lên 14** — là bản vá tôi đề xuất, và Đàm bác: *"14 kỷ khác đang ở 11–13, nên trần chung 14 cho chúng ba lệnh vẽ trống để trôi vào trong im lặng. Cổng chỉ bắt được kỷ tệ nhất."* Anh đúng, và cái sai đã có tên sẵn trong file này: **bẫy Phase 7D — một con số tuyệt đối không diễn đạt được một luật nói về QUAN HỆ.** Lời hứa thật chưa bao giờ là "≤ 13"; nó là *"kỷ này không được tốn hơn chính nó hôm nay"*, và một hằng số thì không nhìn thấy "chính nó". ⇒ **Bốn luật**: (a) đọc mục nợ / lời hứa mình đang đi đóng rồi hỏi *"thứ này là một MỨC hay một QUAN HỆ?"* — nếu là quan hệ thì con số đóng nó cũng phải là con số quan hệ, ở đây là **một bảng 15 mốc riêng, mỗi mốc là số đo của chính kỷ ấy** kèm lệnh đo + ngày đo chép sẵn để tái lập; (b) ⚠️ **một bảng nhiều dòng vẫn có thể thoái hoá về một trần chung mà trông y hệt** — điền cả 15 dòng bằng cùng một số là cách rẻ nhất để bài test hết đỏ, nên phải có một bài canh chính cái bảng (khoảng trải ≥ 2, và ≥ 10/15 dòng nằm dưới dòng cao nhất); (c) **đối chứng phải hỏi TỪNG dòng một, không hỏi tổng** — hỏi tổng thì một kỷ dư chỗ bù cho một kỷ vượt, tức dựng lại đúng cái phễu mà bảng-nhiều-dòng sinh ra để gỡ (Đàm: *"Không có đối chứng thì không biết bài test có còn răng hay không"*); (d) và điều khiến cổng này chạy được trong `npm test` thay vì phải bật Chromium: **`lệnh vẽ thành phố = (số họ vật liệu) + 4`, đúng 15/15 kỷ, không một ngoại lệ** — đây KHÔNG phải một thứ đại diện (`TECH_DEBT #22` là bài học về việc nhầm hai chuyện đó) mà là một phép tính chính xác đã đặt cạnh phép đo thật ở cả mười lăm kỷ; hằng số 4 là một **hiệu số đo được**, không phải kết quả đếm bằng mắt trong `sceneGraph.js`. ⚠️ Kèm một bài phụ đáng nhớ ngang phần trên: **cái tưởng là blocker hoá ra là hệ quả của việc đề xuất sai giải pháp** — mục nợ ghi "phải đụng `materials.js`, ngoài phạm vi file được phép", nhưng cổng đúng chỉ ĐỌC bảng vật liệu chứ không sửa nó, nên nó nằm gọn trong phạm vi. **Trước khi ghi một blocker, hỏi xem nó là blocker của bài toán hay của lời giải mình vừa nghĩ ra.** Xem ADR-028.
- ⚠️ **"BA MẪU LÀ ĐỦ" LÀ MỘT GIẢ ĐỊNH, VÀ KHI HÀM LÀ THUẦN THÌ NÓ RẺ TỚI MỨC KHÔNG CÓ CỚ ĐỂ ĐOÁN** (2026-08-18, cùng phiên). Bài `KHÔNG THÊM MỘT LỆNH VẼ NÀO` chụp thành phố ở đúng ba mốc tuổi (12 · 45 · 120 phiên) rồi kết luận cho MỌI mốc — đúng hình dạng đã sinh ra `TECH_DEBT #38`. Đàm: *"Đừng đoán. `computeCityLayout` là hàm thuần nên quét rẻ: chạy `sessionCount` từ 0 đến ~150 bước 5, gom TẤT CẢ tổ hợp phân biệt được, rồi assert ba nhịp kia phủ trọn tập hợp đó. Biến một giả định thành một phép đo."* Quét xong (31 mốc × 15 kỷ, chạy hết 0,4 giây) thì ba nhịp **phủ trọn thật** — nhưng phép quét trả về hai thứ mà việc đoán không bao giờ cho: **(1)** nhịp 120 **một mình** đã phủ trọn ở cả 15 kỷ (nhịp 12 chỉ với tới 2–3 trên 6–7 kiểu), nên ba nhịp hôm nay là một **lưới rẻ**, không phải một phép đo đang chia nhau việc — và nói thẳng điều đó ra là thứ ngăn phiên sau tưởng chúng đang làm việc; **(2)** ⚠️ **một câu tự trấn an trong chú thích của chính tôi bị bác bỏ**: bản trước viết *"số nhà dân đi từ 6 lên 30 nên bộ họ vật liệu của thành phố TRẺ hẹp hơn hẳn thành phố già"*, đo ra thì `nen` **giống hệt nhau** ở cả ba nhịp, ở cả 15 kỷ — sáu căn đầu tiên đã kéo đủ mọi họ mà hai mươi bốn căn sau dùng. ⇒ **Ba luật**: (a) mọi bảng "ba mốc mẫu" phải kèm một phép quét chứng minh ba mốc ấy phủ trọn — và phải quét **TỪNG TRỤC** của bảng, ở đây trục thứ hai là `level` (chứng minh cạn ở 3 bằng cách đòi cấp 4/5/9 cho mô tả byte-identical với cấp 3, chứ **không** bằng cách đọc hằng số `Math.min(3, …)` ở một file khác); (b) **đối chứng của một phép quét phải bỏ đúng cái mốc đang gánh việc** — bỏ hai nhịp trẻ thì nó vẫn xanh, vì nhịp già một mình đã phủ trọn; viết một đối chứng không thể đỏ rồi tưởng mình đang canh gác là cách hỏng tinh vi nhất; (c) cùng họ *"một câu tự trấn an cũng phải được kiểm như một con số"* (Phase 4G) — khác ở chỗ lần này câu ấy nằm trong chú thích của **bài test**, nơi ai đọc cũng cho là đã được kiểm.
- ⚠️ **MỘT BÀI TEST CÓ THỂ ĐANG ĐÚNG *NHỜ MỘT SỰ TRÙNG HỢP*, VÀ LÚC ẤY NÓ VẪN XANH — "chưa xanh oan" không bằng "không thể xanh oan"** (2026-08-18, Phase 10 Bước 2, bản vá thứ HAI của cùng một bài test). Bài `KHÔNG THÊM MỘT LỆNH VẼ NÀO` hỏi *"tập họ vật liệu của tầng trệt có nằm gọn trong tập họ của phần còn lại không?"*. Bản trước đã sửa một lần rồi (hỏi ở cấp CẢ KỶ thay vì từng công trình — `TECH_DEBT #22`), nhưng nó dựng quần thể bằng **7 loại × 3 hạng = 21 công trình GIẢ ĐỊNH** rồi gọi đó là "cả thành phố". Thành phố thật không phải tập đó: nó là **5 bản vẽ `BLUEPRINT_CATALOG`** (loại và hạng đã ấn định sẵn) cộng **6–30 nhà dân** do `deriveDwellings` sinh ra. Một quần thể RỘNG HƠN thật thì `nen` phình ra, và `them ⊆ nen` có thể xanh trong khi thành phố thật vẫn đẻ thêm một họ. ⚠️ **Đo ra thì phễu ấy hôm nay rộng đúng 0 họ ở cả 15 kỷ** — tức bài cũ **không** hề xanh oan. Nhưng nó đúng nhờ một trùng hợp (`deriveDwellings` tình cờ phủ đủ house/shop/workshop, catalog tình cờ phủ đủ 4 loại còn lại), và **một lời hứa đúng nhờ một thứ chẳng liên quan thì gãy trong im lặng đúng lúc thứ đó đổi** (Phase 7D). ⇒ **Ba luật**: (a) *"đừng DỰ ĐOÁN thứ có thể ĐO"* áp cho cả **quần thể đầu vào của một bài test**, không chỉ cho đại lượng đầu ra — quần thể thật có sẵn dưới dạng một hàm THUẦN (`computeCityLayout`) thì không có lý do gì dùng một quần thể bịa; (b) khi quần thể thay đổi theo thời gian (nhà dân 6 → 30 theo `sessionCount`), phải hỏi **TỪNG NHỊP TUỔI MỘT** — gộp ba nhịp vào một `nen` chung là tự dựng lại đúng cái phễu vừa gỡ, vì họ vật liệu của thành phố già sẽ che cho tầng trệt của thành phố trẻ; (c) một bài test dựng quần thể phải kèm **gác chạy-rỗng khẳng định quần thể ĐÚNG HÌNH DẠNG** (đủ 5 bản vẽ · có nhà dân · tầng trệt có sinh khối) và một **gác đếm số lần duyệt**, nếu không một `continue` đặt nhầm chỗ sẽ làm nó im lặng bỏ qua 10 kỷ.
- ⚠️ **"CÁI KHAY" KHÔNG PHẢI MỘT CÁI MÉP — VÀ BA GIẢ THUYẾT ĐẦU ĐỀU BỊ CHÍNH SỐ ĐO BÁC BỎ** (2026-08-19, VIỆC 1, ADR-038). Đàm nhìn thành phố và nói *"tại sao một thành phố lại được xây trên một ô đất nhô ra"*. Ba lần đi tìm cái mép, ba lần sai: (1) *"có tường đứng ở `APRON_EDGE`"* → cao độ hai bên khớp **0,0000** ở kỷ 1·7·14; (2) *"chỗ nối màu gãy"* → bước màu một-điểm-ảnh lớn nhất qua 353 vị trí chỉ **1,1/255**, tổng **1,9** trải trên 60 điểm ảnh, trong khi ngưỡng mắt là 12; (3) *"vùng gần quá phẳng"* → bản vá gợn sóng đổi 25,6% điểm ảnh nhưng lệch trung bình 1,91 và **0 điểm ảnh** vượt ngưỡng mắt (đã hoàn tác). Thứ chỉ ra sự thật là **phủ ranh giới các vùng lên chính ảnh render rồi nhìn**: KHÔNG có mép nào ở cả hai ranh giới. **Cái khay là hình chữ nhật đường-và-nhà dừng đột ngột giữa một mặt phẳng trống trơn** — mắt tự vạch đường viền từ chỗ NỘI DUNG hết, không từ chỗ HÌNH HỌC đổi. ⇒ **Ba luật**: **(a)** khi hai, ba giả thuyết liên tiếp đều bị số đo bác, **nghi chính CÂU HỎI** chứ đừng đi tìm giả thuyết thứ tư cùng họ — cả ba giả thuyết trên đều là biến thể của *"mép bị lỗi"*, và câu hỏi đúng là *"vì sao mắt thấy có một cái mép ở chỗ không có mép nào?"*; **(b)** cách rẻ nhất để đổi câu hỏi là **vẽ thứ mình tin lên đúng tấm ảnh mình đang nhìn** (ở đây: tô ranh giới hai vùng lên ảnh render) — rẻ hơn mọi phép đo, và nó lật ngược kết luận trong một lần nhìn; **(c)** vùng quê (`city3d/outskirts.js`) là tầng đầu tiên **NẰM NGOÀI lưới 12×12**, và nó là tầng **ĐỊA LÝ** chứ không phải **TIẾN ĐỘ**: không nhận `built`/`levels`/`sessionCount`, có test gọi kèm **dữ liệu rác** khoá điều đó. Nó **KHÔNG có bảng 15 kỷ riêng** — đọc thẳng `floraStyle.js`, vì hai bảng mật độ sẽ trôi khỏi nhau và triệu chứng ("cây trong phố rậm mà cây ngoài phố thưa, ở đúng vài kỷ") rất khó truy. ⚠️ Và nó **KHÔNG vào `blockers`**: bộ hoạch định đường bay chỉ biết CÔNG TRÌNH chứ không biết ĐỊA HÌNH (kỷ 8 có mặt đất vùng quê dâng **+2,18** trong khi nền phố quanh 0), nên chặn cây mà không chặn quả đồi bên dưới là mua một **sự an toàn GIẢ** — `TECH_DEBT #54`.
  - ⚠️ **KÈM THEO — MỘT CÁI GÁC ĐÚNG VẪN CÓ THỂ KHÔNG CÓ GÌ CANH, VÀ CHỈ PHÉP THỬ NGƯỢC MỚI BIẾT.** Gỡ hẳn dòng `if (placement.vungQue) continue;` khỏi `sceneGraph.js` thì **toàn bộ 891 bài vẫn xanh** — vì `cityFocus.test.js` **tự dựng lấy** danh sách khối của nó (và tự bỏ qua vùng quê), nên nó không bao giờ nhìn thấy `blockers` THẬT mà cảnh trả ra. Một bài test dựng lại đầu vào bằng đường riêng thì nó đang canh bản dựng lại ấy, không canh mã sản phẩm. ⇒ Bài mới hỏi **thẳng cảnh thật** (`sceneStats.test.js`), và nó đỏ ngay khi gỡ gác (kỷ 1: vật cản cách tâm 13,73 trên mép lưới 6,0). ⚠️ **Và ngưỡng của chính bài ấy bản đầu tôi ĐOÁN 1,5 trong khi số đo thật là 0,340** (đo 15 kỷ: xa nhất 6,340; 11/15 kỷ không quá 5,88) — một cái phễu rộng gấp **4,4 lần**, đúng bài học Phase 9A. Nay 0,6, kèm đối chứng đếm xem NẾU vùng quê lọt vào thì phép đo còn bắt được bao nhiêu khối.
  - ⚠️ **KÈM THEO — LOẠI TRỪ BẰNG VỊ TRÍ TRONG MẢNG LÀ MỘT CÁI BẪY IM LẶNG.** Bản đầu loại vùng quê khỏi `blockers` bằng `placements.slice(0, length − soKhoiVungQue)`: đúng kết quả hôm nay, nhưng nó **ngầm đòi vùng quê phải LUÔN được đẩy vào cuối cùng**. Ngày nào có ai đẩy thêm một loại khối nữa sau nó, phép cắt lấy nhầm và camera bắt đầu đâm xuyên nhà — không có gì đỏ lên. Nay là một **nhãn tường minh** (`vungQue: true`). Chính bài test đọc-mã-nguồn *"vật cản phải đọc THẲNG từ danh sách khối đã đem đi dựng hình"* đã bắt được chỗ này (regex đòi đúng `for (const placement of placements)`), tức một bài test canh *hình dạng mã* đã chỉ ra một khuyết tật *thiết kế* — đừng vội nới regex khi nó kêu.
- ⚠️ **MỘT THAM SỐ BỊ CHE KHUẤT BỞI MỘT BIẾN CÙNG TÊN ĐÃ GIẾT HAI TÍNH NĂNG TRONG IM LẶNG NHIỀU THÁNG — VÀ THỨ BẮT ĐƯỢC KHÔNG PHẢI MỘT CỔNG NÀO CẢ, MÀ LÀ VIỆC DÁN 15 THỨ CẠNH NHAU RỒI NHÌN** (2026-08-23, ADR-054). `buildScenePalette({ …, era: eraNumber, … })` đổi tên tham số `era` (một SỐ KỶ) thành `eraNumber` ngay dòng khai báo, rồi vài dòng sau gán đè `const era` — một **MÀU**. Từ đó `getFloraStyle(era)` và `getHumanStyle(era)` ở cuối hàm **trông hoàn toàn đúng** và đang truyền một object màu vào hai hàm chờ một số kỷ; cả hai **cố ý** rơi về kỷ 1 với dữ liệu lạ (không được ném lỗi giữa màn hình Thành Phố) ⇒ **15 kỷ dùng chung MỘT màu lá và MỘT màu vải**. Mảng "mỗi kỷ một `leafHue`" của **Phase 8D chưa bao giờ chạy thật trên production**. Build xanh · lint sạch · 1110 bài test xanh · không một cảnh báo. ⇒ **Bốn luật**: **(a)** ⚠️ **một tham số bị đổi tên rồi có một biến khác nhận cái tên cũ là quả mìn im lặng nhất trong JavaScript** — mọi dòng dùng cái tên ấy đọc lên vẫn đúng ngữ pháp lẫn ngữ nghĩa, và không có gì trong ngôn ngữ, lint hay test nói rằng nó đã trỏ sang thứ khác; đổi tên một tham số thì phải `grep` chính cái tên CŨ trong toàn thân hàm; **(b)** một hàm tra cứu **rơi về mặc định thay vì ném** là đúng cho production và **là một cái bịt miệng** cho phép đo — chỗ nào có `?? BẢNG[1]` thì phải có một bài test hỏi ở ĐẦU BÊN KIA rằng *"15 đầu vào khác nhau có ra 15 kết quả khác nhau không"*, vì bản thân hàm ấy sẽ không bao giờ kêu; **(c)** thứ lộ ra sự thật là `scripts/human-strip.mjs` — **dán 15 cư dân cạnh nhau, phóng 5 lần, rồi nhìn**: cả 15 đều nâu. Đúng luật đã ghi từ Phase 3G (*lỗi mỹ thuật gần như luôn là lỗi SO SÁNH*), nhưng ở một cấp khác: bản quét 15 kỷ **không** đủ độ phân giải cho một cư dân 18 điểm ảnh, nên phải có một công cụ CẮT-VÀ-PHÓNG riêng cho mỗi họ chi tiết; **(d)** và cái đáng sợ nhất: **bộ chấm bản sắc bằng SỐ báo 105/105 cặp ĐẠT trong khi màn hình chỉ có một màu** — vì nó chấm **BẢNG**, còn cái đứt nằm ở **DÂY NỐI từ bảng tới bảng màu**. *Một cổng số xanh chỉ chứng minh thứ nó đọc; nó không chứng minh thứ ấy tới được điểm ảnh.*
  - ⚠️ **KÈM THEO — LẦN THỨ SÁU CỦA "MỘT TRƯỜNG GÁNH HAI VIỆC", VÀ LẦN NÀY THỨ GÁNH HAI VIỆC LÀ MỘT VAI MÀU.** `cloth2` được định nghĩa là *"quần/chân, tối nhất bộ"* (suy ra bằng `cloth × 0,66`) và đồng thời được dùng cho **mọi thứ đội trên đầu**. Nghĩa là **cái nón và cái quần bị buộc phải cùng một lò nhuộm**, và cái nón VĨNH VIỄN tối hơn cái áo. Đo 15 kỷ: nón lá kỷ 6 — đan bằng lá cọ, không nhuộm bao giờ — render ra độ đậm **0,170**, tối thứ nhì cả bảng; sáu kỷ sai cùng kiểu (2 · 5 · 6 · 7 · 8 · 15) và bốn kỷ mà tối là ĐÚNG (4 futou lụa đen · 9 casquette · 10 mũ nồi tweed · 11 mũ phớt nỉ). ⇒ **Ba luật**: **(a)** câu hỏi phát hiện vẫn là câu cũ — *"ngoài đời hai thứ này có luôn đi cùng nhau không?"* — nhưng phải hỏi nó cả cho **VAI MÀU**, không chỉ cho trường dữ liệu; một vai màu là một lời khẳng định *"những thứ này cùng vật liệu"*, và nó ít bị soi hơn nhiều so với một trường trong bảng; **(b)** ⚠️ **đừng suy vai màu từ HÌNH** — mũ rơm Firenze và mũ phớt New York **cùng là `brim`** nhưng một cái rơm một cái nỉ nhuộm, nên suy từ `kind` là dựng lại đúng cái bẫy đang gỡ; phải có một trục VẬT LIỆU riêng; **(c)** **chỉ hai giá trị, và đó là một quyết định có đo**: len nhuộm và nỉ ép khác nhau ngoài đời nhưng ở cỡ cư dân 14–31 điểm ảnh chúng chênh dưới ngưỡng mắt ⇒ giá trị thứ ba là một **trục CHẾT** (bài học Phase 11). Giá của cả bản vá: **0 lệnh vẽ, 0 tam giác** — cả cộng đồng đi qua một `InstancedMesh` và màu vào qua `setColorAt`, nên **số vai màu KHÔNG phải một ngân sách**; đừng tiết kiệm ở chỗ không tính tiền.
  - ⚠️ **KÈM THEO — HAI KỶ VẪN "HỎNG" SAU BẢN VÁ, VÀ CẢ HAI ĐỀU ĐÚNG.** Kỷ 12 (mũ sắt cách áo bông **+0,011** độ đậm) và kỷ 15 (ghutra trắng cách kandura trắng **−0,005**) đều dưới ngưỡng mắt. Phản xạ sai là đi ép chúng tương phản. Sự thật: mũ SSh-40 Stalingrad được **SƠN đúng màu áo bông để nguỵ trang**, và ghutra trắng trên kandura trắng ngoài đời cũng không tách nhau — thứ tách chúng là **sợi dây agal ĐEN** mà bộ từ vựng chưa có. ⇒ **Trước khi "sửa" một con số dưới ngưỡng, hỏi *"ngoài đời thứ này có tương phản không?"*** — không thì ép nó tương phản là **mua một con số bằng cách nói dối lịch sử** (ADR-025 cấm), và cách xử lý đúng là ghi **ngoại lệ tường minh đếm được** (`assert.deepEqual(khôngTáchKhỏiÁo, [12, 15])`, đỏ cả khi có kỷ thứ ba rơi vào lẫn khi một trong hai được sửa), tuyệt đối không nới ngưỡng.
  - ⚠️ **KÈM THEO — MỘT NHÁNH CHẾT THÌ TEST BẤT LỰC, VÀ LINT MỚI LÀ LƯỚI.** Lúc đổi tên biến tôi bỏ sót một chỗ, để lại `?? era` (một biến không còn tồn tại). Không một bài test nào có thể đỏ, vì nhánh ấy **không đi tới được**: `getEraStyle` rơi về kỷ 1 với mọi đầu vào lạ nên `roofColor` LUÔN parse được. Thứ duy nhất bắt được là **`no-undef` của ESLint**. ⇒ Đây là mặt bù của bài học *"một bất biến ĐÚNG THEO CẤU TẠO thì không phải một cái gác"*: ở đó test xanh oan vì bất biến không thể sai; ở đây test xanh oan vì mã **không bao giờ chạy**. **Đừng bỏ `npm run lint` khi thấy test đã xanh** — hai cổng ấy canh hai loại lỗi khác nhau, và loại này chỉ có lint thấy.
  - ⚠️ **KÈM THEO — HỎI THEO `role` THÌ MỘT NGUYÊN MẪU NHẬN VƠ ĐỒ CỦA NGUYÊN MẪU KHÁC (bài học Phase 8A, lần thứ hai).** Hai hàm đo trong `humanIdentity.test.js` tìm bộ phận bằng `role === 'gear'` và `joint === 'head' && role !== 'skin'`. Nhưng **mũ trụ kỷ 12 cũng mang vai `gear`** và đứng TRƯỚC trong danh sách hộp ⇒ `find` trả về cái mũ, nên trục "đồ mang" của kỷ 12 xưa nay đo **2,2 điểm ảnh (cái mũ) thay vì 22,7 (khẩu súng)**; và **cái vò kỷ 2 cũng đội trên đầu**, nên hàm kia chỉ đúng **nhờ thứ tự hộp** — tức đúng nhờ một thứ chẳng liên quan (bẫy Phase 7D). ⇒ **Vai màu là VẬT LIỆU, không phải CHỨC NĂNG; hỏi theo `id`.** Và nhớ rằng `find` trả về phần tử ĐẦU TIÊN — một phép tìm có thể "đúng" nhiều tháng chỉ nhờ thứ tự mảng.
  - ⚠️ **KÈM THEO — CÁI BẪY NHÁY NGƯỢC CẮN LẦN THỨ TƯ, LẦN NÀY TRONG MỘT FILE TEST.** Viết `` `kỷ ${era}: vai `straw` mà bảng khai `dyed`` `` là đóng template literal giữa chừng ⇒ `SyntaxError` lúc NẠP, và Node báo `missing ) after argument list` — một thông báo **không hề nhắc tới dấu nháy**, nên rất dễ đi tìm nhầm chỗ. ⇒ Trong một template literal, tên hàm/biến dùng **nháy kép** hoặc nháy cong, tuyệt đối không nháy ngược. Thứ cứu được là **chạy ngay bài test vừa sửa** thay vì đợi cổng cuối — đúng vế đã ghi ở lần thứ ba.

- ⚠️ **MỘT NGÂN SÁCH LẠC HẬU THEO HƯỚNG *SIẾT* THÌ IM LẶNG VĨNH VIỄN — VÀ NÓ ĐÃ GIỮ CƠ THỂ CƯ DÂN Ở DẠNG CHỒNG-GẠCH LÂU HƠN CẦN THIẾT** (2026-08-23, ADR-055). Chú thích ngân sách trong `human.js` tính trần *"136 tam giác mỗi người"* từ mẫu số *"kỷ 1 = 19.434 tam giác thành phố"*. Đo lại: kỷ 1 nay là **104.958** (Phase 14 §1(3) làm mỗi ô thành một KHU PHỐ) ⇒ trần thật là **319**, tức con số đang dùng **lạc hậu 5,4 lần**. ⇒ **Ba luật**: **(a)** dự án đã có bài học *"một ngân sách tự tính mà chưa bao giờ được đặt cạnh sự thật thì không phải ngân sách"* (Performance Gate 2026-08-17), nhưng lần ấy nó sai theo hướng **trấn an** nên còn có ngày bị bắt; **sai theo hướng SIẾT thì KHÔNG BAO GIỜ bị bắt** — nó không làm gì hỏng, không ai kêu máy giật, nó chỉ lặng lẽ làm một hướng đi tốt **trông như đã bị cấm**; **(b)** mọi trần viết dưới dạng *"X% của Y"* phải ghi kèm **giá trị của Y lúc đo và lệnh đo ra nó**, vì Y là thứ phase sau sẽ đổi mà không ngó tới X; **(c)** khi trần được sửa lại cho đúng, hỏi tiếp *"lý lẽ chọn ca xấu nhất còn đúng không?"* — ở đây lý lẽ cũ (*"cư dân tốn một lượng CỐ ĐỊNH nên tỉ lệ cao nhất ở kỷ có mẫu số nhỏ nhất"*) chết ngay khi mỗi kỷ dựng một cơ thể khác nhau, nên ca xấu nhất thành `max` của một tỉ số **hai đại lượng cùng biến thiên** và phải TÍNH ĐỦ 15 DÒNG (đúng bẫy Phase 8C: *một kết luận đúng có thể hết đúng vì TIỀN ĐỀ của nó bị gỡ ở một phase khác*).
  - ⚠️ **KÈM THEO — MỘT BÀI TEST SO MỘT CÔNG THỨC VỚI MỘT BẢNG SUY TỪ CHÍNH CÔNG THỨC ẤY LÀ MỘT CÁI *GƯƠNG*, KHÔNG PHẢI MỘT CÁI *CÂN*.** `drawCallBudget.test.js` có hằng số `TAM_CO_DINH_KHO = 4` đếm cư dân là HAI mesh (thân + đầu). ADR-053 (2026-08-22) gộp hai mesh ấy làm một và tiết kiệm thật một lệnh vẽ; hằng số thì đứng yên ⇒ **cả bảng `MOC_LENH_VE` cao hơn sự thật đúng +1 ở MƯỜI LĂM DÒNG**. Bài test cuối file so `hoVatLieu(era).length + tamCoDinh(era)` với `MOC_LENH_VE[era]` — mà bảng ấy **được suy ra từ chính công thức đó**, nên hai vế cùng chứa một hằng số sai và khớp nhau hoàn hảo trong khi cùng lệch khỏi thực tế. Đây là bài học ngân sách tam giác tái diễn **trong chính file sinh ra để chống trôi âm thầm**, và tệ hơn một bậc: ở đó công thức lệch với thực tế, ở đây công thức lệch với thực tế **rồi cái bảng dùng để kiểm nó cũng lệch y hệt**. ⇒ **Hai luật**: (1) cách vá KHÔNG phải đổi 4 thành 3 (lại một hằng số chờ trôi) mà là **hỏi thẳng thứ đang đếm** (`humanShapesUsed(era).length`); (2) **mọi bảng ngân sách phải có ít nhất một dòng NEO vào một phép đo ĐỘC LẬP** — ở đây ba kỷ (1 · 8 · 13) đo lại bằng `city-preview --bench` trong Chromium ra đúng 11 · 17 · 12. Không có cái neo ấy thì một bảng 15 dòng vẫn chỉ là một công thức tự soi gương.
  - ⚠️ **KÈM THEO — HAI BÀI TEST TRÙNG NGUYÊN VĂN NẰM Ở CUỐI FILE, CẢ HAI ĐỀU XANH, VÀ MỘT TRONG HAI KHẲNG ĐỊNH MỘT LUẬT ĐÃ CHẾT.** Bản cũ của hai bài (`PHÉP GHÉP MA TRẬN…` và `NGÂN SÁCH TAM GIÁC… chấm ở kỷ XẤU NHẤT`) còn nguyên bên dưới bản mới trong `sceneGraphWiring.test.js`. Chúng chạy, chúng xanh, và bài thứ hai còn kiểm `MAX_BOXES × 12 × MAX_RESIDENTS` — tức khẳng định **mọi bộ phận là một cái hộp 12 tam giác**, điều vừa hết đúng. Một bài test lỗi thời mà XANH thì nguy hiểm hơn một bài đỏ: nó đứng đó như một lời bảo chứng cho một thế giới không còn tồn tại. ⇒ **Sau mỗi lần thay một bài test bằng bản mới, đếm `grep -c "^test("` rồi soi tên trùng** — rẻ bằng một dòng, và không cổng nào khác bắt được chuyện này.
  - ⚠️ **KÈM THEO — MỘT ĐẠI LƯỢNG BỊ PHÓNG ĐẠI *CÓ CHỦ Ý* KÉO THEO MỌI THỨ NEO VÀO NÓ, VÀ TỪNG CON SỐ RIÊNG LẺ ĐỀU "ĐÚNG THEO VẬT THẬT".** Cái đầu cư dân to gấp **1,54 lần** đời thật (0,20 chiều cao thay vì ~0,13) — một quyết định đúng về khả năng đọc ở 14 điểm ảnh. Hệ quả không ai viết ra: **mọi thứ đội lên nó cũng bị phóng 1,54 lần**. Nón lá khai 2,2 `headW` (đời thật 2,67 — đã là dè dặt!) render ra rộng **1,76 lần bề ngang vai** trong khi đời thật là 0,89, và ảnh dựng cho ra đúng một **cái nấm trắng nuốt trọn người**, chỉ còn hai chân thò ra. ⇒ **Ba luật**: **(a)** hỏi *"tôi đang đo theo cái gì, và cái đó có đúng tỉ lệ không?"* trước khi chép một kích thước từ đời thực; **(b)** rồi hỏi tiếp — và đây mới là vế cứu tôi khỏi một bản vá sai — ***"cái neo ấy là một TỈ LỆ hay chỉ là một CẬN?"***: nón lá chỉ bị cái đầu chặn DƯỚI (vành phải rộng hơn đầu) nên được phép trôi về hệ quy chiếu vai, còn mũ vành cứng thì **chỏm phải lồng vừa sọ** nên bề rộng bị CỘT CHẶT — tôi đã thu cả hai theo một công thức chung và bài test *"mũ vành phải đội vừa cái đầu"* ĐỎ ngay (vành 1,38 `headW` ⇒ chỏm 0,86 `headW`, hẹp hơn cái sọ); **(c)** thứ không sửa được trong phạm vi thì ghi thành nợ có SỐ ĐO (`TECH_DEBT #81`), đừng ép nó cho vừa.
  - ⚠️ **KÈM THEO — MỘT CÁI TRẦN DO ĐÀM ĐẶT ĐÃ ÉP RA MỘT THIẾT KẾ TỐT HƠN, VÀ PHẢN XẠ ĐẦU TIÊN CỦA TÔI LÀ NỚI NÓ.** Mũ vành bản đầu dựng bằng **HAI khối** (đĩa + chỏm) vì *"một cái mũ vành thì có hai phần"* — nghe hợp lý, và nó đẩy kỷ 8 lên **12 khối**, vượt trần 11. Thay vì nới trần, hỏi lại ***"ngoài đời đây là mấy VẬT?"*** — **một**; và một cái mũ là một mặt tròn xoay liền khối. Kết quả **vừa giữ trần, vừa đúng hình học hơn, vừa rẻ hơn 12 tam giác**. ⇒ Khi một cái trần chặn mình lại, câu hỏi đúng không phải *"nới bao nhiêu thì vừa?"* mà *"mô hình của tôi có đang đếm sai số VẬT không?"*.
  - ⚠️ **KÈM THEO — CÔNG CỤ ĐO TỰ CHẾ KẾT TỘI OAN MỘT KHUÔN LÀNH: "PHÁP TUYẾN PHẢI HƯỚNG RA NGOÀI TỪ GỐC" CHỈ ĐÚNG CHO KHỐI HÌNH SAO.** Bài kiểm chiều quay đầu tiên hỏi `dot(pháp tuyến, tâm mặt) > 0` và tố khuôn `hat` sai **16/60 mặt** — nhưng `hat` có một **bậc lõm** (vành thò ra rồi thụt vào chỗ chỏm), nên ở đó pháp tuyến đúng vẫn quay ngược về phía gốc. Thay bằng hai phép **không mang giả định hình dạng**: **chiều cạnh có hướng** (mỗi cạnh có hướng xuất hiện đúng một lần ⇒ chiều quay nhất quán) + **thể tích có dấu > 0** (⇒ ngửa ra ngoài, không lộn trong ra ngoài), cộng một phép kín (mỗi cạnh vô hướng dùng đúng hai lần). Cả 7 khuôn sạch. ⇒ Lần thứ 25 công cụ nói dối, và lần này theo hướng **chê oan** — trước khi tin một phép đo tố cáo mã, hỏi *"phép đo này ngầm giả định hình dạng gì, và thứ tôi đang đo có thoả không?"*.

- ⚠️ **CÔNG CỤ ĐO NÓI DỐI LẦN THỨ 24 — VÀ KIỂU NÓI DỐI NÀY LÀ KIỂU TỆ NHẤT: NÓ KHÔNG IN GÌ VÀ THOÁT MÃ 0** (2026-08-22). Ba công cụ (`city-preview.mjs`, `png-probe.mjs`, `mask-count.mjs`) khai cổng "chỉ chạy CLI khi được gọi thẳng" bằng ``if (import.meta.url === `file://${process.argv[1]}`)``. Nghe đúng, và sai ở **hai** chỗ mà dự án này gặp cả hai mỗi ngày: **(a)** `process.argv[1]` giữ nguyên đường dẫn **TƯƠNG ĐỐI** người ta gõ (`scripts/city-preview.mjs`) còn `import.meta.url` luôn TUYỆT ĐỐI ⇒ không bao giờ bằng nhau — mà chính lệnh `CLAUDE.md` ghi để chạy bản quét là lệnh tương đối; **(b)** đường dẫn dự án có dấu tiếng Việt và khoảng trắng nên `import.meta.url` **mã hoá phần trăm** còn phép nối chuỗi thì không (cùng họ với cái bẫy NFC/NFD đã giết LaunchAgent app tray). Hậu quả: gõ `node scripts/city-preview.mjs --bench 1` thì **không có một dòng nào in ra và mã thoát là 0** — trông y hệt "chạy xong, không có gì bất thường", nên nó có thể sống rất lâu. ⚠️ Và đây lại là **"một luật hai công thức"**: `water-score.mjs` đã dùng đúng cách (`pathToFileURL(resolve(process.argv[1] ?? '')).href`) từ trước, tức cái viết cẩu thả hơn là cái đang cai trị ba file kia. Nay có **hai** bài trong `scripts/cityPreviewSource.test.js` (đã thử-cho-đỏ): một bài ĐỌC MÃ bắt đúng khuôn sai đã biết, một bài **CHẠY THẬT** bằng đường dẫn tương đối và đòi công cụ phải in ra thứ gì đó — cần cả hai, vì đọc mã chỉ bắt được khuôn sai đã biết. ⚠️ Bài phụ: công cụ thiếu tham số thì in hướng dẫn rồi thoát mã **khác 0**, nên `execFileSync` NÉM — phải đọc `e.stdout`, nếu không thì chính bài đối chứng đỏ oan trong khi công cụ chạy đúng.
- ⚠️ **CON NGƯỜI CÓ NGỮ PHÁP RIÊNG, VÀ DÁNG ĐI LÀ HÀM CỦA QUÃNG ĐƯỜNG ĐÃ ĐI — KHÔNG PHẢI CỦA THỜI GIAN** (2026-08-22, ADR-053). Ba lớp, mỗi lớp một việc, đúng khuôn thực vật ở gạch dưới: `city3d/humanStyle.js` = **BẢNG 15 kỷ × 11 trục** (tỉ lệ cơ thể · dáng đứng · trang phục · đội đầu · đồ mang theo · sải chân · tốc độ · biên độ vung tay · màu vải — mỗi dòng buộc vào `country` mà `eraStyle.js` khai, **có test bắt**) · `city3d/human.js` = **THƯ VIỆN HÌNH** (7 trang phục, 7 kiểu đội đầu, 6 đồ mang theo) · `city3d/humanPose.js` = **DÁNG ĐI** · `residents.js` chỉ còn lo *"bao nhiêu người, đi đâu"* · `sceneGraph.js` chỉ ghép ma trận. **MỘT `InstancedMesh` trên hộp đơn vị 1×1×1 cho cả cộng đồng** (kích thước vào ma trận co giãn, khớp xoay quanh MỘT trục ngang ở tầng ma trận) ⇒ 28 người × 9 hộp vẫn là 1 lệnh vẽ, và lệnh vẽ cả cảnh còn **GIẢM 11 → 10**. ⚠️ **Pha bước = `travelled / (stride × cẳng chân)`, tuyệt đối không lấy một hằng số thời gian riêng** — lấy thời gian thì người nhanh và người chậm cùng nhịp chân và **bàn chân trượt trên đất**; cùng lý do đó cái nhún là **HỆ QUẢ** của chân trụ đang nghiêng (`legLen × (cos θ − 1)`) chứ không phải một sin riêng, và nó đã **chuyển hẳn** khỏi `residents.js` (test cấm `+ spot.bob` quay lại). ⚠️ **`stride` phải là BỘI SỐ CỦA CẲNG CHÂN, không phải số ô**: bản đầu khai `0,78` ô trong khi cẳng chân kỷ 1 dài `0,118` ô ⇒ bàn chân phải với xa hơn cả chiều dài chân, `asin` kẹp hông về 90°, cả 15 kỷ duỗi chân ngang — đúng bài học Phase 7D ("một số tuyệt đối không diễn đạt được một quan hệ") áp cho một giá trị có **hai** đầu vào biến động (`stature`, `legShare`); con số hỏng ấy nay bị nhốt bằng một assert. ⚠️ `lowDetail` quay về **đúng** mô hình 2 hộp cũ, và chính nó là **ĐỐI CHỨNG** của mọi phép đo dáng đi: không khớp nào thì hình bóng phải đổi **0**.
  - ⚠️ **ĐO HÌNH BÓNG CHÉO NHAU GIỮA HAI KHUNG HÌNH LÀ BẤT KHẢ THI Ở ĐÂY — và nó nói dối rất thuyết phục, ba lần liên tiếp.** Trong 0,57 giây cư dân đi được ~10 px, **xa hơn cả bề ngang cơ thể**, nên mọi phép so hai khung đều bị **TỊNH TIẾN** và **CHE KHUẤT** át hẳn: (a) "% điểm ảnh khác nhau giữa hai khung" ra 57–90% — thuần tịnh tiến; (b) đổi sang bề rộng hộp bao chỉ hơn sàn nhiễu 1,23 lần, vì camera lệch 45° nên trục ĐI TỚI chiếu phần lớn vào chiều DỌC màn hình chứ không phải chiều ngang; (c) thêm diện tích thì sự thật lộ ra — mô hình 2 hộp, thứ **không có khớp nào**, đo ra diện tích đổi **94,2%**. ⇒ **Cách chữa không phải nới ngưỡng mà là KHỬ nhiễu**: dựng hai bản (khớp thật ↔ 2 hộp) ở **CÙNG** thời điểm thì vị trí, hướng, vật che giống hệt nhau ⇒ **ghép từng người với chính mình rồi lấy TỈ SỐ**, mọi thứ chung triệt tiêu. Và khâu ghép cặp cũng phải có đối chứng riêng: ghép một ảnh với chính nó phải ra **1,0000 · tản 0,00%**, còn ghép LỆCH thời điểm phải **rụng gần hết cặp** (18 → 3 với gác 5 px) — không có vế thứ hai thì cái gác quá lỏng và số chính có thể đang ghép nhầm người với người.
  - ⚠️ **SO PHA 0 VỚI PHA ½ THÌ HÌNH BÓNG KHÔNG ĐỔI** — ở pha ½ hai chân chỉ **đổi vai** cho nhau, ảnh là ảnh gương của đúng bề rộng ấy, nên một bài test viết như vậy sẽ ĐỎ trên mã đang chạy hoàn toàn đúng. Phải so pha 0 với **pha ¼**. Cùng họ với "phép đo phải chạm đúng đại lượng mình định nói" (Phase 9A), khác ở chỗ lần này đại lượng đúng nhưng **thời điểm lấy mẫu** rơi vào đúng chỗ tín hiệu bằng 0.
  - ⚠️ **CÔNG CỤ ĐO NÓI DỐI LẦN THỨ 23, LỆCH 1,36 LẦN: MỘT NGƯỜI LÀ MỘT KHỐI ĐẶC, KHÔNG PHẢI MỘT ĐOẠN THẲNG.** Bản đầu của `human-scale.mjs` chiếu một đoạn thẳng từ bàn chân lên đỉnh đầu và ra **8,1 px**; đo trên ảnh dựng thật (mặt nạ + phân tích vùng liên thông) ra **11,0 px**. Camera nghiêng 34° nên **mặt TRÊN của khối cũng chiếm chỗ theo chiều dọc màn hình**, mà mắt thì đọc hình bóng chứ không đọc trục. Sau khi chiếu đủ **8 đỉnh của mọi hộp**: 11,1 so với 11,0. ⚠️ **NHƯNG hình bóng đầy đủ lại là phép đo SAI cho câu hỏi về TỈ LỆ giữa hai kỷ**: bề dày hộp chiếu xuống trục dọc gần như không đổi giữa hai kỷ, nên nó nằm trong cả tử lẫn mẫu và pha loãng tỉ lệ thật (đo ra 1,10 thay vì 1,18 — đúng hình dạng `TECH_DEBT #22`). Hai câu hỏi, hai phép đo, và **cả hai đều đúng cho câu của nó**: "Đàm thấy người cao mấy điểm ảnh" thì dùng hình bóng, "kỷ này cao hơn kỷ kia mấy lần" thì dùng đoạn thẳng.
  - ⚠️ **MỘT ĐỐI CHỨNG BẰNG-0 Ở TẦNG THUẦN KHÔNG ĐƯỢC CHÉP NGUYÊN SANG TẦNG ĐIỂM ẢNH.** Mô hình 2 hộp không có khớp nên `silhouetteSpanX` của nó đổi **0 TUYỆT ĐỐI** — đúng, và bài test tầng thuần đòi `< 1e-12`. Nhưng trên MÀN HÌNH nó vẫn **NHÚN** (bob nằm ở chiều cao hông), mà dịch chỗ thì phối cảnh đổi một tí ⇒ bề rộng chiếu đổi **0,0083 px**. Đòi `< 1e-9` ở đó là đòi một thứ phép chiếu không thể cho. Ngưỡng đúng là 0,05 px (1/280 tín hiệu thật) **kèm câu giải thích vì sao nó không phải 0** — không có câu ấy thì phiên sau sẽ đọc con số nới ra thành một cái phễu.
- ⚠️ **THẢM THỰC VẬT CÓ NGỮ PHÁP RIÊNG, ĐỪNG SỬA HÌNH CÂY TRONG `propSpec.js`** (Phase 8D, ADR-020/021). Ba lớp, mỗi lớp một việc: `city3d/floraStyle.js` = BẢNG 15 kỷ (loài + trọng số · cỡ · mật độ · `undergrowth` · màu lá — mỗi dòng buộc vào `country` mà `eraStyle.js` khai, **có test bắt** để hai bảng không trôi khỏi nhau) · `city3d/flora.js` = 7 LOÀI (tán rộng · thông · cọ · trắc bách diệp · đa · cây phố · bụi) · `propSpec.js` = chỉ còn GHÉP. **Luật chống-primitive**: tán là **NHIỀU THUỲ chồng lấn lệch tâm**, KHÔNG phải một khối lồi — một khối lồi tất yếu cho viền trơn + đúng một dải sáng-tối + đối xứng xoay hoàn hảo, và **tăng `sides` không chữa được, càng mượt càng giống hình học**. ⚠️ Ba cái bẫy đã trả giá, đừng lặp lại: (a) `sides`/`taper` **phải theo hạt giống**, viết cứng thì "40 hạt chỉ ra 2–4 dáng" — lỗi này cắn **bốn lần** trong chính file ấy (`cypress`, `streetTree`, `banyan`, `bush`); (b) ngân sách LOD **phải thật sự cắn** — đặt trần mức thấp bằng đúng khoảng mà mức cao có thể ra thì một nửa số hạt cho hai mức y hệt nhau; (c) thêm một loại cảnh vật mới thì **phải thêm hình cho nó ở `render2d/CityTile.jsx`**, chỗ đó trả `null` trong im lặng cho loại lạ nên bản 2D sẽ lặng lẽ thưa đi. Màu lá nằm ở bảng thực vật chứ không ở `palette3d.js`, vì nó là thuộc tính của LOÀI (cọ sa mạc bạc phếch, thông taiga ngả lam) chứ không phải một lựa chọn hoà sắc.
- ⚠️ **MỘT CHÚ THÍCH CHỨNG MINH Ý ĐỊNH, KHÔNG CHỨNG MINH RẰNG CON SỐ ĐI KÈM ĐÃ ĐƯỢC ĐO** (bài học 2026-08-13, Phase 3X — suýt để một khoản nợ nằm im vì đọc nhầm chú thích của chính mình). `CityBackdrop.jsx` có câu *"đây là chỗ mà 'đẹp' và 'dùng được' đối đầu nhau trực diện, và dùng được phải thắng"*. Tôi đọc câu đó rồi kết luận "đây là đánh đổi có chủ đích, phải hỏi Đàm mới được đụng" — và **dừng lại**. Đọc kỹ hơn thì chú thích ấy tuyên bố **HAI** ý định chứ không phải một: (1) chữ phải đọc được, và (2) *"thành phố lộ ra rõ nhất ở khoảng trống phía dưới — đúng chỗ chẳng có chữ gì"*. Ý định (1) đạt; ý định (2) **không đạt**. Không hề có xung đột để mà đánh đổi — chỉ có một vế chưa được thực hiện, vì mốc "dải đậm kết thúc ở đâu" chưa bao giờ được đo (nó dựa trên niềm tin rằng mặt đồng hồ nằm trên nền; đo ra thì đồng hồ nằm trong một thẻ ĐẶC ở tận 82% chiều cao). ⇒ **Luật**: trước khi kết luận "cái này là trade-off, cần Đàm quyết", phải kiểm xem mã có đang làm ĐÚNG mọi điều nó tự nhận không. **Một trade-off chỉ có thật khi cả hai vế đều đã đạt và buộc phải hy sinh một vế.** Nếu một vế đang hỏng thì đó là LỖI, và sửa lỗi không cần xin phép.
- ⚠️ **CHỮ Ở TRANG CHỦ NẰM Ở HAI ĐỘ SÂU KHÁC NHAU tuỳ khung màn hình** — và số đo này là NỀN cho lớp phủ ở `cityBackdropScrim.js`: máy bàn (1280) chữ-trên-nền chỉ ở **7%→21%** chiều cao lớp phủ, điện thoại (≤767) ở **31%→48%** (thẻ đồng hồ chiếm gần hết bề ngang nên đẩy khối lời chào xuống). Mặt đồng hồ `25:00` **không** nằm trên nền ở cả hai khung. **Đổi bố cục trang chủ ⇒ PHẢI đo lại** bằng `textmap3.mjs` rồi cập nhật `TEXT_ENDS_PCT` — sai chỗ này **không có gì đỏ cả**, chỉ là chữ khó đọc dần. ⚠️ Và bộ phân loại "chữ trên nền vs chữ trong thẻ" phải dừng ở **phần tử cha của lớp phủ**, KHÔNG phải `<body>`: bản đầu đi tới `<body>` nên chạm phải lớp bọc trang có nền đục và xếp **mọi** chữ vào "trong thẻ" — một kết quả luôn-luôn-đúng, tức vô giá trị. Luôn chạy kèm `--selftest` (bỏ điều kiện nền đục ⇒ mọi chữ phải nhảy nhóm); số không đổi nghĩa là bộ phân loại đã hỏng.
- ⚠️ **BẢNG MÀU ≠ MÀU TRÊN MÀN HÌNH — đừng đọc test mỹ thuật như một lời bảo đảm.** Giữa hai đầu còn ba tầng: cường độ đèn (nắng 2,15 = nhân màu gốc lên hơn hai lần), phép kẹp kênh khi tràn 255, rồi tone mapping. Đo thật (2026-08-12): mái kỷ 6 trong bảng màu là `#77425a` độ tươi **0,29**, trên màn hình ra `#5a1733` độ tươi **0,59 — gấp đôi**; mặt đất (mặt phẳng) thì khớp gần như hoàn hảo. Chênh lệch nằm ở các mặt DỐC. ⇒ Các bài test ở `palette3d.test.js` canh ĐẦU VÀO; muốn biết Đàm thật sự thấy gì thì chụp rồi đo đầu RA: `node scripts/png-probe.mjs <ảnh.png> --top 10` (hoặc `--grid`, hoặc `x,y`). Công cụ tự giải mã PNG bằng `zlib` của Node, không thêm dependency.
  - ⚠️ **KHÔNG CÓ MỘT HỆ SỐ CHUNG — phải đo từng chỗ, và có chỗ lệch NGƯỢC CHIỀU** (bổ sung 2026-08-13, Phase 3V): mái nhà render ra tươi **gấp đôi** bảng màu, còn BẦU TRỜI render ra nhạt đi **5 lần** — vì `NeutralToneMapping` phơi sáng 1,2 nén mạnh vùng sáng, mà chân trời để độ sáng 0,80 thì nằm đúng giữa vùng bị nén. Thêm một tầng nữa chỉ có ở bầu trời: nắng ấm nhân vào làm **góc màu** lạnh tụt 13–22° về phía lục (đặt 195° đo ra 173°), nên muốn ra đúng sắc phải khai cao hơn đích thật ~15°.
  - ⚠️ **ĐO BẢNG QUÉT thì ĐỪNG DÒ MÉP BẰNG MÀU** (bài học 2026-08-13, suýt chữa một bệnh không có): nền tấm bảng TRÙNG màu nền trang, nên phép dò "chạy tới khi khác màu nền" chạy quá đà 60px ngang + 30px dọc, khiến mỗi ô bị lấy mẫu dính sang ô bên và dính dải nhãn giữa hai hàng. Dải nhãn **sáng trưng ở theme sáng, đen kịt ở theme tối** ⇒ số đo sai theo hai chiều NGƯỢC nhau tuỳ theme và bịa ra hẳn một "lỗi đêm phẳng chỉ ở theme tối". Toạ độ thật lấy thẳng từ CSS trong `sweepPageHtml` (`#wrap { padding: 8px }`, rồi `X0 = 60`, `Y0 = 30`, `CELL_H = round(cell × 0,62)`, `LABEL_H = 22`). **Quy tắc chung: nghi ngờ công cụ đo đúng như nghi ngờ mã sản phẩm — số đo nào gây bất ngờ thì kiểm CÔNG CỤ trước, kiểm mã sau.**
  - ⚠️ **CẢNH ĐÊM: đừng chấm bằng độ tương phản tuyệt đối.** Đêm tối thì tương phản thấp là ĐÚNG, không phải lỗi. Phép đo có ý nghĩa cho chặng đêm là **15 kỷ có còn khác nhau không** (độ tản sắc/sáng giữa các kỷ trong cùng một chặng) — vì thứ thật sự mất nếu đêm hỏng là phần thưởng của việc đi hết 15 kỷ. Đo đúng cách (2026-08-13): tản sắc lúc đêm 28,2°, ngang bằng năm chặng kia ⇒ đêm khoẻ.
  - ⚠️ `.city-preview/` đã có trong `.gitignore` **và** trong `globalIgnores` của `eslint.config.js` — ESLint KHÔNG tự đọc `.gitignore`. Thiếu vế thứ hai thì chạy `npm run lint` đúng lúc đang dựng ảnh sẽ ra **29 lỗi giả** nằm trong ruột three.js được gói tạm.
- ⚠️ **`--virtual-time-budget` LÀM ĐÓNG BĂNG MỌI HOẠT HOẠ rAF — ẢNH CHỤP CÓ THỂ THIẾU HẲN MỘT KHỐI MÀ KHÔNG BÁO GÌ** (bài học 2026-08-13, suýt báo lỗi ma lần thứ hai trong cùng một ngày). framer-motion dựng phần tử ở trạng thái `initial` rồi mới chạy tới `animate` bằng `requestAnimationFrame`. Tua nhanh thời gian ảo thì rAF không nhích ⇒ phần tử **kẹt vĩnh viễn ở `initial`**. Một `motion.section` khai `initial={{opacity:0}}` sẽ ra ảnh là **một mảng trắng**, trong khi mọi phần tử KHÔNG hoạt hoạ quanh nó vẫn vẽ bình thường — nên tấm ảnh trông hoàn toàn hợp lý, chỉ thiếu mất một khối. Thực tế đã gặp ở tab Thành Tích: `Achievements.jsx:488` là phần tử hoạt hoạ DUY NHẤT của cả trang, và đúng nó biến mất, để lại khoảng trắng ~490px trông y hệt một lỗi bố cục nghiêm trọng. ⚠️ **Và `getComputedStyle(el).opacity` KHÔNG giúp phát hiện**: nó trả về độ mờ của CHÍNH phần tử đó, không phải độ mờ hiệu dụng — con chữ vẫn báo `opacity=1 visible` màu đen trong khi tổ tiên đang `opacity:0`. Phải đi NGƯỢC lên cây mới thấy. ⇒ **Đừng dùng `--virtual-time-budget` để soi giao diện nữa. Dùng `node scripts/shot.mjs`** (CDP: đợi bằng thời gian THẬT nên rAF chạy đủ, `Emulation.setDeviceMetricsOverride` cho bề ngang điện thoại THẬT, và luôn in `innerWidth` thật kèm cảnh báo tràn ngang). Hệ quả đáng lo: **mọi lần soi giao diện bằng công cụ cũ đều có thể đã bỏ sót nội dung** — nếu một kết luận mỹ thuật cũ dựa trên ảnh chụp kiểu cũ, hãy chụp lại bằng `shot.mjs` trước khi tin.
- ⚠️ **`scripts/shot.mjs` PHỤC VỤ THƯ MỤC `dist/` VÀ KHÔNG TỰ BUILD** (bài học 2026-08-13, Phase 4D). Sửa mã xong mà chụp ngay thì đang soi **bản build cũ** — và kết luận rút ra sẽ là "sửa rồi mà không ăn thua", tức đi chữa một cái không hỏng. Nguy hiểm gấp đôi khi bản dist tình cờ đã chứa MỘT PHẦN các thay đổi (vì lần build trước nằm giữa hai lần sửa): lúc đó ảnh chụp vừa xác nhận đúng một chi tiết vừa phủ nhận chi tiết kế bên, trông y hệt một lỗi có thật. ⇒ Trình tự bắt buộc: `npm run build` → `node scripts/shot.mjs`. Cờ `--click "<nhãn>"` (lặp được) bấm vào một nút sau khi đã mở tab; **nhãn phải chép y nguyên chuỗi mà chính công cụ in ra trong thông báo lỗi** — `textContent` gộp khoảng trắng nên nút hiển thị "Kỷ 7 · 4/5 · đang xây" lại phải gõ là `"Kỷ 7· 4/5· đang xây"`. Bốn cờ hay dùng nhất khi soi chữ/bố cục: `--fit` (đo chữ có tràn hoặc bị dấu "…" cắt không, quét mọi nút của màn hình đang mở), `--el "<chữ>"` (in font-size/padding/overflow THẬT của đúng một phần tử — dùng ngay khi `--fit` và mắt bất đồng), `--crop "@<chữ>"` hoặc `--crop "x,y,w,h"` (cắt lấy một vùng, khỏi đọc ảnh dài 3000px), `--full` (chụp trọn chiều dài trang).
- ⚠️ **LỚP TAILWIND TRUYỀN QUA `className` CÓ THỂ THUA MÀ KHÔNG BÁO GÌ — "sửa xong" mà thật ra chưa hề sửa** (bài học 2026-08-13, Phase 4E). Nút chính trang chủ bị xén chữ ở 390px. Bản vá đầu truyền `px-2.5 text-[11px] overflow-hidden text-ellipsis` qua `className` của `ActionButton`; đo lại thấy "sạch", tôi tin và đi tiếp. Hỏi thẳng trình duyệt (`shot.mjs --el`) thì nút VẪN chạy **`font-size: 18px`, `padding: 28px`** — tức `text-lg`/`px-7` trong `sizeMap.default` của chính component THẮNG. Dự án không có `tailwind-merge`, mà Tailwind quyết lớp nào thắng theo **thứ tự trong bảng kiểu sinh ra**, KHÔNG theo thứ tự viết trong chuỗi `className`. ⇒ **Hai lớp cùng khai một thuộc tính CSS là một canh bạc**; muốn đổi kích thước thì dùng prop `size` (component đã có `sizeMap[size] ?? sizeMap.default` — chỉ phát ra MỘT bộ). Đã khoá bằng `src/components/actionButtonSizing.test.js` (đọc mã nguồn, 3 bài đều đã thử-cho-đỏ). Chi tiết quy ước: `PROJECT_STRUCTURE.md` mục "Quy tắc Tailwind".
- ⚠️ **CÔNG CỤ ĐO GIAO DIỆN NÓI DỐI THÊM BỐN KIỂU NỮA (số 5–8) — và một trong số đó đã làm tôi tin nhầm là đã sửa xong** (2026-08-13, Phase 4E; tất cả đã vá trong `scripts/shot.mjs`, xem chú thích đầu file). **(5)** `scrollWidth − clientWidth` của NÚT không phải "chữ tràn": con `motion.span` `layoutId` (`position:absolute` + transform của framer-motion) vẫn tính vào vùng cuộn của cha ⇒ nút "Pomo" bị báo THỪA 31px và suýt được ghi thành một mục nợ kỹ thuật. **(6)** Cổng "app đã mọc ra chưa" chỉ vá cho `--fit` nên mọi lần chụp THƯỜNG vẫn có thể ra trang trắng mà báo "✓"; thay bằng "đợi DOM đứng yên" lại dính bẫy ngược — `/seed` và vỏ `index.html` là DOM TĨNH (28 phần tử, 0 nút) nên chúng "ổn định" hoàn hảo lúc React chưa chạy: **điều kiện "không còn gì thay đổi" luôn đúng ở thời điểm chưa có gì**. Phải đủ CẢ HAI: có nội dung (≥3 nút) RỒI mới đợi số phần tử thôi nhảy. **(7)** Gộp `overflow-x: auto`/`scroll` chung với `hidden`/`clip` ⇒ báo 5 chip của thanh chuyển kỷ "BỊ XÉN MẤT 704px" + 2 tab Thống kê; nằm trong băng cuộn ngang thì **vuốt một cái là thấy**, không mất gì. **(8)** Cổng yếu ⇒ đo TRƯỚC khi web font về, mà font dự phòng hẹp hơn ⇒ **mọi kết luận "chữ vừa khung" đo bằng cổng cũ đều đáng ngờ**. ⇒ Hai luật rút ra: **(a)** phép đo nào cũng phải có cổng "đã có nội dung thật chưa", và cổng đó phải nằm ở đường đi CHUNG, không nhét riêng vào một nhánh; **(b)** khi phép đo báo lỗi mà mắt thấy bình thường (hoặc ngược lại), **kiểm CÔNG CỤ trước** — đây là lần thứ 12–15 công cụ tự chế nói dối trong dự án này.
- ⚠️ **MỘT BÀI TEST XANH KHÔNG CHO BIẾT CÓ *BAO NHIÊU* THỨ ĐANG GIỮ NÓ XANH — chỉ thử ngược mới đếm được** (bài học 2026-08-13, Phase 4D). Tôi viết một chú thích khẳng định "chấm theo kỷ TRƯỚC phiên thì công trình vừa xây xong **mất trắng**", rồi viết hẳn một bài test hành vi cho nó. Thử ngược thật (sửa `finalBook` thành kỷ trước phiên) thì bài đó **vẫn xanh**: ở ca lên kỷ, `pruneEraScopedBlueprintState` chạy kèm `sealContext`, nên chính lần **niêm phong** đã ghi công trình ấy vào bảo tàng bằng đường khác. Gỡ riêng đường di sản: xanh. Gỡ riêng niêm phong: xanh. **Gỡ cả hai mới đỏ** — tức hai lưới ĐỘC LẬP che cùng một ca, mà chú thích lại kể như chỉ có một. ⇒ Hai hệ quả phải nhớ: (a) quy tắc "một bài test chưa từng thấy đỏ thì chưa phải test" **chưa đủ** — phải hỏi thêm *nó đỏ khi gỡ CÁI GÌ*, vì gỡ nhầm chỗ sẽ ra kết luận "chỗ này không quan trọng" hoặc tệ hơn là "chỗ này đã được bảo vệ"; (b) cùng họ với bài học sương mù ở Phase 3Y — cả hai lần **kết quả đúng nhưng lời giải thích sai**, và lời giải thích mới là thứ phiên sau kế thừa rồi dựa vào. **Sửa đúng không chứng minh hiểu đúng.**
- ⚠️ **BÀI TEST TẦNG ENGINE CHỨNG MINH HÀM CHẠY ĐÚNG — NÓ KHÔNG CHỨNG MINH HÀM CÓ AI GỌI** (bài học 2026-08-13, Phase 4H). `summarizeMuseum` (`src/engine/cityCompletion.js`) được viết ở Phase 4B với ghi chú đầy đủ, xử lý đúng ca "kỷ thất truyền", **có bài test riêng**, và docstring tự giới thiệu là *"con số duy nhất trả lời tôi đã đi được bao xa"*. `grep` cả cây `src/` ra đúng **hai** chỗ: dòng định nghĩa, và bài test của chính nó. Một tính năng làm xong 90% rồi thiếu đúng MỘT dòng nối, và **không có gì đỏ lên**: build xanh · lint sạch · 551 test xanh · không một cảnh báo "unused" nào — vì hàm CÓ được dùng, bởi bài test của nó. Triệu chứng duy nhất là màn hình thiếu một thứ mà không ai nhớ là đáng lẽ phải có; nó chỉ lộ ra khi **soi bằng mắt vào ảnh chụp** rồi hỏi "ô này nói được gì cho Đàm?". ⇒ **Hai luật**: (a) làm xong một hàm tầng engine thì việc CHƯA XONG cho tới khi có một chỗ gọi thật — cùng tinh thần Definition of Done; (b) canh bằng test **đọc mã nguồn** ở tầng component (`cityViewShellWiring.test.js`), vì đây đúng loại vi phạm mà lint/build không thể bắt. Cùng họ với "một bài test xanh không cho biết có BAO NHIÊU thứ đang giữ nó xanh" (Phase 4D): cả hai đều là chuyện bài test đo một thứ **hẹp hơn** điều ta tưởng nó đo. ⚠️ Kèm theo, cùng lần soi đó: ô số liệu thứ ba của màn Thành Phố đang hiện **"Chuỗi ngày 4"** trong khi thanh tiêu đề hiện **"CHUỖI 4"** cách vài phân — một trong bốn ô dùng để nói lại điều vừa nói. Luật xử lý đã có sẵn ngay trong chính file đó (ô "Cư dân"): **hai chỗ nói cùng một chuyện thì chỗ nói ít hơn phải nhường**.
- ⚠️ **MỘT LUẬT MỚI LÀM CHO ĐIỀU KIỆN CŨ HẾT ĐÚNG ⇒ PHẢI ĐI TÌM MỌI CHỖ PHÁT BIỂU LẠI ĐIỀU KIỆN ẤY, KHÔNG CHỈ CHỖ ĐẦU TIÊN NGHĨ RA** (bài học 2026-08-13, Phase 4D — cùng một lỗi cắn HAI lần trong một task). "Di sản dang dở" biến mệnh đề *"chỉ kỷ đang chơi mới có công trình đang xây"* thành sai. Mệnh đề đó được phát biểu lại ở **ba** chỗ dưới dạng gác `isCurrent`: `withEraCompletion` (chặn `pending` ở kỷ hiện tại), thẻ "Đang xây" trong `CityViewShell`, và nhãn "· đang xây" trên `EraSwitcher`. Sửa xong hai chỗ đầu rồi vẫn tưởng là xong; chỗ thứ ba chỉ lộ ra khi **nhìn vào ảnh chụp**. ⚠️ Không có gì đỏ lên ở cả ba lần — mã chạy đúng theo luật CŨ, chỉ là luật cũ hết đúng; test hiện có cũng xanh vì chúng canh luật cũ. Triệu chứng thì im lặng và rất dễ bỏ qua: bảng ghi "chưa xây" ngay bên dưới giàn giáo đang dựng, rồi "Kỷ 7 · 4/5" đã chết vĩnh viễn trông giống hệt "Kỷ 7 · 4/5" còn cách ngôi sao ba phiên. ⇒ **Cách làm**: đổi một luật thì `grep` chính cái điều kiện đó trên toàn bộ cây (`isCurrent`, `activeBook`, `era ===`…) và duyệt TỪNG chỗ, thay vì sửa theo triệu chứng bắt gặp. Cùng họ với **"một luật chỉ được có một công thức"**.
- ⚠️ **KẾT LUẬN "HAI THỨ NÀY GIỐNG NHAU" CHỈ ĐÁNG TIN KHI PHÉP ĐO PHỦ HẾT NHỮNG GÌ MẮT THẤY** (bài học 2026-08-13, Phase 3Y — một phép đo hẹp vừa kết tội oan vừa bỏ lọt cùng lúc). Bản quét 15 kỷ × 6 chặng được chấm bằng **góc màu của dải trời**, và từ đó kết luận "chặng chiều là chặng xấu nhất, ba chặng ấm nằm gọn trong 20°". Chấm lại bằng **cả cảnh** (vector 9 chiều: dải trời + dải thành phố + dải đất, mỗi dải 3 kênh, trung bình 15 kỷ) thì lật ngược hẳn: chặng chiều cách hoàng hôn **37,6/255** — rõ ràng — còn **bình minh ↔ hoàng hôn chỉ 5,9**, dưới ngưỡng mắt phân biệt (~12). Tức hai chặng đó là CÙNG MỘT BỨC ẢNH suốt nhiều ngày, mà phép đo hẹp không hé một lời. Góc màu chỉ là một trong ba thành phần của màu; dải trời chỉ là một trong ba dải của khung hình. ⇒ **Đo một trục thì vừa BÁO NHẦM vừa BỎ SÓT, và cái bỏ sót nguy hiểm hơn vì nó im lặng.** Kèm theo: **đo tổng cả cảnh nói được CÓ lỗi nhưng không nói được lỗi Ở ĐÂU** — phải đo theo TỪNG DẢI mới truy ra nguyên nhân (chính phép đo từng dải mới lộ ra rằng đổi màu trời không hề chạm tới dải thành phố).
- ⚠️ **MỘT LUẬT CHỈ ĐƯỢC CÓ MỘT CÔNG THỨC — thấy hai chỗ cùng phát biểu một luật thì gộp lại NGAY, đừng chờ nó cắn** (bài học 2026-08-13; cùng một hình dạng sai cắn **hai lần trong một ngày**). (1) `daylight.test.js` phát biểu "chân trời bình minh/hoàng hôn phải ẤM" ở hai chỗ: một chỗ viết `horizonHue < 60`, chỗ kia có hàm `warm(h) = h < 90 || h >= 300` biết quấn vòng màu. Bản `< 60` chặt hơn nên nó âm thầm thắng, và chính nó nhốt hai chặng vào chung một múi 60° — tức là **cái luật viết cẩu thả hơn lại là cái đang cai trị**. (2) `palette3d.js` chặn "mái tím rực" bằng **cửa sổ góc màu** `h >= 255 && h <= 340`, còn `palette3d.test.js` định nghĩa dải tím bằng **quan hệ kênh** (đỏ và lam đều cao hơn lục). Hai vùng chồng nhau nhưng không trùng ⇒ có khe, và mái kỷ 15 ở **247°** lọt đúng khe đó (tươi 0,44 / trần 0,42). ⇒ Khi mã sản phẩm và bài test cùng canh một luật, **mã phải gọi đúng phép thử mà test dùng**, không được diễn đạt lại bằng một công thức "tương đương" — hai công thức tương đương trên giấy gần như luôn lệch nhau ở biên.
- ⚠️ **BẤT BIẾN KIỂU "CÁC THỨ NÀY PHẢI KHÁC NHAU" PHẢI DUYỆT TỔ HỢP ĐÔI, KHÔNG ĐƯỢC DUYỆT DANH SÁCH THEO THỨ TỰ.** `daylight.test.js` có bài "hai chặng liền nhau không được giống nhau", viết dạng `for (i = 1; i < DAY_PHASES.length; i++)` — tức chỉ các cặp KỀ NHAU. `dawn` ở đầu danh sách và `dusk` ở cuối nên **không bao giờ** được đem so với nhau, và lỗi trên sống sót qua mọi lần chạy test. Đây là **lần thứ HAI cùng một hình dạng sai xuất hiện trong chính file đó** (lần trước: bài "hành trình màu" tính cả `night` nên bộ số hỏng vẫn đủ độ trải). Cùng họ với "ngưỡng một phía là cái phễu, không phải hàng rào". ⇒ Khi viết bài test kiểu "N thứ này phải phân biệt được", luôn duyệt `for i` × `for j > i`, và **nhốt sẵn bộ giá trị HỎNG cũ vào một bài đối chứng** bắt buộc phép đo phải còn bắt được nó — đó là cách duy nhất giữ cho ngưỡng không bị nới dần cho tiện. (Muốn ngưỡng thuần-số đáng tin thì phải HIỆU CHUẨN nó với phép đo thật: ở đây 0,31↔5,9px · 0,52↔29,8px · 1,28↔75,1px, Spearman 0,854.) **Và canh cả SỐ LƯỢNG cặp sát nhau, không chỉ cặp gần nhất** — "cặp gần nhất = 8,4" đứng yên y hệt dù có MỘT cặp sát nhau hay NĂM cặp, mà năm cặp nghĩa là một phần ba hành trình 15 kỷ không cho người chơi thấy gì mới. Cực tiểu cũng là một con số gộp.
- ⚠️ **CÔNG CỤ ĐO TỰ CHẾ NÓI DỐI LẦN THỨ 11 — và lần này nó nói dối theo hướng GÂY HOẢNG, không phải trấn an.** Đo "15 kỷ có khác nhau không" bằng cách lấy TRUNG BÌNH cả dải thành phố ra kết luận **70/105 cặp kỷ trùng nhau**. Sai: mái (thứ mang bản sắc kỷ) chỉ chiếm ~1/10 diện tích dải đó, phần còn lại là mặt đất và trời lọt giữa các khối — giống hệt nhau ở mọi kỷ, tức tín hiệu bị pha loãng ~10 lần. Lọc lấy 8% pixel TƯƠI NHẤT (tức mái) thì con số thật là **5/105**. ⇒ Hai cách nhận ra: (a) luôn cắm `--selftest` bỏ bộ lọc — nếu số không nhảy về mức pha loãng thì bộ lọc chưa hề chạy; (b) **khi hai phép đo cãi nhau thì phải truy tới cùng, không được chọn cái nghe hợp ý** — ở đây tầng thuần đã có sẵn bài duyệt 105 cặp màu mái báo "cặp gần nhất 8,4", mâu thuẫn hẳn với "70 cặp trùng", và chính mâu thuẫn đó chỉ ra công cụ mới là thứ hỏng.
  - ⚠️ **LẦN THỨ 13 — VÀ NÓ NẰM NGAY TRONG BẢN VÁ CỦA LẦN THỨ 11 Ở TRÊN** (2026-08-13, Phase 4C). Bộ lọc "8% pixel TƯƠI NHẤT" ấy dùng `sat = (max−min)/max` — độ tươi **TƯƠNG ĐỐI** của HSV, mà mẫu số là `max`, nên **pixel càng TỐI càng dễ đạt điểm cao**: `#010e0a` (max 14, min 1) ra sat **0,93**, cao hơn hầu hết mái đang nắng. Nên bộ lọc "lấy mái" thật ra lấy **mặt mái khuất trong bóng**, và bảng "màu mái đo được" in ra 15 màu gần đen ở giữa TRƯA (độ sáng 14–96). Kết luận cũ **"0/105 cặp, gần nhất 12,6, trung vị 28,2" là số của VÙNG TỐI — SAI, đừng trích lại.** Đo đúng (chroma TUYỆT ĐỐI `max − min`, tức `s×v`) ra độ sáng 83–128 và sự thật là **3/105 cặp dưới ngưỡng** (kỷ 5↔12 = 7,2 · 3↔12 = 10,1 · 5↔7 = 11,1). Đã sửa và nay là 0/105 thật, cặp gần nhất 14,1, trung vị 39,6.
  - ⚠️ **VÌ SAO `--selftest` KHÔNG BẮT ĐƯỢC** — bài học quan trọng hơn cả con số: phép tự kiểm ấy chỉ hỏi *"bỏ bộ lọc thì số có tụt không"*. Số CÓ tụt (12,6 → 4,0) nên nó xanh. **Một phép tự kiểm chứng minh bộ lọc CÓ tác dụng, KHÔNG chứng minh nó có tác dụng ĐÚNG.** Muốn biết bộ lọc chọn đúng thứ cần chọn thì phải IN RA thứ nó chọn rồi nhìn (ở đây: in kèm **độ sáng** của màu đo được — 15 con số quanh 20–60 giữa trưa là báo động không thể bỏ qua). Từ nay mọi bộ lọc pixel phải in kèm một đại lượng cho phép nhận ra nó chọn nhầm họ.
- ⚠️ **BÀI TEST BẢNG MÀU VÀ PHÉP ĐO TRÊN ẢNH KÊU HAI TẬP CẶP RỜI NHAU HOÀN TOÀN** (2026-08-13, Phase 4C — dạng sâu nhất của "bảng màu ≠ màu trên màn hình"). `palette3d.test.js` duyệt 105 cặp màu mái và kêu kỷ 8↔13 (6,9) · 6↔7 (9,9) · 1↔14 (11,9); đo trên ảnh chụp thật thì cả ba cặp ấy cách nhau **≥19** — mắt thấy khác rõ. Ngược lại ba cặp THẬT SỰ trùng trên màn hình (5↔12 = 7,2 · 3↔12 = 10,1 · 5↔7 = 11,1) **không hề bị bài test kêu**. Tức ở tầng bảng màu, con số vừa BÁO NHẦM vừa BỎ SÓT, và hai tập hợp không giao nhau một phần tử nào. ⇒ **Ngưỡng 12 là ngưỡng mắt trên ĐIỂM ẢNH ĐÃ DỰNG; đem áp thẳng vào bảng màu là lỗi "một luật hai công thức".** Bài test tầng thuần chỉ là dây bẫy chống sập bảng màu, KHÔNG phải lời bảo đảm về thứ Đàm nhìn thấy — muốn biết Đàm thấy gì thì `--sweep` rồi đo. (Và khi phải nới số đếm của một dây bẫy như vậy, hãy trả lại bằng một phép canh PHÂN BỐ — trung vị — vì phép đếm đuôi vẫn xanh khi cả 105 cặp cùng tụt sát ngưỡng.)
- ⚠️ **MUỐN ĐỔI MÀU CỦA VẬT THÌ PHẢI ĐỔI THỨ CHIẾU VÀO VẬT (hoặc thứ PHỦ LÊN vật), KHÔNG PHẢI THỨ ĐỨNG SAU VẬT.** Đẩy chân trời bình minh đi thật xa hoàng hôn thì dải TRỜI tách được, nhưng dải THÀNH PHỐ vẫn chỉ cách 8,7/255 (góc màu 51° vs 44°). Lý do: thứ nhuộm màu lên thành phố là ĐÈN MẶT TRỜI (`sunWarmth`) và đèn bán cầu (lấy màu từ **`skyHue` đỉnh trời**, không phải `horizonHue`) — còn `horizonHue` chỉ vẽ tấm phông. Mà `sunWarmth` thì **buộc phải ấm ở cả hai đầu ngày** (mặt trời thấp ⇒ ánh sáng xuyên quãng khí quyển dài — vật lý, không phải lựa chọn mỹ thuật; đã thử nói ngược lại và bị test bắt). Lối thoát là **SƯƠNG MÙ**: nó lấy màu chân trời nên nó sơn lại cả mảng NỀN phía sau và quanh thành phố. Tắt riêng nó rồi bật lại: **17,2 → 75,1/255**. ⚠️ Nhưng phải nói cho đúng nó làm gì: nền/chân trời **12,9 → 74,6**, còn dải THÀNH PHỐ **8,4 → 3,3 — GIẢM**, mặt đất không đổi. Toàn bộ khoảng cách đến từ phần nền, không từ các công trình (sương cố ý bắt đầu SAU rìa thành phố nên không chạm nhà ở gần) — và điều đó ĐÚNG vật lý: cùng một mặt trời thấp thì tường nhà buổi sáng và buổi chiều vốn phải giống nhau; thứ cho ta biết đang là sáng hay chiều là bầu trời, là sương, là đèn đường đã bật hay chưa. ⚠️ **Chú thích đầu tiên tôi viết cho chỗ này khẳng định ngược lại** ("sương quét sắc lên chính những công trình ở xa nên cuối cùng chạm được vào dải THÀNH PHỐ") — nghe cực kỳ xuôi tai, và sai hẳn dấu. **Một cơ chế nghe hợp lý vẫn phải ĐO rồi mới được viết ra**, kể cả khi con số tổng đã xác nhận là việc sửa có tác dụng: "sửa đúng" không chứng minh "hiểu đúng vì sao".
- ⚠️ **NGƯỠNG TƯƠNG PHẢN ÁP CHO THỨ MANG THÔNG TIN, KHÔNG ÁP CHO THỨ TRANG TRÍ** (bài học 2026-08-13, Phase 4B). Thanh chuyển kỷ có một **chấm tròn** màu kỷ và — mới thêm — một **ngôi sao "trọn vẹn"**. Bản đầu tô cả hai bằng `eraSolid(era)` cho nhất quán. Đo tương phản `ERA_METADATA.accentColor` trên nền thẻ qua đủ 8 tổ hợp theme × skin: kỷ 9 `#a3e635` ra **1,49:1**, kỷ 3 `#facc15` ra 1,51:1 (ngưỡng cho ký hiệu là 3:1) — tức ngôi sao **gần như tàng hình ở theme sáng**, đúng chỗ đáng lẽ là phần thưởng. Nhưng cái chấm tròn ở cùng màu ấy thì **không sao cả**, và đã chạy nhiều tháng không ai kêu. Khác biệt không nằm ở màu mà ở VAI TRÒ: chấm tròn là trang trí thuần (số kỷ ghi ngay cạnh nó, mất chấm không mất gì), còn ngôi sao là thứ DUY NHẤT nói "kỷ này trọn vẹn". ⇒ Đừng áp một ngưỡng cho mọi thứ trên màn hình, và cũng đừng bỏ ngưỡng vì "chỗ kia màu đó vẫn ổn". Hỏi: **mất phần tử này thì Đàm có mất thông tin nào không?** Có → 3:1 trở lên (chữ nhỏ 4,5:1); không → tuỳ mỹ thuật. `--accent` đo được **2,97–7,43:1** qua 8 tổ hợp và là màu "cái này quan trọng" sẵn có của app. **Không có gì đỏ lên khi sai chỗ này** — build xanh, lint sạch, test xanh; chỉ có một phần thưởng vô hình. Đã khoá bằng test đọc-mã-nguồn ở `cityRenderers.test.js` (đã thử ngược và thấy đỏ).
- ⚠️ **MỘT CON SỐ KHÔNG CÓ MẪU SỐ THÌ KHÔNG PHẢI MỤC TIÊU** (bài học thiết kế 2026-08-13, Phase 4B — ghi lại vì nó lặp ở nhiều màn hình khác). Màn Thành Phố hiện "Công trình: 3" suốt từ đầu, và **không ai nhận ra đó là lỗi** vì con số hoàn toàn đúng. Nhưng ba trên mấy thì chính Đàm cũng không biết, nên nó không bảo được anh còn phải làm gì. Thêm mẫu số ("3/5") thì đúng con số đó tự đọc ra "còn 2 nữa là trọn vẹn kỷ này". ⇒ Trước khi thêm hiệu ứng/hoạt hoạ cho "đỡ chán", soát xem **các con số đang hiện đã hành động được chưa** — rẻ hơn nhiều và ăn sâu hơn nhiều. ⚠️ Và mẫu số PHẢI tự đếm từ nguồn (`BLUEPRINT_CATALOG`), cấm viết cứng: hôm nay cả 15 kỷ đều đúng 5 bản vẽ, nhưng viết cứng số 5 là gài mìn — ngày nào có bản vẽ thứ 6, màn hình sẽ gắn sao "trọn vẹn" cho một thành phố còn thiếu nhà mà không có gì đỏ lên.
- ⚠️ **FIXTURE ĐỀU TĂM TẮP LÀ FIXTURE VÔ DỤNG** (lần thứ 12 công cụ dev nói dối, 2026-08-13). `scripts/make-fixture.mjs` có dòng `notBuilt.length > 1` kèm lý do chính đáng: "chừa lại một bản vẽ chưa xây để còn `craftingQueue` mà soi giàn giáo". Ý định đúng — nhưng nó áp cho **MỌI kỷ**, kể cả kỷ đã niêm phong nơi chẳng còn ai đang xây gì. Hệ quả: **không kỷ nào trong fixture có thể đạt 5/5**, cả bảo tàng ra một dãy "4/5" giống hệt nhau, và trạng thái "trọn vẹn" gần như không tồn tại để mà nhìn thấy. Một tính năng hỏng vẫn sẽ trông bình thường trên fixture đó. Đúng bài học **"một trade-off chỉ có thật khi cả hai vế đều đã đạt"**: ở đây vế thứ hai (giàn giáo) chỉ liên quan tới kỷ ĐANG chơi, nên nó **không hề cần** cái giá kia. ⇒ Khi soi một màn hình có nhiều phần tử cùng loại (kỷ, thành tích, danh mục…), **kiểm xem dữ liệu mẫu có ĐỦ ĐA DẠNG để phân biệt không** trước khi kết luận "trông ổn" — y như luật đã có cho bảng quét 15 kỷ: lỗi mỹ thuật gần như luôn là lỗi SO SÁNH.
- ⚠️ **CHỤP KHUNG ĐIỆN THOẠI BẰNG `--window-size` LÀ SAI — Chromium headless có SÀN 500px** (bài học 2026-08-13, suýt báo một lỗi không có). Đặt `--window-size=390,844` thì ảnh ra đúng 390 điểm ảnh ngang, **nhưng `window.innerWidth` thật là 500**: trang dàn ở 500 rồi bị cắt còn 390, trông y hệt một lỗi tràn ngang nghiêm trọng (chữ cụt giữa câu, thanh dưới mất nút). Đo bằng số thì `scrollWidth === innerWidth` và **không phần tử nào vượt mép phải** — app hoàn toàn không tràn. ⇒ Muốn khung điện thoại THẬT phải dùng CDP `Emulation.setDeviceMetricsOverride`, không dùng `--window-size`. Và **luôn in kèm `window.innerWidth` thật vào mỗi lần chụp** — nếu không thì mọi kết luận về bố cục hẹp đều dựa trên một bề ngang bịa.
- ⚠️ **LỚP NỀN THÀNH PHỐ Ở TRANG CHỦ NUỐT GẦN HẾT VÒNG NGÀY** (đo 2026-08-13, CHƯA xử lý — chờ Đàm quyết, xem `TECH_DEBT.md` #16). Đo trên ảnh chụp thật ở bề ngang 1280, hai dải thành phố lộ ra hai bên thẻ đồng hồ: cả 6 chặng ngày ra gần như cùng một mảng trắng ngà (`#cececc`–`#dddcd7`, độ tươi **0,02–0,12**), và cặp cách nhau XA NHẤT — giữa trưa với ban đêm — chỉ **14/255**. Nguyên nhân không phải `BACKDROP_OPACITY` mà là **lớp phủ giữ-chữ-đọc-được**: nó pha về `var(--canvas)` (một màu PHẲNG) ở 55–92%, mà pha về màu phẳng thì **giết ĐỘ TƯƠI mạnh hơn giết độ sáng** — đúng thứ vòng ngày dựa vào. Hình khối (tín hiệu độ sáng) sống sót, còn màu thì không. ⚠️ Đây là **thiết kế có chủ đích** (chú thích tại chỗ ghi rõ "đẹp và dùng được đối đầu, dùng được phải thắng"), nên **đừng tự chỉnh** — nhưng cũng đừng tưởng công sức làm bầu trời tới được trang chủ.

## App menu bar Mac (Electron tray) — cách bật, và 4 cái bẫy đã trả giá (2026-08-05, 2026-08-10)
- **Chạy bằng gì**: `node_modules/electron/dist/Electron.app/Contents/MacOS/Electron <đường-dẫn-dự-án>` (binary chạy thẳng). KHÔNG có bản `.app` đóng gói nào cho app tray.
- ⚠️ **BẪY 1 (đã dọn 2026-08-05, ghi lại để đừng tạo lại)**: từng có `DC Pomodoro.app` nằm ngay trong thư mục dự án, trông như app menu bar nhưng **KHÔNG PHẢI** — nó là applet AppleScript đời cũ, khởi động `serve-dist.mjs` ở `localhost:31105` rồi mở Chrome (đúng luồng localhost đã bị cấm ở mục "KHÔNG làm những thứ này"), lại còn trỏ vào thư mục cũ `Pomodoro Game - USING`. Mở nó KHÔNG làm hiện icon tray. **Đã xoá.** Đừng tạo lại kiểu launcher này; muốn bật app tray thì dùng LaunchAgent bên dưới.
- ⚠️ **BẪY 2 — launchd KHÔNG chạy được đường dẫn có ký tự tiếng Việt.** Trỏ `ProgramArguments` thẳng vào đường dẫn chứa "Bản sao…" thì job luôn thoát **mã 78 (EX_CONFIG)**, KHÔNG có stderr, dù `plutil -lint` OK và `test -x` báo file tồn tại (bash chuẩn hoá NFC/NFD, launchd thì không). **Cùng họ với cái bẫy NFC/NFD làm test nạp hai bản React.** Cách vá: LaunchAgent chỉ trỏ tới script bọc ở đường dẫn **thuần ASCII** — `~/Library/Application Support/dc-pomodoro-tray.sh` — script đó mới `cd` vào thư mục có dấu (bash xử lý đúng). Log cũng phải để ở đường dẫn ASCII (`~/Library/Logs/dc-pomodoro-tray.*.log`).
- **Tự khởi động**: LaunchAgent `~/Library/LaunchAgents/com.dcpomodoro.tray.plist` (`RunAtLoad` bật, **`KeepAlive` TẮT** để nút "Thoát" trong menu tray thoát được thật, không bị bật lại ngay).
- ⚠️ **BẪY 3 — `main.js` KHÔNG có khoá chống chạy trùng** (`requestSingleInstanceLock`). Chạy 2 lần = **2 biểu tượng** trên thanh menu. Trước khi nạp LaunchAgent phải `pkill` bản đang chạy tay.
- ⚠️ **BẪY 4 (2026-08-10) — "ảnh trong suốt" KHÔNG bằng "không có ảnh".** Khi có phiên chạy, tray bỏ icon đi và chỉ hiện chữ (`🍅 mm:ss` / `☕ mm:ss`). Trước đây chỗ "bỏ icon" nạp `public/tray-empty.png` (16x16, alpha = 0 toàn bộ). Trong suốt nên KHÔNG nhìn thấy, **nhưng macOS vẫn chừa đủ 16 điểm ảnh chỗ cho nó** → sinh khoảng trắng ngay trước quả cà chua / cốc cà phê. Vá đúng: `nativeImage.createEmpty()` (ảnh 0x0, không chiếm chỗ). File `tray-empty.png` đã xoá hẳn. **Đừng quay lại dùng PNG trong suốt cho mục đích "ẩn icon".**
- **Không có phiên nào chạy thì tray chỉ hiện icon trơn** (`updateTrayTitle` đặt tiêu đề rỗng) — đó là bình thường, không phải hỏng. Có phiên mới hiện `🍅 mm:ss`.
- **(Đã dọn sạch 2026-08-05)** Toàn bộ dấu vết dự án đời cũ đã bị xoá (chuyển Thùng rác): LaunchAgent `com.civjourney.localhost`; thư mục dự án cũ `Downloads/Claude Code/Pomodoro Game - USING` (đã rỗng, chỉ còn log); applet `DC Pomodoro.app`; 2 file backup dữ liệu game cũ + 2 file thiết kế đời CivJourney trong `Downloads`; 2 thư mục phiên Claude của đường dẫn dự án cũ. **Từ nay chỉ còn MỘT thư mục dự án duy nhất**: `Downloads/Claude Code/Bản sao Pomodoro Game - USING`. (Hai thứ tên có "pomodoro" còn lại trong `~/Library` — `com.macpomodoro` và `iCloud~com~limepresso~pomodorofree` — là app Pomodoro của hãng KHÁC, không liên quan dự án này, KHÔNG được xoá nhầm.)

## Quy trình deploy
```
Sửa code → git add . && git commit -m "mô tả" → git push origin main
→ Vercel tự deploy trong ~2 phút
→ Mọi thiết bị thấy bản mới
```
Hoặc bấm đúp file `/Users/damduy/Desktop/🚀 Deploy App.command`

- ⚠️ **PHẢI LÀ NHÁNH `main`, không phải nhánh nào khác** (bài học 2026-08-12, mất công chờ vô ích): Vercel **chỉ cập nhật production khi có push vào `main`**. Push vào nhánh khác (vd nhánh tính năng `claude/...` mà phiên Claude Code trên web tự tạo) chỉ sinh một bản **Preview** ở URL riêng — `pomodoro-dc.vercel.app` KHÔNG đổi gì cả, và bản Preview của gói Hobby thường bắt đăng nhập Vercel mới xem được trên Safari iPhone. Chính trang Overview của Vercel có ghi *"To update your Production Deployment, push to the `main` branch"* nhưng rất dễ lướt qua.
  - ⇒ **Quy tắc cho MỌI AI (Đàm chốt lại 2026-08-22: *"sau này tự deploy, tôi không có việc gì phải tự deploy cả"*)**: làm xong thứ Đàm cần THẤY trên máy thật mà đang ở nhánh phụ → **TỰ gộp vào `main` rồi push, KHÔNG hỏi**. "Deploy" nghĩa là tới được `pomodoro-dc.vercel.app`, KHÔNG phải "đã push lên GitHub" — nhánh phụ = code đã an toàn trên GitHub, KHÔNG có nghĩa là đã lên production, nên dừng ở nhánh rồi báo "đã deploy xong" là báo sai.
    - **CHỈ DỪNG LẠI HỎI** khi việc gỡ xung đột đòi phải **vứt bỏ công của một phiên khác**. Xung đột gỡ được mà không mất gì thì cứ gỡ rồi đi tiếp.
    - **VẪN PHẢI BÁO RÕ đã đưa lên production những gì NGOÀI phần việc của mình.** Nhánh phụ thường mang theo commit của các phiên khác chưa từng lên production (lần 2026-08-22: 11 commit Phase 13–14 của phiên khác đi kèm 2 commit của phiên đang làm). Đàm có quyền biết mình vừa nhận thêm những gì — im lặng chuyện đó là giấu phạm vi thay đổi.
    - Push xong **vẫn phải xác nhận Vercel "Ready"** (gạch đầu dòng ngay dưới).
  - Cách gộp an toàn: `git fetch origin main` → kiểm `git merge-base --is-ancestor origin/main <nhánh>` (trả về true là gộp thẳng được, không xung đột) → `git checkout -B main origin/main && git merge --ff-only <nhánh> && git push origin main`.
- ⚠️ **Push xong PHẢI mở tab Deployments xác nhận "Ready"** — code xanh + commit thành công KHÔNG có nghĩa là đã thực sự lên production (xem sự cố `8ee264d` ở mục "Vercel Hobby: giới hạn 12 Serverless Functions").

## Việc đang dở & sắp tới
→ Xem **`BAN_GIAO.md`** (mục "Sẽ làm tiếp" + "Nhật ký cập nhật"). Web Push iPhone đã xong & deploy.

## KHÔNG làm những thứ này
- ❌ Không biến Electron thành app chính riêng biệt.
- ❌ Không dùng localhost / serve-dist.mjs / LaunchAgent làm luồng chạy chính.
- ❌ Không nhân đôi logic game giữa web và Electron. Logic chính nằm ở web app.
- ❌ KHÔNG start phiên focus trên dev/localhost — dev dùng chung Supabase row với production, sẽ ghi đè dữ liệu thật của Đàm.

## Lưu ý kỹ thuật
- `npm install` cần flag `--legacy-peer-deps`
- Electron còn liên quan tới menu bar Mac. Đừng xoá hoặc bỏ qua khi sửa timer/tray.
- `serve-dist.mjs` và LaunchAgent là luồng local cũ, chỉ đụng khi thật sự cần.
- ⚠️ **(Lịch sử, đã gỡ)** Từng có `coachVoice.js` (giọng cảm xúc) + thư mục `ai-coach-sim/` (bản demo trình duyệt) — cả hai đã bị xoá hẳn ngày 2026-06-21 (xem mục "ĐÃ GỠ HẲN" ở trên). KHÔNG còn `src/engine/coachVoice.js` hay `ai-coach-sim/` trong repo — đừng tạo lại trừ khi Đàm yêu cầu.
